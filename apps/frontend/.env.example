# API base URL prefix. Leave empty for same-origin deployments where
# OpenAPI paths already include /api/v1. Only set for cross-origin
# deployments (e.g. VITE_API_BASE_URL=https://api.example.com).
VITE_API_BASE_URL=
