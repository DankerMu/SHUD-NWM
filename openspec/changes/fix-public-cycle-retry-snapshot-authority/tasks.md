# Tasks and Evidence Floor — #1356

- [x] 1. Reproduce deterministic duplicate submission at the retry-ID selection seam and retain complete cycle rows with identity/status/attempt/reconciliation fields.
- [x] 2. Record real guard-hole qualification (different retry keys, two ownerships and gateway calls), pre-existing provenance, and production possibility; mark test-only alternative inapplicable.
- [x] 3. Make all three retry-ID callsites consume their parent-selection jobs snapshot; preserve explicit retry precedence, suffix semantics and operator-demotion behavior; migrate both direct test callers.
- [x] 4. Add deterministic public-seam interleaving regression while retaining natural concurrency, original attempt/projection/error/cancellation assertions and unique active-master checks.
- [x] 5. Run old-method assertion-red then restored-green with complete row evidence; run affected suites and disposable DB owners on node27 without skipped acceptance owners.
- [x] 6. Run both sources in natural and deterministic modes for20 consecutive rounds under bounded node27 CPU pressure, all passing; run local Ruff and strict OpenSpec.
- [x] 7. Complete independent review/finding verification/final review, CI, SHA-bound evidence/self-audit and merge; record postmerge accountability.

Fixture expanded; risk packs, invariant matrix, mutation seam, baseline and rollback boundaries are in design.md. Required merge evidence is not satisfied by an occasional green rerun. Existing journal/CAS/gateway semantics are immutable; no production deployment or live Slurm claim. Parent owns all verification. Code changes only the two retry-selection modules and related tests; selector edge changes require an observed missing owner, not a new policy.

Verification checkpoint `1255444bbd4325c32fa84ed7b830c28e97c60f3f`: node27 Python3.11.15, 4864 pure tests and one integration test passed with zero skips; the integration-marked case is executed in its disposable DB stage rather than skipped in the pure stage. Deterministic gfs/IFS baseline2pass → exact old method2 assertion failures (two active masters, attempts3/4, four gateway entries per case) → restored2pass; complete cycle rows are retained. Twenty consecutive natural/deterministic × gfs/IFS rounds passed all80 cases under four nice10 CPU workers, each recording about677 CPU seconds and clean termination. Ruff, strict OpenSpec and source-preservation smoke passed; six protected source files and all unrelated production ASTs remain unchanged. Receipts and executed harness are under `.workplans/issue-1356/`; Phase7/CI/merge and postmerge accounting remain separate gates.

The original first-round `3 == 2` criterion is separately proved on the same checkpoint with scheduling-only first-round instrumentation: gfs/IFS fixed2pass → old method2 assertion failures (active attempts2/3 and three gateway calls) → restored2pass. Full rows and logs are in `oracle/first-round/`. Both private worktrees, uploaded harnesses and local throwaways were removed after archival; all pressure workers stopped cleanly. The initial parent probe's circular-import error was rejected before collection and fixed by importing the chain facade first; it was never counted as a red test. No runtime/test/oracle changes were needed for that harness repair.

Postmerge closure: PR #2253 merged as `8cbb744129b98d2a42830efe425ed3fa8821333b`; one comprehensive round and independent final were clean, evidence/work summary were posted, and the branch-tip gate passed. The validated review-loop row and ADR0003 audit deferral are recorded as merge follow-up.

Run-level continuation is tracked separately, not claimed complete by this archive: integrate the merged fix into paused #2208 and rerun its original full matrix before its reviews/merge. This separates the parent workflow continuation from the completed #1356 change; no issue acceptance criterion or test requirement is removed. The shared timeseries change remains active.
