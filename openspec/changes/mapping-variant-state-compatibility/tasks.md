## 1. State snapshot clone-provenance migration

- [ ] 1.1 Add `db/migrations/000046_state_snapshot_clone_provenance.sql` (number resolved per the coordination rule below) adding three nullable columns to `hydro.state_snapshot` — `cloned_from_state_id TEXT DEFAULT NULL`, `cloned_from_model_id TEXT DEFAULT NULL`, `clone_gate_fingerprint TEXT DEFAULT NULL` — using `ADD COLUMN IF NOT EXISTS`, in the same column-only, NULL-default style as `000028_state_lineage.sql`, and never altering the `(model_id, COALESCE(source_id, ''), valid_time)` unique index.
  - Required evidence: `uv run pytest -q tests/test_migrations.py` proves the migration exists with the expected name, is non-empty SQL, is ordered strictly after `000045`, references no future objects, and is a forward-compatible column-only `ALTER` on `hydro.state_snapshot` that adds exactly the three nullable `cloned_from_*` / `clone_gate_fingerprint` columns with `DEFAULT NULL` and touches no existing row.
  - Required evidence: coordination check recorded in the task — the dependency change `source-specific-model-variant-routing` (Change 4) finalized in its design D8 that it introduces NO migration (its `000046` mention is only a reservation for an optional later guard-history index outside its landing scope), so this change takes `000046`. At merge, take the next free integer after the highest migration on master, ensure no two files share a number, and rerun `tests/test_migrations.py::test_migration_dependency_order` after any renumber.
  - Non-goal for 1.1: no clone logic, no data backfill, no change to the unique index, no new table.

- [ ] 1.2 Verify the migration on the node-27 real DB oracle.
  - Required evidence: `ssh -p 32099 nwm@210.77.77.27` then `docker exec nhms-db psql -U nhms -d nhms -c "\d+ hydro.state_snapshot"` shows the three new columns present and NULLable after the migration is applied; `docker exec nhms-db psql -U nhms -d nhms -c "SELECT count(*) FROM hydro.state_snapshot WHERE cloned_from_state_id IS NOT NULL"` returns `0` on a fresh apply (existing rows untouched).
  - Non-goal for 1.2: no production basin activation, no clone insert against production data — a synthetic identity only.

## 2. Fingerprint-gated state clone

- [ ] 2.1 Implement the fingerprint-gated clone: clone the latest qualified `(M0, source, t*)` snapshot into `(M1, source, t*)` only when `verify_hydrologic_core_fingerprint_equal(M0_package, M1_package)` passes, with the full pinned column disposition — reuse the source row's `state_uri` and `checksum` (no physical file copy); preserve `source_id`/`valid_time`/`cycle_id`/`lead_hours`/`usable_flag`/`original_shud_filename`; overwrite `model_id` to `M1`, `model_package_version` to the `M1` package version (`M1`'s `core.model_instance.model_package_uri`), and `model_package_checksum` to the `M1` package checksum. "Qualified" is pinned to the Gate G10 conditions: usable, QC-passing, valid checksum, `lead_hours=12`, and `valid_time == t*` (the cutover cycle boundary; a stale latest snapshot does not qualify). Gate-input caller contract: resolve both package roots from `core.model_instance.model_package_uri`; take `category_files` and both `.sp.att` paths from the `M1` variant's mapping manifest / mapping evidence package; supply real platform-level `state_schema_bytes` / `solver_config_bytes` (empty inputs refused fail-closed); and cross-check the recomputed `M1` fingerprint against the `hydrologic_core_fingerprint` recorded in the `M1` mapping evidence package. Reuse `workers/mapping_builder/rewrite.py::verify_hydrologic_core_fingerprint_equal` as the gate and never reimplement the fingerprint rule.
  - Required evidence: unit tests prove (a) an equal-fingerprint clone writes the `(M1, source, t*)` row with `state_uri`/`checksum` equal to the source, `model_package_version`/`model_package_checksum` equal to the `M1` package version and checksum, preserved `source_id`/`valid_time`/`cycle_id`/`lead_hours`/`usable_flag`/`original_shud_filename`, and no physical file copied; (b) an unequal-fingerprint clone is refused fail-closed with no row written, surfacing the stable error code `state_clone_cold_start_approval_required` plus an audit record naming the blocked scope; (c) a missing qualified source snapshot fails closed with no row and the same stable code; (d) a stale latest snapshot (`valid_time < t*`) does not qualify and is refused exactly as in (c) — Gate G10 condition 4.
  - Required evidence: tests prove the gate-input contract — empty `state_schema_bytes` or `solver_config_bytes` are refused fail-closed (no false-pass on symmetric empty inputs), and a recomputed `M1` fingerprint that mismatches the evidence-recorded value refuses the clone.
  - Required evidence: an integration test drives the EXISTING strict warm-start selection against a cloned `(M1, source, t*)` row with `M1`'s target lineage parameters and asserts acceptance as the exact `+12h` successor with no validator modification, on BOTH planes: the DB-path validator (`services/orchestrator/chain_forecast_state.py::_select_strict_forecast_initial_state` / `_validate_strict_forecast_state`) and the file-index path (`packages/common/state_manager.py::strict_warm_start_evidence`).
  - Non-goal for 2.1: no transaction wiring (group 3), no reverse clone, no replay routing.

- [x] 2.2 Implement clone-row identity and `cloned_from` provenance write: mint the clone row's `state_id` via the existing convention `packages/common/state_manager.py::state_snapshot_id(M1_model_id, valid_time, source_id=…, cycle_id=…, lead_hours=…)` (embedding the `M1` identity, collision-free against the source row); set `cloned_from_model_id` to the source `M0` `model_id`, `cloned_from_state_id` to the source snapshot `state_id`, and `clone_gate_fingerprint` to the gating `hydrologic_core_fingerprint` value; set the clone row's `run_id` to the source `M0` producing run's `run_id` to satisfy the `NOT NULL` FK.
  - Required evidence: unit tests prove the three provenance fields are populated on a successful clone, that the clone row's `state_id` equals the `state_snapshot_id` convention value for the `M1` identity and differs from the source row's `state_id`, that the clone `run_id` equals the source `M0` run's `run_id`, and that a warm-start-lineage read attributes the state to `M1` via `model_id` + `cloned_from_*` rather than via `run_id` (the MUST-level attribution rule).
  - Required evidence: a test proves pre-clone / legacy rows keep `NULL` provenance and remain selectable by the unchanged warm-start path.
  - Non-goal for 2.2: no JSONB metadata column, no change to `strict-warm-start` / `cross-cycle-warm-start-chaining` requirements.

- [x] 2.3 Verify a clone insert against the node-27 real DB oracle honors the unique key and persists provenance.
  - Required evidence: on node-27 (`ssh -p 32099 nwm@210.77.77.27` then `docker exec nhms-db psql -U nhms -d nhms`), insert a synthetic `(M0, source, t*)` source row and a fingerprint-equal `(M1, source, t*)` clone row whose `state_id` is minted by the `state_snapshot_id` convention for the `M1` identity, prove the clone obeys the `state_snapshot_model_source_valid_time_key` unique index, that a second clone into the same `(M1, source, t*)` conflicts, and that `cloned_from_model_id`/`cloned_from_state_id`/`clone_gate_fingerprint` persist as inserted.
  - Non-goal for 2.3: no production `model_id`, no production basin, teardown of the synthetic identity after the check.

## 3. Atomic cutover transaction hook (blocked-by: Change 4 task 2.1 extension point)

- [x] 3.1 Mount the clone as a hook on the lifecycle activation extension point defined by change `source-specific-model-variant-routing`, executing inside the same DB transaction as `M1` activation / `M0` supersede, with the pinned applicability predicate: the hook no-ops with an audited skip reason unless the activation context carries a previous active model (`no_previous_active_model`) AND the target classifies as direct-grid under Change 4's single classifier (`target_not_direct_grid`); when engaged, the clone runs once per source in the activation context's source scope (the variant's normalized `applicable_source_ids`), and any engaged-clone failure rolls back the whole transaction so no "activated-but-not-transferred" state is committed.
  - Required evidence: unit/integration tests prove (a) activation + clone commit atomically (post-commit: `M1` active, `M0` superseded, one clone row per source in scope present); (b) a clone failure (fingerprint refusal, unique-key conflict, or missing/stale source with no approval) rolls back the whole transaction leaving `M1` not active, `M0` not superseded, and no clone row for any source; (c) no intermediate activated-but-not-transferred state is observable mid-transaction; (d) a dual-source scope (`[gfs, ifs]`) writes two clone rows atomically, and the same scope with one source missing a qualified snapshot rolls back entirely with the blocking source named in the audit record.
  - Required evidence: applicability tests prove (e) a first activation for a scope with no previous active model no-ops with skip reason `no_previous_active_model` and commits (fresh-basin path, docs §12); (f) an activation whose target classifies as legacy-mapping no-ops with skip reason `target_not_direct_grid` and leaves pre-existing lifecycle behavior unchanged; (g) the hook is not invoked on the already-current short-circuit (owned by Change 4's contract).
  - Required evidence (dependency declaration): the task records that the extension-point hook signature and its order relative to the Change 8 station-flag-flip hook are owned by change `source-specific-model-variant-routing` (committed contract: `specs/variant-activation-cutover/spec.md` — hooks inside the transaction, hook context = transaction cursor + basin_version_id + previous active model + target model + source scope, reserved state-clone mount point); this change consumes that contract. Change 4's extension-point task (its task 2.1) is a hard blocked-by precondition: this task does not start until it lands, and there is no fallback wiring against the bare `model-activation-deactivation` substrate.
  - Non-goal for 3.1: no definition of the extension point, the activation transaction, or the hook ordering (all owned by Change 4); no display `active_flag` flip (Change 8).

- [ ] 3.2 Implement the explicit cold-start approval route: an engaged-clone refusal surfaces the stable error code `state_clone_cold_start_approval_required` plus an `ops.audit_log` record naming the blocked `(basin_version_id, source_id)` scope and cause; an activation request carrying an explicit cold-start approval input (approver identity, reason, covered source ids) commits with the clone hook skipping the covered sources (no clone row), the approval recorded in `ops.audit_log` in the same transaction, and the spin-up-distortion-announcement obligation marker (§11.3 clause 3) on both the audit record and the activation result.
  - Required evidence: unit/integration tests prove (a) a refusal without approval rolls back and surfaces the stable code plus the audit record; (b) a re-request with an approval covering the blocked sources commits `M1` active with no clone row for the covered sources and records the approval (approver, reason, sources) plus the announcement-obligation marker in the same transaction; (c) the approval is scoped to its named sources only — a dual-source cutover with an approval covering only `ifs` still executes and writes the `gfs` clone through the fingerprint gate.
  - Non-goal for 3.2: no announcement execution and no first-cold-cycle run mechanics (Change 7 rollout scope); no approval UI.

- [ ] 3.3 Publish the cloned checkpoints into the scheduler file state index on the lifecycle post-commit tail, ordered BEFORE the Change 4 `publish_scheduler_registry_manifest` re-publish: one entry per cloned `(M1, source, t*)` row carrying the same identity and lineage as the DB row (`model_id`, `source_id`, `valid_time`, `cycle_id`, `lead_hours`, `state_uri`, `checksum`, `M1` `model_package_version`/`model_package_checksum`), written through the existing `FileStateSnapshotIndexRepository` entry shape (`NHMS_SCHEDULER_STATE_INDEX`).
  - Required evidence: unit/integration tests prove (a) after a committed cutover, `packages/common/state_manager.py::strict_warm_start_evidence` resolves ready for `(M1, source, t*)` from the index with the `M1` lineage parameters, for every source in scope; (b) the index publish is ordered before the registry-manifest re-publish; (c) an index-publish failure holds back `publish_scheduler_registry_manifest` (previous manifest remains the compute-plane authority) and surfaces a recorded retry blocker — node-22 is never routed to `M1` while the index lacks `M1`'s successor checkpoint; (d) an approved cold-start source gets no fabricated index entry.
  - Non-goal for 3.3: no change to the manifest re-publish trigger itself (owned by Change 4); no node-22 code change — the DB-free scheduler consumes the index read-only through the existing path.

## 4. No-rollback state semantics

- [ ] 4.1 Implement the no-reverse-clone guard and the fix-forward fingerprint routing, enforced at the clone function's own signature (source model row, target model row) as defense-in-depth inside the Change 4 hook: refuse fail-closed with no override any clone whose target classifies as legacy-mapping under Change 4's single classifier (`resource_profile.direct_grid_forcing` parses through `workers/forcing_producer/direct_grid_contract.py:load_forcing_mapping_contract_from_manifest` with `forcing_mapping_mode='direct_grid'`; absent, malformed, or non-direct ⇒ legacy-mapping, fail-closed); allow a fingerprint-equal `M1→M1′` fix-forward clone through the same gate; and route a fingerprint-unequal fix-forward candidate to the explicit cold-start approval route (stable code + audit record per task 3.2) instead of a clone.
  - Required evidence: unit tests prove (a) a reverse clone targeting a legacy `M0` (contract absent) is refused with no row and no override; (b) a target declaring `forcing_mapping_mode='direct_grid'` whose contract fails the parser (e.g. missing `binding_checksum`) classifies as legacy-mapping and is refused — mirroring Change 4's classifier fail-closed behavior; (c) a `FORC`/binding-only fix-forward with equal fingerprint clones `(M1, source, t*) → (M1′, source, t*)` and records `M1` as `cloned_from` source; (d) a fingerprint-unequal candidate is classified as a new model and routed to the cold-start approval route (stable code `state_clone_cold_start_approval_required`), not cloned.
  - Non-goal for 4.1: no legacy-mapping reactivation path, no producer-internal fallback/downgrade, no second classifier definition (Change 4's classifier is the single source).

## 5. Provenance-pinned replay (blocked-by: Change 4 legacy-reactivation guard)

- [ ] 5.1 Implement the provenance-pinned replay lineage resolver as a named policy contract in `services/orchestrator/replay_lineage.py`: resolve the cutover time `t*` for a basin scope from the recorded direct-grid activation audit authority (`ops.audit_log` activation-class record; equal by construction to the cloned rows' `valid_time`), classify a requested interval as pre-cutover (`M0`, offline), post-cutover (`M1`), or straddling — a straddling interval is split at `t*` into an `[start, t*)` `M0` sub-interval and a `[t*, end]` `M1` sub-interval, each lineage-pinned separately with no cross-variant splicing — and expose the result for replay tooling as a non-activation operation. The offline replay executor that consumes this resolver is owned by Change 6 (scientific-validation replay) / Change 7 (rehearsal) and is explicitly out of scope here.
  - Required evidence: unit tests prove (a) an interval entirely before `t*` resolves to `M0` with `M0` lineage; (b) an interval at or after `t*` resolves to `M1` with `M1` lineage; (c) an interval straddling `t*` resolves to the two-sub-interval split plan with per-sub-interval lineage and no spliced output series; (d) `t*` is read from the recorded activation audit authority, not derived from data availability, and equals the cloned rows' `valid_time` in a cutover fixture.
  - Required evidence: guard-scope tests prove (e) an offline `M0` replay resolution for a basin with direct-grid activation history performs no lifecycle activation call and is not intercepted by the Change 4 legacy-reactivation hard guard; (f) an actual `activate(M0)` request for that basin is still refused by the guard (requires Change 4's guard implementation — the group blocked-by).
  - Non-goal for 5.1: no scientific A/B comparison (Change 6), no activation of `M0` or `M1`, no replay executor implementation.

## 6. Change verification

- [ ] 6.1 Run the change verification suite.
  - Required evidence: `uv run pytest -q tests/test_migrations.py` and the new clone / hook-applicability / cold-start-approval / index-publish / no-rollback / replay-lineage unit tests pass.
  - Required evidence: `uv run ruff check .` reports `All checks passed!`.
  - Non-goal for 6.1: no node-27 live display receipt — this change lands state-compatibility mechanism only and touches no display surface (that is Change 8).

- [ ] 6.2 Validate the OpenSpec change.
  - Required evidence: `openspec validate mapping-variant-state-compatibility --strict --no-interactive` reports the change is valid.
  - Non-goal for 6.2: no basin activation, no edits to other change directories or `openspec/specs/`.
