# §2.7 Pre-work: Spec vs Code Drift Log

Pre-work receipt for tasks.md `§2.7` ("Record that readiness is certified on pinned-commit evidence, and capture any observed OpenSpec/code drift"). This log is committed under `openspec/changes/cmfd-direct-grid-platform-readiness/evidence/` so it moves into archive together with the change on merge and is discoverable during any post-merge audit.

## Drift entry 1: `shud-runtime` spec says `shud`, code default says `shud_omp`

- **Recorded**: 2026-07-07 (post-PR#928 shud_omp → shud purge, during PR#929 §2.5 Phase 4 review)
- **Spec**: `openspec/specs/shud-runtime/spec.md` (13 MUST-clauses referring to `shud` executable) plus `openspec/specs/basins-runtime-consumption/spec.md`, `openspec/specs/model-registration/spec.md`, `openspec/project-profile.md` (all now say `shud`; previously said `shud_omp`). Additionally, `openspec/changes/cmfd-direct-grid-platform-readiness/specs/platform-release-pinning/spec.md` `Requirement: Pinned identities are read from authoritative sources` scenario says "the runtime identity and staging limits trace to `workers/shud_runtime/runtime.py`" (line ~47), but manifest `source_locations["shud_executable"].file` was refined by the second and third errata to point at `evidence/staging-2.5.node-22.pass.log` D1 (the deployed-binary identity attestation) rather than at `runtime.py`, because `runtime.py:74/85/95` default remains the placeholder `shud_omp` and the derivation authority for the pinned value `shud` is the on-node-22 deployed binary. The platform-release-pinning spec WHEN/THEN is therefore satisfied in the same sense as `proj_crs_database_version` (which also traces to the deployment host, not to a repo file): the "authoritative source" is the deployed-binary identity artifact, transitively via the manifest's source_locations pointer chain.
- **Code (baseline_commit `5e518c15…`, unchanged in PR#928 and PR#929)**:
    - `workers/shud_runtime/runtime.py:74` — `shud_executable: str = "shud_omp"` (SHUDRuntimeConfig dataclass default)
    - `workers/shud_runtime/runtime.py:85` — behavioral gate literally names `shud_omp`
    - `workers/shud_runtime/runtime.py:95` — `SHUDRuntimeConfig.from_env` reads `os.getenv("SHUD_EXECUTABLE", "shud_omp")`
    - `services/production_closure/slurm_validation.py:319,653` — same `shud_omp` fallback
    - `schemas/examples/run_manifest.example.json:45` — `"executable": "shud_omp"`
- **Deployment reconciliation**: production `SHUD_EXECUTABLE` env is set to `/scratch/frd_muziyao/NWM/SHUD/shud` (the deployed binary, sha256 `ef2f6181…`, per `staging-2.5.node-22.pass.log` D1 — that is the load-bearing source of truth for the production value). `infra/env/compute.example:129` declares the env-var *name* under a placeholder value `/opt/shud/bin/shud` (template only; the placeholder is NOT the production path). The Slurm-dispatched hydro run therefore executes `shud`, not `shud_omp`, based on the production env binding captured by D1. The MUST-clauses are satisfied at the deployment envelope, not at the code default.
- **Discovery mechanism**: Phase 4 broad-expanded review packs (`correctness`, `test-evidence`, `spec-compliance`) verified that `grep -n 'shud_executable' workers/shud_runtime/runtime.py` returns `shud_omp` at line 74, contradicting both the spec and the manifest's `source_locations["shud_executable"]` pointer (first found in Round-1). The manifest pointer was fixed by second pre-cert errata (see `design.md`, "Pre-certification manifest v1 second errata" paragraph); the spec/code drift itself is not fixed here.
- **Reason not fixed in this PR**: `§1.1` non-goal for this change is "no production-code change to how any component reports its version". Flipping the Python default would be a semantic behavior change at the Python API layer (any caller that constructs `SHUDRuntimeConfig()` without overriding gets a different binary name), out of `Epic #886` readiness-gate scope.
- **Proposed reconciliation path**: follow-up change flips runtime.py:74/85/95 + slurm_validation.py + example manifest defaults from `shud_omp` to `shud`. This is a mechanical edit but touches production code, and requires its own OpenSpec change + PR + review because it changes runtime code behavior in the absence of the env override (e.g. in local dev, in ad-hoc `python -c`, in unit tests that construct SHUDRuntimeConfig without env). Deferred; not blocking `Epic #886` gate.

## Drift entry 2: `direct-grid-readiness-evidence` spec `Requirement: Minimal-basin execution` scope narrowing

- **Recorded**: 2026-07-07 (during PR#929 §2.5 Phase 4 review; findings test-3, spec-3)
- **Original spec text** (before `§2.5` review): "The platform SHALL execute a minimal basin end-to-end with the production SHUD binary, staging the hand-assembled synthetic multi-station direct-grid evidence package, to confirm the pinned solver stages and runs a standard multi-station direct-grid package."
- **New spec text** (post-`§2.5` review, this PR): "The platform SHALL stage the hand-assembled synthetic multi-station direct-grid evidence package on node-22 and verify the staging invariants ... AND SHALL record the production SHUD binary identity ... End-to-end SHUD simulation of the synthetic minimal basin is deferred to a follow-up change: the synthetic package is a direct-grid CONTRACT fixture, not a full SHUD project ..."
- **Why narrowed**: the `§2.3` synthetic package is a direct-grid CONTRACT fixture (only `.sp.att` with multi-station FORC, `.tsd.forc` multi-station forcing header, per-station CSVs, binding-manifest). It is not a full SHUD project (missing `.sp.mesh`, `.sp.riv`, `.sp.rivseg`, `.para.lc/geol/soil`, `.tsd.lai/mf/rl`, `.cfg.para/calib/ic`). Full end-to-end simulation requires either extending `§2.3` to a full-tree synth basin (out of `§2.3` scope; would re-do `§2.3` evidence), an operator-staged 3-element real basin (out of readiness-gate scope), or grafting into an existing 13-basin production release (element-count mismatch synth=3 vs min production=484 keliya / max=7,800 zhaochen_hhy plus station-count mismatch synth=3 vs min=5 wem / max=1,709 heihe would corrupt any grafted basin). All three paths are proper `Epic #886 + 1` scope items.
- **Discovery mechanism**: Phase 4 `test-evidence` (test-3) and `spec-compliance` (spec-3) reviewer packs both flagged the "SHALL execute end-to-end" wording as unsatisfied by `staging-2.5.node-22.pass.log` D8's explicit deferral of end-to-end simulation.
- **Proposed reconciliation path**: none in this change — the spec is now aligned with the actual scope in this PR. A follow-up change (either full-tree synth basin or operator-staged small real basin) can restore the "end-to-end execute" scenario if hydrology / operations warrant it.
- **Meta contract completeness for entry 2**: (a) spec file+clause cited above (`openspec/changes/cmfd-direct-grid-platform-readiness/specs/direct-grid-readiness-evidence/spec.md` — `Requirement: Minimal-basin staging validation`); (b) **N/A** — this is a spec-scope drift resolved in-PR by narrowing spec text; no code file/line is a party to the drift because no production code changed and no production code contradicted the previous "SHALL execute end-to-end" wording (the runtime's ability to execute end-to-end depends on the presence of a full SHUD project on disk, which is a fixture-composition concern, not a code-behavior concern); (c) discovery mechanism cited above; (d) reconciliation deferred: **not deferred** — narrowed in-PR (spec now matches evidence scope); no follow-up change is required for the platform-readiness gate to close; (e) **N/A** — no deployment/env override reconciles this drift at runtime because the drift is a spec-vs-evidence-scope mismatch, not a spec-vs-runtime-behavior mismatch; runtime behavior was and remains "stage the direct-grid contract when the manifest declares direct-grid".

## Drift entry 3: `tasks.md` §2.3 + §2.4 checkboxes remain unchecked while their evidence artifacts are committed and cited by the §2.7 certification set

- **Recorded**: 2026-07-07 (during PR#931 Phase 4 review; finding spec-1)
- **Spec locus**: `openspec/changes/cmfd-direct-grid-platform-readiness/tasks.md:24` (§2.3 line reads `- [ ] 2.3 Provision the synthetic direct-grid evidence assets and the smoke carrier ...`) + `tasks.md:29` (§2.4 line reads `- [ ] 2.4 Run the real-object-store + real-DB direct-grid smoke on node-27 and capture evidence.`); the durable ledger clause is in `openspec/changes/cmfd-direct-grid-platform-readiness/specs/direct-grid-readiness-evidence/spec.md:80` (Scenario "Checkbox completion does not certify readiness", clause "any drift between OpenSpec task state and code state is recorded in the evidence rather than assumed absent").
- **Code/artifact state**: the §2.3 and §2.4 evidence artifacts are all committed to master and are members of the §2.7 C2 certification set: `openspec/changes/cmfd-direct-grid-platform-readiness/evidence/db-registration-2.3.node-27.pass.log`, `openspec/changes/cmfd-direct-grid-platform-readiness/evidence/smoke-2.4.node-27.pass.log`, and `openspec/changes/cmfd-direct-grid-platform-readiness/evidence/synthetic-package/` (README + `package/` tree + `package.manifest.sha256`). See `certification-2.7.pass.log:14-34` (C2.1 through C2.5) which enumerates them by sha256 as part of the pinned-baseline certification set.
- **Discovery mechanism**: Phase 4 correctness+spec-compliance review of PR #931 (this PR) flagged that the checkbox-vs-artifact-state observation was placed in `validate-3.2.pass.log:21-28` (§3.2 scope) but not in the durable drift-log ledger cited by `certification-2.7.pass.log:34` as C2.5. The §3.2 log is a §3.2 mechanical-gate receipt whose primary purpose is `openspec validate/status/ruff` verdicts; observations about task-state-vs-code-state drift belong in the durable ledger by the meta contract stated at the end of this file, not solely in a §3.2 note comment.
- **Reason not fixed by flipping checkboxes**: per `design.md §"Why checkbox state is not evidence"` (and the mirroring spec Scenario "Checkbox completion does not certify readiness"), the readiness certification is on the pinned-commit artifact set, NOT on tasks.md checkbox state. Flipping §2.3/§2.4 from `[ ]` to `[x]` in this PR would (i) create a per-issue housekeeping obligation across the whole change history to keep checkbox state in sync with committed evidence, (ii) set a precedent that treats checkboxes as an evidence layer — the exact opposite of the design intent, and (iii) provide no additional certification value because the §2.7 certification statement (`certification-2.7.pass.log:6-7`) already CERTIFIES readiness on the pinned artifact set, not on checkbox state. The correct place to record the observation is this drift log, per the meta contract at the end of this file.
- **Reason not fixed by amending the §3.2 receipt**: the §3.2 receipt (`validate-3.2.pass.log`) is a §3.2 mechanical-gate log whose contract is `openspec validate --strict --no-interactive` + `openspec status --change` + `uv run ruff check .`. The current file DOES include a note comment lines 21-28 acknowledging the checkbox-vs-artifact drift, but the durable ledger clause in `spec.md:80` is "recorded in the evidence" (evidence-as-artifact, not evidence-as-inline-note-in-a-different-receipt). Migrating the note to this drift log satisfies the durable-ledger clause without weakening the §3.2 receipt scope.
- **Reconciliation path**: deferred to a follow-up housekeeping change if operations want checkbox-vs-artifact-state parity for tasks.md readability (mechanical: flip `- [ ]` to `- [x]` on lines 24 and 29). Not blocking for readiness certification because the certification set at HEAD is complete and pinned per §2.7 + §3.1 evidence. If the follow-up change lands, it should note in its own drift-log entry that this drift (entry 3) is thereby closed.
- **Meta contract completeness for entry 3**: (a) spec file+clause cited above (`spec.md:80` durable ledger clause; `tasks.md:24` and `tasks.md:29` checkbox lines); (b) **N/A** for production code file/line — this is an OpenSpec-artifact drift (tasks.md checkbox state vs evidence/ artifact presence), not a spec-vs-production-code drift; the "code" in the pair is the evidence-set commit, and evidence-set commit state is: db-registration-2.3.node-27.pass.log committed at baseline, smoke-2.4.node-27.pass.log + synthetic-package/ committed with their respective carriers; (c) discovery mechanism cited above (Phase 4 correctness+spec-compliance review of PR #931); (d) reconciliation deferred to follow-up housekeeping change (mechanical checkbox flip); (e) **N/A** — no deployment/env override applies because this is an OpenSpec-doc/evidence-artifact drift, not a spec-vs-runtime-behavior drift; runtime behavior is entirely governed by the pinned baseline_commit + manifest + evidence set, which are certified in `certification-2.7.pass.log`.

---

## Meta

- This log is `§2.7` **pre-work**, not the `§2.7` receipt itself. `§2.7` proper produces the final "readiness certified on pinned-commit evidence" receipt, at which point this drift log is one of its inputs.
- New drift entries MUST be appended here (chronological order) whenever a `§2.x` review round discovers additional spec/code inconsistency. Do not silently fix them under `Epic #886`; either record and defer, or open a separate change.
- Every entry MUST cite (a) the exact spec file + clause, (b) the exact code file + line, (c) the discovery mechanism (Phase 4 pack name / grep / test failure), (d) whether reconciliation is deferred and to which future change, and (e) whether deployment/env override reconciles at runtime.
