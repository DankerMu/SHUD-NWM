....................F................................................... [  2%]
...................................................FFFF..F.FFFFFFFFFFFF. [  4%]
........................................................................ [  6%]
........................................................................ [  8%]
........................................................................ [ 10%]
..........................................F..F.......................... [ 12%]
................................................................s....... [ 14%]
........................................................................ [ 17%]
........................................................................ [ 19%]
........................................................................ [ 21%]
........................................................................ [ 23%]
........................................................................ [ 25%]
........................................................................ [ 27%]
........................................................................ [ 29%]
........................................................................ [ 31%]
........................................................................ [ 34%]
........................................................................ [ 36%]
........................................................................ [ 38%]
........................................................................ [ 40%]
........F............................................................... [ 42%]
........................................................................ [ 44%]
........................................................................ [ 46%]
........................................................................ [ 48%]
........................................................................ [ 51%]
..............................................................F......... [ 53%]
........................................................................ [ 55%]
........................................................................ [ 57%]
........................................................................ [ 59%]
........................................................................ [ 61%]
........................................................................ [ 63%]
........................................................................ [ 65%]
........................................................................ [ 68%]
........................................................................ [ 70%]
........................................................................ [ 72%]
........................................................................ [ 74%]
........................................................................ [ 76%]
........................................................................ [ 78%]
........................................................................ [ 80%]
........................................................................ [ 82%]
........................................................................ [ 85%]
........................................................................ [ 87%]
........................................................................ [ 89%]
........................................................................ [ 91%]
........................................................................ [ 93%]
........................................................................ [ 95%]
........................................................................ [ 97%]
........................................................................ [ 99%]
..                                                                       [100%]
=================================== FAILURES ===================================
___ test_chain_type_exports_preserve_legacy_identity_and_dataclass_contracts ___

    def test_chain_type_exports_preserve_legacy_identity_and_dataclass_contracts() -> None:
        import services.orchestrator as orchestrator_package
        import services.orchestrator.chain as legacy_chain
        from services.orchestrator import chain_types
    
        type_names = [
            "StageDefinition",
            "ModelContext",
            "ForcingContext",
            "InitialStateSelection",
            "ForecastRunContext",
            "AnalysisRunContext",
            "StageRunResult",
            "PipelineResult",
            "ArrayTaskResult",
            "ArrayAggregation",
            "DisplayLogPublication",
            "DisplayLogPublicationAttempt",
            "TerminalJobObservation",
            "CycleOrchestrationContext",
            "ModelRunAssembly",
        ]
    
        assert legacy_chain.OrchestratorError is chain_types.OrchestratorError
        assert orchestrator_package.OrchestratorError is legacy_chain.OrchestratorError
        assert orchestrator_package.ForecastOrchestrator is legacy_chain.ForecastOrchestrator
        assert orchestrator_package.OrchestratorConfig is legacy_chain.OrchestratorConfig
        assert orchestrator_package.PipelineAlreadyActiveError is legacy_chain.PipelineAlreadyActiveError
        assert orchestrator_package.PsycopgOrchestratorRepository is legacy_chain.PsycopgOrchestratorRepository
        assert orchestrator_package.scenario_for_source is legacy_chain.scenario_for_source
    
        for name in type_names:
            assert getattr(legacy_chain, name) is getattr(chain_types, name)
        assert orchestrator_package.PipelineResult is chain_types.PipelineResult
        assert orchestrator_package.StageRunResult is chain_types.StageRunResult
    
>       assert {
            name: (
                dataclasses.is_dataclass(getattr(chain_types, name)),
                getattr(chain_types, name).__dataclass_params__.frozen,
                _dataclass_field_defaults(getattr(chain_types, name)),
            )
            for name in type_names
        } == {
            "StageDefinition": (
                True,
                True,
                [
                    ("stage", "required"),
                    ("job_type", "required"),
                    ("template_name", "required"),
                    ("success_cycle_status", "required"),
                    ("failure_cycle_status", "required"),
                    ("is_array", False),
                ],
            ),
            "ModelContext": (
                True,
                True,
                [
                    ("model_id", "required"),
                    ("basin_id", "required"),
                    ("basin_version_id", "required"),
                    ("river_network_version_id", "required"),
                    ("segment_count", "required"),
                    ("model_package_uri", "required"),
                    ("output_segment_count", None),
                    ("model_package_checksum", None),
                ],
            ),
            "ForcingContext": (
                True,
                True,
                [
                    ("forcing_version_id", "required"),
                    ("forcing_package_uri", "required"),
                    ("start_time", None),
                    ("end_time", None),
                    ("source_id", None),
                    ("max_lead_hours", None),
                    ("forcing_package_manifest_uri", None),
                    ("forcing_package_manifest_checksum", None),
                ],
            ),
            "InitialStateSelection": (
                True,
                True,
                [
                    ("state_id", "required"),
                    ("state_uri", "required"),
                    ("valid_time", "required"),
                    ("checksum", "required"),
                    ("quality", "required"),
                    ("source_id", None),
                    ("cycle_id", None),
                    ("lead_hours", None),
                    ("model_package_version", None),
                    ("model_package_checksum", None),
                    ("rejection_code", None),
                ],
            ),
            "ForecastRunContext": (
                True,
                True,
                [
                    ("run_id", "required"),
                    ("source_id", "required"),
                    ("scenario_id", "required"),
                    ("cycle_id", "required"),
                    ("cycle_time", "required"),
                    ("model_id", "required"),
                    ("basin_id", "required"),
                    ("basin_version_id", "required"),
                    ("river_network_version_id", "required"),
                    ("segment_count", "required"),
                    ("model_package_uri", "required"),
                    ("forcing_version_id", "required"),
                    ("forcing_package_uri", "required"),
                    ("start_time", "required"),
                    ("end_time", "required"),
                    ("forecast_horizon_hours", "required"),
                    ("run_manifest_uri", "required"),
                    ("output_uri", "required"),
                    ("log_uri", "required"),
                    ("forcing_package_manifest_uri", None),
                    ("forcing_package_manifest_checksum", None),
                    ("init_state_id", None),
                    ("init_state_uri", None),
                    ("init_state_valid_time", None),
                    ("init_state_checksum", None),
                    ("init_state_quality", "cold_start_no_state"),
                    ("init_state_lineage", None),
                    ("output_segment_count", None),
                ],
            ),
            "AnalysisRunContext": (
                True,
                True,
                [
                    ("run_id", "required"),
                    ("source_id", "required"),
                    ("cycle_id", "required"),
                    ("cycle_time", "required"),
                    ("model_id", "required"),
                    ("basin_id", "required"),
                    ("basin_version_id", "required"),
                    ("river_network_version_id", "required"),
                    ("segment_count", "required"),
                    ("model_package_uri", "required"),
                    ("forcing_version_id", "required"),
                    ("forcing_package_uri", "required"),
                    ("start_time", "required"),
                    ("end_time", "required"),
                    ("run_manifest_uri", "required"),
                    ("output_uri", "required"),
                    ("log_uri", "required"),
                    ("forcing_package_manifest_uri", None),
                    ("forcing_package_manifest_checksum", None),
                    ("init_state_id", None),
                    ("init_state_uri", None),
                    ("init_state_valid_time", None),
                    ("output_segment_count", None),
                    ("update_ic_step_minutes", None),
                    ("forcing_causality", None),
                ],
            ),
            "StageRunResult": (
                True,
                True,
                [
                    ("stage", "required"),
                    ("job_type", "required"),
                    ("pipeline_job_id", "required"),
                    ("slurm_job_id", "required"),
                    ("status", "required"),
                    ("exit_code", None),
                    ("error_code", None),
                    ("error_message", None),
                    ("log_uri", None),
                    ("accounting", ("factory", "dict")),
                    ("task_results", ()),
                    ("finished_at", None),
                ],
            ),
            "PipelineResult": (
                True,
                True,
                [
                    ("run_id", "required"),
                    ("cycle_id", "required"),
                    ("status", "required"),
                    ("stages", "required"),
                    ("candidate_outcomes", ()),
                ],
            ),
            "ArrayTaskResult": (
                True,
                True,
                [
                    ("task_id", "required"),
                    ("slurm_job_id", "required"),
                    ("status", "required"),
                    ("exit_code", None),
                    ("error_code", None),
                    ("error_message", None),
                    ("log_uri", None),
                    ("accounting", ("factory", "dict")),
                ],
            ),
            "ArrayAggregation": (
                True,
                True,
                [
                    ("total", "required"),
                    ("succeeded", "required"),
                    ("failed", "required"),
                    ("cancelled", "required"),
                    ("task_results", "required"),
                ],
            ),
            "DisplayLogPublication": (
                True,
                True,
                [
                    ("candidate_uri", "required"),
                    ("advertised_uri", "required"),
                    ("should_persist_logs", "required"),
                ],
            ),
            "DisplayLogPublicationAttempt": (
                True,
                True,
                [
                    ("advertised_uri", "required"),
                    ("error", None),
                ],
            ),
            "TerminalJobObservation": (
                True,
                True,
                [
                    ("job", "required"),
                    ("publication_attempt", None),
                ],
            ),
            "CycleOrchestrationContext": (
                True,
                False,
                [
                    ("source_id", "required"),
                    ("cycle_time", "required"),
                    ("cycle_id", "required"),
                    ("run_id", "required"),
                    ("all_basins", "required"),
                    ("active_basins", "required"),
                    ("restart_stage", None),
                    ("had_partial", False),
                    ("last_partial_status", None),
                    ("task_outcomes", ("factory", "dict")),
                    ("retry_attempt", None),
                    # #1199: invocation-local, at-most-one mixed-cohort forced-
                    # resubmit veto receipt; additive with a default so positional
                    # constructors stay valid.
                    ("forced_resubmit_veto", None),
                    # #2076: invocation-local last canonical precipitation mirror
                    # status, read by the chain-exit recovery; additive with a default.
                    ("canonical_precip_mirror_status", None),
                ],
            ),
            "ModelRunAssembly": (
                True,
                True,
                [
                    ("identity", "required"),
                    ("forcing", "required"),
                    ("runtime", "required"),
                    ("outputs", "required"),
                    ("display", "required"),
                    ("quality_states", "required"),
                    ("residual_blockers", "required"),
                ],
            ),
        }
E       AssertionError: assert {'AnalysisRun...), ...]), ...} == {'AnalysisRun...), ...]), ...}
E         
E         Omitting 14 identical items, use -vv to show
E         Differing items:
E         {'CycleOrchestrationContext': (True, False, [('source_id', 'required'), ('cycle_time', 'required'), ('cycle_id', 'required'), ('run_id', 'required'), ('all_basins', 'required'), ('active_basins', 'required'), ...])} != {'CycleOrchestrationContext': (True, False, [('source_id', 'required'), ('cycle_time', 'required'), ('cycle_id', 'required'), ('run_id', 'required'), ('all_basins', 'required'), ('active_basins', 'required'), ...])}
E         Use -v to get more diff

tests/test_orchestration_chain.py:934: AssertionError
_ test_canonical_precip_mirror_downstream_restart_pass_recovers_a_missing_mirror_at_chain_exit _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c374ec50>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_d0')

    def test_canonical_precip_mirror_downstream_restart_pass_recovers_a_missing_mirror_at_chain_exit(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
    ) -> None:
        """#2076 scenario 1: a `restart_stage == "forecast"` pass skips `convert` at the
        `stage_index < start_stage_index` guard, so the terminal hook never fires; the
        chain-exit recovery is the only thing that can mirror the cycle."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(monkeypatch, tmp_path, repository, client, copyback_root=copyback_root)
        payloads = _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        assert result.status == "succeeded"
        # `convert` was neither submitted nor resumed on this pass.
        assert "convert" not in {submission["stage"] for submission in client.submissions}
        assert "convert" not in {stage.stage for stage in result.stages}
        assert not any(job.get("stage") == "convert" for job in repository.jobs.values())
>       assert _mirrored_files(copyback_root) == payloads
E       assert {} == {'canonical/g... "gfs_0p25"}'}
E         
E         Right contains 3 more items:
E         {'canonical/gfs/2026050100/prcp_rate_or_amount/gfs_2026050100_prcp_rate_or_amount_f003.nc': b'prcp'
E                                                                                                     b':gfs'
E                                                                                                     b':202'
E                                                                                                     b'6050'
E                                                                                                     b'100:'...
E         
E         ...Full output truncated (9 lines hidden), use '-vv' to show

tests/test_orchestration_chain.py:4207: AssertionError
_ test_canonical_precip_mirror_chain_exit_recovery_probes_the_normalized_ifs_spelling _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b360b8d0>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c0')

    def test_canonical_precip_mirror_chain_exit_recovery_probes_the_normalized_ifs_spelling(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
    ) -> None:
        """#2076 on IFS: the recovery's local-tree probe must use the normalized
        `IFS` segment, not the lowercase `ifs` the cycle id carries. On case-sensitive
        Linux a lowercase probe misses `canonical/IFS/...` and the cycle is never
        recovered. macOS is case-insensitive, so the probe's argument is recorded and
        its path parts are asserted directly -- that is what makes this test bite here."""
    
        from packages.common import safe_fs
        from services.orchestrator import chain_forecast_execution
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(monkeypatch, tmp_path, repository, client, copyback_root=copyback_root)
        object_store_root = Path(orchestrator.config.object_store_root)
        payloads = _seed_canonical_precip_tree(object_store_root, storage_source="IFS", cycle_token="2026050100")
        probed: list[Path] = []
    
        def recording_verify_directory_no_follow(path: Any, *args: Any, **kwargs: Any) -> Any:
            probed.append(Path(path))
            return safe_fs.verify_directory_no_follow(path, *args, **kwargs)
    
>       monkeypatch.setattr(chain_forecast_execution, "verify_directory_no_follow", recording_verify_directory_no_follow)
E       AttributeError: <module 'services.orchestrator.chain_forecast_execution' from '/home/nwm/tmp/wt-2447-deploy/services/orchestrator/chain_forecast_execution.py'> has no attribute 'verify_directory_no_follow'

tests/test_orchestration_chain.py:4244: AttributeError
_ test_canonical_precip_mirror_downstream_restart_pass_skips_an_identical_mirror_without_rewriting _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b38d9d90>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_d1')

    def test_canonical_precip_mirror_downstream_restart_pass_skips_an_identical_mirror_without_rewriting(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
    ) -> None:
        """The accepted per-pass cost (#2076): an already-identical tree still yields one
        `skipped` (`trees_already_mirrored`) receipt, and no destination byte is rewritten."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(monkeypatch, tmp_path, repository, client, copyback_root=copyback_root)
        object_store_root = Path(orchestrator.config.object_store_root)
        payloads = _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
        # Both trees -- the cycle and the grid -- or the grid tree plans a copy and the
        # receipt reads `ok`.
        shutil.copytree(object_store_root / "canonical", copyback_root / "canonical")
        before = {
            key: ((copyback_root / key).stat().st_ino, (copyback_root / key).stat().st_mtime_ns)
            for key in payloads
        }
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        assert result.status == "succeeded"
        events = _precip_mirror_events(repository)
>       assert [(event["status_to"], event["message"]) for event in events] == [("skipped", _PRECIP_RECOVERY_MESSAGE)]
E       AssertionError: assert [] == [('skipped', ...chain exit.')]
E         
E         Right contains one more item: ('skipped', 'Canonical precipitation mirror recovered at chain exit.')
E         Use -v to get more diff

tests/test_orchestration_chain.py:4293: AssertionError
_ test_canonical_precip_mirror_failed_hook_is_healed_by_a_later_downstream_restart_pass _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c31165d0>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_f0')

    def test_canonical_precip_mirror_failed_hook_is_healed_by_a_later_downstream_restart_pass(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
    ) -> None:
        """#2076 scenario 2. Pass 1 runs the full chain with the mirror faulted: the
        `convert` hook records `failed` and its same-pass chain-exit retry fails too.
        Pass 2 enters downstream of `convert` after the fault clears and heals it."""
    
        from services.orchestrator import chain_forecast_execution
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(monkeypatch, tmp_path, repository, client, copyback_root=copyback_root)
        payloads = _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
        real_publisher = chain_forecast_execution.TilePublisher
        fault = {"active": True}
    
        class FlakyPublisher(real_publisher):  # type: ignore[misc, valid-type]
            def copyback_canonical_precip(self, source: str, cycle: str) -> dict[str, Any] | None:
                if fault["active"]:
                    raise OSError("copyback mount stalled")
                return super().copyback_canonical_precip(source, cycle)
    
        monkeypatch.setattr(chain_forecast_execution, "TilePublisher", FlakyPublisher)
    
        first = orchestrator.orchestrate_cycle("gfs", "2026050100", _basins(2))
    
        assert first.status == "succeeded"
        assert "convert" in {submission["stage"] for submission in client.submissions}
        first_events = _precip_mirror_events(repository)
>       assert [(event["status_to"], event["message"]) for event in first_events] == [
            ("failed", _PRECIP_HOOK_MESSAGE),
            ("failed", _PRECIP_RECOVERY_MESSAGE),
        ]
E       AssertionError: assert [('failed', '...er convert.')] == [('failed', '...chain exit.')]
E         
E         Right contains one more item: ('failed', 'Canonical precipitation mirror recovered at chain exit.')
E         Use -v to get more diff

tests/test_orchestration_chain.py:4338: AssertionError
_ test_canonical_precip_mirror_failed_hook_with_a_failed_receipt_write_is_still_recovered_at_chain_exit _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c0c72a90>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_f1')

    def test_canonical_precip_mirror_failed_hook_with_a_failed_receipt_write_is_still_recovered_at_chain_exit(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
    ) -> None:
        """The hook's `failed` outcome reaches the chain-exit recovery even when the
        hook's own receipt write raises: the status is recorded on the context before
        that write, so the recovery retries and heals the cycle in the same pass."""
    
        from services.orchestrator import chain_forecast_execution
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(monkeypatch, tmp_path, repository, client, copyback_root=copyback_root)
        payloads = _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
        real_publisher = chain_forecast_execution.TilePublisher
        mirror_attempts: list[str] = []
    
        class FirstAttemptFailsPublisher(real_publisher):  # type: ignore[misc, valid-type]
            def copyback_canonical_precip(self, source: str, cycle: str) -> dict[str, Any] | None:
                mirror_attempts.append(cycle)
                if len(mirror_attempts) == 1:
                    raise OSError("copyback mount stalled")
                return super().copyback_canonical_precip(source, cycle)
    
        monkeypatch.setattr(chain_forecast_execution, "TilePublisher", FirstAttemptFailsPublisher)
        real_insert = repository.insert_pipeline_event
        receipt_writes: list[tuple[str, str]] = []
    
        def first_receipt_write_raises(**kwargs: Any) -> dict[str, Any]:
            if kwargs.get("event_type") == "canonical_precip_mirror":
                receipt_writes.append((str(kwargs.get("status_to")), str(kwargs.get("message"))))
                if len(receipt_writes) == 1:
                    raise OrchestratorError("PIPELINE_EVENT_FAILED", "event write failed")
            return real_insert(**kwargs)
    
        monkeypatch.setattr(repository, "insert_pipeline_event", first_receipt_write_raises)
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", _basins(2))
    
        assert result.status == "succeeded"
        assert "convert" in {submission["stage"] for submission in client.submissions}
>       assert mirror_attempts == ["2026050100", "2026050100"]
E       AssertionError: assert ['2026050100'] == ['2026050100', '2026050100']
E         
E         Right contains one more item: '2026050100'
E         Use -v to get more diff

tests/test_orchestration_chain.py:4448: AssertionError
_ test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[tree_absent] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b3576410>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c1')
case = 'tree_absent'

    @pytest.mark.parametrize(
        "case",
        ["tree_absent", "symlinked_leaf", "symlinked_ancestor", "no_copyback_root"],
    )
    def test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        case: str,
    ) -> None:
        """The recovery predicate refuses a symlink at ANY component of
        `canonical/<S>/<cycle>/prcp_rate_or_amount` (not just the leaf), treats an
        absent tree as nothing to recover (a retention-pruned cycle), and is fenced by
        `NHMS_OBJECT_STORE_COPYBACK_ROOT` -- in every case no receipt is appended."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch,
            tmp_path,
            repository,
            client,
            copyback_root=None if case == "no_copyback_root" else copyback_root,
        )
        object_store_root = Path(orchestrator.config.object_store_root)
        cycle_dir = object_store_root / "canonical/gfs/2026050100"
        if case == "tree_absent":
            # Grid only: the source tree the recovery keys on was pruned.
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
        elif case == "symlinked_leaf":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir / "prcp_rate_or_amount")
            (cycle_dir / "prcp_rate_or_amount").symlink_to(outside / "canonical/gfs/2026050100/prcp_rate_or_amount")
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        elif case == "symlinked_ancestor":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
            cycle_dir.symlink_to(outside / "canonical/gfs/2026050100")
            # The leaf itself is a real directory; only its parent is a link.
            assert not (cycle_dir / "prcp_rate_or_amount").is_symlink()
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        else:
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
        from packages.common import safe_fs
        from services.orchestrator import chain_forecast_execution
    
        probed: list[Path] = []
    
        def recording_verify_directory_no_follow(path: Any, *args: Any, **kwargs: Any) -> Any:
            probed.append(Path(path))
            return safe_fs.verify_directory_no_follow(path, *args, **kwargs)
    
>       monkeypatch.setattr(chain_forecast_execution, "verify_directory_no_follow", recording_verify_directory_no_follow)
E       AttributeError: <module 'services.orchestrator.chain_forecast_execution' from '/home/nwm/tmp/wt-2447-deploy/services/orchestrator/chain_forecast_execution.py'> has no attribute 'verify_directory_no_follow'

tests/test_orchestration_chain.py:4544: AttributeError
_ test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[symlinked_leaf] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b3a0bc10>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c2')
case = 'symlinked_leaf'

    @pytest.mark.parametrize(
        "case",
        ["tree_absent", "symlinked_leaf", "symlinked_ancestor", "no_copyback_root"],
    )
    def test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        case: str,
    ) -> None:
        """The recovery predicate refuses a symlink at ANY component of
        `canonical/<S>/<cycle>/prcp_rate_or_amount` (not just the leaf), treats an
        absent tree as nothing to recover (a retention-pruned cycle), and is fenced by
        `NHMS_OBJECT_STORE_COPYBACK_ROOT` -- in every case no receipt is appended."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch,
            tmp_path,
            repository,
            client,
            copyback_root=None if case == "no_copyback_root" else copyback_root,
        )
        object_store_root = Path(orchestrator.config.object_store_root)
        cycle_dir = object_store_root / "canonical/gfs/2026050100"
        if case == "tree_absent":
            # Grid only: the source tree the recovery keys on was pruned.
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
        elif case == "symlinked_leaf":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir / "prcp_rate_or_amount")
            (cycle_dir / "prcp_rate_or_amount").symlink_to(outside / "canonical/gfs/2026050100/prcp_rate_or_amount")
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        elif case == "symlinked_ancestor":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
            cycle_dir.symlink_to(outside / "canonical/gfs/2026050100")
            # The leaf itself is a real directory; only its parent is a link.
            assert not (cycle_dir / "prcp_rate_or_amount").is_symlink()
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        else:
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
        from packages.common import safe_fs
        from services.orchestrator import chain_forecast_execution
    
        probed: list[Path] = []
    
        def recording_verify_directory_no_follow(path: Any, *args: Any, **kwargs: Any) -> Any:
            probed.append(Path(path))
            return safe_fs.verify_directory_no_follow(path, *args, **kwargs)
    
>       monkeypatch.setattr(chain_forecast_execution, "verify_directory_no_follow", recording_verify_directory_no_follow)
E       AttributeError: <module 'services.orchestrator.chain_forecast_execution' from '/home/nwm/tmp/wt-2447-deploy/services/orchestrator/chain_forecast_execution.py'> has no attribute 'verify_directory_no_follow'

tests/test_orchestration_chain.py:4544: AttributeError
_ test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[symlinked_ancestor] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b434f210>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c3')
case = 'symlinked_ancestor'

    @pytest.mark.parametrize(
        "case",
        ["tree_absent", "symlinked_leaf", "symlinked_ancestor", "no_copyback_root"],
    )
    def test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        case: str,
    ) -> None:
        """The recovery predicate refuses a symlink at ANY component of
        `canonical/<S>/<cycle>/prcp_rate_or_amount` (not just the leaf), treats an
        absent tree as nothing to recover (a retention-pruned cycle), and is fenced by
        `NHMS_OBJECT_STORE_COPYBACK_ROOT` -- in every case no receipt is appended."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch,
            tmp_path,
            repository,
            client,
            copyback_root=None if case == "no_copyback_root" else copyback_root,
        )
        object_store_root = Path(orchestrator.config.object_store_root)
        cycle_dir = object_store_root / "canonical/gfs/2026050100"
        if case == "tree_absent":
            # Grid only: the source tree the recovery keys on was pruned.
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
        elif case == "symlinked_leaf":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir / "prcp_rate_or_amount")
            (cycle_dir / "prcp_rate_or_amount").symlink_to(outside / "canonical/gfs/2026050100/prcp_rate_or_amount")
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        elif case == "symlinked_ancestor":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
            cycle_dir.symlink_to(outside / "canonical/gfs/2026050100")
            # The leaf itself is a real directory; only its parent is a link.
            assert not (cycle_dir / "prcp_rate_or_amount").is_symlink()
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        else:
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
        from packages.common import safe_fs
        from services.orchestrator import chain_forecast_execution
    
        probed: list[Path] = []
    
        def recording_verify_directory_no_follow(path: Any, *args: Any, **kwargs: Any) -> Any:
            probed.append(Path(path))
            return safe_fs.verify_directory_no_follow(path, *args, **kwargs)
    
>       monkeypatch.setattr(chain_forecast_execution, "verify_directory_no_follow", recording_verify_directory_no_follow)
E       AttributeError: <module 'services.orchestrator.chain_forecast_execution' from '/home/nwm/tmp/wt-2447-deploy/services/orchestrator/chain_forecast_execution.py'> has no attribute 'verify_directory_no_follow'

tests/test_orchestration_chain.py:4544: AttributeError
_ test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[no_copyback_root] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c4ccfe10>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c4')
case = 'no_copyback_root'

    @pytest.mark.parametrize(
        "case",
        ["tree_absent", "symlinked_leaf", "symlinked_ancestor", "no_copyback_root"],
    )
    def test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        case: str,
    ) -> None:
        """The recovery predicate refuses a symlink at ANY component of
        `canonical/<S>/<cycle>/prcp_rate_or_amount` (not just the leaf), treats an
        absent tree as nothing to recover (a retention-pruned cycle), and is fenced by
        `NHMS_OBJECT_STORE_COPYBACK_ROOT` -- in every case no receipt is appended."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch,
            tmp_path,
            repository,
            client,
            copyback_root=None if case == "no_copyback_root" else copyback_root,
        )
        object_store_root = Path(orchestrator.config.object_store_root)
        cycle_dir = object_store_root / "canonical/gfs/2026050100"
        if case == "tree_absent":
            # Grid only: the source tree the recovery keys on was pruned.
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
        elif case == "symlinked_leaf":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir / "prcp_rate_or_amount")
            (cycle_dir / "prcp_rate_or_amount").symlink_to(outside / "canonical/gfs/2026050100/prcp_rate_or_amount")
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        elif case == "symlinked_ancestor":
            outside = tmp_path / "outside"
            _seed_canonical_precip_tree(outside, storage_source="gfs", cycle_token="2026050100")
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
            shutil.rmtree(cycle_dir)
            cycle_dir.symlink_to(outside / "canonical/gfs/2026050100")
            # The leaf itself is a real directory; only its parent is a link.
            assert not (cycle_dir / "prcp_rate_or_amount").is_symlink()
            assert (cycle_dir / "prcp_rate_or_amount").is_dir()
        else:
            _seed_canonical_precip_tree(object_store_root, storage_source="gfs", cycle_token="2026050100")
        from packages.common import safe_fs
        from services.orchestrator import chain_forecast_execution
    
        probed: list[Path] = []
    
        def recording_verify_directory_no_follow(path: Any, *args: Any, **kwargs: Any) -> Any:
            probed.append(Path(path))
            return safe_fs.verify_directory_no_follow(path, *args, **kwargs)
    
>       monkeypatch.setattr(chain_forecast_execution, "verify_directory_no_follow", recording_verify_directory_no_follow)
E       AttributeError: <module 'services.orchestrator.chain_forecast_execution' from '/home/nwm/tmp/wt-2447-deploy/services/orchestrator/chain_forecast_execution.py'> has no attribute 'verify_directory_no_follow'

tests/test_orchestration_chain.py:4544: AttributeError
_ test_canonical_precip_mirror_chain_exit_recovery_is_fail_open[publisher_raises] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b36b38d0>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c5')
fault = 'publisher_raises'

    @pytest.mark.parametrize("fault", ["publisher_raises", "mirror_raises", "receipt_write_raises"])
    def test_canonical_precip_mirror_chain_exit_recovery_is_fail_open(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        fault: str,
    ) -> None:
        """#2076 scenario 4: whatever the recovery's mirror or receipt write raises,
        `_run_cycle_chain` returns what it would have returned without the recovery."""
    
        from services.orchestrator import chain_forecast_execution
    
        baseline_repository = FakeCycleRepository()
        baseline = _precip_chain_orchestrator(
            monkeypatch, tmp_path / "baseline", baseline_repository, FakeCycleSlurmClient(), copyback_root=None
        ).orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        repository = FakeCycleRepository()
        copyback_root = tmp_path / "faulted" / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch, tmp_path / "faulted", repository, FakeCycleSlurmClient(), copyback_root=copyback_root
        )
        _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
        attempts: list[str] = []
        if fault == "publisher_raises":
    
            class ExplodingPublisher:
                def __init__(self, **kwargs: Any) -> None:
                    del kwargs
    
                def copyback_canonical_precip(self, source: str, cycle: str) -> dict[str, Any] | None:
                    attempts.append(cycle)
                    raise RuntimeError("mirror exploded")
    
            monkeypatch.setattr(chain_forecast_execution, "TilePublisher", ExplodingPublisher)
        elif fault == "mirror_raises":
    
            def exploding_mirror(self: Any, context: Any, **kwargs: Any) -> None:
                attempts.append(str(kwargs.get("message")))
                raise RuntimeError("mirror entry exploded")
    
            monkeypatch.setattr(chain_forecast_execution, "_mirror_canonical_precip", exploding_mirror)
        else:
            real_insert = repository.insert_pipeline_event
    
            def failing_insert_pipeline_event(**kwargs: Any) -> dict[str, Any]:
                if kwargs.get("event_type") == "canonical_precip_mirror":
                    attempts.append(str(kwargs.get("message")))
                    raise OrchestratorError("PIPELINE_EVENT_FAILED", "event write failed")
                return real_insert(**kwargs)
    
            monkeypatch.setattr(repository, "insert_pipeline_event", failing_insert_pipeline_event)
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        assert _downstream_restart_outcome(result) == _downstream_restart_outcome(baseline)
        # The recovery really ran into the fault -- otherwise a green here proves nothing.
>       assert len(attempts) == 1
E       assert 0 == 1
E        +  where 0 = len([])

tests/test_orchestration_chain.py:4619: AssertionError
_ test_canonical_precip_mirror_chain_exit_recovery_is_fail_open[mirror_raises] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c0e88850>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c6')
fault = 'mirror_raises'

    @pytest.mark.parametrize("fault", ["publisher_raises", "mirror_raises", "receipt_write_raises"])
    def test_canonical_precip_mirror_chain_exit_recovery_is_fail_open(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        fault: str,
    ) -> None:
        """#2076 scenario 4: whatever the recovery's mirror or receipt write raises,
        `_run_cycle_chain` returns what it would have returned without the recovery."""
    
        from services.orchestrator import chain_forecast_execution
    
        baseline_repository = FakeCycleRepository()
        baseline = _precip_chain_orchestrator(
            monkeypatch, tmp_path / "baseline", baseline_repository, FakeCycleSlurmClient(), copyback_root=None
        ).orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        repository = FakeCycleRepository()
        copyback_root = tmp_path / "faulted" / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch, tmp_path / "faulted", repository, FakeCycleSlurmClient(), copyback_root=copyback_root
        )
        _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
        attempts: list[str] = []
        if fault == "publisher_raises":
    
            class ExplodingPublisher:
                def __init__(self, **kwargs: Any) -> None:
                    del kwargs
    
                def copyback_canonical_precip(self, source: str, cycle: str) -> dict[str, Any] | None:
                    attempts.append(cycle)
                    raise RuntimeError("mirror exploded")
    
            monkeypatch.setattr(chain_forecast_execution, "TilePublisher", ExplodingPublisher)
        elif fault == "mirror_raises":
    
            def exploding_mirror(self: Any, context: Any, **kwargs: Any) -> None:
                attempts.append(str(kwargs.get("message")))
                raise RuntimeError("mirror entry exploded")
    
            monkeypatch.setattr(chain_forecast_execution, "_mirror_canonical_precip", exploding_mirror)
        else:
            real_insert = repository.insert_pipeline_event
    
            def failing_insert_pipeline_event(**kwargs: Any) -> dict[str, Any]:
                if kwargs.get("event_type") == "canonical_precip_mirror":
                    attempts.append(str(kwargs.get("message")))
                    raise OrchestratorError("PIPELINE_EVENT_FAILED", "event write failed")
                return real_insert(**kwargs)
    
            monkeypatch.setattr(repository, "insert_pipeline_event", failing_insert_pipeline_event)
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        assert _downstream_restart_outcome(result) == _downstream_restart_outcome(baseline)
        # The recovery really ran into the fault -- otherwise a green here proves nothing.
>       assert len(attempts) == 1
E       assert 0 == 1
E        +  where 0 = len([])

tests/test_orchestration_chain.py:4619: AssertionError
_ test_canonical_precip_mirror_chain_exit_recovery_is_fail_open[receipt_write_raises] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23be52f250>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c7')
fault = 'receipt_write_raises'

    @pytest.mark.parametrize("fault", ["publisher_raises", "mirror_raises", "receipt_write_raises"])
    def test_canonical_precip_mirror_chain_exit_recovery_is_fail_open(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        fault: str,
    ) -> None:
        """#2076 scenario 4: whatever the recovery's mirror or receipt write raises,
        `_run_cycle_chain` returns what it would have returned without the recovery."""
    
        from services.orchestrator import chain_forecast_execution
    
        baseline_repository = FakeCycleRepository()
        baseline = _precip_chain_orchestrator(
            monkeypatch, tmp_path / "baseline", baseline_repository, FakeCycleSlurmClient(), copyback_root=None
        ).orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        repository = FakeCycleRepository()
        copyback_root = tmp_path / "faulted" / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(
            monkeypatch, tmp_path / "faulted", repository, FakeCycleSlurmClient(), copyback_root=copyback_root
        )
        _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
        attempts: list[str] = []
        if fault == "publisher_raises":
    
            class ExplodingPublisher:
                def __init__(self, **kwargs: Any) -> None:
                    del kwargs
    
                def copyback_canonical_precip(self, source: str, cycle: str) -> dict[str, Any] | None:
                    attempts.append(cycle)
                    raise RuntimeError("mirror exploded")
    
            monkeypatch.setattr(chain_forecast_execution, "TilePublisher", ExplodingPublisher)
        elif fault == "mirror_raises":
    
            def exploding_mirror(self: Any, context: Any, **kwargs: Any) -> None:
                attempts.append(str(kwargs.get("message")))
                raise RuntimeError("mirror entry exploded")
    
            monkeypatch.setattr(chain_forecast_execution, "_mirror_canonical_precip", exploding_mirror)
        else:
            real_insert = repository.insert_pipeline_event
    
            def failing_insert_pipeline_event(**kwargs: Any) -> dict[str, Any]:
                if kwargs.get("event_type") == "canonical_precip_mirror":
                    attempts.append(str(kwargs.get("message")))
                    raise OrchestratorError("PIPELINE_EVENT_FAILED", "event write failed")
                return real_insert(**kwargs)
    
            monkeypatch.setattr(repository, "insert_pipeline_event", failing_insert_pipeline_event)
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        assert _downstream_restart_outcome(result) == _downstream_restart_outcome(baseline)
        # The recovery really ran into the fault -- otherwise a green here proves nothing.
>       assert len(attempts) == 1
E       assert 0 == 1
E        +  where 0 = len([])

tests/test_orchestration_chain.py:4619: AssertionError
_ test_canonical_precip_mirror_chain_exit_recovery_runs_when_a_downstream_stage_raises _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b34749d0>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_c8')

    def test_canonical_precip_mirror_chain_exit_recovery_runs_when_a_downstream_stage_raises(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
    ) -> None:
        """Every exit counts: a stage raising `OrchestratorError` on a downstream-restart
        pass still gets the recovery, and the original exception is what propagates."""
    
        repository = FakeCycleRepository()
        client = FakeCycleSlurmClient()
        copyback_root = tmp_path / "shared-object-store"
        orchestrator = _precip_chain_orchestrator(monkeypatch, tmp_path, repository, client, copyback_root=copyback_root)
        payloads = _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="gfs", cycle_token="2026050100"
        )
        raised = OrchestratorError("STAGE_EXPLODED", "forecast stage exploded")
        stages_entered: list[str] = []
    
        def exploding_stage(stage: StageDefinition, context: Any, **kwargs: Any) -> Any:
            del context, kwargs
            stages_entered.append(stage.stage)
            raise raised
    
        monkeypatch.setattr(orchestrator, "_submit_and_wait_cycle_stage", exploding_stage)
    
        with pytest.raises(OrchestratorError) as error_info:
            orchestrator.orchestrate_cycle("gfs", "2026050100", _downstream_restart_basins())
    
        assert error_info.value is raised
        assert stages_entered == ["forecast"]
        events = _precip_mirror_events(repository)
>       assert [(event["status_to"], event["message"]) for event in events] == [("ok", _PRECIP_RECOVERY_MESSAGE)]
E       AssertionError: assert [] == [('ok', 'Cano...chain exit.')]
E         
E         Right contains one more item: ('ok', 'Canonical precipitation mirror recovered at chain exit.')
E         Use -v to get more diff

tests/test_orchestration_chain.py:4662: AssertionError
_ test_canonical_precip_mirror_runs_after_the_entry_cycle_status_write[succeeded] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b37c3bd0>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_r0')
result_status = 'succeeded'

    @pytest.mark.parametrize("result_status", ["succeeded", "failed"])
    def test_canonical_precip_mirror_runs_after_the_entry_cycle_status_write(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        result_status: str,
    ) -> None:
        """#2070: a stalled copyback mount must not delay the `convert` entry's own
        cycle-status write, so that write is recorded before the mirror starts -- on
        `succeeded` and on the failure tail alike."""
    
        calls: list[tuple[str, str]] = []
        repository = _CallOrderCycleRepository(calls)
        orchestrator = _orchestrator(tmp_path, repository, FakeCycleSlurmClient(), terminal_stage="forecast_state_save_qc")
        copyback_root = tmp_path / "shared-object-store"
        monkeypatch.setenv("NHMS_OBJECT_STORE_COPYBACK_ROOT", str(copyback_root))
        _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="IFS", cycle_token="2026050100"
        )
        _record_mirror_start(monkeypatch, calls)
    
        orchestrator._after_cycle_stage_terminal(
            _PRECIP_CONVERT_STAGE, _precip_context(), result_status, {"status": result_status}, None
        )
    
        expected_status = (
            _PRECIP_CONVERT_STAGE.success_cycle_status
            if result_status == "succeeded"
            else _PRECIP_CONVERT_STAGE.failure_cycle_status
        )
>       assert calls == [("cycle_status", expected_status), ("mirror_start", "2026050100")]
E       AssertionError: assert [('mirror_sta...nical_ready')] == [('cycle_stat...'2026050100')]
E         
E         At index 0 diff: ('mirror_start', '2026050100') != ('cycle_status', 'canonical_ready')
E         Use -v to get more diff

tests/test_orchestration_chain.py:4728: AssertionError
_ test_canonical_precip_mirror_runs_after_the_entry_cycle_status_write[failed] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c03076d0>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_r1')
result_status = 'failed'

    @pytest.mark.parametrize("result_status", ["succeeded", "failed"])
    def test_canonical_precip_mirror_runs_after_the_entry_cycle_status_write(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        result_status: str,
    ) -> None:
        """#2070: a stalled copyback mount must not delay the `convert` entry's own
        cycle-status write, so that write is recorded before the mirror starts -- on
        `succeeded` and on the failure tail alike."""
    
        calls: list[tuple[str, str]] = []
        repository = _CallOrderCycleRepository(calls)
        orchestrator = _orchestrator(tmp_path, repository, FakeCycleSlurmClient(), terminal_stage="forecast_state_save_qc")
        copyback_root = tmp_path / "shared-object-store"
        monkeypatch.setenv("NHMS_OBJECT_STORE_COPYBACK_ROOT", str(copyback_root))
        _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="IFS", cycle_token="2026050100"
        )
        _record_mirror_start(monkeypatch, calls)
    
        orchestrator._after_cycle_stage_terminal(
            _PRECIP_CONVERT_STAGE, _precip_context(), result_status, {"status": result_status}, None
        )
    
        expected_status = (
            _PRECIP_CONVERT_STAGE.success_cycle_status
            if result_status == "succeeded"
            else _PRECIP_CONVERT_STAGE.failure_cycle_status
        )
>       assert calls == [("cycle_status", expected_status), ("mirror_start", "2026050100")]
E       AssertionError: assert [('mirror_sta...led_convert')] == [('cycle_stat...'2026050100')]
E         
E         At index 0 diff: ('mirror_start', '2026050100') != ('cycle_status', 'failed_convert')
E         Use -v to get more diff

tests/test_orchestration_chain.py:4728: AssertionError
_ test_canonical_precip_mirror_still_runs_when_the_entry_cycle_status_write_raises[succeeded] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b3551b10>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_s0')
result_status = 'succeeded'

    @pytest.mark.parametrize("result_status", ["succeeded", "failed"])
    def test_canonical_precip_mirror_still_runs_when_the_entry_cycle_status_write_raises(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        result_status: str,
    ) -> None:
        calls: list[tuple[str, str]] = []
        repository = _CallOrderCycleRepository(calls, fail_status_write=True)
        orchestrator = _orchestrator(tmp_path, repository, FakeCycleSlurmClient(), terminal_stage="forecast_state_save_qc")
        copyback_root = tmp_path / "shared-object-store"
        monkeypatch.setenv("NHMS_OBJECT_STORE_COPYBACK_ROOT", str(copyback_root))
        payloads = _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="IFS", cycle_token="2026050100"
        )
        _record_mirror_start(monkeypatch, calls)
    
        with pytest.raises(_CycleStatusWriteFailed, match="cycle status write failed"):
            orchestrator._after_cycle_stage_terminal(
                _PRECIP_CONVERT_STAGE, _precip_context(), result_status, {"status": result_status}, None
            )
    
>       assert [name for name, _ in calls] == ["cycle_status", "mirror_start"]
E       AssertionError: assert ['mirror_star...cycle_status'] == ['cycle_statu...mirror_start']
E         
E         At index 0 diff: 'mirror_start' != 'cycle_status'
E         Use -v to get more diff

tests/test_orchestration_chain.py:4753: AssertionError
_ test_canonical_precip_mirror_still_runs_when_the_entry_cycle_status_write_raises[failed] _

monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23b3604f10>
tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_canonical_precip_mirror_s1')
result_status = 'failed'

    @pytest.mark.parametrize("result_status", ["succeeded", "failed"])
    def test_canonical_precip_mirror_still_runs_when_the_entry_cycle_status_write_raises(
        monkeypatch: pytest.MonkeyPatch,
        tmp_path: Path,
        result_status: str,
    ) -> None:
        calls: list[tuple[str, str]] = []
        repository = _CallOrderCycleRepository(calls, fail_status_write=True)
        orchestrator = _orchestrator(tmp_path, repository, FakeCycleSlurmClient(), terminal_stage="forecast_state_save_qc")
        copyback_root = tmp_path / "shared-object-store"
        monkeypatch.setenv("NHMS_OBJECT_STORE_COPYBACK_ROOT", str(copyback_root))
        payloads = _seed_canonical_precip_tree(
            Path(orchestrator.config.object_store_root), storage_source="IFS", cycle_token="2026050100"
        )
        _record_mirror_start(monkeypatch, calls)
    
        with pytest.raises(_CycleStatusWriteFailed, match="cycle status write failed"):
            orchestrator._after_cycle_stage_terminal(
                _PRECIP_CONVERT_STAGE, _precip_context(), result_status, {"status": result_status}, None
            )
    
>       assert [name for name, _ in calls] == ["cycle_status", "mirror_start"]
E       AssertionError: assert ['mirror_star...cycle_status'] == ['cycle_statu...mirror_start']
E         
E         At index 0 diff: 'mirror_start' != 'cycle_status'
E         Use -v to get more diff

tests/test_orchestration_chain.py:4753: AssertionError
_ test_stacked_retry_ids_mint_next_attempt_past_last_suffix_through_orchestrate_cycle _

tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_stacked_retry_ids_mint_ne0')

    def test_stacked_retry_ids_mint_next_attempt_past_last_suffix_through_orchestrate_cycle(tmp_path: Path) -> None:
        """#2254: stacked retry ids must not be under-counted by the cycle-stage retry minting.
    
        Constructed evidence, public entry point: the ``retry_missing_forecast_output``
        decision is hand-built (not derived by its scheduler producer from a real state),
        and ``context.retry_attempt`` is unset, so the attempt comes from the job rows.
        ``B_retry_1_retry_2_retry_3`` carries attempt 3 in its last suffix
        (``retry_identity.retry_suffix_attempt``), so the next attempt is 4.  Before the fix
        the whole tail after ``B_retry_`` was parsed, the stacked rows were skipped, and
        ``B_retry_2`` was minted.
        """
    
        store, reserve_records, repository = _stacked_retry_repository()
        client = FakeCycleSlurmClient()
        orchestrator = _orchestrator(tmp_path, repository, client)
        basins = _basins(1)
        basins[0].update(
            {
                "orchestration_run_id": _STACKED_RUN_ID,
                "restart_stage": "forecast",
                "state_evidence": {
                    "decision": "retry_missing_forecast_output",
                    "restart_stage": "forecast",
                    "retry_policy": {
                        "automatic_retry_allowed": True,
                        "manual_retry_required": False,
                        "override_reason": "missing_forecast_output_recompute",
                    },
                },
            }
        )
    
        result = orchestrator.orchestrate_cycle("gfs", "2026050100", basins)
    
        forecast = result.stages[0]
        assert forecast.stage == "forecast"
>       assert forecast.pipeline_job_id == f"{_STACKED_BASE_JOB_ID}_retry_4"
E       AssertionError: assert 'job_cycle_gf...ecast_retry_2' == 'job_cycle_gf...ecast_retry_4'
E         
E         Skipping 47 identical leading characters in diff, use -v to show
E         - ast_retry_4
E         ?           ^
E         + ast_retry_2
E         ?           ^

tests/test_orchestration_chain.py:16081: AssertionError
____ test_explicit_context_retry_attempt_keeps_precedence_over_stacked_rows ____

    def test_explicit_context_retry_attempt_keeps_precedence_over_stacked_rows() -> None:
        """#2254 preservation: an explicit ``context.retry_attempt`` wins over the row derivation."""
    
        stage = M3_STAGES[2]
        run_id = _STACKED_RUN_ID
        base_job_id = f"job_{run_id}_{stage.stage}"
        jobs = [_retry_row(f"{base_job_id}_retry_1_retry_2_retry_3")]
        orchestrator = types.SimpleNamespace()
        derived = ForecastOrchestrator._retry_cycle_stage_job_id(
            orchestrator,  # type: ignore[arg-type]
            types.SimpleNamespace(run_id=run_id, retry_attempt=None),  # type: ignore[arg-type]
            stage,
            jobs,
        )
        explicit = ForecastOrchestrator._retry_cycle_stage_job_id(
            orchestrator,  # type: ignore[arg-type]
            types.SimpleNamespace(run_id=run_id, retry_attempt=2),  # type: ignore[arg-type]
            stage,
            jobs,
        )
>       assert derived == f"{base_job_id}_retry_4"
E       AssertionError: assert 'job_cycle_gf...ecast_retry_1' == 'job_cycle_gf...ecast_retry_4'
E         
E         Skipping 47 identical leading characters in diff, use -v to show
E         - ast_retry_4
E         ?           ^
E         + ast_retry_1
E         ?           ^

tests/test_orchestration_chain.py:16150: AssertionError
____ test_retry_with_stale_cold_manifest_is_upgraded_to_warm_forecast_rerun ____

    def test_retry_with_stale_cold_manifest_is_upgraded_to_warm_forecast_rerun() -> None:
        decision = CandidateStateDecision(
            action="retry",
            reason="resume_after_completed_stage",
            evidence={
                "decision": "retry_after_completed_stage",
                "restart_stage": "forcing",
                "run_manifest_initial_state": {
                    "state_id": None,
                    "quality": "cold_start_no_state",
                },
            },
        )
        strict = {
            "ready": True,
            "candidate_state": {
                "init_state_id": "state_gfs_model_a_2026052112",
                "init_state_uri": "s3://nhms/states/gfs/model_a/2026052112/state.cfg.ic",
                "init_state_quality": "fresh",
                "init_state_valid_time": "2026-05-21T12:00:00Z",
            },
        }
    
        upgraded = scheduler_candidates_module._upgrade_retry_for_strict_warm_start_manifest(
            decision,
            strict,
        )
    
        assert upgraded is not None
>       assert upgraded.reason == "strict_warm_start_retry_run_manifest_mismatch"
E       AssertionError: assert 'resume_after_completed_stage' == 'strict_warm_...fest_mismatch'
E         
E         - strict_warm_start_retry_run_manifest_mismatch
E         + resume_after_completed_stage

tests/test_production_scheduler.py:195: AssertionError
_ test_missing_forcing_repair_rechecks_blocker_created_by_warm_manifest_upgrade _

tmp_path = PosixPath('/home/nwm/tmp/pytest-of-nwm/pytest-219/test_missing_forcing_repair_re0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f23c0e44fd0>

    def test_missing_forcing_repair_rechecks_blocker_created_by_warm_manifest_upgrade(
        tmp_path: Path,
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        cycle_time = _dt("2026-05-21T06:00:00Z")
        candidate = _scheduler_candidate_fixture()
        raw_root = tmp_path / "nfs-raw-post-upgrade"
        readiness = _write_missing_forcing_repair_raw_manifest(raw_root, cycle_time=cycle_time)
        state = {
            **_production_identity_fixture(),
            "candidate_id": candidate.candidate_id,
            "nfs_raw_manifest": readiness,
        }
        monkeypatch.setattr(
            scheduler_module,
            "_candidate_state_decision",
            lambda *_args: CandidateStateDecision(
                "retry",
                "resume_after_completed_stage",
                {
                    "decision": "retry_after_completed_stage",
                    "reason": "resume_after_completed_stage",
                    "restart_stage": "forcing",
                    "run_manifest_initial_state": {
                        "state_id": None,
                        "quality": "cold_start_no_state",
                    },
                },
            ),
        )
        scheduler = ProductionScheduler(
            _missing_forcing_repair_config(
                tmp_path,
                monkeypatch,
                cycle_time=cycle_time,
                dry_run=True,
                raw_root=raw_root,
            ),
            registry=FakeRegistry(
                [_model("model_a", "basin_a", resource_profile=_missing_forcing_repair_direct_grid_profile())]
            ),
            adapters={"gfs": FakeAdapter("gfs", [("2026-05-21T06:00:00Z", True)])},
            active_repository=RawCandidateStateRepository(state),
            canonical_readiness_provider=_AlwaysReadyCanonicalReadinessProvider(),
            orchestrator_factory=lambda _source_id: pytest.fail("plan mode must not submit"),
        )
        scheduler._strict_warm_start_for_candidate = (  # type: ignore[method-assign]
            lambda *_args: _complete_missing_forcing_repair_warm_state()
        )
    
        result = scheduler.run_once()
    
        assert result.status == "planned"
        assert result.evidence["blocked_candidates"] == []
        state_evidence = result.evidence["candidates"][0]["state_evidence"]
>       assert state_evidence["missing_forcing_repair"]["status"] == "authorized"
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       KeyError: 'missing_forcing_repair'

tests/test_production_scheduler.py:14773: KeyError
----------------------------- Captured stdout call -----------------------------
{"schema_version": "nhms.scheduler_pass_timing.v1", "ts": "2026-09-17T05:46:49.917119Z", "pass_id": "scheduler_2026052112_ea8d6fc60e40", "level": "stage", "phase": "pass:started"}
{"schema_version": "nhms.scheduler_pass_timing.v1", "ts": "2026-09-17T05:46:50.016035Z", "pass_id": "scheduler_2026052112_ea8d6fc60e40", "level": "stage", "phase": "pass:finished"}
============================= slowest 10 durations =============================
109.68s call     tests/test_gateway_reconcile_comment_capability.py::test_rejected_submit_batch_write_failure_reopens_as_unbound_recoverable_reservation[256]
38.28s call     tests/test_gateway_reconcile_file_cohort_identity.py::test_file_cohort_physical_task_identity_mismatch_is_zero_mutation[duplicate-256]
38.04s call     tests/test_gateway_reconcile_file_cohort_identity.py::test_file_cohort_physical_task_identity_mismatch_is_zero_mutation[malformed-256]
37.99s call     tests/test_gateway_reconcile_file_cohort_identity.py::test_file_cohort_physical_task_identity_mismatch_is_zero_mutation[swapped-256]
37.08s call     tests/test_production_scheduler.py::test_scheduler_run_once_drives_accepted_submit_to_state_save_on_same_journal[True-IFS]
36.85s call     tests/test_production_scheduler.py::test_scheduler_run_once_drives_accepted_submit_to_state_save_on_same_journal[True-gfs]
34.56s call     tests/test_production_scheduler.py::test_scheduler_run_once_drives_accepted_submit_to_state_save_on_same_journal[False-IFS]
34.33s call     tests/test_production_scheduler.py::test_scheduler_run_once_drives_accepted_submit_to_state_save_on_same_journal[False-gfs]
7.96s call     tests/test_orchestration_chain.py::test_file_journal_post_window_concurrent_public_cycles_submit_one_retry[natural-gfs]
7.86s call     tests/test_orchestration_chain.py::test_file_journal_post_window_concurrent_public_cycles_submit_one_retry[deterministic-gfs]
=========================== short test summary info ============================
FAILED tests/test_orchestration_chain.py::test_chain_type_exports_preserve_legacy_identity_and_dataclass_contracts
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_downstream_restart_pass_recovers_a_missing_mirror_at_chain_exit
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_probes_the_normalized_ifs_spelling
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_downstream_restart_pass_skips_an_identical_mirror_without_rewriting
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_failed_hook_is_healed_by_a_later_downstream_restart_pass
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_failed_hook_with_a_failed_receipt_write_is_still_recovered_at_chain_exit
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[tree_absent]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[symlinked_leaf]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[symlinked_ancestor]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_needs_a_local_unsymlinked_tree_and_a_copyback_root[no_copyback_root]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_is_fail_open[publisher_raises]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_is_fail_open[mirror_raises]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_is_fail_open[receipt_write_raises]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_chain_exit_recovery_runs_when_a_downstream_stage_raises
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_runs_after_the_entry_cycle_status_write[succeeded]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_runs_after_the_entry_cycle_status_write[failed]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_still_runs_when_the_entry_cycle_status_write_raises[succeeded]
FAILED tests/test_orchestration_chain.py::test_canonical_precip_mirror_still_runs_when_the_entry_cycle_status_write_raises[failed]
FAILED tests/test_orchestration_chain.py::test_stacked_retry_ids_mint_next_attempt_past_last_suffix_through_orchestrate_cycle
FAILED tests/test_orchestration_chain.py::test_explicit_context_retry_attempt_keeps_precedence_over_stacked_rows
FAILED tests/test_production_scheduler.py::test_retry_with_stale_cold_manifest_is_upgraded_to_warm_forecast_rerun
FAILED tests/test_production_scheduler.py::test_missing_forcing_repair_rechecks_blocker_created_by_warm_manifest_upgrade
22 failed, 3363 passed, 1 skipped in 798.68s (0:13:18)


exit=1