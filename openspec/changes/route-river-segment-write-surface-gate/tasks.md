# Tasks — route the river-segment write-surface scan into the targeted CI lane (#2185)


**Reference frame for `path:line` citations.** A citation to code that already existed names the line at the branch base `c21bacf9`, the tree this change was planned against. A citation to code this change ADDS names the line in the branch tree, since it has no base line. The Phase 7 final review and its follow-up flagged four citations to pre-existing code that did not honour this frame. Measured, they were TWO defects and TWO refutations, not four drifts: one genuine merge drift (written against the implementation commit `776cbec5`, moved by the `origin/master` merge at `9e8f5fd1`), one plain off-by-one that was never right in any tree, and two citations that were correct at base and only looked wrong because the reviewer measured them at the branch tip. §6 D15 carries the per-citation measurements. Evidence Floor 4.12a checks only that a citation resolves IN RANGE — the gap both defects fell through — and 4.12b, added afterwards, checks that the cited line still holds the content the sentence claims.

## 1. Selector routing

- [x] 1.1 Add `RIVER_SEGMENT_WRITE_SURFACE_TEST = "tests/test_river_segment_write_surface_scan.py"` and `RIVER_SEGMENT_WRITE_SURFACE_ROOTS: tuple[str, ...] = ("apps/**", "services/**", "workers/**", "packages/**", "scripts/**")` to `scripts/select_ci_tests.py`, beside the `TIMESCALE_WRITE_GUARD_INVARIANT_*` pair (`:251`, `:258`), with a comment recording that the roots mirror the scan's own `PRODUCTION_DIRS` and that a selector meta-guard derives the required set from that binding.
- [x] 1.2 Add the supplemental loop inside `select_tests`, immediately after the `#1656` loop at `scripts/select_ci_tests.py:4317`: for each changed path ending in `.py` that matches `RIVER_SEGMENT_WRITE_SURFACE_ROOTS`, `selected.add(RIVER_SEGMENT_WRITE_SURFACE_TEST)`. No `matched` assignment, no `stop_on_match`, no `only_when_any_changed`, no `PathTestRule` entry. The loop MUST consult the module constant, never an inline copy of the roots — a meta-guard in 2.6 replaces the constant at runtime and asserts the selection moves.

## 2. Selector meta-guards (`tests/test_select_ci_tests.py`)

- [x] 2.1 Add a local `WRITE_SURFACE_SCAN_PATH` literal and a test anchoring it to `RIVER_SEGMENT_WRITE_SURFACE_TEST` and to the file existing on disk.
- [x] 2.2 Add `_write_surface_scan_dirs(path=...)`: parse the scan source, collect **every** binding of `PRODUCTION_DIRS` anywhere in the module — every `ast.Name` store of that identifier at any nesting depth, which covers plain assignment, annotated assignment, augmented assignment, `for` targets, `with ... as`, walrus and comprehension targets alike — assert there is exactly **one**, then require that binding to be a module-level `ast.Assign`/`ast.AnnAssign` whose value is an `ast.Tuple` or `ast.List` of `ast.Constant` strings, and return them. A `tree.body`-only, `Assign`/`AnnAssign`-only collection is NOT sufficient: round 1 measured `PRODUCTION_DIRS += ("db",)` producing `['Assign', 'AugAssign']` at module level and exactly **one** binding found, i.e. the stale tuple returned silently. The same hole swallows a module-level `if some_flag: PRODUCTION_DIRS = (...)` and a `for PRODUCTION_DIRS in ...:` rebind, because neither statement is a direct child of `tree.body`. Record the residual boundary in the comment, accurately: a rebind producing no `ast.Name` store (`globals()["PRODUCTION_DIRS"] = ...`) is out of reach, and the scan's own runtime non-vacuity assertion backstops it only for a rebind executing AFTER `tests/test_river_segment_write_surface_scan.py:110`, where `_LITERALS = _sql_literals()` materializes the walk — a store-less rebind above that line leaves both sides of the `:123` comparison reading the same rebound name, so nothing catches it. Every failure message names `PRODUCTION_DIRS`. Read the scan through `_parse_tracked` with a **relative** path literal (`"tests/test_river_segment_write_surface_scan.py"`), exactly as `_invariant_scan_roots()` does at `tests/test_select_ci_tests.py:10266`. It must be relative and must not be joined onto a `REPO_ROOT`-style absolute base: 2.7's tests work by `monkeypatch.chdir(tmp_path)`, so an absolute default would make the chdir a no-op and leave all three of those tests vacuously green.
  - Do NOT use `next(...)` / first-match: a second module-level binding such as `PRODUCTION_DIRS = PRODUCTION_DIRS + ("db",)` would leave a first-match read returning the stale five-element tuple, which still equals the selector constant, so nothing reds while the scan walks six directories. The scan's own non-vacuity assertion at `tests/test_river_segment_write_surface_scan.py:123` cannot catch that either — at runtime the binding and the scanned set still agree.
  - Do NOT reuse `_invariant_scan_roots()` (`tests/test_select_ci_tests.py:10266`): it hard-codes the timescale suite's path and only reads a `_scan_roots` FunctionDef returning `REPO_ROOT / "workers"` BinOp chains, so it finds nothing here.
- [x] 2.3 Add `_write_surface_root_globs()` → `{f"{d}/**" for d in _write_surface_scan_dirs()}` and a test asserting `set(RIVER_SEGMENT_WRITE_SURFACE_ROOTS) == _write_surface_root_globs()`.
- [x] 2.4 Add `_write_surface_roots_violations(roots, *, probe)`, the positive oracle, with the expected set taken from `_write_surface_root_globs()` and never from the production constant under test.
- [x] 2.5 Add the routing test. Its probe set MUST be **derived**, not a fixed list of five paths: build `{d: f"{d}/brand_new_thing.py" for d in _write_surface_scan_dirs()}` so a sixth scanned directory grows a probe automatically. Assert the three modules holding a river-segment write surface (`workers/model_registry/basins_registry_import.py` and `workers/model_registry/qhh_production_bootstrap.py`, which the scan names by literal at `tests/test_river_segment_write_surface_scan.py:50` and `:52`, plus `packages/common/model_registry.py`, which it discovers by walking — its `INSERT INTO core.river_segment` is at `:964`) and every derived future probe select the scan; assert the negative probes do not — `apps/frontend/src/main.ts`, `openspec/tools/x.py`, and the literal `db/brand_new_thing.py`, which must still select the timescale suite through the sibling #1656 route. Write that last probe as a literal: the obvious derivation `set(TIMESCALE_WRITE_GUARD_INVARIANT_ROOTS) - _write_surface_root_globs()` is a plain string difference and yields `{db/**, packages/common/**}`, and `packages/common/brand_new_thing.py` DOES select the scan (it is under `packages/**`), so using it as a negative probe reds. Derivation is required for the POSITIVE probe set only, where it buys a probe for a sixth scanned directory; and assert the live positive oracle reports no violations. Every failure message MUST embed `WRITE_SURFACE_SCAN_PATH` as a formatted value, so the red names the suite rather than relying on pytest's assertion rewriting.
- [x] 2.6 Add the mutant test, in two legs. `monkeypatch.setattr(select_ci_tests, "RIVER_SEGMENT_WRITE_SURFACE_ROOTS", reduced)` where `reduced` drops `scripts/**`, then assert **both**: (a) the SAME oracle returns a violation naming `scripts/**`, and (b) `WRITE_SURFACE_SCAN_PATH not in select_tests(["scripts/brand_new_thing.py"], repo_root=Path("."))` while a retained root (`workers/brand_new_thing.py`) still selects it. Leg (b) is the one that makes the constant the live routing authority; without it an implementation that inlines the roots in the loop passes every other test in this section. The #1656 analogue at `tests/test_select_ci_tests.py:10372-10385` has only leg (a) and its `monkeypatch` is therefore decorative — do not copy that shape.
- [x] 2.7 Add the derivation-robustness tests against **fixture copies**, never the tracked scan. The derivation ships TWO guards — a whole-module store count and a module-level-readability check — and each needs its own red leg, or the second could be deleted with every test still green. `_parse_tracked` keys on `Path(path).resolve()` (`tests/test_select_ci_tests.py:3194`), so `monkeypatch.chdir(tmp_path)` plus a repo-shaped `tests/test_river_segment_write_surface_scan.py` written under `tmp_path` gives a distinct cache key and leaves the tracked file untouched. Three cases:
  - a copy naming a sixth directory: `_write_surface_root_globs()` grows that root, the 2.3 equality assertion reds naming it, and the 2.5 derived probe set has grown a probe under it;
  - a copy with two module-level bindings of `PRODUCTION_DIRS`: the derivation raises, naming `PRODUCTION_DIRS`;
  - a copy that binds `PRODUCTION_DIRS` to something unreadable — a `tuple(...)` call, no binding of that name at all, a lone `PRODUCTION_DIRS += ("db",)`, or a lone rebind nested inside a module-level `if` body: the derivation raises, naming `PRODUCTION_DIRS`. The last two shapes have ONE store and ZERO module-level assignments, so they are the only cases that reach the second guard;
  - a copy that rebinds `PRODUCTION_DIRS` non-linearly after an ordinary binding — by `+=`, inside a module-level `if` body, and as a `for` target: the derivation raises, naming `PRODUCTION_DIRS`. These are the three shapes the round-1 finding measured slipping past a `tree.body`-only collection.
- [x] 2.8 Pin the CI-output consequence of the §4b flip with the existing `_github_output_fields` helper shape (`tests/test_select_ci_tests.py:5625`): for `apps/__init__.py`, assert `count == "1"`, `meta_guard_only == "false"`, `collection_smoke_required == "false"`, and that the single target is the scan. Without this the trade `proposal.md` §4b accepts has no test.
- [x] 2.9 Place all of the above adjacent to the `#1656` block — after its last test ends at `tests/test_select_ci_tests.py:10386` and before the `#1744` comment header at `:10388` — not at end-of-file: PR #1997 is open with a hunk at `@@ -13660,3 +13680,103` in this same file (its other hunk is at `@@ -306,6 +306,26`, so both placements avoid it).

## 3. Absorb the intended widening in existing expectations

Thirty test functions red under the candidate selector. They do not all absorb the same way; the three named exceptions below cannot be fixed by "add a target to the expected set".

- [x] 3.1 The 28 `select_tests(...) == an exact list or set` expectations: add `tests/test_river_segment_write_surface_scan.py` to the expected set. Do not loosen any assertion from equality to containment.
- [x] 3.2 `tests/test_select_ci_tests.py:682` additionally carries `assert len(selected) == 1 + len(DIRECT_GRID_CONTRACT_TESTS) + len(DIRECT_GRID_CONTRACT_IMPORTER_TESTS) + 1`; the trailing `+ 1` (#1656's rider) becomes `+ 2`.
- [x] 3.3 `tests/test_select_ci_tests.py:5634` asserts `fields["count"] == "2"`, not a selection set; change it to `"3"` and update the surrounding comment at `:5626-5631`, which says "the meta-guard PLUS the write-site invariant" and "the two targeted suites".
- [x] 3.4 Remove the `py-under-apps-frontend` param from `test_select_tests_pins_known_empty_selection_classes` (`tests/test_select_ci_tests.py:5457`), correct the "first six"/"All seven" wording in the comment block at `:5417-5428` for the new counts, and add the positive replacement asserting `select_tests(["apps/__init__.py"]) == ["tests/test_river_segment_write_surface_scan.py"]` — use the tracked `apps/__init__.py`, which really is in the flipped class, rather than only the hypothetical `apps/frontend/scripts/gen.py`.

## 3b. Comment prose this change falsifies

These do not red on their own. Each is a required edit, not a courtesy.

- [x] 3b.1 `tests/test_select_ci_tests.py:857-858` — the ledger comment "the literal below — not the arithmetic above — is the authority: it holds 47 entries, the rule's 46 targets plus the meta-guard rider". It becomes 48, and the new entry is #2185's supplemental rider, not a rule target.
- [x] 3b.2 `tests/test_select_ci_tests.py:5005-5009` — "selects exactly the five core-smoke suites and NO meta-guard rider… so a refactor that gains a sixth target on every unknown route reds here before it silently costs ~15 s across the whole tree". This change is that refactor, so the comment must record the new count, name #2185 as the source of the extra target, and replace the "~15 s" estimate with the measured cost from `proposal.md` §3 — the estimate was a projection for exactly this move, and it is now measurable.
- [x] 3b.3 Provenance comments naming only `#1656` beside a rider that now has two sources — `tests/test_select_ci_tests.py:678-680`, `:5041-5042`, `:5162-5165` — must name `#2185` as well.

## 4. Evidence Floor

Every clause below is mechanically re-checked by the change's verifier script before the merge gate; a clause that cannot be run is a blocker, not a waiver. That script is a scratch artifact of this change and is not tracked in the repo, so every clause is restated here in full and this ledger stands alone. The clause list and the script agree one-for-one: 21 clauses, 4.1 through 4.18 plus 4.12a, 4.12b and 4.12c.

- [x] 4.1 **Diff scope**: the branch touches exactly `scripts/select_ci_tests.py`, `tests/test_select_ci_tests.py`, and `openspec/changes/route-river-segment-write-surface-gate/**`. `tests/test_river_segment_write_surface_scan.py` and the two worker modules are byte-identical to `origin/master`.
- [x] 4.2 **Named write surfaces**: for each of `workers/model_registry/basins_registry_import.py`, `workers/model_registry/qhh_production_bootstrap.py`, `packages/common/model_registry.py`, `select_tests` returns the scan, and returns a superset of what `origin/master`'s own selector returns for the same path. (Cardinality is not asserted: `select_tests` returns `sorted(set(...))`, so "exactly once" would be tautological.)
- [x] 4.3 **Future paths**: a probe under each root **derived from the scan's `PRODUCTION_DIRS`**, not a fixed list, selects the scan.
- [x] 4.4 **Negative controls**: `apps/frontend/src/main.ts`, `openspec/tools/x.py`, `packages/common/sql/x.sql`, `infra/docker/some_hook.sh` each select nothing, unchanged from `origin/master`; `db/brand_new_thing.py` selects the timescale invariant suite and not the scan.
- [x] 4.5 **Gain-only differential**: over every tracked non-test path, one path at a time, `origin/master`'s selector versus this branch's — the set of paths gaining the scan equals the set of tracked non-test `.py` paths under the five roots (457 at `c21bacf9`), no path loses a target, and no other difference appears.
- [x] 4.6 **Sibling route untouched**: `services/brand_new_thing.py` still yields all of `CORE_SMOKE_TESTS` and still does not yield `tests/test_timescale_write_guard_wire_site_invariant.py`.
- [x] 4.7 **Derivation robustness is tested, not asserted**: the fixture-copy tests from 2.7 exist and pass, and BOTH of the derivation's `len(...) == 1` guards are load-bearing — deleting either one, one at a time in a scratch copy, reds at least one `write_surface` test, and the two guards red DIFFERENT tests, so neither is redundant. The clause locates the guards by AST inside the `_write_surface_scan_dirs` span and by the shape `len(...) == 1`, never by variable name or by a whole-file regex: a whole-file `re.search` leftmost-matches an unrelated `assert len(matching) == 1` at `tests/test_select_ci_tests.py:1559`, and a first-match search inside the span would only ever reach the first of the two guards. The tracked scan file is never written.
- [x] 4.8 **Routing removal reds by name**: with the supplemental loop deleted in a scratch copy of the selector, at least one new meta-guard fails with `tests/test_river_segment_write_surface_scan.py` in its message.
- [x] 4.9 **Constant is the live routing authority**: with `RIVER_SEGMENT_WRITE_SURFACE_ROOTS` replaced at runtime by the tuple minus one root, `select_tests` stops selecting the scan for a path under that root — the mutant that inlines the roots in the loop is rejected.
- [x] 4.10 **The flipped class is pinned at the CI-output layer**: for `apps/__init__.py`, the selector's GitHub output reports `count=1`, `meta_guard_only=false`, `collection_smoke_required=false`.
- [x] 4.11 **Stale prose is gone**: the phrases the change falsifies no longer appear verbatim in `tests/test_select_ci_tests.py` — "the two targeted suites", "it holds 47 entries", "NO meta-guard rider". The citation checks are 4.12a and 4.12b.
- [x] 4.12a **Citations resolve in range**: every `path:line` citation added to the edited files and to this fixture names a file that exists and a line range inside it. Its input is the fixture's LIVE citations only: a line number quoted in §6 as a historical measurement — what a citation used to say, or what it reads at another SHA — is written as a plain number rather than as a `path:line` citation, precisely so it is not mistaken for a claim about the current tree. This is a range check, not a content check — it catches the class that produced D1 and the round-0 miscount of the archived requirement's span, not a citation that points at the wrong true line.
- [x] 4.12b **Citations hold the content they claim**: 4.12a checks a citation resolves in range, which is what let two bad citations survive it — one that drifted under the `origin/master` merge and one that was off by a line from the start (§6 D15). For every `path:line` citation, the backticked code fragments in the same sentence — skipping any fragment carrying its own citation there — must appear within the cited span at the declared frame. A fragment found elsewhere in the same file is a failure naming the real line. A sentence with no usable fragment is reported unchecked, never passed, so the clause's recall is stated rather than assumed. Like 4.12a it reads live citations only; §6's historical line values are plain numbers and outside both clauses. This is the mechanical form of the by-hand check the Phase 7 final review ran; it independently re-found the 4.7/D4 citation.
- [x] 4.12c **Bare `:NNN` continuations resolve where a reader reads them**: 4.12a and 4.12b see only fully-qualified `path:line` citations, so the fixture's bare `:NNN` continuations were neither checked nor declared unchecked. Each one is now resolved against the nearest file named earlier on its own line — the way a reader resolves it — and must name a line that exists at the declared frame. The final review found this gap; running the check found seven that did not resolve, including two that landed on a one-line `packages/__init__.py` and two on a scratch script that is not in the tree at all. All seven are now spelled with their full path.
- [x] 4.12 **Suite green**: `uv run pytest -q tests/test_select_ci_tests.py tests/test_river_segment_write_surface_scan.py` passes, and each new test name collects exactly once under `-k` — including `test_write_surface_derivation_rejects_nonlinear_rebinds_of_production_dirs`, added by the round-1 fix pass.
- [x] 4.13 **Lint / spec**: `uv run ruff check .` clean; `uv run openspec validate route-river-segment-write-surface-gate --strict --no-interactive` valid. Note that `--strict` checks structure only; the `MODIFIED Requirements` block's job — keeping the archived capability internally consistent — is a review obligation, not something this command can verify.
- [x] 4.14 **Cost bound**: `uv run pytest -q tests/test_river_segment_write_surface_scan.py` runs exactly 4 tests in under 10s. The decimals in `proposal.md` §3 are a recorded measurement, not a threshold; asserting them would only produce a clause that gets waived under machine load.
- [x] 4.15 **Branch-tip integrity**: local HEAD equals the origin tip and equals the PR head SHA; the PR is OPEN and not `CONFLICTING`; the required merge-gate check exists on that SHA.
- [x] 4.16 **MODIFIED requirements carry every archived scenario**: `openspec`'s apply step replaces a `MODIFIED` requirement WHOLESALE (`dist/core/specs-apply.js` does `nameToBlock.set(key, mod)`), so a scenario the delta omits is permanently deleted on archive and `--strict` cannot see it. For every `### Requirement:` under `## MODIFIED Requirements`, every scenario title in the archived `openspec/specs/ci-contract-baseline/spec.md` must be present in the delta block, and every scenario whose body was edited must be named in §6 below.
- [x] 4.17 **No archived scenario is left falsified**: every `emits exactly` list the delta writes equals what the live selector really returns for that path, and a sweep of every `#### Scenario:` in every `openspec/specs/**/spec.md` whose THEN pins an exact selection finds no scenario naming a path that gains the scan outside a requirement this delta MODIFIES. This is the check that found the four falsified scenarios under `openspec/specs/ci-contract-baseline/spec.md:909` and `:958`; it exists because `openspec validate --strict` checks structure only and cannot see cross-requirement contradiction.
- [x] 4.18 **The respelled empty-selection class is exhaustive in both directions**: every tracked non-test `.py` path under none of the three root families (backend prefixes, the write-surface scan's five roots, the timescale invariant's four roots) really selects nothing, and every such tracked path lies inside the enumeration the delta writes. Plus the two probes the round-1 finding measured: `db/brand_new_thing.py` selects the timescale invariant and the migration suite (so a two-clause spelling would have been wrong), and a `.agents/**` `.py` selects nothing (so an enumeration naming only `openspec/**` would have been wrong).

## 5. Oracle routing

Local only. This change touches no runtime, no DB, no HTTP surface and no display path, so neither node-27 nor node-22 is an oracle for it; `uv run pytest`, `uv run ruff`, `openspec validate` and the CI targeted lane are the whole verification matrix.

## 6. Deviations

Implementation deviations, then round-1 fix-pass deviations. Each is a departure
from the plan as written, not a courtesy note.

### Implementation (rounds 0–1, at `776cbec5`)

- **D1 — 2.9's placement anchors moved.** The plan cited `tests/test_select_ci_tests.py:10386`/`:10388` as the end of the `#1656` block and the start of the `#1744` header. The real block ends at `:10386` and the header runs `:10388-10390`; the new block was placed at `tests/test_select_ci_tests.py:10521` after the absorbed edits above it shifted every anchor. Impact: none on content; the block is still adjacent to `#1656` and still clear of PR #1997's two hunks.
- **D2 — 3.1 said 28 exact-set expectations; 30 test functions were edited.** The AST pass that produced "28" classified only equality comparisons. The implementer found a fourth shape at `tests/test_select_ci_tests.py:701` — `all("::" in ...)` over the selection — which needed narrowing by name rather than a new expected element. Impact: the count in 3.1 was low by two; no assertion was loosened.
- **D3 — 2.5's negative probe could not be derived.** The obvious derivation `set(TIMESCALE_WRITE_GUARD_INVARIANT_ROOTS) - _write_surface_root_globs()` is a plain string difference yielding `{db/**, packages/common/**}`, and `packages/common/brand_new_thing.py` DOES select the scan, so a derived negative probe reds. The probe is written as the literal `db/brand_new_thing.py`; derivation is kept for the POSITIVE probe set only. Recorded in 2.5.
- **D4 — the verifier's 4.7 clause was rewritten mid-implementation.** Its `re.search` over the whole test file matched an unrelated `assert len(matching) == 1` at `tests/test_select_ci_tests.py:1559` (leftmost match), so the guard-mutation leg was passing for the wrong reason. It is now AST-scoped to the `_write_surface_scan_dirs` span.
- **D5 — the verifier's 4.11 clause was rewritten mid-implementation.** The falsified phrases are line-wrapped inside `#` comments, so a verbatim search could not find them; comment markers and whitespace are normalized before matching.

### Round-1 fix pass

- **D6 — the delta's first ADDED requirement became a MODIFIED extension of `Tree-scanning invariant suites MUST follow every scanned source root`.** Two ADDED requirements standing beside an archived requirement whose title is already plural and whose subject is exactly this class would leave three competing authorities for tree-scanning supplemental routing. The archived requirement is generalized to cover both suites; its three scenarios carry over verbatim; the derivation contract stays ADDED because it is genuinely new normative content. Impact: the delta grew from 2 blocks to 5.
- **D7 — three further archived requirements are now MODIFIED.** A mechanical sweep of every scenario in every archived spec (`sweep_scenarios.py`) found 11 selection-shaped scenarios naming a path that gains the scan; 6 use `includes`/`contains` and stay true, 1 (`the shared attribution path tuple keeps its other members unchanged`) asserts only negative containment of three named suites and stays true, and **4 use `emits exactly`** and were falsified. Those four live under `precip service tree changes MUST select the prewarm reader suite` (`openspec/specs/ci-contract-baseline/spec.md:909`) and `the precipitation application-composition owners MUST select the precipitation surface oracles` (`openspec/specs/ci-contract-baseline/spec.md:958`). The scenario bodies edited are, by title: `a precip tree module diff selects the prewarm reader suite`, `a precip route-only diff keeps its existing selection`, `a route-registry diff selects the precipitation oracles and keeps its attribution riders`, `a main.py diff selects the precipitation oracles and keeps its error-logging rider`. Each exact list gains `tests/test_river_segment_write_surface_scan.py`, verified against the live selector by `check_exact_lists.py` (5/5). `openspec/specs/ci-contract-baseline/spec.md:958`'s prose "SHALL remain unchanged" additionally gained an escape clause naming the supplemental route, because all five remaining attribution route paths, all three attribution store paths and all four separately named paths lie under a scanned root. `openspec/specs/ci-contract-baseline/spec.md:637` needed no change: its scenario already carries "unless another requirement explicitly adds a supplemental oracle".
- **D8 — a scenario dropped by the round-0 fixture is restored.** The `MODIFIED` block for `Empty targeted-test selection MUST be loudly self-identifying` had ended at `non-collapsed selections suppress the flag`, silently deleting `#### Scenario: selector-development PRs fire the flag honestly` (`openspec/specs/ci-contract-baseline/spec.md:216-224`) on archive, because the authoring brief stated the requirement spans lines 139-215 when it spans `openspec/specs/ci-contract-baseline/spec.md:139-225`. The scenario is restored verbatim, and clause 4.16 now checks this mechanically for every MODIFIED requirement rather than by reading.
- **D9 — the derivation's binding collection was widened.** See 2.2: `ast.Assign`/`ast.AnnAssign` over `tree.body` was not "EVERY binding" as the delta's SHALL claimed.
- **D10 — the narrowed empty-selection class sentence was wrong in both directions.** It said the remaining members are the `.py` paths under neither a backend prefix nor a scanned root, "`openspec/**` today". Measured: `db/brand_new_thing.py` satisfies that definition but selects `['tests/test_migrations.py', 'tests/test_timescale_write_guard_wire_site_invariant.py']`, so the definition needs a third clause for the timescale invariant's roots; and `.agents/**` `.py` files (e.g. `.agents/skills/subagent-workflow/scripts/evidence_check.py`) select nothing, so "`openspec/**` today" was not an exhaustive enumeration. Both directions are corrected and a new scenario pins the `db/**` case. The archived scenario body edited by this fix is, by title: `known empty-selection input classes are pinned`.
- **D11 — a hardcoded test count left the permanent spec.** "gaining four executed assertions" froze today's cardinality of `tests/test_river_segment_write_surface_scan.py` into an archived requirement; it now reads "gaining an assertion-executing targeted run instead".
- **D12 — the residual-boundary claim was too generous, and the implementer said so.** The brief told it to record that a store-less rebind (`globals()["PRODUCTION_DIRS"] = ...`) is backstopped by the scan's own runtime non-vacuity assertion. That is only true for a rebind executing AFTER `tests/test_river_segment_write_surface_scan.py:110`, where `_LITERALS = _sql_literals()` materializes the walk; a store-less rebind above that line is walked by the scan itself, so both sides of the `:123` comparison read the same rebound name and agree at six directories exactly as they did at five — nothing catches it. Recorded in 2.2, in the meta-guard comment, and in the delta's derivation requirement.
- **D13 — the second guard had no red leg when it first landed.** All four existing fixture cases place an ordinary binding first, so every one of them trips the whole-module store count; the module-level-readability guard could have been deleted with the suite still green. Two cases with ONE store and ZERO module-level assignments — a lone `+=` and a lone `if`-nested rebind — were added, and Evidence Floor 4.7 now mutates both guards separately and requires them to red different tests.
- **D14 — the new test's `# #2185` comment sits inside the function body, not above the `def`.** The brief said above; all four neighbouring `test_write_surface_*` tests put it on the first body line. Impact: layout only.
- **D15 — the fixture's `path:line` citations used two reference frames without saying so; the Phase 7 final review and its follow-up together sorted four flagged citations into two real defects and two refutations.** Real: (a) the citation in 4.7 and D4 for the unrelated `assert len(matching) == 1` that the verifier's old whole-file search leftmost-matched had been written as line 1610, which held that assertion at `776cbec5` and `6ef7fa86` and drifted when `9e8f5fd1` merged `origin/master` (base `1559` → impl `1610` → tip `1626`); at the declared base frame it is `tests/test_select_ci_tests.py:1559`. (b) the 2.7 citation of `_parse_tracked`'s cache key, written as line 3193, was never right in any tree — base `3194`, impl `3249`, tip `3265` — so it was a plain base-frame off-by-one, NOT merge drift; the first draft of this entry claimed drift and was wrong. Refuted: (c) `scripts/select_ci_tests.py:4371-4377` — `git show c21bacf9:scripts/select_ci_tests.py | sed -n '4371,4377p'` is exactly the `BACKEND_PYTHON_SOURCE_PREFIXES` tuple; the reviewer had measured it at the tip, where the binding is at line 4415. (d) `.github/workflows/ci.yml:354` — the `count != "0"` branch is at `354` at base, `776cbec5` and `6ef7fa86`, and only moved to `355` at the tip. The first fix pass "corrected" it to `355` anyway, i.e. applied the very tip frame it had just declared invalid, and the follow-up review caught the regression and it was reverted. The final review then found that this whole family of checks saw only fully-qualified citations, leaving the fixture's bare `:NNN` continuations unchecked and undeclared; clause 4.12c now resolves them as a reader does, and it found seven that pointed nowhere — two at a one-line `packages/__init__.py`, two at a scratch script absent from the tree, three with no named file earlier on their line. All seven are spelled in full. D1's citation was corrected the other way, from line 10505 to `tests/test_select_ci_tests.py:10521`: it names a block this change ADDS, and the declaration puts added code in the branch-tree frame.
