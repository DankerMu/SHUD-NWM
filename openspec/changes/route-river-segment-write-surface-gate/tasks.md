# Tasks — route the river-segment write-surface scan into the targeted CI lane (#2185)

## 1. Selector routing

- [ ] 1.1 Add `RIVER_SEGMENT_WRITE_SURFACE_TEST = "tests/test_river_segment_write_surface_scan.py"` and `RIVER_SEGMENT_WRITE_SURFACE_ROOTS: tuple[str, ...] = ("apps/**", "services/**", "workers/**", "packages/**", "scripts/**")` to `scripts/select_ci_tests.py`, beside the `TIMESCALE_WRITE_GUARD_INVARIANT_*` pair (`:251`, `:258`), with a comment recording that the roots mirror the scan's own `PRODUCTION_DIRS` and that a selector meta-guard derives the required set from that binding.
- [ ] 1.2 Add the supplemental loop inside `select_tests`, immediately after the `#1656` loop at `scripts/select_ci_tests.py:4317`: for each changed path ending in `.py` that matches `RIVER_SEGMENT_WRITE_SURFACE_ROOTS`, `selected.add(RIVER_SEGMENT_WRITE_SURFACE_TEST)`. No `matched` assignment, no `stop_on_match`, no `only_when_any_changed`, no `PathTestRule` entry. The loop MUST consult the module constant, never an inline copy of the roots — a meta-guard in 2.6 replaces the constant at runtime and asserts the selection moves.

## 2. Selector meta-guards (`tests/test_select_ci_tests.py`)

- [ ] 2.1 Add a local `WRITE_SURFACE_SCAN_PATH` literal and a test anchoring it to `RIVER_SEGMENT_WRITE_SURFACE_TEST` and to the file existing on disk.
- [ ] 2.2 Add `_write_surface_scan_dirs(path=...)`: parse the scan source, collect **every** module-level binding of `PRODUCTION_DIRS` (both `ast.Assign` and `ast.AnnAssign`, since the house style annotates such constants), assert there is exactly **one**, assert its value is an `ast.Tuple` or `ast.List` of `ast.Constant` strings, and return them. Every failure message names `PRODUCTION_DIRS`. Read the scan through `_parse_tracked` with a **relative** path literal (`"tests/test_river_segment_write_surface_scan.py"`), exactly as `_invariant_scan_roots()` does at `tests/test_select_ci_tests.py:10266`. It must be relative and must not be joined onto a `REPO_ROOT`-style absolute base: 2.7's tests work by `monkeypatch.chdir(tmp_path)`, so an absolute default would make the chdir a no-op and leave all three of those tests vacuously green.
  - Do NOT use `next(...)` / first-match: a second module-level binding such as `PRODUCTION_DIRS = PRODUCTION_DIRS + ("db",)` would leave a first-match read returning the stale five-element tuple, which still equals the selector constant, so nothing reds while the scan walks six directories. The scan's own non-vacuity assertion at `tests/test_river_segment_write_surface_scan.py:123` cannot catch that either — at runtime the binding and the scanned set still agree.
  - Do NOT reuse `_invariant_scan_roots()` (`tests/test_select_ci_tests.py:10266`): it hard-codes the timescale suite's path and only reads a `_scan_roots` FunctionDef returning `REPO_ROOT / <part>` BinOp chains, so it finds nothing here.
- [ ] 2.3 Add `_write_surface_root_globs()` → `{f"{d}/**" for d in _write_surface_scan_dirs()}` and a test asserting `set(RIVER_SEGMENT_WRITE_SURFACE_ROOTS) == _write_surface_root_globs()`.
- [ ] 2.4 Add `_write_surface_roots_violations(roots, *, probe)`, the positive oracle, with the expected set taken from `_write_surface_root_globs()` and never from the production constant under test.
- [ ] 2.5 Add the routing test. Its probe set MUST be **derived**, not a fixed list of five paths: build `{d: f"{d}/brand_new_thing.py" for d in _write_surface_scan_dirs()}` so a sixth scanned directory grows a probe automatically. Assert the three named write-surface modules (`workers/model_registry/basins_registry_import.py`, `workers/model_registry/qhh_production_bootstrap.py`, `packages/common/model_registry.py`) and every derived future probe select the scan; assert the negative probes do not — `apps/frontend/src/main.ts`, `openspec/tools/x.py`, and the literal `db/brand_new_thing.py`, which must still select the timescale suite through the sibling #1656 route. Write that last probe as a literal: the obvious derivation `set(TIMESCALE_WRITE_GUARD_INVARIANT_ROOTS) - _write_surface_root_globs()` is a plain string difference and yields `{db/**, packages/common/**}`, and `packages/common/brand_new_thing.py` DOES select the scan (it is under `packages/**`), so using it as a negative probe reds. Derivation is required for the POSITIVE probe set only, where it buys a probe for a sixth scanned directory; and assert the live positive oracle reports no violations. Every failure message MUST embed `WRITE_SURFACE_SCAN_PATH` as a formatted value, so the red names the suite rather than relying on pytest's assertion rewriting.
- [ ] 2.6 Add the mutant test, in two legs. `monkeypatch.setattr(select_ci_tests, "RIVER_SEGMENT_WRITE_SURFACE_ROOTS", reduced)` where `reduced` drops `scripts/**`, then assert **both**: (a) the SAME oracle returns a violation naming `scripts/**`, and (b) `WRITE_SURFACE_SCAN_PATH not in select_tests(["scripts/brand_new_thing.py"], repo_root=Path("."))` while a retained root (`workers/brand_new_thing.py`) still selects it. Leg (b) is the one that makes the constant the live routing authority; without it an implementation that inlines the roots in the loop passes every other test in this section. The #1656 analogue at `tests/test_select_ci_tests.py:10372-10385` has only leg (a) and its `monkeypatch` is therefore decorative — do not copy that shape.
- [ ] 2.7 Add the derivation-robustness tests against **fixture copies**, never the tracked scan. `_parse_tracked` keys on `Path(path).resolve()` (`tests/test_select_ci_tests.py:3193`), so `monkeypatch.chdir(tmp_path)` plus a repo-shaped `tests/test_river_segment_write_surface_scan.py` written under `tmp_path` gives a distinct cache key and leaves the tracked file untouched. Three cases:
  - a copy naming a sixth directory: `_write_surface_root_globs()` grows that root, the 2.3 equality assertion reds naming it, and the 2.5 derived probe set has grown a probe under it;
  - a copy with two module-level bindings of `PRODUCTION_DIRS`: the derivation raises, naming `PRODUCTION_DIRS`;
  - a copy that binds `PRODUCTION_DIRS` to something unreadable (a `tuple(...)` call, or no module-level binding at all): the derivation raises, naming `PRODUCTION_DIRS`.
- [ ] 2.8 Pin the CI-output consequence of the §4b flip with the existing `_github_output_fields` helper shape (`tests/test_select_ci_tests.py:5625`): for `apps/__init__.py`, assert `count == "1"`, `meta_guard_only == "false"`, `collection_smoke_required == "false"`, and that the single target is the scan. Without this the trade `proposal.md` §4b accepts has no test.
- [ ] 2.9 Place all of the above adjacent to the `#1656` block — after its last test ends at `tests/test_select_ci_tests.py:10386` and before the `#1744` comment header at `:10388` — not at end-of-file: PR #1997 is open with a hunk at `@@ -13660,3 +13680,103` in this same file (its other hunk is at `@@ -306,6 +306,26`, so both placements avoid it).

## 3. Absorb the intended widening in existing expectations

Thirty test functions red under the candidate selector. They do not all absorb the same way; the three named exceptions below cannot be fixed by "add a target to the expected set".

- [ ] 3.1 The 28 `select_tests(...) == <exact list/set>` expectations: add `tests/test_river_segment_write_surface_scan.py` to the expected set. Do not loosen any assertion from equality to containment.
- [ ] 3.2 `tests/test_select_ci_tests.py:682` additionally carries `assert len(selected) == 1 + len(DIRECT_GRID_CONTRACT_TESTS) + len(DIRECT_GRID_CONTRACT_IMPORTER_TESTS) + 1`; the trailing `+ 1` (#1656's rider) becomes `+ 2`.
- [ ] 3.3 `tests/test_select_ci_tests.py:5634` asserts `fields["count"] == "2"`, not a selection set; change it to `"3"` and update the surrounding comment at `:5626-5631`, which says "the meta-guard PLUS the write-site invariant" and "the two targeted suites".
- [ ] 3.4 Remove the `py-under-apps-frontend` param from `test_select_tests_pins_known_empty_selection_classes` (`tests/test_select_ci_tests.py:5457`), correct the "first six"/"All seven" wording in the comment block at `:5417-5428` for the new counts, and add the positive replacement asserting `select_tests(["apps/__init__.py"]) == ["tests/test_river_segment_write_surface_scan.py"]` — use the tracked `apps/__init__.py`, which really is in the flipped class, rather than only the hypothetical `apps/frontend/scripts/gen.py`.

## 3b. Comment prose this change falsifies

These do not red on their own. Each is a required edit, not a courtesy.

- [ ] 3b.1 `tests/test_select_ci_tests.py:857-858` — the ledger comment "the literal below — not the arithmetic above — is the authority: it holds 47 entries, the rule's 46 targets plus the meta-guard rider". It becomes 48, and the new entry is #2185's supplemental rider, not a rule target.
- [ ] 3b.2 `tests/test_select_ci_tests.py:5005-5009` — "selects exactly the five core-smoke suites and NO meta-guard rider… so a refactor that gains a sixth target on every unknown route reds here before it silently costs ~15 s across the whole tree". This change is that refactor, so the comment must record the new count, name #2185 as the source of the extra target, and replace the "~15 s" estimate with the measured cost from `proposal.md` §3 — the estimate was a projection for exactly this move, and it is now measurable.
- [ ] 3b.3 Provenance comments naming only `#1656` beside a rider that now has two sources — `tests/test_select_ci_tests.py:678-680`, `:5041-5042`, `:5162-5165` — must name `#2185` as well.

## 4. Evidence Floor

Every clause below is mechanically re-checked by the change's verifier script before the merge gate; a clause that cannot be run is a blocker, not a waiver.

- [ ] 4.1 **Diff scope**: the branch touches exactly `scripts/select_ci_tests.py`, `tests/test_select_ci_tests.py`, and `openspec/changes/route-river-segment-write-surface-gate/**`. `tests/test_river_segment_write_surface_scan.py` and the two worker modules are byte-identical to `origin/master`.
- [ ] 4.2 **Named write surfaces**: for each of `workers/model_registry/basins_registry_import.py`, `workers/model_registry/qhh_production_bootstrap.py`, `packages/common/model_registry.py`, `select_tests` returns the scan, and returns a superset of what `origin/master`'s own selector returns for the same path. (Cardinality is not asserted: `select_tests` returns `sorted(set(...))`, so "exactly once" would be tautological.)
- [ ] 4.3 **Future paths**: a probe under each root **derived from the scan's `PRODUCTION_DIRS`**, not a fixed list, selects the scan.
- [ ] 4.4 **Negative controls**: `apps/frontend/src/main.ts`, `openspec/tools/x.py`, `packages/common/sql/x.sql`, `infra/docker/some_hook.sh` each select nothing, unchanged from `origin/master`; `db/brand_new_thing.py` selects the timescale invariant suite and not the scan.
- [ ] 4.5 **Gain-only differential**: over every tracked non-test path, one path at a time, `origin/master`'s selector versus this branch's — the set of paths gaining the scan equals the set of tracked non-test `.py` paths under the five roots (457 at `c21bacf9`), no path loses a target, and no other difference appears.
- [ ] 4.6 **Sibling route untouched**: `services/brand_new_thing.py` still yields all of `CORE_SMOKE_TESTS` and still does not yield `tests/test_timescale_write_guard_wire_site_invariant.py`.
- [ ] 4.7 **Derivation robustness is tested, not asserted**: the three fixture-copy tests from 2.7 exist and pass, and deleting the "exactly one binding" check from the derivation makes the double-binding test red. The tracked scan file is never written.
- [ ] 4.8 **Routing removal reds by name**: with the supplemental loop deleted in a scratch copy of the selector, at least one new meta-guard fails with `tests/test_river_segment_write_surface_scan.py` in its message.
- [ ] 4.9 **Constant is the live routing authority**: with `RIVER_SEGMENT_WRITE_SURFACE_ROOTS` replaced at runtime by the tuple minus one root, `select_tests` stops selecting the scan for a path under that root — the mutant that inlines the roots in the loop is rejected.
- [ ] 4.10 **The flipped class is pinned at the CI-output layer**: for `apps/__init__.py`, the selector's GitHub output reports `count=1`, `meta_guard_only=false`, `collection_smoke_required=false`.
- [ ] 4.11 **Stale prose is gone**: the phrases the change falsifies no longer appear verbatim in `tests/test_select_ci_tests.py` — "the two targeted suites", "it holds 47 entries", "NO meta-guard rider" — and no `path:line` citation added to the edited files or the fixture points outside its file.
- [ ] 4.12 **Suite green**: `uv run pytest -q tests/test_select_ci_tests.py tests/test_river_segment_write_surface_scan.py` passes, and each new test name collects exactly once under `-k`.
- [ ] 4.13 **Lint / spec**: `uv run ruff check .` clean; `uv run openspec validate route-river-segment-write-surface-gate --strict --no-interactive` valid. Note that `--strict` checks structure only; the `MODIFIED Requirements` block's job — keeping the archived capability internally consistent — is a review obligation, not something this command can verify.
- [ ] 4.14 **Cost bound**: `uv run pytest -q tests/test_river_segment_write_surface_scan.py` runs exactly 4 tests in under 10s. The decimals in `proposal.md` §3 are a recorded measurement, not a threshold; asserting them would only produce a clause that gets waived under machine load.
- [ ] 4.15 **Branch-tip integrity**: local HEAD equals the origin tip and equals the PR head SHA; the PR is OPEN and not `CONFLICTING`; the required merge-gate check exists on that SHA.

## 5. Oracle routing

Local only. This change touches no runtime, no DB, no HTTP surface and no display path, so neither node-27 nor node-22 is an oracle for it; `uv run pytest`, `uv run ruff`, `openspec validate` and the CI targeted lane are the whole verification matrix.

## 6. Deviations

None yet.
