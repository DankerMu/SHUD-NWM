# Tasks — file-journal read-blocked consumers (#2385 #2387 #2306)

## Risk Packs

- [ ] Public API / CLI / script entry — **selected**: `POST /api/v1/runs/{run_id}/retry` status codes and body (409 `RETRY_EVIDENCE_INVALID`, recorded failure code on the provenance walk, URI values in the 503) **and** the node-22 operator CLI `scripts/node22_manual_retry_failed_runs.py` (receipt entry and exit code on the new refusal). Covered by 2.x, 4.x, 5.x.
- [ ] Error handling / rollback / partial outputs — **selected**: three refusal/degrade shapes; zero-write assertions on the two **refusal** paths (#2385 selector, #2387 chain classifier). The #2387 provenance-walk half is a **degrade, not a refusal**: the pending retry row is already minted before it, so unresolved roots still record a `submission_failed` row — the assertion there is on the recorded code and message, not on zero writes. Covered by 2.x, 3.x, 4.x.
- [ ] Schema / columns / field names — **selected**: durable pipeline-event `details.runtime_root_resolution` value shape changes from `null` to `[uri]`/`[object-uri]`; the private runtime-root recovery record starts carrying real roots. Covered by 5.x.
- [ ] Legacy compatibility — **selected**: historical events keep their recorded shape (bare `[local-path]`, `null`); the blocked row's shape and its three fail-closed guards unchanged; DB lane untouched. Covered by 3.6, 5.4, 6.x.
- [ ] Concurrency / shared state / ordering — **selected**: the #2385 refusal happens under the same cycle write lock the selector already holds; no new lock. Covered by 2.2.
- [ ] File IO / path safety — **selected**: journal bytes unchanged on refusal; recovery record path unchanged. Covered by 2.3, 3.3, 5.3.
- [ ] Documentation / migration notes — not selected: no runbook states these behaviours; no migration (old events are read as recorded).
- [ ] Auth / permissions — not selected: no policy or role change.
- [ ] Resource limits — not selected: no budget or byte-bound change (#1953 owns those).
- [ ] Config / project setup — not selected.
- [ ] Release / packaging — not selected.

## 0. Setup

- [x] 0.1 Branch `feat/issue-2385-2387-2306-file-journal-blocked-consumers` from `origin/master`. Locate every site by symbol, not by the issue's line numbers (they are from older baselines).
- [x] 0.2 New behaviour for #2385 + #2387 lands in one new module `tests/test_file_journal_read_blocked_consumers.py` (≤1000 lines, else add to `.large-file-guard.json`); #2306 extends `tests/test_retry.py`. Add `scripts/select_ci_tests.py` routing for the new module from every source it pins, with a routing pin test, and update the frozen selection lists in `tests/test_select_ci_tests.py`.
  - Module is 863 lines, so no `.large-file-guard.json` entry. #2306 / `tests/test_retry.py` belongs to section 5 and is NOT touched by this batch.
  - Routing: `FILE_ORCHESTRATION_JOURNAL_IMPORTER_TESTS` (the journal stop rule), the `services/orchestrator/**` directory rule (`chain_forecast_execution.py` / `chain_forecast_orchestrator_cycle.py`), a new `PathTestRule("scripts/node22_manual_retry_failed_runs.py", ...)`, and the existing `apps/api/routes/pipeline.py` rule. Every import in the module is function-local, so no importer derivation reaches it and those four rules are its only routes.
  - Routing pin: `tests/test_select_ci_tests.py::test_select_tests_routes_every_read_blocked_sentinel_source_to_its_coupling_pin`, which also asserts the new script rule does not displace `tests/test_node22_manual_retry_failed_runs.py`.
- [x] 0.3 Every new-behaviour test is shown red on pre-change source, then green; both outputs recorded inline below.
  - Red (pristine source, final test text): `11 failed, 6 passed in 1.76s`. The 6 that pass on both sides are the regression/shape pins that must not move (blocked-row field-by-field, the three fail-closed guards, the `chain_array_accounting` allowlist, the store-backed DB lane, the three distinguishable answers).
  - Green: `17 passed in 0.89s`.

## 1. Discriminator (design D1)

- [x] 1.1 One helper in `file_orchestration_journal.py` next to `_blocked_query_job` deciding blocked by `file_journal.status == "blocked"` on a Mapping, plus the `(reason, field)` extractor. A mapping with no `file_journal` key reads as not blocked.
  - `_is_blocked_query_job(job: Any) -> bool` and `_blocked_query_job_fault(job) -> tuple[str, str]`, defined immediately above `_job_is_active` and directly below `_blocked_query_job`. `Any` in, `False` out for non-Mappings, which is what the chain classifier's duck-typed `repository.jobs` path needs.
- [x] 1.2 `chain_forecast_execution.py` imports both. Assert no import cycle (`import services.orchestrator.file_orchestration_journal` must not load the chain module) and that `chain_compat_static.py`'s inventory needs no edit.
  - `test_journal_import_does_not_load_the_chain_execution_module`. Red: `AttributeError: module '...file_orchestration_journal' has no attribute '_is_blocked_query_job'` (the sibling test); the subprocess probe prints `False` on both sides, and `chain_compat_static._CHAIN_RETRY_COMPAT_LOCAL_METHOD_NAMES` still carries `_retry_job_for_stage_result` unchanged.
- [x] 1.3 Test: the helper is true for all five `_blocked_query_job` lanes' rows and false for a real row and for a plain dict without the marker.
  - `test_discriminator_is_true_for_every_blocked_lane_and_false_for_a_real_row`. Red: `AttributeError: module 'services.orchestrator.file_orchestration_journal' has no attribute '_is_blocked_query_job'`. Green: all five lanes true, and a real row / `{}` / `{"status": "failed"}` / `{"file_journal": {"status": "ok"}}` / `{"file_journal": "blocked"}` / `None` all false.

## 2. #2385 — manual-retry source selector

- [x] 2.1 Red first: with `_iter_pipeline_job_records_scoped` raising a typed fault, `attempt_manual_retry` and `_create_pending_manual_retry_job` raise `RetryEvidenceInvalidError` carrying `journal_reason`/`journal_field`, not `RetryNotFoundError`.
  - `test_blocked_by_run_read_refuses_with_retry_evidence_invalid`. Red (both callers): `services.orchestrator.retry.RetryNotFoundError: No retryable failure found for this run.` Green: `RETRY_EVIDENCE_INVALID`, `status_code == 409`, `journal_reason == file_journal_record_limit_exceeded`, `journal_field == pipeline_job_records`.
- [x] 2.2 Blocked check precedes `safe_jobs` construction and the `_hydro_run_for` read; the `_hydro_run_for` call is wrapped and translated to the same error. No new lock.
  - `test_refused_durable_run_read_takes_the_same_classified_boundary`. Red: `FileOrchestrationJournalError: file_journal_unsafe_identity` escaping the retry lane untyped. Green: the same `RETRY_EVIDENCE_INVALID` carrying `file_journal_unsafe_identity` / `run_id`. The refusal loop runs before `safe_jobs` is built; both callers already hold the cycle write lock, so no lock was added.
- [x] 2.3 Same test asserts zero writes: no gateway request, journal jsonl bytes unchanged, no `manual_retry_marker` record.
  - `test_blocked_by_run_read_writes_nothing_and_never_allocates_a_retry_id`. The snapshot is every file under the journal root except `*.lock` (not just `*.jsonl`), so a stray direct-record or `latest/` write cannot slip past.
- [x] 2.4 Delete the `job_id != "file_journal_read_blocked"` filter; rewrite the `_blocked_query_job` docstring sentence that names it so the declared dependency is the marker. Same edit in `tests/test_file_journal_full_tree_budget_contract.py:169-171`, whose docstring also names that filter as the dependent.
  - Filter deleted; both docstrings now name the marker. `test_both_consumer_ends_key_on_the_single_discriminator_definition` reads both consumers' sources and fails if `file_journal_read_blocked` or a local `"blocked"` literal reappears in either.
- [x] 2.5 Regression, one test per case: active → `RetryConflictError` (409); failed → selected as before; genuinely no jobs → still 404 `RETRY_NOT_FOUND`; durable-success statuses → unchanged.
  - `test_absence_conflict_and_selection_stay_three_distinguishable_answers`; green on both sides of the change (it is the anti-regression half of D2's distinguishability claim).
- [x] 2.6 Route test: the refusal is HTTP 409 `RETRY_EVIDENCE_INVALID` with no traceback, journal path or root text.
  - `test_route_answers_409_retry_evidence_invalid_for_a_blocked_read`. Red: `assert 404 == 409` (the route answered `RETRY_NOT_FOUND`). Green: 409 `RETRY_EVIDENCE_INVALID` with `journal_reason`/`journal_field` and no `Traceback`, no journal root text, no `full_tree_replay` evidence.
- [x] 2.7 `scripts/node22_manual_retry_failed_runs.py` `_preview` catches the new `RetryError` and returns `{"decision": "refused", "reason": "journal_read_blocked", "journal_reason": ..., "journal_field": ...}`; `--execute` still takes the refused branch and its exit code is unchanged. Red first: today the blocked read yields `reason: no_retryable_failed_job`, and after D2 without this task it is an uncaught exception with no receipt.
  - `test_node22_operator_cli_reports_the_blocked_read_as_a_refused_receipt`, two-stage red as the task requires. Red 1 (pristine): the receipt printed `"reason": "no_retryable_failed_job"`. Red 2 (D2 applied, `_preview` not yet fixed): `RetryEvidenceInvalidError: Retry evidence failed file-journal validation.` raised out of `_preview` at `main`'s `:100`, outside its `try` — no receipt file written at all. Green: the receipt entry is exactly the required mapping, `outcome == "refused"`, exit code still `1`, zero marker records.

## 3. #2387 — chain retry classifier and provenance readers

- [x] 3.1 Red first: with `_pipeline_job_for_id_unlocked` raising a typed fault on a real failed `SLURM_TIMEOUT` job, `_retry_job_for_stage_result` raises `OrchestratorError("FILE_JOURNAL_READ_BLOCKED")` carrying the journal reason/field; the failed-job handler is never called; no `_retry_1` row; the durable row still reads `failed` / `SLURM_TIMEOUT`; the raised reason is the journal's, not `file_journal_unsafe_identity`.
  - `test_chain_classifier_refuses_a_blocked_by_id_read_with_a_classified_error`. Driven through the REAL caller `_schedule_cycle_stage_retry` (`chain_forecast_orchestrator_cycle.py:290`) on a real `ForecastOrchestrator`, because a direct call to the classifier cannot reach `handle_failed_job` and the "handler never called" assertion would be vacuous. Red: the spy records the call, and `handle_failed_job -> mark_permanently_failed -> _file_retry_job_record` raises `FileOrchestrationJournalError: file_journal_unsafe_identity`. Green: `OrchestratorError.error_code == "FILE_JOURNAL_READ_BLOCKED"` with the journal's own reason/field, `handled == []`, journal bytes unchanged, durable row still `failed` / `SLURM_TIMEOUT`, no `_retry_1` row.
- [x] 3.2 The check sits after the `store.get_job` short-circuit so the DB-lane path is untouched; a store-backed job still returns unchanged.
  - `test_chain_classifier_leaves_the_store_backed_database_lane_untouched`; green on both sides (the guard was inserted below that short-circuit, so this is the anti-regression half).
- [x] 3.3 Counterfactual test: the same path fed a `_blocked_query_job(error, run_id=..., cycle_id=...)` row still writes nothing — proving the guard, not the identity sanitizer, carries the safety. The test must fail if the guard is removed.
  - `test_the_guard_not_the_identity_sanitizer_carries_the_no_write_guarantee`. The test first pins its own precondition: `_file_retry_job_record` on the `PipelineJob` the classifier would build from this row does NOT raise (it returns the real `run_id`/`cycle_id`), so the by-id lane's accidental safety is gone. Red: `assert 'PIPELINE_JOB_NOT_FOUND' == 'FILE_JOURNAL_READ_BLOCKED'` — `handle_failed_job` IS reached, classifies the journal reason as non-transient, and only a SECOND accident (the sentinel's default `job_id` being absent from the journal) stops the `permanently_failed` write. Green: the classified refusal precedes the handler.
- [x] 3.4 `_file_retry_event_runtime_root_candidates` and `_file_retry_previous_job_id` return the empty batch / `None` with a `LOGGER.warning` carrying reason and field; the sibling at `submission_runtime_root_resolution` is not modified.
  - `test_provenance_readers_degrade_with_a_warning_and_the_sibling_is_unchanged`. Red: `FileOrchestrationJournalError: file_journal_missing_identity` out of `_source_id_from_job`. Green: `None` / empty batch plus exactly two warnings, both carrying `file_journal_record_limit_exceeded` and `pipeline_job_records`. The sibling `submission_runtime_root_resolution` already recognised the marker and returns `None` on that branch without a warning of its own; it is asserted unchanged rather than modified.
- [x] 3.5 Red first, against the corrected premise (the issue's "unclassified 500" is stale): with the predecessor by-id read refused, today's run persists the freshly minted retry row as `submission_failed` with code `SBATCH_SUBMISSION_FAILED` and a `file_journal_missing_identity`-derived message. After the fix the walk skips that candidate with a `LOGGER.warning` carrying the journal `reason`/`field`, and the outcome is either a resolved submission (no failure recorded) or a `submission_failed` carrying the runtime-root lane's own code — never a fabricated gateway code. Assert the recorded row/event code and message, the warning, and that no `FileOrchestrationJournalError` escapes the route.
  - `test_blocked_predecessor_read_no_longer_poisons_the_recorded_submission`, through `POST /runs/{run_id}/retry`. The seam is scoped twice on purpose — armed only AFTER `_create_pending_manual_retry_job` returns, and only for the PREDECESSOR's id — because `_create_pending_manual_retry_job` and both `update_pipeline_job_status` outcomes read the same private method, so an unconditional fault would kill the mint and both outcomes rather than the predecessor read.
  - Red (verbatim from the route): `503` with `{'code': 'SBATCH_SUBMISSION_FAILED', 'details': {... 'job_id': 'fcst_gfs_2026072000_model_a_retry_active', 'status': 'submission_failed', 'error_code': 'SBATCH_SUBMISSION_FAILED', 'error_message': 'file_journal_missing_identity'}}` — the freshly minted retry row persisted as a gateway submission failure for a gateway that was never called.
  - Green: `200`, `status == "submitted"`, the gateway WAS called, no `submission` event carries `status_to == "submission_failed"` / `SBATCH_SUBMISSION_FAILED` / `file_journal_missing_identity`, the durable retry row reads `submitted` with `error_code is None` and `error_message is None`, and a warning carrying the journal `reason` and `field` was logged. This half is the fixture's "resolved submission, no failure recorded" branch; it is a degrade, not a zero-write case.
- [x] 3.6 Regression: `chain_array_accounting`'s terminal-allowlist gate, `_pipeline_job_conflicts_unlocked`, `_job_is_active`, `_file_auto_retry_job_can_be_reused` keep their verdicts on the unchanged row; assert `_next_file_manual_retry_job_id_for_run` is unreachable for a blocked read.
  - `test_blocked_row_keeps_its_load_bearing_guards` (active `True`, reusable `False`, conflicts `True`) and `test_chain_array_accounting_terminal_allowlist_still_excludes_the_blocked_status` (the allowlist set literal is parsed out of `record_cycle_stage_status_override`'s AST, so the pin re-points loudly if the literal moves). Both green on both sides. The allocator-unreachable assertion rides 2.3's test as an `AssertionError`-raising stand-in for `_next_file_manual_retry_job_id_for_run`.

## 4. Coupling pin (#2385 × #2387)

- [x] 4.1 One module pins both consumer ends against the single discriminator and pins `_blocked_query_job`'s row field-by-field across all five lanes, so a later producer-side change cannot satisfy one consumer and silently break the other.
  - `test_blocked_row_shape_is_pinned_field_by_field_across_the_five_lanes` (exact key set, per-lane `job_id`/`cycle_id`/`run_id`/`idempotency_key`, marker keys, reason token, `stage`, `error_code`, and non-membership in `TERMINAL_PIPELINE_STATUSES`) plus `test_both_consumer_ends_key_on_the_single_discriminator_definition`, which asserts object identity between the chain's imported names and the journal's definitions — a second local copy cannot satisfy it.

## 5. #2306 — one rendering at the journal's event boundary

- [ ] 5.1 Red first in `tests/test_retry.py`: a file-lane manual retry with `OBJECT_STORE_ROOT=s3://…` and a `https://` published-artifact root produces a 503 whose `resolved.<field>.value` equals `[object-uri]`/`[uri]` and matches the DB lane field-for-field; also `rejected[].value`, `db_free_runtime.resolved.*.value`, and both `runtime_root_contract` keys (a URI-shaped `workspace_dir` goes `null` → `[uri]`).
- [ ] 5.1b Same suite, local-path roots: one render at the event boundary must be byte-identical to today's two renders — every `*_root`/`*_path` entry still `[local-path]`, no double-bracketing, no token drift. This is the silent-regression risk of D4 and must be measured, not reasoned about.
- [ ] 5.2 Remove the writer-side `_public_evidence(...)` from `_manual_retry_submission_failure_details` **and** from the successful-submission writer in the same method family; state the audit of both call sites in the PR body.
- [ ] 5.3 Assert the durable event carries no raw `s3://` / `https://` root text, and pin the private runtime-root recovery record's new content (real roots, under `private/runtime-root-recovery/`) as intended.
- [ ] 5.4 Remove the shared oracle's "file lane persists `None`" allowance (`_assert_public_runtime_root_resolution`'s `expected_placeholders=None` branch and its docstring paragraph; the only caller passing it is `tests/test_retry.py:3575-3583`) rather than weakening it; also retire the now-false explanation of the `None` withholding at `tests/test_retry.py:3569-3574`. Keep the historical-shape passthrough tests green.
- [ ] 5.5 Regression: the #1592/#1630/#1652 anti-laundering tests stay green (caller-supplied placeholders on non-event records still persist as `None`); update `submission_runtime_root_resolution`'s docstring to attribute the rendering to `_public_pipeline_event_payload`.

## 6. Verification (Evidence Floor)

- [ ] 6.1 Local: `uv run ruff check .` clean.
- [ ] 6.2 Local: `uv run pytest -q tests/test_file_journal_read_blocked_consumers.py tests/test_file_orchestration_journal.py tests/test_retry.py tests/test_orchestration_chain.py tests/test_select_ci_tests.py`.
- [ ] 6.3 Local: `openspec validate file-journal-read-blocked-consumers --strict --no-interactive`.
- [ ] 6.4 **node-27 oracle** (`export PATH=$HOME/.local/bin:$PATH`, `mkdir -p /home/nwm/tmp && export TMPDIR=/home/nwm/tmp`): the same pytest selection plus `tests/test_pipeline_api.py` at the frozen review head; receipt (host, sha, counts) recorded in the PR body. Any red must be attributed to a pre-existing master failure or fixed.
- [ ] 6.5 CI green on the PR (`Unit Tests`, `Markdown Lint`, `Detect changed areas`, entropy report-only).
- [ ] 6.6 Issues #2385, #2387, #2306 acceptance boxes each mapped to a named test in the PR body, with any deviation recorded.
