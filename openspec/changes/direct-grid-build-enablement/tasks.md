## 1. z_policy Verdict and Binding Spec Repoint

- [x] 1.1 Produce and commit the narrow three-question solver-audit verdict evidence file at `evidence/z-policy-solver-audit-verdict.md`, audited against pinned solver commit `3aec65755926c478e13ca7d4fea80715e4e90345`: answer (a) does the `.tsd.forc` read chain read the station-row `Z`, (b) what is `Z` used for, (c) what is the production `.cfg` correction-switch status; record a verdict ∈ `{sentinel, model_dem_at_cell_center, canonical_orography}`.
  - Required evidence: the committed evidence file records verdict `model_dem_at_cell_center` with line-level SHUD citations — `TimeSeriesData.hpp:31` (station `xyz[3]`), `ForcingProvider.hpp:33,63` (`z()` → `xyz[2]`), `MD_ET.cpp:32` (temperature lapse consumer), `Equations.hpp:66-72` (`TemperatureOnElevation`), `Macros.hpp:50,83` (`dTdZ`, `NA_VALUE`), `Element.cpp:132` (pressure uses element `z_surf`, not station `Z`).
  - Required evidence: provenance is reproducible — `git -C SHUD rev-parse HEAD` equals the pin, or the node-22 forensic command (`ssh -p 32099 frd_muziyao@210.77.77.22 'git -C /scratch/frd_muziyao/NWM/SHUD rev-parse HEAD'`) is recorded in the file.
  - Non-goal for 1.1: no full solver re-audit — only the three §7.5/§P0.3-gating questions; no change to `workers/mapping_builder/` library stages.
- [x] 1.2 Author the `direct-grid-binding-artifact` `## MODIFIED Requirements` delta at `specs/direct-grid-binding-artifact/spec.md`, repointing the station-`z` `z_policy` authority from the descoped `cmfd-direct-grid-platform-readiness` (Epic #886) solver audit to this change's verdict evidence file; reproduce the entire "Station coordinates and derived fields obey the tolerance rule" requirement and change only the `z` clause.
  - Required evidence: `openspec validate direct-grid-build-enablement --strict --no-interactive` passes; the delta contains all three original scenarios (cell-center rounding equality, WGS84 basis, x/y + z); `openspec/specs/` is not edited directly (the delta applies at archive time only).
  - Non-goal for 1.2: no behavior change to the tolerance/rounding/WGS84 scenarios; no edit to any file under `openspec/specs/`.
- [ ] 1.3 Wire the verdict as the mapping builder's `z_policy` input authority in the NEW module `workers/mapping_builder/z_policy_verdict.py` (named in proposal Impact; landed here in §1 so the §2 CLI only imports it — no existing library stage is modified): resolve the verdict file against pinned code constants — expected verdict value `model_dem_at_cell_center` and the committed evidence file's SHA-256 (`bdd34965337c76cc385d12588a277a7eea161a5c287f7c0a60a6ffe10daf756d` at pin time; any verdict-file change requires an explicit re-audit + re-pin) — from the default path `openspec/changes/direct-grid-build-enablement/evidence/z-policy-solver-audit-verdict.md` (post-archive: the archive relocation `openspec/changes/archive/<archive-date>-direct-grid-build-enablement/evidence/z-policy-solver-audit-verdict.md`), with any explicit path override evidence-recorded and still required to hash to the pin (mismatch → fail closed, no binding); construct the existing `ZPolicy` (`workers/mapping_builder/binding.py:955`) binding the verified checksum through the existing `readiness_manifest_checksum` provenance slot; for the `model_dem_at_cell_center` verdict, populate `per_cell_z` for every used cell with the pinned sampler `nearest_mesh_node_elevation_v1`: transform the registered WGS84 cell center into the package CRS via the checksum-bound `gis/*.prj`, take the `Elevation` of the nearest mesh node from the `.sp.mesh` node table (planar distance in the package CRS; distance ties → smallest node `ID`), total over used cells including centers outside the mesh hull (never a numeric default).
  - Required evidence: `uv run pytest -q tests/test_mapping_builder_z_policy_verdict.py` (new test file — distinct from the pre-existing Epic #909 `-k "z_policy"` cases in `tests/test_mapping_builder_binding.py`) proves the builder consumes the verdict `policy_name`, binds the verdict-file provenance checksum, and fails closed on (a) a missing verdict file, (b) a verdict file whose SHA-256 or recorded verdict value does not match the pinned constants, (c) a blank provenance checksum, and (d) a used cell with no per-cell `z` (`ZPolicyCellMissingError`).
  - Required evidence: numeric-correctness oracle — for designated keliya-fixture used cells (including at least one whose registered center lies outside the mesh hull) the sampled `z` equals the nearest-node `Elevation` recomputed independently from `tests/fixtures/mapping_builder/keliya/keliya.sp.mesh` through `gis/keliya.prj` and pinned in the test as literal expected values; a constructed distance tie selects the smallest node `ID`.
  - Non-goal for 1.3: do NOT rename `ZPolicy.readiness_manifest_checksum` (deferred naming debt); do NOT add a new value to `ALLOWED_Z_POLICIES`; no change to existing `workers/mapping_builder/` stage modules; no runtime-consumer change.

## 2. Mapping Builder CLI

- [ ] 2.1 Implement `workers/mapping_builder/cli.py` as the operator entrypoint that chains the existing library stages in order — G0/G1 integrity → G2 + ownership algorithm → `.sp.att` `FORC` rewrite (G4) → binding + manifest (G5, consuming the §1 verdict) → evidence — and writes the variant package only after every gate passes, with no partial output on any blocker.
  - Required evidence: `uv run pytest -q tests/test_mapping_builder_cli.py -k "end_to_end or stage_order"` proves the stages run in order and that an injected G0 checksum mismatch and a G5 contract mismatch each fail closed with no variant/binding/manifest/evidence written; the emitted manifest (embedding `station_bindings`) parses cleanly through `workers/forcing_producer/direct_grid_contract.py::load_forcing_mapping_contract_from_manifest`, and the standalone binding artifact matches the manifest per the `direct-grid-binding-artifact` G5 cross-consistency requirement (`binding_checksum` equals the SHA-256 of the binding bytes; `station_bindings` row sets equal element-for-element).
  - Required evidence: the CLI chains the existing stages and does not duplicate stage logic (import-site assertion or coverage that stage functions from `integrity`/`algorithm`/`rewrite`/`binding`/`evidence` are the ones invoked).
  - Non-goal for 2.1: no new gate logic, no change to any library stage, no basin activation.
- [ ] 2.2 Implement the CLI package-path resolver: accept only the object-store release-frozen shape `<object-store-root>/models/basins_<basin>_shud/<release>/package/`, with the root defaulting to `/home/ghdc/nwm/object-store` and changeable only via an explicit `--object-store-root` option whose non-default use is recorded in the evidence package (the sanctioned test channel — tests point the root at a tmp directory where the keliya fixture is staged under `models/basins_keliya_shud/<release>/package/`); reject any `Basins` dev-workspace path (node-27 `/home/ghdc/nwm/Basins/...`, node-22 `/volume/nwm/Basins/...`) unless `--allow-dev-workspace` is set, recording the override path + rationale in evidence; fail closed on any path that is neither object-store-shaped under the configured root nor a recognized dev-workspace path.
  - Required evidence: `uv run pytest -q tests/test_mapping_builder_cli.py -k "input_authority or dev_workspace or object_store_root"` proves an object-store-shaped path under the configured root is accepted, a `--object-store-root` override is accepted with the non-default root recorded in the evidence package, a dev-workspace path fails closed with no read/output by default, the dev-workspace override proceeds while recording the override in the evidence package, and a path of neither shape fails closed with no read/output.
  - Non-goal for 2.2: no network/object-store client change; the resolver validates the path shape/authority, it does not re-implement package loading.
- [ ] 2.3 Enforce the §8.1 forbidden-runtime-output guarantee on the actual CLI path over the written variant tree (no cycle-dated `.tsd.forc`, no per-station weather CSVs, no legacy CMFD weather CSVs in active forcing, no `met.*` rows), and prove zero production writes (baseline files opened read-only; writes confined to the variant tree).
  - Required evidence: `uv run pytest -q tests/test_mapping_builder_cli.py -k "forbidden_output or read_only or zero_write"` proves the written tree contains no forbidden artifact, that an injected forbidden artifact fails closed leaving no variant, and that baseline pre/post checksums are unchanged.
  - Non-goal for 2.3: no producer/runtime code change (§8.1 is enforced against the CLI here, not the producer).
- [ ] 2.4 Prove CLI determinism and land the keliya end-to-end fixture run: two runs on identical inputs produce byte-identical binding/`.sp.att`/manifest/evidence compared raw with no field masking — the CLI leaves the library's checksum-excluded `build_timestamp` evidence field unset (`None`), so no wall-clock enters any emitted byte — exercised through the keliya fixture (484 elements / 32 stations / 8 used cells) already in `tests/test_mapping_builder_integration.py` / `tests/fixtures/mapping_builder/keliya/`, staged under a tmp `--object-store-root` as `models/basins_keliya_shud/<release>/package/` (the Task 2.2-specced resolver channel, not an ad-hoc bypass).
  - Required evidence: `uv run pytest -q tests/test_mapping_builder_cli.py -k "deterministic or keliya"` proves raw byte-identical outputs across two runs (including that the emitted evidence records `build_timestamp` as unset) and a full G0→G5 CLI build over the keliya fixture routed through the specced `--object-store-root` mechanism; `uv run ruff check .` passes.
  - Non-goal for 2.4: no new fixture basin; reuse the existing keliya fixture.

## 3. Producer bbox Preflight

- [ ] 3.1 Wire `verify_download_bbox_matches_registry` into the direct-grid producer preflight at the top of the direct-grid branch of `workers/forcing_producer/producer.py::produce` — before the branch's first repository write (`ensure_direct_grid_met_stations` / `upsert_interp_weights` / any forcing-version write) — with this resolution chain: take the verified contract identity (normalized `source_id`, `grid_id`, `grid_signature`) from the already-parsed direct-grid contract, resolve it via the registry's supersession-aware current-version query (`find_snapshot_by_identity` semantics), and read the snapshot's four bbox corners + `superseded_at` through a new DB-only `ForcingRepository` protocol method (no `object_reader`; touches `workers/forcing_producer/producer.py` (protocol + call site), `workers/forcing_producer/store.py`, `workers/forcing_producer/file_store.py`, and the producer test fakes). Fail closed with zero direct-grid production side effects on: bbox mismatch (`BboxMismatchError`), no registered snapshot resolving for the identity, and a snapshot with `superseded_at` non-NULL (the producer-preflight half of the `grid-drift-lifecycle` §"Consumers of a superseded snapshot fail closed" cross-change contract; the mapping-asset-build half already ships in `workers/mapping_builder/algorithm.py`). Note: `workers/forcing_producer/` issues no raw downloads — download-time bbox guarding lives upstream in `workers/data_adapters` (shared with legacy IDW) and is an explicit non-goal here.
  - Required evidence: `uv run pytest -q tests/test_producer_bbox_preflight.py -k "match or mismatch or missing or superseded"` proves a matching bbox lets production proceed; a mismatched bbox raises `BboxMismatchError` with `expected_bbox`/`actual_bbox`/`grid_snapshot_id` populated and zero production side effects (no repository/store writes, no forcing output); a missing registered snapshot fails closed; a superseded snapshot fails closed; and a `ValueError` propagated from the env reader / finiteness gate aborts production un-swallowed.
  - Non-goal for 3.1: no change to the guard in `packages/common/grid_registry_bbox_guard.py`; no legacy-IDW path change; no `workers/data_adapters` download-stage change.
- [ ] 3.2 Ensure the preflight reuses the pinned guard (default `env_reader=china_buffered_bbox_from_env`) with no producer-local re-implementation, and land the longitude-convention (0..360 vs -180..180) responsibility that `canonical-source-grid-registry` Task 3.2 handed off — the producer treats `workers/data_adapters/region.py`'s -180..180 as canonical.
  - Required evidence: `uv run pytest -q tests/test_producer_bbox_preflight.py -k "delegates or convention"` proves the shared guard is the comparison site and that a 0..360-style deployment longitude not equal to the registered -180..180 snapshot value fails closed rather than clipping a shifted region.
  - Non-goal for 3.2: no change to the guard's signed-zero/finiteness compare semantics (the signed-zero joint-normalization deferred by registry Task 3.2 stays a separate follow-up).
- [ ] 3.3 Run the change-level validation gate and confirm no DB/schema surface is touched.
  - Required evidence: `openspec validate direct-grid-build-enablement --strict --no-interactive` passes; `uv run ruff check .` passes; migration head is unchanged at `000045` (no new file under `db/migrations/`).
  - Non-goal for 3.3: no real-DB pytest (this change does not touch DB schema); no node-27 live receipt (no display/readonly surface changed).
