# Design — six-basin-production-replay

## 风险分级与 fixture 级别

- Suggested fixture level: **expanded**(生产数据受控覆盖 + 调度器准入面改动 + 双端运维执行)。
- 风险轴:生产数据不可逆覆盖(最高)、调度器准入语义(高,env-gated 默认关)、双 lane 索引一致性(高)、node-27 展示正确性(中)、运维执行序(中)。
- Minimal mergeable slice:代码 + 测试 + schema + 驱动器(tasks 1-4)可独立合并;实机执行(tasks 5-7)为评审通过 + 执行授权后的运维,以 receipt 验收。(用户裁定:本变更 branch-only 执行、永不合并 master,分支保留作审计记录——执行前置是 Phase 7 终审通过,不是 merge;与 runbook §6 口径一致。)

## 生产拓扑事实基座(node-22 实机,2026-07-30 两轮只读勘察)

| 事实 | 出处 |
|---|---|
| 12 scope = 6 basin × {gfs, IFS},dg model_ids 见 registry `manifest-last.json`(36 行全 active) | NFS `scheduler/registry/manifest-last.json` |
| 首业务时次 2026070500,frontier 2026072100,00/12 双时次,期望 33 cycle | NFS `runs/`/`forcing/`/journal 三面一致 |
| IFS run 33/33 全六流域;GFS 32/33(070712 缺 run+forcing,huai_main 除外);状态链 12 scope 各 33 条 usable | NFS 实测 |
| 首时次缺陷 12/12:`quality=cold_start_no_state`,`init_mode=1` | 审计 receipt sha256 `088e753b` |
| 完成探针 cohort 级:终态 job `model_id=null`,`run_id=cycle_<src>_<stamp>_convert_cohort_*`,匹配整 cycle 所有候选 | `file_orchestration_journal.py:513-538,8443-8462` |
| journal 读三面:`latest/`、`<cycle>.jsonl`(+轮转)、`pipeline-jobs/`(+`by-cycle/`);`latest/` 会在 append 时从 jsonl 重物化 | `file_orchestration_journal.py:3492-3572,5717` |
| 链 resume 只看 journal job 记录,不看对象在场性 | `chain_forecast_cycle.py:467-478` |
| discovery raw-manifest 门;raw 起 2026070712;070500..070700 五 cycle × 2 源无 raw | `scheduler_core.py:547-600`;NFS `raw/` 实测 |
| repair-missing-forcing 面:`restart_from_stage="forcing"`、需 verified warm state + verified raw manifest + direct-grid 契约 | `scheduler_candidates.py:1340-1349,1505-1600` |
| retention 内嵌 pass,`--submit` 真删,cutoff 按名字中 cycle token;`states/`+`tiles/`+published 受保护;单开关 `NHMS_RETENTION_ENABLED=false` 足以禁用 | `scheduler_runtime.py:1387-1390,1780-1818`;`retention.py:26-63,166-237` |
| `exists_any_generation` 接受任意 valid_time 的 usable 条目 → 有史即无 bootstrap | `state_manager.py:1401-1414` |
| IC 精确选择:valid_time==T、cycle_id==cycle_id_for(T−12h)、lead=12 | `state_manager.py:1150-1257` |
| 覆盖不可逆:同 run_id 原地替换删旧树;索引 upsert 丢旧条目;无版本机制;归档先例 `/ghdc/data/nwm/recovery/<op>-<ts>/`+receipt | `run_tree_copyback.py:309-351`;`state_manager.py:1072-1092` |
| 双 lane 索引:NFS(control-plane 读)+ scratch(Slurm 写),copyback merge 合流 | `compute.scheduler-dbfree.env`;`state_manager.py:1863-1902` |
| node-27 autopipe:runs/ 目录名正则发现;re-ingest 条件 = DB init_state_id ≠ manifest 或产物 mtime > MAX(created_at);parser 删除窗口 [min,max] 后 upsert | `node27_autopipeline.py:76-78,867-933`;`parser.py:741-796` |
| MVT tile 缓存 key 无 run_id/数据 checksum,无任何失效路径 | `services/tiles/mvt.py:135-149` |
| 定向驱动入口既存:`scripts/ops/node22-run-cycle-once.sh` → `plan-production --cycle-time --source --model-id --disable-backfill --plan/--submit`;`--cycle-time` 接受任意历史时次 | `cli.py:312-325,607-634,737-744` |
| timer 已停(is-active inactive,无 next),最后 pass 2026-07-30T08:45Z | systemd 实测 |

## D1 Replay 准入 = 候选级终态覆写,journal 零触碰

**决策**:新增 env 三元组 `NHMS_SCHEDULER_REPLAY_MODE`(bool,默认 false)、`NHMS_SCHEDULER_REPLAY_MODEL_IDS`(逗号分隔封闭集)、`NHMS_SCHEDULER_REPLAY_WINDOW`(`<start10>..<end10>`)。全部齐备且 pass 带 `--cycle-time` 时才生效;任何一项缺失/格式错 → replay 不生效且(若 MODE=true 而其余缺失)fail-closed 报配置错,不得静默降级为普通 pass。

**座位修正(fixture review r1,P1-1)**:生产 env(`NHMS_REQUIRE_FORECAST_WARM_START=true` + `FileOrchestrationJournalRepository` 提供 `candidate_state`)下,`completed_duplicate_pipeline` 短路(`scheduler_candidates.py:358-366`)恒不可达——975 份实机 pass 证据零出现。真正拦截回放候选的是 **strict-warm-start 终态分支**(`scheduler_candidates.py:413-502`):`state_decision=terminal_pipeline_success` 后进入 `_terminal_decision_matches_strict_warm_start`(`:1846-1886`),分流为 mismatch 分支(restart `forecast`,但受 retry budget 降级 `:2101-2113`)或 successor-retry 分支(`:435-443` → `restart_stage="state_save_qc"`——复用旧的错初值 forecast 输出重存 state,恰好把缺陷链再生)。(该段为 r1 叙述留档;触发 token 见下段 r2 修正。)

生效时对候选判定的**唯一**影响:model_id ∈ 集合 且 cycle ∈ window 且 state_decision ∈ **完成型终态族** `_STRICT_WARM_START_TERMINAL_SKIP_REASONS = {terminal_hydro_success, terminal_pipeline_success}`(`scheduler_candidates.py:67`——进入 413-502 分支的门票;**实机形态是 `terminal_hydro_success`**:六流域 run `hydro_run.status="succeeded"` ∈ `DURABLE_HYDRO_SUCCESS_STATUSES`(`scheduler_state_types.py:30`),hydro-success 分支(`scheduler_state_decision.py:186-205`)排在 `terminal_completed_cycle`(:207)与 `terminal_pipeline_success`(:226)之前,fixture review r2 R1 修正)的候选,**整体覆写**该终态分支为 replay 决策:decision token `replay_resubmit`(新,typed)、**不消费 retry budget**(旧史已烧预算不得继承,`_state_retry_attempt` 不参与;实机证据:2026072914 pass 中 36/36 候选 `strict_warm_start_retry_budget_exhausted`)、evidence 记 `replay_terminal_override`(含被覆写的原分支形态)。覆写同时压过 mismatch 与 successor-retry 两条既有分支。

**Evidence 形态(r2 R4)**:覆写点之后 `_upgrade_retry_for_strict_warm_start_manifest`(`scheduler_candidates.py:503-506`,定义 `:1940-1968`)只在 `native_shud_resubmitted is True ∧ restart_stage=="forecast"` 时原样放行,否则改标为 `strict_warm_start_retry_run_manifest_mismatch` 而丢失 token。replay evidence 必须对齐既有 mismatch retry evidence 形态(`:2154-2167`):`decision="replay_resubmit"`、`reason`(typed)、`restart_stage="forecast"`、`restart_from_stage="forecast"`、`native_shud_resubmitted=True`、`durable_output_reused=False`。

**第三个完成型 token 显式表态(r2 R1)**:`terminal_completed_cycle`(`scheduler_state_decision.py:207-208`)**不在**覆写族内——它不进 413-502 分支,直落 `:495-502` 通用 skip,replay 对它 no-op。按实机证据回放窗口 33 时次全部为 hydro-success 形态,该 token 不应出现;若出现,该 cycle 不会重投,驱动器等待超时即停(fail-closed),operator 依 receipt 中断点排查——不为理论形态扩第二个覆写缝。

非完成型终态族的 state_decision(如清场+无旧 run 的 GFS 070712 五流域)不覆写,走原判定链。集合外模型即使出现在同 pass(不应,双保险:driver 同时传 `--model-id` 过滤)也走原判定。

**chain 白名单同步(fixture review r1,P1-3)**:`chain_forecast_orchestrator_cycle.py:781-806` 对终态成功 job 强制重投要求每个 active basin 的 `state_evidence["decision"]` ∈ `_FORCE_TERMINAL_RESUBMIT_DECISIONS`(`:19-26` 封闭集)——`replay_resubmit` 必须加入该白名单(append-only),否则 resume no-op。该文件入 Impact 与测试范围。

**否决的替代**:
- journal 清场:完成记录 cohort 级(`model_id=null` 匹配整 cycle),模型级清场结构性不可能;整 cycle 清场波及 12 无关流域、销毁审计痕迹、且 `latest/` 会从 jsonl 重物化导致部分删除复活。
- 整 cohort 重跑:覆盖 12 个无关流域的正确历史,直接违反指令的"受控覆盖六流域"。
- 一次性 hack(手改 journal 后跑):不可评审、不可重复、无 fail-closed。

**审计痕迹按构造保留**:旧 journal 记录(含旧终态 job)原位不动;回放 pass 追加新记录并重物化 `latest/`;替换 receipt 引用旧 job id 而非复制。

## D2 Discovery 门的 replay 分支(raw 缺失时以 forcing readiness 准入)

**决策**:replay 生效且 pinned cycle 的 raw manifest 缺失时,cycle 准入证据改为:replay 集合内**全部**模型的 dg forcing 包(`forcing/<source>/<cycle>/basins_<b>_vbasins/dg_<hash>/`)经有界 no-follow 检查在场且非空。任一缺失 → 整 cycle 拒绝(typed reason `replay_forcing_evidence_missing`),**不回落**到普通 discovery,也不尝试 convert(raw 已不存在,convert 必败;fail-closed 优于可预见的运行时失败)。raw manifest 在场时(070712 及未来)走原门,replay 分支不介入。

适用面:070500/070512/070600/070612/070700 × 2 源(forcing 全在,NFS 实测)。GFS 070712 raw 在场走原门 + repair 面,不经此分支。

## D3 阶段重启:经 replay_resubmit 决策承载,repair 面处理 070712

**决策**:阶段重启不再作为独立机制,由 D1 的 `replay_resubmit` 决策统一承载(`restart_from_stage="forecast"` + chain 白名单成员资格)。效果:convert/forcing 旧 succeeded 记录被尊重(阶段产物复用——forcing 包即"对应历史 forcing"的字面实现),forecast + state_save_qc 以新 job 重跑。候选按 restart stage 分 cohort(`scheduler_execution.py:836-880`),`chain_forecast_orchestrator_cycle.py:793-806` 的 all-basins 合取按 cohort 求值。

GFS 2026070712(5 流域 forcing 缺、raw 在):driver 对该 cycle 切换参数组 `NHMS_SCHEDULER_REPAIR_MISSING_FORCING=1` + `_CYCLE_TIME=2026-07-07T12:00:00Z`(既有面,`restart_from_stage="forcing"`)。repair 面要求 verified warm state——回放序列到达 070712 时,070700 的回放 state(valid_time=070712)已在索引,天然满足;它与首时次 bootstrap 互斥的既有约束不构成冲突(070500 forcing 在场,不走 repair)。huai_main GFS 070712 forcing 在场,同 pass 内走 replay 常规分支;repair(`forcing`)与 replay(`forecast`)按 restart stage 天然进不同 cohort,互不干扰(fixture review r1 P3-11 已核实分 cohort 机制;保留共存测试)。

**首时次判定不改**:070500 pass 中,状态链已被 D4 清场 → `exists_any_generation=False` → 变更 1 契约自然给出 `PACKAGED_IC_BOOTSTRAP`(两级资格 tier-b object probe,12/12 已实证 qualified)→ runtime 消费包 IC(`init_mode=3`)。replay 模式对首时次分支零介入——这是本设计的核心验收点:新路径的实弹首验。注意首时次的调度形态(round-1 A-P2-4):D4 只清**状态索引**,journal 不动,故 070500 的 state_decision 仍是 `terminal_hydro_success`(`hydro_run.status="succeeded"` 留存于 journal)——覆写照常发生;"零介入"指 strict-warm-start 证据为 bootstrap payload(无 `candidate_state`,含 `packaged_ic_checksum`)且该证据穿过覆写与 `_upgrade_retry_for_strict_warm_start_manifest` 原样存活,不是 decision=None。

## D3.5 候选装配下游门:canonical-readiness fresh-zero-row 合并点(round-1 review A-P1-1/A-P1-2)

D1 覆写发生在终态分支(:413-502),但候选随后仍经 canonical-readiness 合并点(`scheduler_candidates.py:745-810`)。回放窗口内 canonical 产物已被 retention 清除(node-22 实测:scratch canonical 仅 2026071612 起,NFS 无 `canonical/`,readiness 索引 36 条全为 2026-07-20)→ `evaluate_canonical_readiness` 恒 `canonical_incomplete` 零行 → `_canonical_evidence_is_fresh_zero_row=True`,进入两条互斥腿:

- **raw-less 腿**(:783-792):`_source_raw_manifest_restart_evidence` 为 None(raw 已删且 journal 无 `manifest_uri`)时,无条件 block `nfs_raw_manifest_required`——**不看 `required` 标志**,`NHMS_SCHEDULER_REQUIRE_NFS_RAW_MANIFEST=false` 只化解 :714-727 的早期门,拦不住这里。未设防时 070500..070700 × 2 源(10 个 cycle-pass,含首时次 packaged-IC 验收点)提交为零。
- **raw-ready 腿**(:776-782):`state_evidence.update(raw_manifest_restart)` last-writer-wins,把覆写的 `restart_stage="forecast"` 降为 `convert`(`raw_manifest_ready_without_canonical`)→ chain 按 `_STAGE_ORDER` 把已 succeeded 的 convert/forcing 也强制重投,违反 D3 阶段复用与 spec"forecast-stage restart"。raw 自 070712 起在场而 canonical 仅 071612 起 → ~18/33 cycle/源命中。

**Guard 决策(单一 guard site,两腿同修)**:当候选 decision 为 `replay_resubmit` 时:(a) raw-less 腿以 D2 的 forcing evidence 作为 raw-manifest 替代证据放行,不 block;(b) raw-ready 腿的 merge 不得覆盖 `restart_stage`/`restart_from_stage`/`fresh_ingestion` 键(或对该候选整体跳过 merge)。guard 位于合并点本地(`scheduler_candidates.py:775-793`),不在 `scheduler_replay.py`;replay 缺省时该合并点行为 byte-identical(must-preserve 3)。测试 harness 必须提供非 None 的 canonical readiness(round-1 A-P2-3:`readiness=None` stub 曾使整个门在全部候选测试中为死代码,两条 P1 腿因此漏网)。

**v5 增补(round-2 A2-1/A2-3)**:该门实为**三腿**——第一腿在 fresh-zero-row 之前:`_decision_is_authorized_missing_forcing_repair`(:1673-1683)命中且 canonical 未 ready 时无条件 block `missing_forcing_package_uri`(:760-788)。GFS 070712 的 5 个 repair 候选(huai_main 走覆写腿存活)在此被拦——Phase-2 GFS 在 6/33 cycle 处停机,而 070500..070700 已被不可逆覆盖。修复不是单纯放行:repair 的 `restart_stage="forcing"` 在 canonical 已清的前提下无输入(`chain_stages.py:17-30`:convert 先于 forcing)。**决策**:仅对 **replay 窗口覆盖的 cycle**,authorized-repair 候选(其 raw manifest 本就经 :1686-1720 verified present)以 raw-manifest-restart 证据**完整合并**放行——restart 降至 `convert`,从 raw 重建 canonical→forcing→forecast(与 replay_resubmit 候选相反:后者压制 merge 保 forecast,repair 候选需要 merge 降级);窗口外(env 缺省或 cycle 不在窗)行为 byte-identical。raw-less 腿(A2-3)不得仅凭 decision token 放行:discovery 来源为 replay forcing 分支时要求 `replay_forcing_evidence.status=="ready"`,否则 typed block;其他 provenance 落到该腿(Phase-2 raw 路径 discovery + journal candidate state 无可用 raw 证据 → `_source_raw_manifest_restart_evidence` 为 None)一律 typed block——fail-closed 优于 fail-late,guard 记录不得声称一个不存在的替代证据。

**v6 不变量收口(three-round gate retro,深度形态;取代逐腿 guard 策略)**:三轮评审中同一不变量在四个 merge 腿上反复失守(r1 fresh-zero-row 两腿、r2 repair 腿、r3 post-Slurm-sync 重建 merge `:1013`)。**决策:后装配不变量钳(post-assembly invariant clamp)**——`build_candidates` 在**全部**证据装配/merge(含 post-sync 重建)之后、返回之前,对每个 admitted 候选做一次终检并强制:decision=`replay_resubmit` ⇒ `restart_stage=restart_from_stage="forecast"` 且 native/durable 证据键原样;replay 窗口内 authorized-repair 且 raw-restart 已并入 ⇒ `restart_stage="convert"`、`fresh_ingestion.mode="reuse_raw_then_convert"`。钳检测到偏差时**修复并记录**(typed evidence `replay_invariant_clamp_applied`,列出被覆写的键与覆写前值)——未来任何新 merge 腿的 clobber 自愈且可见,不再静默。

**v8 增补(执行期缺陷 2:manual-retry 在 fresh-zero-row 合并点无准入腿)**:D3.5 各版 guard/钳只覆盖 `replay_resubmit` 与 authorized-repair 两种 decision;fresh-zero-row 合并点的 raw-less 准入键**只有** `_decision_is_replay_resubmit`(:887),落空即无条件 `nfs_raw_manifest_required`(:935,同样不看 `required`)。lh_gl 070500 失败后经 journal `manual_retry` 事件(event 988,retry-binding id 满足 identity-filter 留存规则 `scheduler_state_identity_filter.py:475-477`)把 decision 翻为 action=retry/reason=`manual_retry_requested`,但该 decision 在 :607 处被 `_upgrade_retry_for_strict_warm_start_manifest` 升级为 `retry_strict_warm_start_retry_run_manifest_mismatch`——到合并点已与普通 retry 不可区分,attempt-3 实机零提交复现(canonical_readiness 零行,53 leads × 7 变量缺失)。Phase-1 各 cycle 无任何 raw 留存(raw 自 070712 起,这正是 campaign REQUIRE 翻转点),"补 manifest"与 exact-cycle repair 门皆物理不可用。**决策:窄准入腿**——在 :607 升级**之前**新增 sibling 判定 `_replay_manual_retry_admission_decision`,与 replay terminal override 同一 `ReplayAdmission.covers(model_id, cycle_time)` 窗口谓词,要求 reason=`manual_retry_requested` **且** evidence decision=`manual_retry`(双键合取,防 journal 单键伪造);decision token 不改写,仅添加完成标记键;合并点 raw-less 腿放行条件扩为 `replay_covered or replay_manual_retry_covered`,替代证据生成参数化(decision/restart_stage),`replay_resubmit` 路径 byte-identical。窗口外/replay 缺省/非 manual retry 行为不变(红证 3 例 + 定向 183 + 调度域回归 2272 全绿)。

**v9 增补(v8 执行期评审 P1-1/P1-2)**:v8 的准入腿本身可达两处新缺陷。**P1-1 cohort 合取塌陷**:`_terminal_stage_needs_forced_resubmit` 是对 `context.active_basins` 的**合取**,一个 decision 不在白名单内的 basin 会让**整个 cohort** 返回 False;被准入的 manual-retry(`COLD_START_QUARANTINED` 形态经 `_manual_retry_state_evidence` 写 restart_stage=`forecast`,`_upgrade_retry_for_strict_warm_start_manifest` 原样早退,token 保持裸 `manual_retry`)与同 cycle 的 `replay_resubmit` 同属 cohort key `(3,"forecast")` —— v8 之前它在 :935 就被挡下永远进不了 cohort,准入腿把这个塌陷变成可达,replay 兄弟零提交、convergence_timeout 死锁复现。**决策:门侧按准入标记豁免**——`chain_forecast_orchestrator_cycle._basin_carries_replay_manual_retry_admission` 使白名单判定在 basin 携带 `replay_manual_retry_admission` 键时同样成立(键仅在 `ReplayAdmission.covers` 成立时写出,故豁免天然窗口化;**当时写作"看键存在,不看 status 值",已被下面的 round-2 C2 收窄为 `status == "admitted"`——eligible-only 候选走 raw-ready 腿,不得豁免**)。**不**把 `manual_retry` 加入全局白名单(窗口外 manual retry 不得强制重投已成功的 forecast),**不**改 cohort key,**不**改写 token;stage-order 判定对被豁免 basin 照常生效;标记键常量上移至 `scheduler_replay.py` 由生产者/消费者共享。同时更正 v8 的一处**事实错误**:升级后的 token `retry_strict_warm_start_retry_run_manifest_mismatch` **本来就在**白名单内,"token 留在白名单外"从不成立;真正的安全故事是「token 家族永不改写 + 标记窗口化」。**P1-2 post-sync 重建洗白**:v6 钳只在 :521/:868 注册 expectation,manual-retry 家族**不注册**,`_clamp_replay_invariants` 对它是 no-op;post-Slurm-sync 重建(:1055 重判 + :1101 merge)可把已准入 decision 换成 `retry_downstream`(restart_stage=parse、`durable_shud_output_reused=True`)而标记仍称 admitted,manifest 投影随之输出 restart_stage=parse + `native_shud_resubmitted=False`——在已清空 cycle 的**旧** forecast 输出上重跑 parse/publish。**决策:补注册第三种钳模式** `replay_manual_retry`,保护集同 v7 消费者推导法(6 键),但 required **显式且完整**:准入时不存在的键以 `_REPLAY_CLAMP_ABSENT` 哨兵钉**缺席**(裸 manual_retry 只带 `decision`,present-keys-only 会放过 restart_stage=parse),钳据此**删除**该键并按既有格式记入 `replay_invariant_clamp_applied`(新增 `removed_keys`)。配套 fold-in:标记 status 分两级(:607 写 `eligible`,raw-less 腿真正放行时翻 `admitted`,且翻在 decision 上以免下游 re-merge 回退);raw-less guard receipt 无条件输出 `restart_stage`(裸形态显式 null——round-2 C1 之后裸形态已不被准入,该 null 只可能出现在别的家族);新增生产者字面量联动测试(真实 `_candidate_state_decision` 产出两种形态)与"每个准入家族必须注册 expectation"的家族审计。

**v9 round-2 复审增补(C1/C2/C4;C3 deferred)**:v9 的两处修复本身又被复审 + 独立验证确认三处 FIX_NOW。**C1(P1)裸 manual retry 无 restart 前提即被准入**:raw-less 腿的放行条件 `replay_covered or replay_manual_retry_covered`(:962)**从不看 restart_stage**;而生产者 `scheduler_state_failure._manual_retry_state_evidence` **只在 COLD_START_QUARANTINED 分支**写 restart 键,裸形态在 `strict_warm_start is None` 或 run-manifest 已匹配时**原样穿过**升级腿 → 以 restart_stage=None 被准入 → `_candidate_restart_stage` 返回 None → cohort `(0,"full")` → chain 从 `convert_canonical`(stage index 0)起跑,正是本腿刚刚记为**缺席**的 raw 的消费者;fail-closed 退化为 fail-late,且钳的缺席钉法会主动删除后续 merge 并入的 restart 键、把全链形态固化。**决策:restart 前提设在 raw-less 腿的触发点(eligible→admitted 翻转处),不设在 :660 的资格戳**,并对**当前(升级后)** decision 求值:`_replay_manual_retry_restart_is_supported` 要求规范 restart 阶段 ≥ `forecast`(推导法取 top-level restart_stage/restart_from_stage + native-SHUD 别名折叠;**round-3 更正**:此处原写「与 `scheduler_execution.candidate_restart_stage` 逐字相同」是**事实错误**——消费者第一行还有 `fresh_ingestion` 短路,见下条 F1),否则**保留** `nfs_raw_manifest_required` typed block(reason token 不变),仅把 marker 富化为 `declined_reason=replay_manual_retry_restart_stage_unsupported` 供操作者判读。**放置理由**:campaign 的 lh_gl 形态在戳标时是裸的,`forecast` restart 由**其后**的 `_upgrade_retry_for_strict_warm_start_manifest` 写入——前提若设在戳标处会误杀在飞的 campaign 路径;设在触发点则三形态各归其位(裸→拦、裸经升级→准入、COLD_START→准入)。**不**向 decision 伪造 restart_stage / native_shud_resubmitted(违反"家族永不改写",且会强制一次生产者对非 cold-start retry 刻意不给的 native 重跑)。消费者 oracle:被准入候选喂真实 `_candidate_restart_stage`/`_candidate_restart_cohort_key`,断言恒为 `(3,"forecast")`、永不 `(0,"full")`。钳的 6 键期望仍捕获准入时真相:两种被准入形态的 restart 键均**按值**钉住,缺席哨兵只留给真正缺席的 durable 键。**C2(P2)门侧豁免过度授予**:标记键的 `eligible` 是对**每个**窗口内 manual retry 都写的,包括走 raw-ready 腿(restart=`convert`、无钳注册、cohort `(1,"convert")`)那些——按键存在豁免会把它们也放进强制重投门。**决策:chain 侧收窄为 `status == "admitted"`**(翻转发生在 decision 上且先于每次 merge、post-sync 重建后仍在,故不重开 round-1 的 cohort 塌陷)。生产者侧 scope 谓词 `_decision_is_replay_manual_retry_scoped` **保持双 status**(它就是开腿的那把钥匙,读标记时翻转尚未发生)——这处**不对称是刻意的**,两侧各留一条注释指名。**C4(P2)runbook 判别字段不可判别**:§3 行原写 `state_evidence.decision`,但窗口外 manual retry 与窗口内非 manual retry 在 `decision`/`reason` 上**逐字相同**(升级改标),判别字段更正为 `state_evidence.manual_retry.marker`;并补 `replay_raw_manifest_substitute_unavailable` 行(组合特征 marker=eligible + guard=blocked,处置=修 forcing evidence 在场性,不得伪造 manifest/空包)与 C1 之后的第四种可观测态(窗内 manual retry 但 restart < forecast,仍 `nfs_raw_manifest_required`)。**C3 deferred**:raw-ready manual-retry 腿被准入却**不注册**钳 expectation(该腿的 `convert` restart 是它合法想要的,补钳需另设模式),且原"家族审计"测试以 `replay_invariants[...]` 赋值**计数**对 `_REPLAY_CLAMP_MODE_*` **计数**,对"一族两腿一注册"结构性失明。本轮只做**测试质量修复**:审计改为枚举**准入路径**(AST 取 `build_candidates` 内 replay-scoped 局部名与其分支,逐分支声明所调用的 expectation builder 与是否注册),已知缺口作为**显式带注释的 allowlist 条目**可见(`REPLAY_ADMITTING_PATHS["replay_covered"]`),新增准入路径若既不注册也无 allowlist 条目则按构造失败;缺口本身不在本轮修,**待开跟踪 issue**(截至本次提交尚未建号,不得以"已跟踪"结案)。

**v9 round-3 复审增补(F1/F2/F3;第三次同类结构性复发)**:round-2 修复后第三轮复审 + 独立验证再确认两处 FIX_NOW,retro 见 `.workplans/issue-1164-change2/review/review-failure-retro-repair2-r3.md`。**F1(P1)`fresh_ingestion` 不在任何钳保护集内,且它 OUTRANK 所有被钳的 restart 键**:`scheduler_execution.candidate_restart_stage` **第一行**就对 `candidate_is_fresh_full_chain` 短路返回 None,`scheduler_candidate_manifest` 随之整个丢掉 `restart_stage`,`_candidate_skips_pre_orchestration_forcing` 亦读它——即钳把 `restart_stage` 恢复成 `forecast` 也没用,cohort 仍是 `(0,"full")`,chain 从 `convert_canonical` 起跑去要本腿刚记为缺席的 raw。触达路径是**默认可达**的:post-sync 重建重判可落到 `scheduler_state_failure._missing_raw_manifest_repair_evidence`(raw 对象在回放窗内本就不存在 → `_object_manifest_is_missing` 恒真;manual_retry_requested 在 sync 写入更新终态阻塞后转假),它写 `fresh_ingestion={"required":True,"mode":"full_chain"}` + `restart_stage=None`。**决策**:两个 whole-set 模式(`_REPLAY_MANUAL_RETRY_CLAMPED_KEYS`、`_REPLAY_RESUBMIT_CLAMPED_KEYS`)**均补该键并钉缺席**——无任何被准入的 replay 腿会写它(raw-ready 腿经 `_REPLAY_SUPPRESSED_RESTART_KEYS` 剥掉),故缺席即准入时真相。关键点:**只往 resubmit 集加键是 no-op**——该模式原为 present-keys-only,期望里根本不会出现覆写方从未写过的键,因此 `_replay_resubmit_invariant` 同步改为与 manual-retry 模式相同的 whole-set + 缺席钉法(其余键仍按值钉)。`_REPLAY_REPAIR_CLAMPED_KEYS` **不动**:repair 腿刻意按值钉 `mode="reuse_raw_then_convert"`,且其期望由本模块自行装配的证据构造,「准入时未携带」不是可达形态——三集意图不同,已就地互相交叉注释。**F2(P1)资格戳只跑一次且在首次 sync 之前**:戳(主路径)与 strict-warm 升级都在两个 Slurm-status-sync 块之前,而两块都会用新一次 journal 重判**整体替换** `state_decision`;只在 sync 之后才成立的 manual retry(首判 active_slurm_job → sync 报终态 → 重判 manual_retry,`scheduler_state_manual_retry` 以 previous_job_id 绑定的 marker 判定、不看时间戳)因此**无标记**地到达 raw-less 腿,被 `nfs_raw_manifest_required` 挡下且**连 marker 都没有**——第五种无 runbook 行的可观测态,零提交,单趟驱动器随即 convergence_timeout。**决策:两个 sync 块的重判之后各补一次重戳,升级只补在第一块**(第一块顺序与主路径一致:先戳后升级,让 marker 穿过 token 改标;**round-4 delta 修正**:原写「各补一次『重戳 + 重升级』」,第二块的升级半是缺陷,见下条 round-4 增补)。第一块两半缺一不可:只重戳会得到裸形态、被 round-2 C1 的 restart 前提正当地拒绝——runbook 链路可读了但仍零提交。**作用域按家族收窄**:`_replay_manual_retry_restamped_decision`(戳)与包住它的 `_replay_manual_retry_resynced_decision`(戳 + 升级)对非 manual-retry 家族/窗口外/replay 关闭一律返回 None,故升级**绝不**跑在主路径不会戳的 decision 上(否则会改标普通生产重试,破坏 replay-off 逐字不变)。第二块在 canonical 门**下游**,已开不了准入,其职责**只是**别把已准入 marker 洗回 `eligible`——故 `admitted` 状态取 **first-wins**(从候选身上读,候选才是最终出货物),戳本身 last-wins;两处均不注册 expectation,已注册者原样保留。**F3(retro 强制的 Phase 6.2 不变量审计)**:v7 的「保护集从消费者推导」是**一次性手工推导**,不是被强制的属性,三次都漏键。新增**推导闭包测试**:AST 从四个具名消费者(`candidate_restart_stage`、`_candidate_is_fresh_full_chain`、`_candidate_skips_pre_orchestration_forcing`、`_candidate_basin_manifest`)出发,按同模块调用求闭包,抽出所有对 state_evidence 顶层键的读取,断言每个键要么属于**全部** whole-set 保护集,要么带**具名理由**在 allowlist 里(`raw_manifest_reuse`:仅与被钉缺席的 `fresh_ingestion` 合取才生效,结构上不可达;`manual_retry`:仅喂 manifest 的重试计数;`candidate_state`:warm-start 初值透传,由 strict-warm 门而非钳拥有)。配套断言 whole-set 模式**确实**整集钉住(以空证据驱动真实 builder,断言 required 键集 == owned 键集且全为缺席哨兵)——否则「加键」会再次退化成 no-op。防空转:抽取结果必须仍包含四个已知键,重命名导致抽取为空不得静默通过。

**v9 round-4 delta 复审增补(第二处 resync 站点重跑 strict-warm 升级)**:round-3 F2 的「两块各补重戳 + 重升级」把为**第一块**写的升级半原样复用到了**第二块**,复审 + 独立验证确认为 FIX_NOW P1。**缺陷**:第二块(`scheduler_candidates.py:1237`)在 canonical 门(`:963/:1000`)**下游**,而 `_strict_warm_start_retry_run_manifest_evidence` 无条件写 `restart_stage=restart_from_stage="forecast"`;`:1281` 的 merge 与主路径 `:1122` 不同,**没有** `raw_manifest_restart_applied` 守卫,于是它把 `forecast` 覆写到 raw-ready 腿(`:1048`)刚合法降下来的 `convert` 之上。raw-ready manual-retry 腿**不注册**钳 expectation(C3 deferred,已建号 #1198),没有任何东西再把形态复位,出货 receipt 自相矛盾:`restart_stage=forecast` + `restart_reason=raw_manifest_ready_without_canonical` + `fresh_ingestion.mode=reuse_raw_then_convert` + `raw_manifest_reuse.status=ready` → `_candidate_skips_pre_orchestration_forcing` 为真(跳过前置 forcing)**且** chain 从 forecast 起跑在一个零行 canonical 上,cohort 从 `(1,"convert")` 变成 `(3,"forecast")`。**决策(方案 a)**:拆函数——`_replay_manual_retry_restamped_decision` = 戳 + `admitted` first-wins(第二块用,**不升级**);`_replay_manual_retry_resynced_decision` = 前者 + strict-warm 升级(**只**第一块用,行为逐字不变,它在 canonical 门上游、其 `forecast` restart 正是 C1 前提所接受的东西)。第二块的 sync 块与 `slurm_state_sync` 审计证据**保留不动**。**不**为 raw-ready 腿补钳(仍是 #1198 的作用面:COLD_START + raw-ready 这一面本就在其既有范围内);方案 a 对本缺陷形态可把 receipt 恢复到 delta 前逐字相同。配套:`_replay_manual_retry_restart_stage` docstring 中「钳对**每个**被准入 replay 候选都把 `fresh_ingestion` 钉缺席」为**过度声称**(raw-ready manual-retry 腿从不注册 expectation,且合法携带 `mode=reuse_raw_then_convert`)——改述为三条真实理由(该 helper 判的是 merge 之前的 decision;两个注册腿确实钉缺席;raw-ready 的 `required=False` 形态不是 `_candidate_is_fresh_full_chain` 的短路形态,且该腿从不被 C1 前提评判)。

**v7 保护集推导规则(round-4 A4-1)**:钳的保护键集**从下游消费者实际读取的键推导,不从本节蕴含式的措辞推导**。replay_resubmit 模式保护集 = chain gate 读的 `decision`(`chain_forecast_orchestrator_cycle` 对 `_FORCE_TERMINAL_RESUBMIT_DECISIONS` 的字面匹配——蕴含式的前件本身就是被 merge 腿覆写的对象,不是稳定前提)+ manifest 投影读的 `restart_stage`/`restart_from_stage`/`native_shud_resubmitted`/`durable_output_reused`/`durable_shud_output_reused`(后者不钳会使 `retry_downstream` 形态下 manifest 把 `native_shud_resubmitted` 反转为 False,证据自相矛盾);repair 模式保护集同法加 `decision`(token `retry_repair_missing_forcing`)。**每个保护键必须配消费者侧 oracle**:admitted 候选的 state_evidence 直接喂 `_terminal_stage_needs_forced_resubmit`(succeeded forecast job)断言 True——生产者侧键相等断言不足以证明 replay 真的会重投。审计测试的 "upstream of clamp" 只是位置断言,覆盖还需 expectation 注册 + 键在 owned set 内(allowlist 注释写明)。配套**不变量审计测试**:枚举 `scheduler_candidates.py` 中全部 `_merge_state_evidence`/`_candidate_with_state_evidence` 调用点,断言各点位于钳上游或自带 guard——新增 merge 点按构造使审计失败。repair 腿准入前提收紧(A3-2):`_replay_repair_raw_restart_evidence` 要求 `_canonical_evidence_is_fresh_zero_row`,否则返回 None 落回 pre-change typed block(`canonical_unavailable`/`no_expected_leads` 是依赖态未知/配置错,不是重建授权)。replay 缺省时钳为纯 no-op(byte-identical,must-preserve 3)。

## D4 Scoped 状态链清场(双 lane,归档先行,fail-closed)

**决策**:新 `scripts/replay_state_scope_reset.py`,输入 (model_id, source) 集合。顺序:

1. 前置拒绝(任一命中即 exit 非零,零写):timer active(systemd is-active 探测不可用时以 journal 锁**活性**判定:对 `.locks/<source>/<cycle>.lock` 逐个 NB-flock 探测——`O_RDWR` 不带 `O_CREAT`,`EWOULDBLOCK`=active,任何异常=undeterminable=active;辅以 journal 内容新鲜度信号(cycle-row/pipeline-jobs 树最新 mtime < 10min 判活)。锁文件自身 mtime **不可**作新鲜度信号——journal 锁 O_CREAT+flock、从不写入/touch/unlink,mtime 恒为首次创建时间(round-2 B2-3);**探测失败视为 active**)、任一 lane 索引不可读、归档目标不可写。
2. 归档:两 lane 索引文件全量字节快照;被移除条目 JSON 清单;每条目对应 `states/...` 对象的 stat+sha256(对象缺失/不可读如实三分记录,不阻断——索引条目仍移除,receipt 记 `object_unreadable`);对象字节归档(实测 12×33 个 `state.cfg.ic`,单个 28KB-4.3MB,总量上限 ~3.4GB,归档前 `df` 检查)。目的地 `/ghdc/data/nwm/recovery/six-basin-replay-<ts>/`。
3. 移除:scratch lane 先、NFS lane 后(NFS 为准入读面,后清使"清场未完成即跑"窗口只可能表现为 bootstrap 不可达 = fail-closed 方向);每 lane 原子写回(临时文件 + rename),写回后读回校验条目数=预期。
4. receipt(schema `nhms.replay_state_scope_reset.v1`):per-lane {index_path, snapshot_path, snapshot_sha256, removed_count, removed_entries[], readback_verified};per-object {state_id, object_key, stat 三分, sha256, archived_path};全局 {scopes[], enforced, started/finished_at}。dry-run 输出同 shape(enforced=false,零写)。

错误分类承袭变更 1 不变量:每个 stat/IO 点三分(成功/确证缺失/不可判定),不可判定绝不折叠为负结果;写回后 readback 失败 → receipt 记 `commit_uncertain`(exit 3),不得报成 refused(承 #1190 invariant)。

**范围**:仅 12 dg scope。legacy `basins_*_shud` 条目(13 条,不同 model_id,不挡 dg bootstrap)不动。`states/` 对象文件不删(回放逐 cycle 原地覆盖;pre-image 已归档)。

## D5 串行驱动器与替换 receipt

**驱动器**(`scripts/ops/node22_six_basin_replay.sh` 薄壳 + `scripts/replay_driver.py` 内核):

- 输入:source(gfs|ifs)、cycle 区间(默认 2026070500..2026072100)、model 集合(默认该 source 6 dg id)、`--execute`(默认 dry-run:打印计划 + 前置校验,零提交)。
- replay env 文件(`infra/env/compute.replay.env.example` 模板):在生产 env 基础上叠加 `NHMS_RETENTION_ENABLED=false`(**驱动器启动时断言该值,否则拒跑**)、replay 三元组、其余与生产一致(§8/warm-start 门全开——回放走完整生产语义)。
- 启动时全局步骤(fixture review r1 P2-7/P3-13):
  0a. **存量现场核定**:per-scope 实际 cycle 存量(run 目录、索引条目、journal 终态)由驱动器现场枚举并写入 receipt——不沿用勘察冻结计数(勘察后 frontier 可能已动);回放终点 = 现场核定的 frontier。
  0b. **旧半 state 字段来源 = reset receipt**:清场先于逐 cycle 回放,到达 cycle N 时索引条目已移除——替换 receipt 行的旧 state 字段(state_id/checksum/created_at)一律从 reset receipt 的 `removed_entries[]` 对账引用,禁止读已清场索引(读到空即静默降级,禁止)。
- 每 cycle 步骤:
  1. 预捕获(在该 cycle 提交**之前**,旧 run 树此时仍完整):该 cycle 6 模型旧 run manifest sha256 + 输出清单 sha256 →替换 receipt 行(旧半,state 字段见 0b);旧 run 缺失(GFS 070712 五流域)如实记 `no_prior_run`。**无条件**记录该 cycle 每模型的 forcing 包 manifest checksum 与模型包 checksum(用户指令"输入 checksum"的字面承载,r1 P2-8)。
  2. 预 stage:scratch 缺该 cycle forcing 时从 NFS 复制(sha256 校验后写,receipt 记录);模型包在 `models/`(非 retention 域)无需 stage。
  3. 提交:`plan-production --cycle-time <T> --source <s> --model-id×6 --disable-backfill --submit`(单 pass 语义;`--max-passes` 仅 `--continuous` 下有意义,不传,r1 P3-12)。070712 切 repair 参数组。
  4. 等待(round-1 B-P1-1/B-P2-7 修正,两条腿都不用时间戳;v8 修正终态腿为双路):每模型的**终态腿**满足其一——(a) 出现**pass 前基线之外的新终态 job id**(旧 run 的终态 journal 记录在清场后留存,布尔"有无完成"恒真);或 (b) **resume receipt 行级先前证据**:`prior_eligible` 在等待开始前从 resume plan **一次性纯函数推导**(不读活索引、不轮询)——该模型的结转行存在,且满足 `status ∈ {completed, verified_skip}`,或**合取判据**(review F4):`convergence.state_entry_present=true`(后继替换是唯一 per-model 判别子——cohort 级 journal 归属连失败成员也记 True)**且**终态锚(行有 `terminal_evidence` 键时须 ∈ {new_job, prior_pass}(post-fix 行,跨跳传递);无该键的 pre-fix 行回退 `journal_terminal=true`)。注意 `journal_terminal` 是 **pass 相对量**:零提交 pass(如 attempt-2)的行恒 False、不携带可用先前证据,续跑应指向持有同样旧半且行级证据最强的那份 receipt(通常即真正跑过活儿的那份;B4-1 逐字结转保证旧半相同)。接受时仍须当轮 `state_entries` 中该后继**当前已替换**,且结转行带 `successor_checksum` 时须与当前条目 checksum 相等(身份对账,镜像 verified_skip 的标准;不等即视为被重跑,退回要求新终态 job),receipt 行记 `terminal_evidence=new_job|prior_pass` 与 `prior_eligible`。**无 `--resume-from` 即无先前证据腿**——对已回放世界的裸重跑必然 `convergence_timeout`,这是防伪造 pre-image 的蓄意 fail-safe(review F1);等待期间才出现的后继替换仍须新终态 job id(review F2:证据只来自持久 receipt,本 pass 观测永不扩大集合)。**后继替换腿不变**:NFS 索引 (valid_time=T+12h) 后继条目的 checksum 与 reset receipt `removed_entries` 归档值**不同**(生产索引条目 `created_at` 恒为 null,since-gate 不可满足);索引不可读 = 不可判定,永不算收敛并以 `state_index_undeterminable` 停机。超时(默认 90min/cycle)或任一失败 → **立即停**,receipt 记录中断点,不自动重试不跳过。
  5. 后捕获:新 manifest sha256、新 state checksum、init_mode/quality(回放序列原点行——`--origin-cycle`,默认 2026070500,非本次调用的 `cycles[0]`,round-1 B-P1-2——必须 `init_mode=3`+`quality=packaged_calibrated_state`+`packaged_ic_checksum` 非空,否则视为失败停机);同时断言新 run 与旧半的键一致:`river_network_version_id` + 输出段数 + 输出文件清单(round-1 B-P1-3:实机 manifest 无 `outputs.variables`,原断言恒不执行;见 R3)→ receipt 行(新半)。
  6. 每 cycle 结束后原子重写替换 receipt(未完成时 `outcome=in_progress`),中途崩溃保留已完成行(round-1 B-P1-4)。
- 串行序:先 IFS 全序列后 GFS(或反之,单序执行,不并行两源——控变量、控 Slurm 负载、失败面清晰)。
- 全局 receipt `nhms.production_replay_replacement.v1`:rows[] 66×6 行 + per-source reset receipt 引用 + 中断/恢复记录(驱动器可从 receipt 断点续跑:已完成 cycle 经完成校验后跳过——校验=新 state 在场且 checksum 与 receipt 一致,非盲跳)。
- **v5 续跑与 receipt 硬化(round-2 B2-1/B2-2/B2-4)**:(a) 续跑时任何 (cycle, model) 在 resume receipt 中已有 `prior` 的行(**不论 status**,含 halted)一律沿用该 prior(标 `prior_source=resumed_receipt`),仅重采 new 半——旧 run 树无任何归档,从已回放的树重采即永久销毁唯一 pre-image,键一致断言随之退化为回放对回放;(b) `--receipt-path` 与 `--resume-from` 解析为同一路径时拒跑(typed `receipt_path_is_resume_source`,镜像解压工具的 fresh-path 规则);(c) 首次提交前先落 `in_progress` receipt 作写权限前置——不可写 → `receipt_path_unwritable` refused(exit 2,零提交);中途写失败 → typed halt 非零退出,绝不静默继续;(d) reset receipt 装载时校验其 `scopes[]` 覆盖本次 (source, 全部 model),不覆盖 → `reset_receipt_scope_mismatch` refuse——否则 `prior.state` 全空且收敛 oracle 退化为"有条目即算替换"。
- **v6 续跑语义集中化(three-round gate retro;取代 v5(a) 的逐点修补,B3-1/B3-2)**:续跑行处置收拢到单一函数:resume receipt 的**每一行**先原样结转进新 receipt(链条跨多跳不断——中间 attempt 未覆盖的 cycle 不再从已回放树重采);随后按 (cycle, model) 定处置:`status ∈ RESUME_COMPLETED_STATUSES = {completed, verified_skip}` **且** `_row_assertion_failure(row) is None` ⇒ 才有资格进 verified-skip 复核(索引 checksum 对账);其余(halted/planned/断言失败)⇒ 重跑,prior 沿用结转值(`prior_source=resumed_receipt`)——断言失败的 cycle 重跑后以**原始** pre-image 重判,该停必再停。receipt 顶层增 `resume_from: {path, sha256}`(schema 同步,`additionalProperties:false` 下显式加属性),多跳链路可审计。runbook §3 补:续跑必须指向**持有目标 cycle 旧半的那份** receipt(通常即上一份)。
- **v7 全行结转的实现语义(round-4 B4-1/B4-2)**:"每一行原样结转"按**运行时产出**判定,不按静态窗口判定——receipt `rows` 是按 `(cycle, model)` 键的有序映射,以 resume receipt **全部**行(含本窗口内的行)按原序播种,仅当本 pass 实际产出该键的行时替换该条;`_halt`、逐 cycle checkpoint、pre-flight 写共用同一映射序列化(单一 owner)。这使 halt/中途崩溃(runbook 自己的"只跑了窗口的一段"情形)也不丢未触达 cycle 的旧半——v6 的静态 in-scope 排除法只覆盖窗口收窄分支,halt 分支丢行已实证(三跳后 pre-image 从已回放树重采)。结转的陈旧行安全:每跳仍经 verified-skip 复核对账 live index。**consumer oracle**:三跳 halt 测试——attempt-2 在更早 cycle 停机,断言其 receipt 仍含 attempt-1 后续 cycle 行,且 attempt-3 该 cycle `prior_source=resumed_receipt`、`prior.run_manifest_sha256` 等于 attempt-1 原值。`outcome` 语义为**本 pass**(completed 不代表全窗口落地;rows 可含结转的 halted 行),runbook 同步。附:`_resume_plan` 在 schema 校验后**拒绝跨 scope 误用**——payload `source_id`(规范化)≠ config source,或本次 `model_ids` 未被该 receipt 覆盖时 `resume_receipt_scope_mismatch` 拒跑;IFS/GFS 窗口与模型全同、receipt 同目录同命名族,行键 `(cycle, model)` 恰好碰撞,串源后 key_consistency 因共享 river network 恒 consistent——真 pre-image 被静默销毁且不可回滚。**覆盖判据同样是运行时的(round-5 B5-1)**:按 receipt 实际持有的行 `rows[].model_id` ∪ 顶层声明 `model_ids` 判定,不按顶层声明单判——顶层字段记的是**写这份 receipt 的那个 pass 自己的 scope**,全行结转之后二者必然发散(收窄 pass 声明窄、却逐字结转全部模型的行)。只看声明会把 runbook §3 自己的 `forcing_source_absent` 恢复路径(收窄→修复→加回,每跳都续上一份)判成拒跑,而被拒那份 receipt 里正躺着所需 pre-image;绕开拒跑只能去续更早的 receipt,即 B4-1 的丢 pre-image 复现。收窄续跑仍合法(子集),加回也合法(行在即覆盖);真缺行才拒。**consumer oracle**:full→收窄→加回三跳,断言第三跳不被拒且加回模型的行 `prior_source=resumed_receipt` + sha 等于首跳原值。

## D6 node-27 刷新、验证与 timer 重启

0. **TimescaleDB 压缩块前置(fixture review r1,P1-5)**:回放窗口(2026-07-05..07-21)为历史区间;`parser.py:733-755` 替换 DELETE 前经 `check_batch_targets_uncompressed`(`timescale_write_guard.py:121-137`)fail-closed——任一压缩 chunk 与窗口相交即整批失败。re-ingest 之前必须:压缩块普查(受影响 run 的行所在 chunk 压缩状态)→ 命中则以既有 `scripts/node27_timeseries_decompression_replay.py` 面解压 → 普查与解压 receipt 入 Evidence Floor。(本 fixture 勘察未连 node-27,压缩现状待 6.0 实测。)
1. copyback 后 autopipe 自动 re-ingest:32/33 时次旧 manifest 带真实 `init_state_id` 而新 manifest 为回放形态 → `_ingested_run_is_current`(`node27_autopipeline.py:915-933`)的 init_state_id 分支触发;唯首时次旧 manifest `state_id=null` 落到 mtime 分支——`_run_product_mtime`(`:953-973`)看 `input/manifest.json` + 输出文件,回放必改写,亦触发(r1 P3-10 修正)。`--force` 仅作首时次行的兜底并记录。
2. tile 失效(新 `scripts/node27_invalidate_tiles.py`,dry-run 默认):hydro 图层的 `map.tile_cache.source_id` 实为 **run id**(`hydro_display.py:313-321`,deviation 1 实测认定,round-1 已裁 ACCEPT)——scope 天然精确到单个回放 run,域外流域结构性不可及,v2 的"仅按 source_id 会波及域外"论证随之失效(round-1 C-F6)。删除范围 = source_id ∈ **{回放 run_ids} ∪ {`hydro-national`}**(全国聚合层 `source_id="hydro-national"`、valid_time 非空,内容 join 六回放流域,是 `/` 默认视图,round-1 C-F1)且 valid_time ∈ [2026-07-05T00Z, **末次 cycle + FORECAST_HORIZON_HOURS(168h)= 2026-07-28T00Z**](上界取展示跨度而非状态链 lead——末次 cycle 的 168h 预报展示只有 12h 落在旧窗口内,round-1 C-F2;valid_time 窗口在 run-id scope 下已退化为下界防御,取宽不取窄)的 `map.tile_cache` 行 + 对应文件缓存条目;receipt 记删除行数。执行于 re-ingest 完成后。负验证以 DB 行断言而非缓存行断言。**v5(round-2 C2-1)**:回放全程产出 4 份替换 receipt(IFS phase1/2 + GFS phase1/2),`--from-replacement-receipt` 必须可重复传(append,run_ids 取并集),单次调用覆盖 4 receipt 并集 ∪ `hydro-national`;装载 replacement receipt 校验 `outcome=="completed"`(`in_progress`/`halted` typed 拒绝——半程 receipt 意味着部分 scope)。**v6(round-3 C3-1)**:替换 receipt 是跨节点共享证据,驱动器该路 receipt 写 mode 改 `0o644`(不动 `safe_fs` 默认 0600——reset receipt 等仅 node-22 消费的不变);runbook §2.3.2 增前置:node-27 侧对四份 receipt `test -r`,失败则回 node-22 `chmod 0644` 处置;"无人工拷贝步骤"表述以该前置为条件(uid 巧合不可依赖,先例见 tier-node27-timeseries-storage 的 STATES_ACCESS_DENIED)。失效工具部分变更(partial mutation)的 reason 族按"有无已 unlink 文件"分名:有 unlink 用 `*_after_file_unlink` 族;无 unlink 但 DELETE 已发出的用 `db_delete_failed` / `db_commit_uncertain` / `interrupted_delete_scope_uncertain`;**dry run 不算 in-flight**(dry run 的 `deletable_keys` 仅作报告,探测异常不得写出 partial-mutation receipt)——判据为具名谓词 `unlinked_paths or (execute and deletable_keys)`。注:national cache-key 摘要(`mvt.py:1126-1163`)按每河网**全局最新** run 取键——本次回放安全仅因窗口终点=冻结 frontier(回放 run 即最新 run,ingest 的 `updated_at` 无条件 bump 使 key 轮换);该前提写入 runbook note,timer 重启后不成立。
3. 验证清单(live receipt,C1-C4 风格):
   - DB:12 scope × 33 cycle `hydro_run` 行 manifest 引用为新 sha256;070500 行 `init_mode=3`/`quality=packaged_calibrated_state`;
   - timeseries:parser 删除窗口为**新旧并集**(`parser.py:724-730`,r1 P2-6 修正——旧行超出新 span 也会被删,无窗口残留问题);真正的残留风险是键漂移:断言新旧 run 的 `river_network_version_id` 与 variable 键集合一致(D5 步骤 5 已前置断言,此处 DB 复核:每 (run_id, variable) 无两版本混行);
   - API/前端:display API 抽样返回新数据(对比替换 receipt 中新 manifest 的产物 checksum 链)、`https://test.nwm.ac.cn` 单图 + `/ops` 浏览器验证;
   - 负验证:六流域外任一流域 **DB 行数据** checksum 回放前后不变(缓存行见上,不作为负验证对象)。
4. timer 重启(全部 receipt 绿后):`systemctl --user start nhms-compute-scheduler.timer`;次一自然 pass 证据:frontier 前进到 072112+ backlog、回放时次被新 cohort 完成记录跳过(`completed_duplicate_pipeline`)、无 replay env 残留(生产 env 未被改动,replay env 是独立文件——驱动器退出后无进程持有)。

## Must-preserve

1. 六流域外 12 模型:run/状态/journal/展示零字节变化(负验证入 receipt)。
2. journal 只追加:全程无删改任何 journal 文件。
3. replay env 三元组默认缺省时,调度器全行为 byte-identical(含 §8、warm-start、首时次、completion、discovery、retention 全链;测试以 env 缺省断言零介入)。
4. 变更 1 契约零改动:`PACKAGED_IC_BOOTSTRAP` 判定、runtime 消费、审计工具原样。
5. `states/`/`tiles/` retention 保护、copyback merge 语义(#1190 三窄化)不动。
6. 生产 env 文件不改(replay 走独立 env 文件);timer 单位文件不改。
7. repair-missing-forcing 面语义不改(replay 仅作为参数组消费方)。

## 残余风险(具名)

- R1 双 lane 清场与 copyback 并发:timer 停机 + 驱动器单进程串行下无并发写者;若外部违规启动 pass,NFS 后清顺序保证错误方向为 bootstrap 不可达(fail-closed)。
- R2 GFS 070712 repair 与 replay 常规分支同 pass 共存(huai_main forcing 在场):分 cohort 机制已核实(`scheduler_execution.py:836-880`),风险降级;保留共存测试,driver 仍支持模型子集参数作为逃生门。
- R3 键漂移残留(取代原窗口断言,r1 P2-6;断言轴按 round-1 B-P1-3 改为实机可得证据):parser 删除并集窗口,无窗口残留;若新 run 的 `river_network_version_id`、输出段数或输出文件清单与旧不同,旧键下的行不在删除条件内而残留。驱动器步骤 5 + node-27 验证双重断言键一致;破则人工介入(不自动删旧键行——避免误删无关数据)。
- R4 回放结果与旧结果物理量差异显著(初值不同,本就是目的):display 侧无平滑处理,切换即生效;记录于验证 receipt,无需额外动作。
- R5 `state` (单数) 目录在 scratch root 存在(勘察见 `state/` 与 `states/` 并存):实现时确认其归属,清场工具只动 `states/` 域。

## Retry budget(fixture review r1,P1-4)

回放候选继承旧史已烧的 forecast retry 预算(`_state_retry_attempt` 读持久化 journal 的 `retry_count`/`_retry_<n>` 后缀,`scheduler_state_rows.py:425-479`);实机证据:2026072914 pass 36/36 候选 `strict_warm_start_retry_budget_exhausted`。D1 的 `replay_resubmit` 覆写**不消费预算**(在预算判定之前覆写整个终态分支)。回放期间真实失败的有界性由驱动器承担(任一失败立即停,不自动重试)——预算旁路不引入无界重试。非终态路径(070712 五流域)不受影响(无旧 forecast 史即无烧预算)。

## 修订记录

- v8(2026-07-31,执行期缺陷修复——起点 cycle 部分成功后的续跑死锁;含定向 review 两 P1 的重设计):IFS Phase-1 attempt-1 于 070500 五成一败;续跑时调度器(正确地)拒绝重提已完成模型,而 D5 步骤 4 原终态腿要求**每模型**新终态 job id → 结构性 `convergence_timeout`(attempt-2 实机复现,journal_terminal=[])。终态腿改双路;第一版"入口冻结活索引观测"方案被定向 review 推翻(F1:journal 归属腿恒真——reset 前终态记录留存 + cohort 记录归属全模型,腿塌缩为纯 checksum 判据,且拆掉"忘带 `--resume-from`"误操作的死锁兜底,会以 `prior_source=captured` 伪造 pre-image;F2:冻结点实为"首个可判轮",首轮索引不可判时本 pass 自产后继被冻入——两者均有可执行红证),终版改为**先前证据与 resume receipt 行绑定**(见步骤 4 (b)),冻结机制删除。delta review F4(P1)再收紧一轮:锚点从"上一 pass 观测到替换"改为合取判据(`state_entry_present` ∧ 终态锚),其中 review 提议的"缺键回退 `journal_terminal`"经实机 receipt 核验修正——attempt-1 行 `journal_terminal` 六行全 True(cohort 归属不分成败)、attempt-2(零提交 pass)行全 False(pass 相对量),故终态锚必须与 per-model 判别子 `state_entry_present` 合取,且 attempt-3 续 attempt-1 的 receipt;收敛决策处每行补记 `successor_checksum`(halted 与 completed 行同一代码路径)供跨跳身份对账(F4 rider;completed 结转行同样过对账,与 `_verified_skip` 口径合流);F5(P2)措辞对齐。receipt 行 `prior_eligible` 保持 receipt 级语义(入场≠过关:身份对账不合的模型显示 `prior_eligible=true` 而 `terminal_evidence=null`,该组合即诊断信号)。F3(reset receipt 键序列化漂移可使 `replaced_successor_entries` 把未变旧后继判为已替换)经实机抽验关闭:396 条 `removed_entries` 的 `valid_time` 全为规范 `…Z` 形,attempt-2 六行 `prior.state` 全非空。receipt 行增 `terminal_evidence`/`prior_eligible`,schema/runbook §3 同步。伴随处置:lh_gl 坏 IC 的 scratch 镜像副本修复(receipt `lh-gl-ic-repair-scratch-20260731T042030Z`)与 manual-retry 事件解锁(失败 run 的 retry 因 state 缺 forcing URI 被 fail-closed 拦截,exact-cycle repair 门因 070500 无 raw manifest 不可用;事件必须携带 retry-binding id 才能过 identity filter 留存,event 988 生效)。执行期缺陷 2:manual-retry decision 到 D3.5 fresh-zero-row 合并点无准入腿(升级改标后与普通 retry 不可区分 → `nfs_raw_manifest_required`,attempt-3 实机零提交),修复为窄准入 sibling 腿,见 D3.5 v8 增补段。
- v1(2026-07-30):初稿,基于两轮 node-22 只读勘察 + 变更 1 审计 receipt。
- v2(2026-07-30,fixture review r1 修订):P1-1 旁路座从不可达的 `completed_duplicate_pipeline` 重定位为 strict-warm-start 终态分支整体覆写(`replay_resubmit`);P1-2 覆写显式压过 successor-retry(`state_save_qc` 重启=复用错初值输出,必须消除);P1-3 chain `_FORCE_TERMINAL_RESUBMIT_DECISIONS` 白名单入 Impact;P1-4 retry budget 不消费;P1-5 node-27 压缩块前置;P2-6 R3 改键漂移断言(parser 删并集窗口);P2-7 旧半 state 字段来源=reset receipt;P2-8 forcing/模型包 checksum 无条件入 receipt;P2-9 tile 失效收窄 (source, valid_time 窗口);P3-10 re-ingest 触发修正(32/33 走 init_state_id 分支);P3-11 R2 降级(分 cohort 已核实);P3-12 去 `--max-passes`;P3-13 存量现场核定。
- v3(2026-07-30,fixture review r2 修订):R1 覆写触发 token 修正——实机形态是 `terminal_hydro_success`(hydro-success 分支先于 pipeline-success),触发条件改为完成型终态族 `_STRICT_WARM_START_TERMINAL_SKIP_REASONS`,`terminal_completed_cycle` 显式不覆写(通用 skip → 驱动器超时停);R4 evidence 形态对齐 mismatch retry(`native_shud_resubmitted=True` 等,防 `_upgrade_retry_for_strict_warm_start_manifest` 改标丢 token);R2 tasks 4.1/4.2 tile 收窄同步;R3 proposal 3/5 同步;R5 tasks 重编号;R6 proposal 作用面两处。
- v4.1(2026-07-30,4R 修复落地后的正文对齐):D5 步骤 4/5/6 与 R3 按实现后的 oracle 改写(收敛判定去 created_at、完成判定基线为旧终态 job id、首时次绑定 `--origin-cycle`、键一致断言轴改为 river-network 版本 + 输出段数 + 输出文件清单、receipt 逐 cycle 落盘);spec "Replacement traceability receipt" 同步同一措辞。行为口径以 tasks 4R 与实现为准,本条只是消除正文 stale。
- v4(2026-07-30,round-1 实现评审修订,verifier 全裁决 head 4293b474):新增 D3.5——canonical-readiness fresh-zero-row 合并点是第三道生产门(A-P1-1 raw-less 腿无条件 block 不看 `required`;A-P1-2 raw-ready 腿 merge 把 restart 降为 convert),guard site `scheduler_candidates.py:775-793`,proposal 作用面两处 → 三处;D3 首时次形态澄清(journal 留存 ⇒ 070500 仍 `terminal_hydro_success` 被覆写,零介入指 bootstrap 证据原样存活,A-P2-4);D6.2 重写——deviation 1(source_id=run_id)裁 ACCEPT,scope 补 `hydro-national`(C-F1),窗口上界改末次 cycle+168h=2026-07-28T00Z(C-F2),v2 收窄论证废止(C-F6);spec 同步新增两 scenario(raw-less 存活装配、raw-ready 保持 forecast restart)并修 node-27 措辞;C-F5 由 D3.5 + spec 修订承载(candidate 级门与两相位 env 姿态入 design 正文)。
- v5(2026-07-30,round-2 实现评审修订,verifier 全裁决 head 9836bfe9):D3.5 增补第三腿——authorized-repair 候选在 canonical 门第一腿被拦(A2-1),replay 窗口内以 raw-manifest-restart 完整合并放行(restart 降至 convert 从 raw 重建,窗口外不变);raw-less 腿要求替代证据实际在场且 ready,按 discovery provenance 区分,缺失一律 typed block(A2-3);D4 锁探针改 NB-flock+内容新鲜度(锁 mtime 恒为创建时间,B2-3);D5 续跑沿用 resume receipt 旧半 + 同路径拒跑 + receipt 写前置/写失败 typed halt + reset receipt scope 覆盖校验(B2-1/B2-2/B2-4);D6.2 失效工具多 receipt 并集 + outcome 门 + 真实 NFS 路径(C2-1);A2-2 probe 循环序无关化重开 4R.2;spec 同步(repair convert-restart scenario、替代证据必须验证、receipt 韧性与续跑保真句)。
- v6(2026-07-30,three-round gate retro 纠正动作,verifier 全裁决 head 4b01348b,retro 见 `.workplans/issue-1164-change2/review/review-failure-retro-r3.md`):深度形态——"replay 证据必须穿过全部 merge 腿"不变量三轮四腿反复失守(A-P1-1/A-P1-2→A2-1→A3-1),弃逐腿 guard,改 D3.5 后装配不变量钳 + merge 点审计测试;repair 腿收紧 fresh-zero-row 前提(A3-2);D5 续跑语义集中化(全行结转 + status/断言双门,B3-1/B3-2)+ `resume_from` 溯源;D6.2 替换 receipt mode 0644 + 可读性前置(C3-1);运维文档口径(C3-2 信号退出码、C3-3 守卫放宽)。
- v8.1(2026-07-31,执行期 repair-2 round-3 修复落地,retro 见 `.workplans/issue-1164-change2/review/review-failure-retro-repair2-r3.md`):D3.5 v9 round-3——`fresh_ingestion` 补入两个 whole-set 钳保护集且 resubmit 模式一并改为 whole-set 缺席钉法(只加键是 no-op,F1);两个 Slurm-status-sync 块的重判后各补重戳、家族收窄、`admitted` first-wins(F2;**其中第二块的「重升级」半已由 v8.2 撤除**);新增消费者推导闭包审计测试与 whole-set 自检(F3);并更正 v9 round-2 与 `_replay_manual_retry_restart_stage` docstring 里「与 `candidate_restart_stage` 逐字相同」的事实错误。

- v8.2(2026-07-31,round-4 delta 复审 P1 修复落地):D3.5 v9 round-4——第二处 resync 站点(`:1237`,canonical 门下游)不再重跑 strict-warm 升级,helper 拆为 `_replay_manual_retry_restamped_decision`(戳 + `admitted` first-wins)与 `_replay_manual_retry_resynced_decision`(前者 + 升级,仅第一处站点 `:829` 使用);修复 raw-ready manual-retry 腿被 `forecast` restart 覆写导致的自相矛盾 receipt(跳过前置 forcing + 零行 canonical 上从 forecast 起跑、cohort 由 `(1,"convert")` 变 `(3,"forecast")`);raw-ready 腿补钳仍 deferred(#1198);并更正 `_replay_manual_retry_restart_stage` docstring 中「钳对每个被准入 replay 候选钉缺席 `fresh_ingestion`」的过度声称。
- v7.1(2026-07-30,round-5 B5-1 local-repair rider,verdict 见 `.workplans/issue-1164-change2/review/round5-verdicts.md`):D5 v7 的 resume scope 覆盖判据由顶层声明改为**行覆盖 ∪ 声明**——与该条自身"运行时产出"论旨一致;runbook §3 的"续上一份"指引对加回 scope 的跳同样成立。
- v7(2026-07-30,second gate entry retro 纠正动作,round-4 verifier 全裁决 head 64370e99,retro 见 `.workplans/issue-1164-change2/review/review-failure-retro-r4.md`):深度形态第二次——v6 机制架构核实可靠但保护集欠推导且 round-3 oracle 全在生产者侧;改**消费者侧闭合**:D3.5 v7 保护集从下游消费者实际读取的键推导(clamp 补 `decision` + `durable_shud_output_reused`,oracle 直接驱动 chain gate,A4-1);D5 v7 全行结转按运行时产出判定(rows 键控映射全行播种,halt/checkpoint/pre-flight 单一 owner,三跳 halt oracle,B4-1)+ resume receipt scope 校验(`resume_receipt_scope_mismatch`,B4-2);runbook exit-4 段落随守卫放宽同步改写(C4-2)。
