## 1. Station-Set Atomic Flip

- [x] 1.1 Implement the cutover station-flag flip mounted on the Change `source-specific-model-variant-routing` lifecycle extension point, with the row-selection predicate pinned to the Change 4 discriminators: inside the activation transaction, set `active_flag=true` for exactly the mirror rows bound to the ACTIVATING target variant's built mapping-asset identity (resolved from the hook context's target model `resource_profile.direct_grid_forcing` `model_input_package_id` + `binding_checksum`, matched against the Change 4 mirror row shape: `station_id = "<mapping_asset_identity>::cell:<grid_cell_id>"`, `station_role='direct_grid_cache'`, `properties_json` binding identity, `grid_snapshot_id` FK), and set `active_flag=false` for every other `met.met_station` row of the `basin_version` (legacy rows with `grid_snapshot_id IS NULL` AND every non-target direct-grid generation's mirror rows), without changing the station-MVT source query. The hook self-gates per the engagement classifier: it engages only when the target classifies as direct-grid under Change 4's single classifier AND the hook context carries a previous active model; otherwise it no-ops writing no row, with an audited skip reason (`target_not_direct_grid` / `no_previous_active_model`).
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_flip.py` proves that on commit the whole set re-points together (exactly the target's mirror rows → `true`, every other row of the `basin_version` → `false`), and that a forced failure at any step of the cutover transaction rolls the whole transaction back with no `active_flag` change persisted (no "previous set off / target not on" and no "target on before activation" intermediate state).
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_flip.py -k "fix_forward or generations"` proves (a) the direct→direct′ fix-forward re-flip: with M1 active and M1′ registered (both generations' mirror rows coexisting, same `basin_version`), cutover to M1′ ends with only M1′'s mirror rows `active_flag=true` and every M1 mirror and M0 legacy row `false`; and (b) with two registered-but-inactive generations, cutover activates only the target generation's mirror.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_flip.py -k "legacy_noop or no_previous"` proves the negative engagement cases: a legacy-target `activate`/`switch_version`/`rollback_version` (the 13 production basins' routine operations) leaves every `met.met_station` row of the basin untouched and records the audited skip reason `target_not_direct_grid`; a direct-grid activation with no previous active model no-ops with `no_previous_active_model` and touches no station row.
  - Required evidence: a test asserts `apps/api/routes/hydro_display.py` `_station_source_version` still filters only by `basin_version_id + active_flag=true` and is not modified to add a `model_id` predicate.
  - Non-goal for 1.1: no basin activation, no change to the Change 4 lifecycle op itself, no `model_id` filter on the MVT query, no fresh-basin first-display bring-up (Change 7 owns it).
- [x] 1.2 Lock the no-mixed-display invariant: prove the shadow-period mirror stays `active_flag=false`, that the pre-cutover station-MVT layer returns only the previously active set, that the post-cutover layer returns only the target generation's cell stations (zero legacy rows AND zero non-target-generation mirror rows), and that at no committed instant is the MVT set the union of two station sets.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_flip.py -k "shadow or mixed or post_cutover or feature_budget"` proves the shadow mirror is registered/kept `active_flag=false` (regression-locked, fails closed if ever `true`), the pre-cutover MVT set excludes every mirror generation, the post-cutover MVT set excludes legacy rows and non-target generations, and the union is never emitted (so the feature budget is never fed a mixed set) — including across a direct→direct′ re-flip.
  - Non-goal for 1.2: no MVT feature-budget constant change; the invariant is "never mix", not "raise the budget".

## 2. Historical-Cycle Display Degradation

- [x] 2.1 Wire the new-pin-on-old-cycle miss to the existing retention empty state: after cutover, a new M1 cell-station pin on a pre-cutover cycle whose station-series disk file is absent renders the `adapt-cycle-picker-retention-window` retention empty state, drawing no chart and adding no DB/archive fallback.
  - Required evidence: `cd apps/frontend && pnpm test` (station popup suite) proves a new-pin-on-old-cycle `STATION_FORCING_FILE_NOT_FOUND` renders the retention-specific empty state, draws no chart, raises no generic error, and introduces no DB fallback / archive read / synthetic points; and asserts the two mechanical properties that make the mismatch retention-bounded: the issue-time picker offers only catalog-provided cycles (it never synthesizes or persists a pre-cutover option of its own), and the retained-out marking is per-cycle, per-session popup state with no persistence (boundedness itself follows from upstream retention rotation, which is not a frontend-testable property and is not claimed as one).
  - Non-goal for 2.1: no rebuild of the retention empty state (reuse only), no backend reader change, no timeline redesign outside the station popup.
- [x] 2.2 Regression-lock pre-cutover product/timeseries answerability by `(cycle, model)` key, independent of the flag: pre-cutover flow products and station/forcing timeseries stay queryable with their old `model_id`, and the flip deletes/hides no historical record.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_history.py` proves a pre-cutover flow product resolves by `(cycle, model)` unaffected by the flip, a pre-cutover station/forcing timeseries resolves from the immutable old-variant assets with its old `model_id` — explicitly including the cross-requirement case: a post-flip `active_flag=false` M0 legacy station whose pre-cutover file exists within retention still serves its series (the B4 fix must not break this; see task 3.2) — and no pre-cutover product/timeseries row is deleted or made unqueryable by the flip.
  - Non-goal for 2.2: no new history/archive API; only regression coverage of the existing data-layer answerability.

## 3. Active-Model Dynamic Resolution (incl. B4 leak fix)

- [x] 3.1 Audit and close every `model_id` propagation path so no display/frontend surface pins or caches a `model_id` across a cutover, including the MVT tile cache key; keep the MVT tile cache key derived from the `active_flag=true` station source identity (`_station_source_version`) so it self-invalidates on flip. There is no server-side active-model resolution on the station-series path by design — `model_id` is a required client-supplied filter — and the audit locks that property rather than inventing a server-side resolver.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_model_resolution.py` proves (a) the station-series endpoint keeps `model_id` a required client-supplied filter with no server-side active-model default and no server-side `model_id` cache (`MISSING_REQUIRED_FILTER` regression when absent — `apps/api/routes/data_sources.py`:141 + `packages/common/object_store_forcing.py`), (b) the MVT tile cache key is not keyed to a pinned `model_id` and self-invalidates on flip (the `_station_source_version` string changes when the active station set flips), and (c) an explicitly-requested historical cycle may still carry its own old `model_id`.
  - Required evidence: `cd apps/frontend && pnpm test` proves the frontend resolves the live `model_id` from the currently selected latest product at request time (`stationSeriesProductIdentity` uses `product.model_id`) — after a cutover a live (latest-product) request carries the newly active `model_id`, not one captured pre-cutover — and does not reuse a pre-cutover `model_id` for a live request; and `cd apps/frontend && pnpm exec tsc --noEmit` passes.
  - Non-goal for 3.1: no change to the historical per-request explicit `model_id` path (that is legal immutable-asset resolution), no `model_id` predicate added to the MVT source query, no new server-side active-model resolver.
- [x] 3.2 Close the B4 single-query inactive-row metadata leak (deferred by the readiness §2.3 registration receipt's station-by-id lookup note, `openspec/changes/archive/2026-07-10-cmfd-direct-grid-platform-readiness/evidence/db-registration-2.3.node-27.pass.log`): for an inactive (`active_flag=false`) / evidence-only row, the single-station endpoint (`packages/common/object_store_forcing.py` `StationForcingFileNotFoundError` feeding `apps/api/routes/data_sources.py` `/met/stations/{station_id}/series`) keeps the stable `STATION_FORCING_FILE_NOT_FOUND` code on a disk miss but desensitizes its `details` to at most `{station_id}` — no object-store-root-relative storage key (`expected_path`), no `(basin_version_id, model_id, source_id, cycle_time)` tuple. The fix form is pinned to this 404-details desensitization: do NOT filter `active_flag` in `PsycopgStationLookup` (that would 404 post-flip historical M0 reads whose files exist, breaking task 2.2's evidence, and would contradict the deployed `STATION_NOT_FOUND` trigger). Active-station behavior is unchanged. Implement the matching `object-store-station-series-read` MODIFIED delta in this change.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_b4_leak.py` proves (negative test) an inactive / evidence-only `station_id`'s disk-miss response cannot enumerate the object-store-root-relative storage key (`expected_path`) or the `(basin_version_id, model_id, source_id, cycle_time)` tuple; (positive test) an active station legitimately missing a cycle file still returns its normal `STATION_FORCING_FILE_NOT_FOUND` details (no over-filtering); and (cross-requirement test) an inactive post-flip M0 legacy station whose requested pre-cutover file exists still returns its series successfully (the lookup keeps resolving inactive rows).
  - Non-goal for 3.2: no change to the object-store path scheme, the active-station error contract, or the `STATION_NOT_FOUND` trigger; no `active_flag` filtering in the lookup; no tightening of the station-MVT 404 body (that is a separate surface, outside the §2.3-recorded leak).

## 4. Display-Cutover Rehearsal Receipt (node-27 live)

- [x] 4.1 Provision the rehearsal preconditions under the evidence-only identity on the synthetic `basin_version` (`basin__evidence_cmfd_p02_synth__v1`, per `openspec/changes/archive/2026-07-10-cmfd-direct-grid-platform-readiness/evidence/register-synth-p02.sql`), recording every step in the receipt with its cleanup/restore: (a) a synthetic previous-active baseline `core.model_instance` made `active` by recorded SQL (recorded bypass — the rehearsed lifecycle op is the cutover, not the baseline), with the legacy-shaped readiness `synth-station-001..003` rows (`forcing_proxy`, `grid_snapshot_id IS NULL`) provisioned `active_flag=true` as its display set for the window; (b) a Change-4-shaped synthetic M1 target registered through the Change 4 registration surface — conforming mirror rows (`station_id = "<mapping_asset_identity>::cell:<grid_cell_id>"`, `station_role='direct_grid_cache'`, derived-cache `properties_json`, `grid_snapshot_id` FK, `active_flag=false`) and an object-store-hosted synthetic package URI + rereadable checksum so Change 4 activation preflight passes; (c) a Change 5 explicit cold-start approval covering the synthetic variant's `applicable_source_ids` so the engaged clone hook traverses its real sanctioned approved-skip path (the positive clone body stays owned by Change 5's verification — recorded in the receipt); (d) one seeded pre-cutover synthetic cycle: a ready forecast `hydro.hydro_run` (`cycle_time` earlier than the rehearsal cutover) + latest-product row bound to the baseline model, making the synthetic basin discoverable in the `/` basin selector (`GET /api/v1/basins` `has_display_product` discovery) so the popup path is reachable; seeded rows deleted in the recorded cleanup.
  - Required evidence (node-27): the committed receipt under this change's `evidence/` records each provisioning step, its SQL/registration output, and the matching cleanup/restore, plus an exercised-versus-bypassed enumeration (real path: preflight, engaged clone hook via approved-skip, flip hook, swap, audit, manifest re-publish; recorded bypasses: SQL-provisioned baseline, clone body skipped under approval).
  - Non-goal for 4.1: no production row written; no forcing CSV written to the production object store; no Change 4/5 mechanism re-verified beyond what the rehearsal traverses.
- [x] 4.2 Run the synthetic-identity rehearsal cutover on node-27 and capture the receipt end-to-end: one real Change 4 `activate` cutover transaction on the provisioned synthetic identity, captures while committed, then restore.
  - Required evidence (node-27,取证方式): SSH `ssh -p 32099 nwm@210.77.77.27`, run the rehearsal and capture via `docker exec nhms-db psql` a pass log recording (a) the synthetic set flip inside the transaction (exactly the conforming M1 mirror rows `true`, the provisioned legacy set `false`); (b) the station-MVT source-identity before/after diff on the synthetic `basin_version_id` (via `https://test.nwm.ac.cn` MVT source-version / `_station_source_version`) — before = a source-version string over the provisioned synthetic legacy active set (a defined observation, not `MVT_SOURCE_IDENTITY_NOT_FOUND`), after = a different string over exactly the M1 mirror; (c) production-scoped zero-impact SQL assertions evaluated BOTH during the committed window AND after restore: the 13 production basins' `met.met_station.active_flag` unchanged and the active `core.model_instance` count excluding `model_id LIKE 'model__evidence%'` equals 13 at both points, with the transient GLOBAL active count of 14 during the window recorded explicitly as expected; (d) the restore, ordered before the final assertions: the synthetic model deactivated via the Change 4 `deactivate` lifecycle op committed with the sys_admin missing-active override (returning it to a non-active `lifecycle_state`), every synthetic `met.met_station` row restored to `active_flag=false` by recorded SQL, and the seeded run/product rows cleaned up; (e) scheduler-plane cleanliness: the post-restore re-published scheduler registry manifest contains no `model__evidence%` model, and no `hydro.hydro_run` row was created for any synthetic model during the window (rehearsal timed between scheduler cycle boundaries). Commit the pass log under this change's `evidence/`.
  - Required evidence (node-27 display, screenshot): after the flip and before the restore, browse the seeded pre-cutover synthetic cycle on `https://test.nwm.ac.cn` (the popup resolves the seeded product's baseline `model_id`), open a new cell-station pin whose station-series file is absent for that cycle, and screenshot the retention empty state as a live receipt (CLAUDE.md red line — not substituted by local ruff).
  - Non-goal for 4.2: no production basin activation; no write to any production `met.met_station` row; no touch of `active_flag` on the 13 production basins; append-only lifecycle audit rows remain by design and are recorded, not scrubbed.
- [x] 4.3 Record the flow-curve cross-cutover continuity receipt as DEFERRED-to-pilot: write the deferral and its rationale into the rehearsal receipt so the absence is a recorded deferral bound to backfill, not a passed verification.
  - Required evidence: the committed `evidence/` receipt records the flow-curve cross-cutover continuity receipt as DEFERRED-to-pilot with the rationale (no production basin is activated here, so no real cross-cutover flow curve exists to sample), and a certification note declares it a recorded absence of evidence — not a certification gap — bound to backfill at the pilot's first real cutover.
  - Non-goal for 4.3: no synthetic/mocked flow curve manufactured to fake continuity; the deferral is honest absence, not a placeholder pass.

## 5. Verification

- [ ] 5.1 Run the full change verification suite.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_display_cutover_flip.py tests/test_direct_grid_display_cutover_history.py tests/test_direct_grid_display_cutover_model_resolution.py tests/test_direct_grid_display_cutover_b4_leak.py` passes.
  - Required evidence: `uv run ruff check .` reports `All checks passed!`.
  - Required evidence: `cd apps/frontend && pnpm test` and `cd apps/frontend && pnpm exec tsc --noEmit` pass.
  - Non-goal for 5.1: node-27 live receipt is covered by group 4, not re-run here.
- [ ] 5.2 Validate the OpenSpec change.
  - Required evidence: `openspec validate direct-grid-display-cutover --strict --no-interactive` reports `Change 'direct-grid-display-cutover' is valid`.
  - Non-goal for 5.2: no archive; the change is inert mechanism until `direct-grid-batch-rollout` (Change 7) references it.
