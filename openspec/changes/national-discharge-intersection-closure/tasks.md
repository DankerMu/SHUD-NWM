# Tasks — national discharge intersection closure (#2457 #2458 #2459)

## Risk Packs

- [x] Legacy compatibility — **selected**: the no-argument `/valid-times` answer flips to `[]` on an incomplete intersection; complete intersections unchanged (row 37 regression). Covered by 2.x.
- [x] Concurrency / shared state — **selected (ruling only)**: the two-statement snapshot seam; no order or SQL change. Covered by 1.2, 3.x.
- [x] Documentation / migration notes — **selected**: helper + function docstrings, matrix rows 40d/40e/40f/40g. Covered by 1.3, 3.2, 4.2.
- [ ] Public API / CLI, Config, File IO, Error handling, Schema, Auth, Resource limits, Release — not selected (no schema/OpenAPI/SQL change).

## 0. Setup

- [x] 0.1 Branch `feat/issue-2457-2458-2459-national-intersection` from `origin/master` @`67a10613d`. Locate sites by symbol, not by the issues' line numbers (the cited `tests/test_hydro_display_mvt_scaling.py` has been split into `tests/test_hydro_display_mvt_scaling_*.py` + `tests/hydro_display_mvt_helpers.py`).

## 1. #2458 pre-check and #2457 ruling (orchestrator)

- [x] 1.1 node-27 read-only pre-check (`nhms_display_ro`, `statement_timeout=20000`, `lock_timeout=2000`): active set vs the helper's covered-network set.
  - Executed verbatim (`psql -X -v ON_ERROR_STOP=1 -x -c "select current_user" -f probe.sql`, file preceded by `\timing on`): `WITH active AS (SELECT DISTINCT mi.river_network_version_id AS n FROM core.model_instance mi WHERE mi.active_flag AND mi.river_network_version_id IS NOT NULL), covered AS (SELECT DISTINCT mi.river_network_version_id AS n FROM hydro.hydro_run h JOIN core.model_instance mi ON mi.basin_version_id = h.basin_version_id JOIN hydro.run_display_coverage rdc ON rdc.run_id = h.run_id AND rdc.segment_count > 0 WHERE h.status IN ('succeeded','parsed','published') AND mi.river_network_version_id IS NOT NULL AND mi.active_flag) SELECT now() AS observed_at, (SELECT count(*) FROM active) AS active_count, (SELECT count(*) FROM covered) AS covered_count, (SELECT coalesce(array_agg(n ORDER BY n), '{}') FROM (SELECT n FROM active EXCEPT SELECT n FROM covered) d) AS active_minus_covered, (SELECT coalesce(array_agg(n ORDER BY n), '{}') FROM (SELECT n FROM covered EXCEPT SELECT n FROM active) d) AS covered_minus_active;` — the helper's no-argument coverage predicate (source/cycle/since all NULL) vs its active query.
  - 2026-09-23T12:18:57Z: `current_user nhms_display_ro`, `active_count 48`, `covered_count 48`, `active_minus_covered {}`, `covered_minus_active {}`, 277 ms → #2458 is a production no-op on today's data (design §Stated consequences for why that is not a stable property).
- [x] 1.2 #2457 ruling written (design D1): keep two statements; frequency unquantifiable, harm same-type and render-safe, cost quantified; reopen triggers.
- [x] 1.3 (implementer) `services/tiles/mvt.py` `_national_discharge_coverage_rows` docstring: the numerator-SHRINK paragraph cites #2457 and states the ruling in one or two sentences (two statements kept; criteria in the change design; reopen triggers). `test_national_cycles_still_list_a_cycle_whose_covered_run_stopped_being_display_ready` docstring points at #2457's ruling; expectation unchanged.
  - Done: the SHRINK paragraph now carries "RULED by #2457" with the criteria in one sentence, the reopen triggers, and a pointer to design D1 by change name (survives archive); the prior sentence "merging … changes the returned row shape" narrowed to the LEFT JOIN form, UNION ALL costed. Characterization test docstring points at the ruling; expectation unchanged.

## 2. #2458 — no-argument branch compares against the active set

- [x] 2.1 `national_discharge_valid_times`: keep `active_networks` from the helper; after `latest_by_network` is built, `if frozenset(latest_by_network) != active_networks: return ValidTimeDiscovery(valid_times=[], limit=sample_limit, observed_count=0, truncated=False)` (design D2 placement). The `if not latest_by_network:` empty return STAYS (design D2: rows = ∅ ∧ active = ∅ must not reach `max()` of an empty list). Docstring's no-argument bullet states the fail-closed rule.
  - Done: `rows, active_networks = …`; empty guard kept first (commented why); `if frozenset(latest_by_network) != active_networks: return ValidTimeDiscovery([] …)` before the per-row window validation. Docstring no-argument bullet states the rule.
- [x] 2.2 Tests (no-argument form `national_discharge_valid_times(session)` with `_NationalDiscoverySession(rows, active_networks=...)`), next to the existing no-argument cases:
  - partial: rows cover `{rn-a, rn-b}`, active `{rn-a, rn-b, rn-c}` → `valid_times == []`, `observed_count == 0`; non-vacuity: rows non-empty, `len(active_networks) == 3`.
  - equal-size mismatch: rows `{rn-a, rn-c1, rn-c2}`, active `{rn-b, rn-c1, rn-c2}` → `[]`.
  - superset: rows `{rn-a, rn-b, rn-c}`, active `{rn-a, rn-b}` → `[]`.
  - empty/empty: rows `[]`, `active_networks=[]` → `[]`, `observed_count == 0`, no exception (pins the kept empty guard).
  - regressions stay green unchanged: `test_no_argument_national_valid_times_keep_a_network_whose_newest_run_predates_the_lookback`, `test_valid_times_route_without_arguments_keeps_serving_the_national_list`, `test_no_argument_national_valid_times_are_empty_with_no_coverage_rows`, and every existing `national_discharge_cycles` / `national_discharge_valid_times` case with zero expectation edits.
  - RED before 2.1 for the three new cases (record).
  - `tests/test_hydro_display_mvt_scaling_discovery.py::test_no_argument_national_valid_times_fail_closed_unless_the_covered_set_equals_the_active_set` (parametrized `partial` / `equal-size-mismatch` / `covered-superset`; non-vacuity is stronger than asked: the same rows judged against their own networks yield a non-empty timeline, plus `frozenset(covering) != frozenset(active)`); `tests/test_hydro_display_mvt_scaling_catalog.py::test_no_argument_national_valid_times_are_empty_with_no_rows_and_no_active_network`. No existing expectation edited.
  - RED (pre-2.1, the 4 target tests / 10 node ids): `3 failed, 7 passed` — exactly the three new no-argument ids (e.g. `covered-superset / assert ['2026-09-05T…:00:00Z', …] == []`); empty/empty green before, as designed. GREEN: `10 passed`.

## 3. #2459 — superset oracle at every judging site

- [x] 3.1 Add `("covered-superset", ("rn-a", "rn-b", "rn-c"), ["rn-a", "rn-b"], False)` to `test_national_cycle_coverage_helper_compares_sets_like_the_per_cycle_valid_times`; route-level superset case on the canonical national tile route (`_NationalRouteSession(active_networks=("rn-a","rn-b"), coverage_networks=("rn-a","rn-b","rn-c"))`) → 424 `MVT_NATIONAL_IDENTITY_INCOMPLETE`, counts 3/2, `"tile" not in _statement_kinds(session)`.
  - `("covered-superset", ("rn-a","rn-b","rn-c"), ["rn-a","rn-b"], False)` added (the `executions` bind-sequence assertion holds unchanged); `tests/test_hydro_display_mvt_scaling_coverage_order.py::test_canonical_national_tile_refuses_a_covered_superset_of_the_active_set` → 424 `MVT_NATIONAL_IDENTITY_INCOMPLETE`, counts (3, 2), `"tile" not in _statement_kinds`, nothing built. `test_national_cycles_close_when_a_covered_network_is_deactivated`'s "only strict-superset case in this file" docstring narrowed to the `national_discharge_cycles` site.
- [x] 3.2 Mutation receipts (no mutation committed), all on the 4.3 file set at the PR head, each paired with an UNMUTATED run of the same set at the same head (the issue's `352/0` is pre-split history, not a comparable baseline): (a) `NationalCycleCoverage.complete` → `>=` must go red; (b) no-argument site, three mutants: check deleted (= 2.2's RED run), `!=` → superset containment (kills 2.2's superset case), `len(latest_by_network) != len(active_networks)` (kills the equal-size-mismatch case). Record `passed / failed` and the failing node ids per mutant. Also re-measure row 40d's mutation (restore the pre-#2087 statement order) on the same set: the `covered-superset` parametrize case asserts the `executions` order too, so 40d's kill list grows. #2459-attributable diff to production files is comment-only.
  - 4.3 set at the final code, unmutated: `338 passed, 0 failed, 21 skipped` (skips = integration-gated). (a) `complete` → `>=`: `336 / 2 fail` — `coverage_order.py::test_canonical_national_tile_refuses_a_covered_superset_of_the_active_set`, `…::test_national_cycle_coverage_helper_compares_sets_like_the_per_cycle_valid_times[covered-superset-covering4-active4-False]`. (b1) check deleted: `335 / 3 fail` (the three no-argument ids). (b2) superset containment: `337 / 1 fail` (`[covered-superset-covering2-active2]`). (b3) cardinality: `337 / 1 fail` (`[equal-size-mismatch-covering1-active1]`). (40d) pre-#2087 order: `326 / 12 fail`, 8 test names, 12 node ids (the new one: `[covered-superset-covering4-active4-False]`). Mutants reverted; `git diff` byte-identical to the pre-mutation diff (`cmp`). Production diff: the only logic hunk is 2.1 (#2458); everything #2459/#2457-attributable in `mvt.py` is docstring/comment.

## 4. Verification

- [x] 4.1 `uv run ruff check .`; `openspec validate national-discharge-intersection-closure --strict --no-interactive`.
  - `uv run ruff check .` → `All checks passed!`; `openspec validate national-discharge-intersection-closure --strict --no-interactive` → valid. (`display-v2-national-timeline-precip-overlay --strict` is red on master already — three MODIFIED-omits-scenario errors in its `frontend-mvt-layer-consumption` / `map-layer-timeline-controls` deltas, untouched here; out of scope, reported.)
- [x] 4.2 Matrix `openspec/changes/display-v2-national-timeline-precip-overlay/invariant-matrix-i5-2009.md` (orchestrator, from the implementer's counts): row 40d's trade text points at #2457; row 40d's node-id count ("×4", "EIGHT test names, ELEVEN node ids", `193 pass / 11 fail`) re-stated from 3.2's re-measure; row 40e "Site gap, recorded not fixed" → fixed by #2459 with the 3.2(a) counts, "routed to a follow-up issue" → #2459, and its "the ONLY case in the file where covered is a strict SUPERSET" sentence corrected (the #2459 cases add more); new row 40f (#2457 ruling, no mutation — ruling row) and 40g (#2458 no-argument set rule, 3.2(b) counts). Update the row-count sentence.
  - Rows 40d (×5 parametrize, TWELVE node ids, re-measured `326 / 12` vs unmutated `338 / 0` on the post-split set, trade → #2457/40f), 40e (site gap FIXED with `336 / 2`, the ONLY-superset sentence narrowed), new 40f (ruling) and 40g (`335/3`, `337/1`, `337/1`); program note at the #2087 CLOSED paragraph and the row-count sentence (87 rows) updated.
- [x] 4.3 Local focused suites green: `tests/test_hydro_display_mvt_scaling*.py tests/test_display_publish_status_only.py tests/test_precip_overlay.py tests/test_mvt_national_identity_probe_integration.py tests/test_api_contract.py` + `tests/test_select_ci_tests.py`.
  - `338 passed, 21 skipped`; `tests/test_select_ci_tests.py` `791 passed`.
- [x ] 4.4 (orchestrator) node-27 isolated oracle at the PR head (scratch PG, `NHMS_RUN_INTEGRATION=1`): selector output for the changed files ∪ the 4.3 set.
  - @`743f775` (45 suites = selector output for the diff ∪ the 4.3 set; Python 3.11.15; disposable scratch PG from the nhms-db image on :55559; `NHMS_RUN_INTEGRATION=1`): `3592 passed in 576.76s`, 0 skipped, rc=0. The final head adds only openspec text (review Notes: MODIFIED consumer list, D1 render-safety scope) on top of it.
- [ ] 4.5 CI green on the PR head.

Evidence Floor: 1.1 receipt; 2.2 RED→GREEN; 3.2 mutation counts (a) and (b); 4.3 local; 4.4 node-27 oracle; `uv run ruff check .`; openspec strict validate.
