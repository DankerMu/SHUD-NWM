## 1. Selector implementation

- [x] 1.1 Introduce one five-prefix backend Python source authority and make `_is_backend_python_path` plus the renamed/generalized same-name helper consume it.
- [x] 1.2 Preserve the existing caller's existence gate, explicit-rule set union, `matched` semantics, missing-target filtering, and core-smoke fallback for sources without a same-name suite.

## 2. Mechanized regression guards

- [x] 2.1 Explicitly rewrite the scripts-only scope test to prove a non-`scripts/` source such as `packages/common/state_qc.py` now selects its same-name suite while a source without a suite still follows its existing route.
- [x] 2.2 Generalize the tracked pair helper and completeness guard across `apps/api/`, `packages/`, `services/`, `workers/`, and `scripts/`, deriving pairs from `git ls-files` rather than a frozen list.
- [x] 2.3 Make the completeness guard distinguish unknown-fallback leakage from targets independently selected by explicit rules, especially `apps/api/**` selecting `tests/test_api.py`; directly pin `apps/api/runtime_mode.py` to the exact explicit-rule plus same-name union.
- [x] 2.4 Add a tree-derived cross-prefix stem-collision guard that requires the shared suite to import every colliding source module; current `best_available` passes without a name-specific exception.
- [x] 2.5 Preserve existing guards for scripts mappings, explicit differently named rules, missing same-name suites, changed-test/meta-guard behavior, guarded-module closure, and node-id targets.
- [x] 2.6 Independent assert-only five-prefix boundary oracle: each exact backend prefix routes a present same-name target without core smoke; target-present negatives (a `.py` outside the five prefixes, a non-Python path inside a backend prefix) never same-name route; a direct live victim pin (`workers/grid_registry/shared_binding_eligibility.py` -> `tests/test_shared_binding_eligibility.py`) without fallback. Mutant-proofed on the final head: deleting `workers/` from the production tuple reds 12 (12 failed / 198 passed = 210 on the final tree — the workers positive, the membership equality, the victim pin, the five derived-CORE_SMOKE valid rows, the zero-collision hermetic row, the mixed known+unknown union, and the state-matrix same-name-class / multiple-paths rows — all of which place fixtures under `workers/`); widening to any `.py` reds the outside-domain target-present negative; adding `db/` reds the membership equality. The name-agnostic reachability regression is not a prefix-boundary check (it derives its non-colliding set from the same-name pairs), so it stays green on the prefix-deletion mutant by design.
- [x] 2.7 Same-name source routes schedule the selector meta-guard (D6), so a source-only PR that adds a colliding source runs the collision/import contract in the targeted lane. A reachability regression pins the meta-guard on every live collision source plus a non-colliding same-name path, and an end-to-end tracked-tree red proof (temporary git repo with a generic two-source `collision_probe` pair) reds the real collision guard naming the missing import. Corrected the stale CORE_SMOKE pair comment in the completeness guard (today `services/slurm_gateway/gateway.py` -> `tests/test_gateway.py`) and scoped the no-fallback check to exclude only the current pair's accepted derived target (CAND-R2-01), not a whole-pair exemption.

## 3. Evidence floor

- [x] 3.1 Batched red proof (final-head): with all final tests present and production same-name routing reverted to scripts-only, selector suite reported **21 failed / 189 passed** — the broad orchestrator pin, the non-scripts scope pin, the four boundary-prefix positives, the live victim pin, the explicit-plus-derived union oracle, the tracked-tree completeness guard, the five derived-CORE_SMOKE valid rows, the true fallback-leak row, the synthetic collision-reachability regression, the mixed known+unknown union pin, the state-matrix rows (3/4/5 same-name class, 5b explicit+same-name union, collision cardinality, multiple-changed-paths accumulation), and the live-collision leg. Restored production source: **210 passed**; no `red-proof` stash remained. (The earlier 2 failed / 173 passed and 10 failed / 176 passed records were stale: both predated the final union oracle, the boundary-oracle tests, and the Round-3 invariant-closure rows.)
- [x] 3.2 `uv run pytest -q tests/test_select_ci_tests.py` -> **210 passed** after syncing the final branch with `origin/master` at `dd72124f` (209 on the pre-#1697 base plus 1 upstream support-module routing contract test; the evidence-binding tools add no selector tests).
- [x] 3.3 Selector CLI: `packages/common/storage.py` -> `tests/test_storage.py` plus the selector meta-guard (same-name rider); `packages/common/auth_policy.py` -> the unchanged five-test core-smoke fallback; `apps/api/runtime_mode.py` -> the explicit-rule plus same-name union plus the rider; `services/slurm_gateway/gateway.py` -> the slurm-gateway rule set plus its same-name suite plus the rider; mixed known+unknown change set -> known route's suite + rider UNION the unknown route's core-smoke fallback.
- [x] 3.4 Final tracked census: 304 backend sources, 66 source/suite pairs, 65 unique suites, 0 misses, and one tree-derived two-source collision. Final master-vs-current selector comparison over the branch `git ls-files` (3,004 tracked paths; `origin/master` at `dd72124f` has 2,998; +6 = this change's tracked OpenSpec files): **65 changed selections**, decomposed as 15 pre-fix gaps that now add their same-name suite plus the meta-guard rider (13 fallback-only also drop the unrelated core smoke, 2 API-additive add alongside explicit targets), 50 already-correct same-name source routes that add ONLY `tests/test_select_ci_tests.py` (D6 intentionally broadens every same-name route), and 1 of the 66 pair sources (`scripts/select_ci_tests.py`) whose selection was already exactly the meta-guard and is unchanged.
- [x] 3.5 Full 14-suite gap set plus the meta-guard rider after syncing with `origin/master` at `dd72124f`: **603 collected**; timed run 585 passed / 18 skipped in 17.92 s pytest time (19.93 s wall). The 14 suites are 393 collected / 375 passed / 18 skipped in 1.39 s pytest time (2.88 s wall); the 13 core-smoke-only suites are 319 collected / 301 passed / 18 skipped (the issue baseline was 309 before upstream expanded `tests/test_object_store_forcing.py`); the rider contributes 210 collected tests. All far below 35 minutes.
- [x] 3.6 `uv run ruff check scripts/select_ci_tests.py tests/test_select_ci_tests.py` -> clean; `openspec validate ci-selector-backend-same-name-routing --strict --no-interactive` -> valid.
- [x] 3.7 Invariant closure matrix (Round 3, CAND-R2-01/02/03): (a) exact five-prefix membership vs nonbackend/non-Python target-present negatives (independent assert-only oracle); (b) same-name ordinary suite AND same-name CORE_SMOKE-member suite both route with exactly `{same_name, meta}` and pass the completeness oracle, hermetic per stem; (c) explicit-only differently named, explicit+same-name set union, no-suite fallback exact, mixed known+unknown union; (d) missing same-name target does not schedule the rider, missing meta target is dropped with a warning under a temporary root; (e) collision cardinality: zero legal (hermetic `{}`/`[]`), live tree leg cardinality/name-agnostic (every derived collision, possibly none, has no missing imports and every colliding source selects its derived shared suite plus the rider — no `best_available` lock), newly missing import red (generic `collision_probe` tracked repo plus synthetic red arms); (f) multiple changed paths accumulate and one unknown path arms fallback without erasing known/rider targets; (g) stop-mirror closure: `_effective_explicit_targets` mirrors production `stop_on_match` — a target owned only by a rule behind an earlier stop match is unowned. Direct constructed-rules test (exact stop rule owns target A, later broad rule owns CORE_SMOKE target B -> helper returns only A) plus downstream provenance composition through `_same_name_completeness_offenders` on a DISTINGUISHABLE topology (`apps/api/runtime_mode.py`; constructed rules report the shadowed `tests/test_api.py` as unowned behind the stop, while the DEFAULT live `apps/api/**` rule legitimately owns it and reports no offender — the contrast proves the `rules=rules` forwarding edges offenders->overlap->helper are load-bearing). Mutants: deleting the helper's `stop_on_match` break reds both new tests; removing either `rules=rules` forwarding edge reds the composition test. The live shadow topology (`services/orchestrator/file_orchestration_journal.py` exact stop before the broad `services/orchestrator/**`) is audited as a real instance but the constructed rules are the standing invariant. Mutation results: empty-collision-map rejection reds the zero-collision pin; fallback-gains-rider reds the exact no-suite pin and the mixed union; stop-break deletion reds the two stop-mirror tests; rules-forwarding deletion reds the composition test; workers-prefix deletion, any-`.py` widening, and scripts-only derivation each still red their preserved Round-1 arms. Production source untouched; no stash.

## 4. Review boundary

- [x] 4.1 Diff audit confirms no changes to `PATH_TEST_RULES`, `CORE_SMOKE_TESTS`, `.github/workflows/ci.yml`, production/test assertion behavior, suite-to-suite routing (#1561), or non-same-name importer closure (#1455).
- [x] 4.2 Fixture review, Phase 2 repair, no-deviation statement, selected risk-pack evidence, and current-tree runtime numbers are recorded in `.workplans/1587/` and this checklist.
- [x] 4.3 Replay-backed evidence tooling authored and preflight-validated: generator `.workplans/pr-1679/review/generate-final-mutation-receipt.py` owns one ordered 12-mutation matrix and runs pristine plus each mutation in fresh `git archive HEAD` replicas; FINAL mode rejects working-tree/staged differences in the bound source/test files and any non-`HEAD` ref. Schema 3 binds exact HEAD blobs, generator bytes, replay-contract version, a deterministic matrix-definition digest, and HEAD's frozen merge-base with the fixed `origin/master` ref. Checker `.workplans/pr-1679/review/check-final-mutation-receipt.py --strict` rejects structural/binding drift before execution, accepts unrelated `origin/master` fast-forward descendants only while the frozen merge-base remains unchanged and ancestral to both sides, then independently replays the full matrix and compares every stable outcome/provenance field (durations excluded). Fast self-tests pass 32/32; slow end-to-end mode passes 35/35 and proves a recomputed-sidecar tamper plus a structurally current-bound but never-executed fabricated final receipt both fail strict replay.
- [x] 4.4 Final schema-3 receipt procedure is enforced: `.workplans/pr-1679/review/final-mutation-receipt.json` plus `.sha256` bind the complete 12-mutation matrix to the committed HEAD, source/test HEAD blobs, frozen merge-base compatibility with `origin/master`, generator bytes, replay contract, and matrix digest; pristine is 210 and every mutant total is 210. After every later tracked commit or base sync, the orchestrator regenerates the canonical receipt and reruns `check-final-mutation-receipt.py --strict` (full replay) before using it as current evidence; unrelated remote fast-forwards do not invalidate the frozen receipt, while divergence or changed common ancestry fails closed. Phase 7/8 manifests record the matching final HEAD and digest.
