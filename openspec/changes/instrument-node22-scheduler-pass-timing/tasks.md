# Tasks — instrument-node22-scheduler-pass-timing

## 1. Timing collector core (module: `services/orchestrator`)

- [ ] 1.1 Add `services/orchestrator/scheduler_timing.py` implementing `SchedulerPassTiming` with `pass_span()`, `stage_span(name)`, `candidate_span(name)` context managers using `time.monotonic()` deltas for `_ms` durations and `time.process_time()` deltas for `total_cpu_ms`; snapshot `_cpu_start = time.process_time()` at construction and compute `total_cpu_ms = int((time.process_time() - _cpu_start) * 1000)` in `finalize_evidence()`. Every timing record (evidence dict + stdout JSON line) SHALL carry a fixed `schema_version="nhms.scheduler_pass_timing.v1"` field.
- [ ] 1.2 Read `NHMS_SCHEDULER_TIMING_LEVEL` in `scheduler_config.py` as a plain string (case-insensitive lowercase-normalisation); **validation is deferred to `run_once` per D4** (not at config load), so an unrecognised value does not crash the daemon at startup. The `run_once` validator maps unrecognised values to `SchedulerPassResult(status="preflight_blocked", reason="scheduler_timing_level_unrecognised")` while keeping the `timing.pass` block populated.
- [ ] 1.3 Serialisation: `SchedulerPassTiming.finalize_evidence()` returns the `timing:` dict with `pass`, `stages`, `candidates`, and `restart_reconcile` (candidates omitted below level `candidate`, stages omitted at level `pass`, `restart_reconcile` present at levels `stage` and `candidate` regardless of code path taken); guarantee `pass.python_time_ms + pass.slurm_wait_ms == pass.total_wall_ms` within ±5 ms in the returned dict; guarantee for every stage record `stage.python_time_ms + stage.slurm_wait_ms == stage.total_wall_ms` within ±5 ms; guarantee `pass.slurm_wait_ms == union_ms(all direct-measured Slurm spans across stages + restart_reconcile)` within cumulative ±5 ms.
- [ ] 1.4 Stdout emission: one-line JSON per `pass:started|pass:finished|stage:started|stage:finished` including `schema_version`, `ts`, `pass_id`, `level`, `phase`; no candidate-layer stdout. Use `print(..., flush=True)` so systemd-journald sees lines immediately.

## 2. Wire collector into runtime (module: `services/orchestrator`)

- [x] 2.1 In `scheduler_runtime.run_once`, construct a `SchedulerPassTiming` as the **first statement of `run_once`** — before `root_preflight`, before `db_free_runtime_preflight`, before `NHMS_SCHEDULER_TIMING_LEVEL` validation — so `timing.pass` populates even for the earliest-exit branches. Thread the collector via `SchedulerExecutionContext`; attach the finalised `timing:` block to every `SchedulerPassResult.evidence` return path — every enumerated `SchedulerPassResult.status` value including `submitted`, `preflight_blocked`, `lock_contended`, `lease_lost`, `resource_limit_blocked`, `slurm_status_synced`, `slurm_status_sync_failed`, `restart_reconciled`, `restart_reconcile_unknown`, `planned`, and blocked-pass derivatives. Wrap the `self._run_restart_reconcile()` call at `scheduler_runtime.py:543` in a `restart_reconcile_span` that internally splits python vs `sacct` subprocess time.
- [x] 2.2 In `scheduler_execution.execute_candidate_cohort`, open a `stage_span("<stage_name>")` per (source, cycle, stage) around the cohort loop; record `build_candidates_ms` (time from stage entry to entering the cohort loop; encompasses `context.candidate_output_uri` L248 and `context.candidate_basin_manifest` L276 preparation for the whole cohort) and `dispatch_ms` (remaining python work up to `orchestrator.orchestrate_cycle(...)` return at L358). Stage `slurm_wait_ms` is the sum of direct-measured Slurm spans inside per-stage `_submit_and_wait` calls (see 2.3).
- [x] 2.3 In `chain_forecast_execution._submit_and_wait`, wrap **both** Slurm-boundary call sites in `slurm_wait` sub-spans so `slurm_wait_ms` is a direct measurement, never inferred by subtraction: (a) `slurm_client.submit_job(payload)` at L505 — covers the already-terminal-on-submit fast path at L568-572 where `_poll_until_terminal` is never called; (b) `self._poll_until_terminal(...)` at L574-582 — covers the non-terminal-then-poll path.
- [x] 2.4 In candidate dispatch (`execute_candidate_cohort` inner loop plus `_submit_and_wait`), open `candidate_span(basin, source, stage)` around the concrete code regions that actually exist:
  - Inside `services/orchestrator/scheduler_execution.py execute_candidate_cohort` (L233-410) per basin, sub-spans: `output_uri_lookup` (L248-273), `basin_manifest_build` (L276-283), `slurm_env_check` (L289-303), `secret_manifest_scan` (L305-308), `resource_profile_check` (L309-322), `stage_raw_input` (L329, per-cohort but attributed as equal share per basin for accounting simplicity).
  - `orchestrator_dispatch` (L358 `orchestrator.orchestrate_cycle`) SHALL be measured per-cohort at cohort scope and attributed as equal share across basins.
  - Inside `services/orchestrator/chain_forecast_execution.py _submit_and_wait` (invoked per stage per model by `orchestrate_cycle`, L489-599) per candidate, sub-spans: `build_stage_manifest` (L498), `submit_sbatch` (L505, direct-measured Slurm wait per 2.3 (a)), `poll_until_terminal` (L574-582, direct-measured Slurm wait per 2.3 (b); this sub-phase is `0` when the fast path at L568-572 fires), `post_stage_hook` (L590-594 `_after_stage_success` / `_after_stage_failure`).
  - All spans SHALL no-op when level < `candidate`.

## 3. Unit tests (module: `tests`)

- [x] 3.1 `tests/test_scheduler_timing.py::test_pass_layer_invariant` — mock gateway, verify `python_time_ms + slurm_wait_ms == total_wall_ms` on a synthetic pass with 2 stages each doing `time.sleep(0.1)` inside `_poll_until_terminal`; also assert `total_cpu_ms >= 0` and `schema_version="nhms.scheduler_pass_timing.v1"`.
- [x] 3.2 `test_stage_layer_slurm_wait_split_poll_branch` — inject `_poll_until_terminal` stub sleeping 100 ms with `submit_job` returning non-terminal status; assert emitted stage `slurm_wait_ms ∈ [90, 150]` and stage `dispatch_ms < 50`.
- [x] 3.3 `test_stage_layer_slurm_wait_split_terminal_fast_path` — inject `submit_job` stub sleeping 100 ms then returning a **terminal** status (`_poll_until_terminal` never called, L568-572 branch); assert emitted stage `slurm_wait_ms ∈ [90, 150]` attributed to the `submit_sbatch` sub-span and stage `dispatch_ms < 50`.
- [x] 3.4 `test_stage_invariant_holds_per_record` — assert every stage record satisfies `stage.python_time_ms + stage.slurm_wait_ms == stage.total_wall_ms` within ±5 ms.
- [x] 3.5 `test_pass_slurm_wait_is_union_of_intervals` — with `concurrent_submit_bound=2` and overlapping stage waits, assert `pass.slurm_wait_ms` equals the union of intervals (strictly less than the naive `sum(stage.slurm_wait_ms)`); with `concurrent_submit_bound=1` assert equality with the sum.
- [x] 3.6 `test_restart_reconcile_span_is_included` — stub `_run_restart_reconcile` to spend 300 ms in a fake `sacct` subprocess; assert `timing.restart_reconcile.slurm_wait_ms ∈ [280, 320]` and that this time is counted in `pass.slurm_wait_ms`, not `pass.python_time_ms`.
- [x] 3.7 `test_level_pass_suppresses_stages_and_candidates` — set env level `pass`, verify evidence `timing.stages` and `timing.candidates` absent/empty and no `level=stage` stdout.
- [x] 3.8 `test_level_stage_default_emits_stages_only` — env unset, verify stage records present, `restart_reconcile` present, and candidate records absent.
- [x] 3.9 `test_level_candidate_populates_all_layers` — level `candidate`, verify candidate records emitted to evidence keyed on `(basin_model_id, source_id, stage_name)` but not to stdout.
- [x] 3.10 `test_unknown_level_blocks_pass_with_timing_pass` — level `verbose`, verify `run_once` returns `status=preflight_blocked` with reason `scheduler_timing_level_unrecognised` AND the evidence contains a populated `timing.pass` block (invariant "always emit `timing.pass`").
- [x] 3.11 `test_every_pass_status_emits_timing_pass` — parametrised across every enumerated `SchedulerPassResult.status` value (`submitted`, `preflight_blocked`, `lock_contended`, `lease_lost`, `resource_limit_blocked`, `slurm_status_synced`, `slurm_status_sync_failed`, `restart_reconciled`, `restart_reconcile_unknown`, `planned`, and blocked-pass derivatives); assert `timing.pass` is populated with the correct `status`, `total_wall_ms > 0`, and `slurm_wait_ms == 0` for statuses in which no Slurm dispatch occurred.
- [x] 3.12 `test_very_early_root_preflight_exit_emits_timing_pass` — force `root_preflight` to return blocked immediately (before `db_free_runtime_preflight`); assert the evidence still contains a `timing.pass` block with `total_wall_ms < 5 ms` and `slurm_wait_ms == 0`.
- [x] 3.13 `test_case_insensitive_level_parse` — level `STAGE` accepted.
- [x] 3.14 `test_stdout_single_line_json_versioned` — every stdout emission is a single line ending in `\n`, parses via `json.loads`, and contains `schema_version="nhms.scheduler_pass_timing.v1"`, `ts`, `pass_id`, `level`, `phase`.
- [x] 3.15 `test_instrumentation_overhead_bounded` — compare wall of a mock pass with instrumentation disabled vs level `candidate`; assert delta < max(1 % baseline, 50 ms).
- [x] 3.16 `test_evidence_schema_additive` — evidence artefact produced with `timing:` block still contains every pre-existing top-level key required by consumers of `SchedulerPassResult.evidence`.

## 4. Documentation of new env variable

- [x] 4.0 Create `infra/env/compute.scheduler-dbfree.env.example` as the on-node template that currently only lives on node-22 (per `receipts/2026-06-28-node22-dbfree-scheduler-live-proof.md` line 15/51). The template SHALL enumerate the canonical DB-free scheduler keys observed in the receipt: `NHMS_SCHEDULER_DB_FREE_REQUIRED=true`, `NHMS_SCHEDULER_LOCK_ROOT`, `NHMS_SCHEDULER_STATE_BACKEND=file`, `NHMS_SCHEDULER_LOCK_BACKEND=file`, `NHMS_SCHEDULER_REGISTRY_BACKEND=file`, `NHMS_SCHEDULER_REGISTRY_MANIFEST`, `NHMS_SCHEDULER_CANONICAL_READINESS_BACKEND=file`, `NHMS_SCHEDULER_CANONICAL_READINESS_INDEX`, `NHMS_SCHEDULER_JOURNAL_BACKEND=file`, `NHMS_SCHEDULER_JOURNAL_ROOT`, `NHMS_SCHEDULER_STATE_INDEX_BACKEND=file`, `NHMS_SCHEDULER_STATE_INDEX`, `NHMS_SCHEDULER_EVIDENCE_ROOT`, `NHMS_SCHEDULER_RUNTIME_ROOT`, `NHMS_SCHEDULER_TEMP_ROOT`, `NHMS_SCHEDULER_ALLOWED_ROOTS`, `NHMS_SCHEDULER_SOURCES`, `NHMS_SCHEDULER_ALLOWED_CYCLE_HOURS_UTC`, plus placeholder comments for `NHMS_SCHEDULER_MODEL_IDS`. See design D8 for the rationale.
- [x] 4.1 Add `NHMS_SCHEDULER_TIMING_LEVEL` to the newly created `infra/env/compute.scheduler-dbfree.env.example` with a comment enumerating `pass|stage|candidate` and stating default `stage`. Add `NHMS_SCHEDULER_EVIDENCE_RETENTION_DAYS` (default `90`) and `NHMS_SCHEDULER_EVIDENCE_MAX_MB` (default `512`) with comments describing the retention policy.
- [x] 4.2 Add one paragraph to `docs/runbooks/qhh-continuous.md` describing the new evidence `timing:` block and the env variable's effect on stdout volume.

## 5. Evidence retention script (module: `scripts`)

- [x] 5.1 Add `scripts/node22_scheduler_evidence_retention.py` reading `NHMS_SCHEDULER_EVIDENCE_ROOT`, `NHMS_SCHEDULER_EVIDENCE_RETENTION_DAYS` (default `90`), `NHMS_SCHEDULER_EVIDENCE_MAX_MB` (default `512`), `NHMS_SCHEDULER_EVIDENCE_RECEIPT_RETENTION_DAYS` (default `180`), and `NHMS_SCHEDULER_EVIDENCE_RETENTION_WHITELIST_GLOBS` (colon-separated `fnmatch` patterns, default empty), implementing the age-then-size policy. The script SHALL `mkdir -p <evidence-root>/retention/` before writing any receipt, and SHALL exit non-zero with a diagnostic (without partial writes) if `<evidence-root>` itself does not exist.
- [x] 5.2 Enforce the three-branch whitelist explicitly: (a) scheduler pass evidence — `<pass_id>.json` and `<pass_id>.pre_execution.json` where `pass_id` starts with the literal prefix `scheduler_` (real on-disk shapes: `scheduler_<cycle>_<hex12>.json`, `scheduler_<cycle>_<hex12>.pre_execution.json`, per `services/orchestrator/scheduler_evidence.py:294,356` and `services/orchestrator/scheduler_runtime.py:387`); (b) retention receipts — `retention/retention-*.json` subject to the receipt-retention-days rule (default 180); (c) explicitly whitelisted names via `NHMS_SCHEDULER_EVIDENCE_RETENTION_WHITELIST_GLOBS`. Every non-matching file SHALL be logged as `skipped: unrecognised`. Safety filters: skip files with sibling `.tmp` or `.lock`; skip files younger than 1 hour; declare `evidence_write_error.json` (emitted at `services/orchestrator/scheduler_runtime.py:991` on evidence-write failure) out of scope and log as `skipped: unrecognised`.
- [x] 5.3 Emit retention receipt JSON at `<evidence-root>/retention/retention-<utc-iso>.json` containing `started_at`, `finished_at`, `total_before_bytes`, `total_after_bytes`, `deleted_count`, `deleted_paths` (with pre-deletion mtime), `receipt_pass` (list of retention-receipt files deleted this run under the receipt-retention-days rule; usually `[]`), `skipped_count`, `skipped_paths_by_reason` (grouped by `in-flight`, `safety-window`, `unrecognised`), `policy` (resolved `retention_days`, `max_mb`, `receipt_retention_days`, `whitelist_globs`). Always emit even on empty-diff. The script SHALL create `<evidence-root>/retention/` if absent before writing.

## 6. Systemd user units for retention (module: `infra/systemd`)

- [ ] 6.1 Add `infra/systemd/nhms-scheduler-evidence-retention.service` (sibling of the existing `infra/systemd/nhms-node27-raw-retention.service` template) invoking the retention script with the standard `EnvironmentFile=` pointing at the same env file used by `nhms-compute-scheduler.service` (`/scratch/frd_muziyao/NWM/infra/env/compute.scheduler-dbfree.env` on node-22).
- [ ] 6.2 Add `infra/systemd/nhms-scheduler-evidence-retention.timer` (sibling of the existing `infra/systemd/nhms-node27-raw-retention.timer` template) firing every 24 h with an initial delay avoiding overlap with peak scheduler pass activity.
- [ ] 6.3 Add a paragraph to `docs/runbooks/qhh-22-business-bringup.md` describing the retention timer's deployment step (`systemctl --user daemon-reload && systemctl --user enable --now nhms-scheduler-evidence-retention.timer`); reference the on-disk path `infra/systemd/nhms-scheduler-evidence-retention.{service,timer}` (same convention as `nhms-node27-raw-retention.{service,timer}`; both are user-scope units that still live under `infra/systemd/`).

## 7. Retention unit tests (module: `tests`)

- [ ] 7.1 `tests/test_scheduler_evidence_retention.py::test_age_based_deletion_under_cap` — synthetic dir with mixed ages, size under cap; only files older than retention-days deleted; receipt records each deletion.
- [ ] 7.2 `test_size_based_eviction_after_age` — synthetic dir over cap after age pass; oldest-first deletion until under cap; receipt records age-pass and size-pass sets separately.
- [ ] 7.3 `test_in_flight_write_skipped` — file with sibling `.tmp` never deleted; receipt records `skipped: in-flight`.
- [ ] 7.4 `test_safety_window_skipped` — file with mtime < 1h never deleted; receipt records `skipped: safety-window`.
- [ ] 7.5 `test_foreign_file_untouched` — `notes.txt` in evidence root not touched; receipt records `skipped: unrecognised`.
- [ ] 7.6 `test_empty_pass_still_emits_receipt` — no eligible file; receipt still written with `deleted_count: 0`.
- [ ] 7.7 `test_pre_execution_json_subject_to_age_pass` — `scheduler_<pass_id>.pre_execution.json` older than retention-days and without sibling `.tmp` is deleted; both it and the sibling `<pass_id>.json` appear in `deleted_paths`.
- [ ] 7.8 `test_receipt_files_have_longer_window` — `retention/retention-<old>.json` receipt older than 180 days is deleted during the receipt-specific pass; current run's receipt records the deletion under `receipt_pass` with resolved `policy.receipt_retention_days=180`; receipts younger than 180 days are preserved regardless of the scheduler-evidence age pass.
- [ ] 7.9 `test_evidence_write_error_json_is_out_of_scope` — `evidence_write_error.json` in evidence root is never deleted regardless of age; receipt records the path under `skipped_paths_by_reason.unrecognised`.
- [ ] 7.10 `test_retention_creates_receipt_subdir_on_first_run` — evidence root exists but `retention/` subdirectory does not; retention script creates the subdirectory and writes the receipt successfully with no `FileNotFoundError`.

## 8. Local verification gate

- [ ] 8.1 `uv run ruff check .` — All checks passed.
- [ ] 8.2 `uv run pytest -q tests/test_production_scheduler.py tests/test_file_orchestration_journal.py tests/test_scheduler_timing.py tests/test_scheduler_evidence_retention.py` — full green.
- [ ] 8.3 `openspec validate instrument-node22-scheduler-pass-timing --strict --no-interactive` — pass.

## 9. Implementation-merge gate (single-session, blocks PR merge)

- [ ] 9.1 Push to origin/master; on node-22 `cd /scratch/frd_muziyao/NWM && git pull --ff-only`; redeploy systemd units for the timer and set `NHMS_SCHEDULER_TIMING_LEVEL=candidate` in `infra/env/compute.scheduler-dbfree.env` for one deliberate collection pass.
- [ ] 9.2 Trigger a single production pass (do not block on the pass finishing 1h04–2h43 wall-clock at merge time; capture the running-state receipt showing that instrumentation writes structured JSON stdout and that `timing.pass` starts populating — one live pass fully completing is deferred to §10 post-merge).
- [ ] 9.3 Attach a short `journalctl --user -u nhms-compute-scheduler.service --since <deploy-utc>` excerpt showing at least one single-line JSON `phase=pass:started` record parsed cleanly by `jq`.
- [ ] 9.4 Deploy the retention timer on node-22 and record `systemctl --user is-active nhms-scheduler-evidence-retention.timer == active` plus `systemctl --user list-timers` next-fire timestamp — the 24 h fire itself is deferred to §10 post-merge.
- [ ] 9.5 Revert env to `NHMS_SCHEDULER_TIMING_LEVEL=stage` (or unset for default) after the collection pass has started so steady-state stdout volume matches the design contract; the running pass keeps the level it started with per D4 (`NHMS_SCHEDULER_TIMING_LEVEL` is read at pass entry).

## 10. Post-merge live audit (deferred deliverables, NOT blocking PR merge)

- [ ] 10.1 Wait for one full production pass at `NHMS_SCHEDULER_TIMING_LEVEL=candidate` to complete; attach the evidence JSON `timing:` block to the Epic issue as `receipts/instrument-live-<utc>.json` and assert on the live receipt: `python_time_ms + slurm_wait_ms == total_wall_ms` within ±5 ms; `pass.slurm_wait_ms == union_ms(stage direct-measured Slurm spans + restart_reconcile.slurm_wait_ms)` within cumulative ±5 ms; every stage record satisfies `stage.python_time_ms + stage.slurm_wait_ms == stage.total_wall_ms` within ±5 ms; every stage has non-negative python-time; `schema_version == "nhms.scheduler_pass_timing.v1"`.
- [ ] 10.2 Deploy retention timer on node-22 (already done in §9.4); wait one 24 h fire; attach the resulting `retention-<utc>.json` receipt to the Epic issue confirming `retention/` subdirectory was created (if this was the first fire) and `total_after_bytes ≤ NHMS_SCHEDULER_EVIDENCE_MAX_MB * 1024 * 1024`.
- [ ] 10.3 Produce a short summary (in the Epic issue or a receipt file) ranking the Top-3 python-time consumers observed across 3–5 consecutive production passes, with `source_id`, `stage_name`, mean `python_time_ms`. This ranking is the required Stage-1 input for the follow-up optimisation change #2. Because the runtime cost is 3–5 × 1h04–2h43 wall-clock, this task is explicitly post-merge and is tracked on the Epic issue rather than the PR.
