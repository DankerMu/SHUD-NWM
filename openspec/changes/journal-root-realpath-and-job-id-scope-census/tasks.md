# Tasks

Fixture level: expanded · repair intensity: high · issues: #1943 #1944 (one
PR). Line cites are against `origin/master` `6ca2e0b3`
(`services/orchestrator/file_orchestration_journal.py` 15,0xx lines; other
files named); symbol names are authoritative.

## 0. Evidence Floor

Oracles: local pytest (macOS, `/var` alias available by construction via
`tempfile`), node-27 for the journal suites, **node-22 for the live census
receipt** (read-only, pre-maintenance-window interpreter rule: only
`/scratch/frd_muziyao/NWM/.venv/bin/python -m …` from a detached worktree;
never `uv run`, `uv sync`, or a pull of the live checkout).

- [x] Red proof (pre-change, scratch `red_1943.py`, recorded in the PR): alias-ancestor root → `_db_free_path_check(kind="directory")` blocker `None`; `list_stage_statuses(model_id=None)` → `file_journal_unsafe_scanned_entry`, `model_id="model_a"` → `file_journal_unreadable`; `verify_directory_no_follow` → `SafeFilesystemError: Path component is not a directory`; realpath root → legal rows. After the change the same alias config raises `FILE_JOURNAL_INVALID_ROOT` from `ProductionScheduler.from_env`.
- [x] `uv run pytest tests/test_scheduler_journal_root_authority.py tests/test_scheduler_journal_scope_census.py tests/test_orchestrator_demote_cli_security.py tests/test_node22_entrypoint_invariant.py -q` green locally
- [x] `uv run pytest tests/test_file_orchestration_journal.py tests/test_file_orchestration_journal_read_cache.py tests/test_production_scheduler.py -q` green locally (gate, reconcile propagation and factory consumers unchanged)
- [ ] The four new/changed test files plus `tests/test_file_orchestration_journal.py` green on node-27 (`/home/nwm/NWM`, `git status --porcelain` → `git pull --ff-only`, `export PATH=$HOME/.local/bin:$PATH`, `uv run pytest … -q`); receipt (host, HEAD SHA, pass line) in the PR evidence
- [x] Fail-closed unchanged: `git diff origin/master -- services/orchestrator/file_orchestration_journal.py` is empty (the census imports, it does not edit); `scheduler_runtime.py:1555-1595` untouched
- [x] `uv run ruff check .` clean
- [x] `tests/test_node22_entrypoint_invariant.py` green after the runbook edits, including the new positive assertion (task 3.3) that the census command line appears in the ops runbook in the `/scratch/frd_muziyao/NWM/.venv/bin/python -m services.orchestrator.cli census-job-id-scope` form (the scanner itself only inspects `uv run` / `uv sync` lines)
- [ ] node-22 live census receipt (D4): JSON at `.workplans/pr-journal-root-and-scope-census/node22-census-receipt.json` + the command transcript, produced from a detached worktree at the pushed branch SHA with the active interpreter; fields: configured root, `readlink -f` equality, worktree SHA, interpreter path + `-V`, UTC time, per-surface counts, `divergent_total`, `reconcile_abort_triggers`, residue, exit code, `nhms-compute-scheduler.timer`/`.service` state at run time; worktree removed afterwards (`git worktree list` shows none)
- [ ] Decision recorded from the receipt: `divergent_total == 0` → §3 runbook is the recovery path, task §5 not built (stated in the PR); `> 0` → §5 built and verified before merge
- [x] `openspec validate journal-root-realpath-and-job-id-scope-census --strict --no-interactive`

## 1. #1943 — journal root authority seam (D1)

- [x] 1.1 New `services/orchestrator/journal_root_authority.py`: `JOURNAL_ROOT_INVALID_MESSAGE` constant and `verify_journal_root_authority(journal_root: str | Path, *, setting: str) -> Path` — `verify_directory_no_follow(Path(journal_root))`; `except (OSError, SafeFilesystemError)` → `raise OrchestratorError("FILE_JOURNAL_INVALID_ROOT", JOURNAL_ROOT_INVALID_MESSAGE, {"error_type": type(error).__name__, "journal_root": str(journal_root), "setting": setting}) from error`. The message is one constant for both callers: it speaks of "the configured journal root" (not of any one setting — the scheduler sets it through `NHMS_SCHEDULER_JOURNAL_ROOT`, the demotion CLI through `--journal-root`), states every path component must be a real directory (no symlink), and gives the remedy `readlink -f`; no path, traceback or module name in the message (the configured value and the setting name ride in `details`).
- [x] 1.2b `from_env`'s two preflight-blocked branches pass the sentinel `_DB_FREE_REPOSITORY_BLOCKED` instead of `None`, and `__init__` maps the sentinel to `active_repository = None` before the `is not None` chain, so a rejected root is never verified or constructed (implementer deviation 1; design D1 "Strictness").
- [x] 1.2 `_db_free_orchestration_repository_from_config` (`scheduler_core.py:44-49`): `FileOrchestrationJournalRepository(str(verify_journal_root_authority(config.scheduler_journal_root, setting="NHMS_SCHEDULER_JOURNAL_ROOT")))` — the repository root is the verified (tilde-expanded, un-resolved) authority path, the same object the demotion lane uses. Confirm the only caller is `scheduler_core.py:111` and that `plan-production` (Click and argparse) surfaces the error as `code: message` on stderr with exit 1.
- [x] 1.3 `operator_reserved_demotion.py:71-82`: replace the inline `verify_directory_no_follow` + `OrchestratorError` block with the helper (`setting="--journal-root"`); keep the surrounding comments' intent. `tests/test_orchestrator_demote_cli_security.py:838` updated to the new message (deviation recorded); its traceback / module-name / zero-bytes assertions unchanged.
- [x] 1.4 New `tests/test_scheduler_journal_root_authority.py`. Every `from_env` bullet builds its config from the complete db-free environment fixture already used in `tests/test_production_scheduler.py` (manifest, state index, readiness index, allowed roots covering both the alias and its realpath) — `from_env` returns `active_repository=None` unless the whole db-free preflight passes, so a partial env would test nothing:
  - alias-ancestor root (an explicit `alias -> real` symlink two levels above the root, so the case is true on Linux as well as under macOS `/var`): `_db_free_path_check(kind="directory")` still returns no blocker (pin of the trap) **and** `ProductionScheduler.from_env(config)` raises `OrchestratorError` with `error_code == "FILE_JOURNAL_INVALID_ROOT"`, `details["error_type"]` set, `details["setting"] == "NHMS_SCHEDULER_JOURNAL_ROOT"`, message containing `readlink -f` and "real directory";
  - realpath root at node-22 depth (six real components under `tmp_path`): `from_env` constructs, `active_repository.root == verified path`;
  - root that is itself a symlink and a symlink-loop root: same error code — asserted on `_db_free_orchestration_repository_from_config(config)` directly, with `config.db_free_runtime_preflight()["status"] == "blocked"` asserted alongside, because these two shapes are exactly the ones the preflight already catches and `from_env` short-circuits into its blocked branch before the factory (implementer deviation 2);
  - preflight-blocked pass builds no repository: `from_env` with a missing / symlink-leaf / symlink-loop root returns a scheduler whose `active_repository is None`, and the factory is not called (implementer deviation 1, see design D1 "Strictness");
  - `plan-production` CLI (both entrypoints, reuse the existing db-free env pattern from `tests/test_production_scheduler.py`): stderr exactly `FILE_JOURNAL_INVALID_ROOT: <JOURNAL_ROOT_INVALID_MESSAGE>`, no `Traceback`, exit 1, zero bytes written under either side of the alias;
  - seam identity: `operator_reserved_demotion.verify_journal_root_authority is journal_root_authority.verify_journal_root_authority`.
- [x] 1.4b Direct-construction audit: grep the tests for `ProductionScheduler(` with `db_free_required=True` and no injected `active_repository`; any such construction now reaches the factory — report the count and make the affected tests pass a real directory root (recorded in the implementer report; expected zero per design D1).
- [x] 1.5 Docs (count-independent): one comment line above `NHMS_SCHEDULER_JOURNAL_ROOT` at `infra/env/compute.scheduler-dbfree.env.example:42`, `infra/env/compute.example:58`, `infra/README.two-node-docker.md:115`; `docs/runbooks/current-production-ops.md` §3.1 (one sentence in the db-free scheduler paragraph) + new subsection `### 8.10 NHMS_SCHEDULER_JOURNAL_ROOT 必须是 realpath`（symptom: preflight PASS, every cycle a blocked row, darwin `Path component is not a directory` / Linux ELOOP text, neither says symlink; check: `readlink -f "$NHMS_SCHEDULER_JOURNAL_ROOT"` equals the value; remedy; since this PR the scheduler refuses to start with `FILE_JOURNAL_INVALID_ROOT`）; one cross-reference line each at `qhh-22-business-bringup.md:380` region and `failed-basin-retry.md:88` region. Every node-22 command line in the runbooks uses the exact interpreter path `/scratch/frd_muziyao/NWM/.venv/bin/python` (the invariant test only scans `uv run` / `uv sync` lines, so this is a review obligation as well as a test one).

## 2. #1944 — read-only census (D2)

- [x] 2.1 New `services/orchestrator/journal_scope_census.py` (< 1000 lines): `CENSUS_JOB_ID_SCOPE_HELP`, `CENSUS_SCHEMA_VERSION = "nhms.scheduler.job_id_scope_census.v1"`, `census_job_id_scope(journal_root, *, max_files=None, max_records=None, now=None) -> dict`, `register_click_census_command(cli)`, `add_argparse_census_subparser(subparsers)`, `run_argparse_census_command(args) -> int`; `cli.py` gets the import plus one register line, one subparser line, one dispatch line (pattern: `operator_released_reservation_recovery`).
- [x] 2.2 Root: `verify_journal_root_authority` (task 1.1) then `FileOrchestrationJournalRepository(str(root), max_files=…, max_records=…)` when overrides are given (defaults otherwise).
- [x] 2.3 Surfaces, each read through existing lock-free readers or pure validators — none of `_iter_reconcile_inventory_records`, `_iter_reconcile_pipeline_job_records`, `_ensure_reconcile_inventory_migrated`, `_reconcile_inventory_entry_names_unlocked`, `_write_lock`, `_locked_cycle_write`:
  - `flat_direct`: `repository._iter_direct_pipeline_job_records()` plus the path listing (`_iter_regular_json_files(root/"pipeline-jobs", root=root, max_files=, max_depth=)`, non-recursive) for the presence set;
  - `by_cycle_direct`: `_iter_regular_json_files(root/"pipeline-jobs"/"by-cycle", recursive=True, …)` → `_read_optional_json` → `_validated_direct_pipeline_job_record(payload, expected_job_id=_safe_segment(path.stem))`;
  - `journal_replay`: `repository._iter_pipeline_job_records(include_direct=False)` (latest views + journal segments); counts of `latest/**.json` and `journal/**.jsonl` files reported alongside;
  - `reconcile_inventory`: `list_directory_no_follow_limited(root/"reconcile-inventory", max_entries=max_files, containment_root=root)` (absent directory → `present: false`); a listing longer than `max_files` (the sentinel entry) fails loud exactly as the journal's own listing does (`:7775-7779`); names matching `_RECONCILE_INVENTORY_TEMP_RE` counted as `residue` and left in place; a name that is neither a safe `.json` anchor name nor residue fails loud with `file_journal_reconcile_inventory_invalid`, mirroring `:7792-7795`; `.json` names read with `_read_optional_json` and validated with `_validated_reconcile_inventory_anchor(anchor, expected_job_id=stem)`; the anchor mapping (`job_id`, `source_id`, `cycle_time` — written from the row's own pair by `_sync_reconcile_inventory_for_row_unlocked` `:8494-8501`, so an anchor of a divergent row is itself divergent) is classified by the same gate call and counted in `surfaces.reconcile_inventory.divergent`;
  - `active_reconcile` (`_LEGACY_ACTIVE_RECONCILE_DIRECTORY`): `present: false` when absent; when present, `.json` entries read through `_read_optional_json` + `_validated_direct_pipeline_job_record` — the same reader `_canonical_reconcile_job_unlocked` (`:8443-8449`) uses on that directory.
- [x] 2.4 Classification: per row `repository._require_job_id_cycle_scope({"payload": row}, record_type="pipeline_job")`; `FileOrchestrationJournalError` with `reason == "file_journal_job_id_scope_mismatch"` → divergent (evidence `expected` / `actual` copied into the receipt); any other error propagates (fail loud, exit 1, `code: message`). No second comparison of `_cycle_scope_from_job_id` output anywhere in the module.
- [x] 2.5 Cross-match per divergent `job_id` (deduplicated across surfaces): `surfaces` lists the **row-bearing** surfaces the id was found divergent on (`flat_direct`, `by_cycle_direct`, `journal_replay`, `active_reconcile`) — anchors are not rows and never appear in `surfaces`; the anchor axis is `anchor_present` alone (true when an anchor file for that id exists, whether or not the anchor is itself divergent). Fields: `surfaces`, `own_scope`, `job_id_scope` (from the gate's evidence; for an anchor-only id from the anchor's evidence), `anchor_present`, `flat_direct_present`, `by_cycle_present`, `journal_present`, `reconcile_abort_trigger = anchor_present and not flat_direct_present`. Receipt top level: `schema_version`, `generated_at` (UTC ISO), `journal_root` (configured), `journal_root_verified`, `limits`, `surfaces` (per-surface `present`/`files`/`rows`/`divergent`, plus `residue` for the inventory), `divergent_rows` (sorted by `job_id`), `divergent_total`, `reconcile_abort_triggers`, `exit_code`.
- [x] 2.6 CLI: `census-job-id-scope --journal-root PATH [--max-files N] [--max-records N] [--output FILE]`; JSON receipt on stdout (`sort_keys=True`); `--output` refuses a path whose realpath is inside the verified root (typed error, nothing written); exit 0 when `divergent_total == 0`, 2 when `> 0`, 1 on any typed failure — `OrchestratorError` prints `error_code: message`, `FileOrchestrationJournalError` (which carries `reason` / `field` / `evidence`, not a code) prints `reason: field` — on stderr, no traceback. This deliberately departs from the CLI's existing journal-error convention (`cli.py:657` / `:948`: `print(str(error))`, exit 2) because exit 2 means "divergent rows found" for this command; recorded in the module docstring.
- [x] 2.7 New `tests/test_scheduler_journal_scope_census.py` (< 1000 lines), parametrized over both CLI entrypoints where the CLI is exercised:
  - legal tree (rows reserved through the public API across two cycles, one anchor written the way `_sync_reconcile_inventory_for_row_unlocked` writes it) → `divergent_total == 0`, exit 0, every surface `present` with the expected counts;
  - **segment-only divergent row with anchor** (mint: reserve a legal row, rewrite its `job_id` bytes in every segment/latest file that carries it — the shape `_rewrite_persisted_job_id` in `tests/test_file_orchestration_journal.py:19131` already replays through the scan-abort test at `:19241` — delete its flat and by-cycle direct files, write an anchor for the rewritten id in the `:8494-8501` shape) → counted once, `surfaces == ["journal_replay"]`, `anchor_present`, `not flat_direct_present`, `reconcile_abort_trigger`, `reconcile_abort_triggers == 1`, `surfaces.reconcile_inventory.divergent == 1`, exit 2;
  - divergent row present flat + by-cycle + replay → counted once (dedup), `reconcile_abort_trigger` false;
  - anchor-only divergent id (no row anywhere) → one `divergent_rows` entry with `surfaces == []`, `anchor_present`, `not flat_direct_present`, abort trigger true, `surfaces.reconcile_inventory.divergent == 1`;
  - inventory entry that is neither a safe `.json` name nor residue → exit 1, `file_journal_reconcile_inventory_invalid: reconcile_inventory` on stderr, nothing written;
  - `.tmp` residue in `reconcile-inventory/` → `residue == 1`, file still present afterwards;
  - **zero-write**: snapshot of every directory and file (relative path, kind, bytes; `os.walk(followlinks=False)`) equal before/after on the divergent tree, and monkeypatched `_iter_reconcile_inventory_records`, `_ensure_reconcile_inventory_migrated`, `_reconcile_inventory_entry_names_unlocked`, `_locked_cycle_write` raising `AssertionError` are never reached;
  - **predicate-reuse pin**: monkeypatch `file_orchestration_journal._cycle_scope_from_job_id` to return `None` → the same divergent tree censuses to `0`;
  - alias-ancestor root → exit 1, stderr `FILE_JOURNAL_INVALID_ROOT: …`, no traceback; `--output` inside the root → typed refusal, no file;
  - `--output` outside the root writes the same JSON the stdout carries.

## 3. #1944 — runbook (D3, count-independent)

- [x] 3.1 `docs/runbooks/current-production-ops.md` new subsection `### 8.11 #1760 scope gate 与既存分叉 job_id 行`: definition (job_id-encoded `(source, cycle)` ≠ row's own pair; the gate's predicate); census command in node-22 form (`/scratch/frd_muziyao/NWM/.venv/bin/python -m services.orchestrator.cli census-job-id-scope --journal-root "$NHMS_SCHEDULER_JOURNAL_ROOT"`), exit-code meaning, when to run (before any post-#1939 checkout goes live on node-22, and after any hand edit under the root); consequences (a) whole reconcile scan aborts on "anchor + missing flat direct", `evidence["status"]="error"`, zero cohorts restored, every pass; (b) the row cannot be transitioned through any API — non-terminal rows toward any target, terminal rows toward `partially_failed` / `permanently_failed`, which is what `FileJournalRetryService.mark_permanently_failed` on auto-retry's decline path uses; recovery: stop `nhms-compute-scheduler.timer`, back up the files, delete the row's flat and by-cycle direct files **and** its `reconcile-inventory/<job_id>.json` anchor together (an anchor left behind re-aborts the next scan), rerun the census, restart the timer; a segment-resident row stays as inert history and keeps appearing under `journal_replay` with `reconcile_abort_trigger: false` — retiring it is out of scope; link to 8.10 for the root precondition.
- [ ] 3.2 §8.11 also records the node-22 receipt outcome (count, date, SHA) once D4 has run.
- [x] 3.3 `tests/test_node22_entrypoint_invariant.py`: add a positive assertion in the pattern of its existing `:122` check that `docs/runbooks/current-production-ops.md` contains `/scratch/frd_muziyao/NWM/.venv/bin/python -m services.orchestrator.cli census-job-id-scope` (the scanner only inspects `uv run` / `uv sync` lines, so without this the new runbook lines are never checked).

## 4. node-22 live census receipt (D4)

- [ ] 4.1 Pre-checks over ssh (read-only): `git -C /scratch/frd_muziyao/NWM status --porcelain` (expect only the known untracked `.nhms-work/`), HEAD still `3acea778`, `systemctl --user is-active nhms-compute-scheduler.timer nhms-compute-scheduler.service`, `readlink -f` of the configured root equals the value.
- [ ] 4.2 `git -C /scratch/frd_muziyao/NWM fetch origin <branch>` then `git -C /scratch/frd_muziyao/NWM worktree add --detach /scratch/frd_muziyao/nhms-census-<sha> <sha>`; from that worktree `cd` there and `/scratch/frd_muziyao/NWM/.venv/bin/python -c "import services.orchestrator.file_orchestration_journal as m; print(m.__file__)"` must print the worktree path (this module exists at both SHAs, so the check proves the shadowing rather than the import); a `-c "import services.orchestrator.journal_scope_census"` that fails on a dependency the 3.12.7 `.venv` lacks is fail-closed — report it, never `uv sync`.
- [ ] 4.3 Run between timer ticks (`is-active` of the service `inactive`): `/scratch/frd_muziyao/NWM/.venv/bin/python -m services.orchestrator.cli census-job-id-scope --journal-root "$R" --output /scratch/frd_muziyao/nhms-census-<sha>-receipt.json`; a `file_journal_unreadable` from a torn segment read → rerun, never skip; a rejection of a legitimately old row for a non-scope token → finding routed as an issue, no skip added.
- [ ] 4.4 Copy the receipt to `.workplans/pr-journal-root-and-scope-census/node22-census-receipt.json`, record the transcript, `git -C /scratch/frd_muziyao/NWM worktree remove /scratch/frd_muziyao/nhms-census-<sha>` and `git worktree prune`; verify `git worktree list` is clean, the live checkout's working tree and HEAD (`3acea778`) unchanged (`fetch` / `worktree add` touch only `.git/`), and `.venv` untouched.

## 5. Conditional — guarded repair command (only when D4 finds `divergent_total > 0`)

- [ ] 5.1 `repair-job-id-scope --journal-root PATH --job-id ID [--apply]` in `journal_scope_census.py` (or a sibling module under the line guard): dry-run default prints the exact files it would delete (flat direct, by-cycle direct, anchor) for one divergent id, refuses when the id is not divergent per the census predicate, `--apply` deletes through `unlink_no_follow` with `containment_root`, writes a receipt outside the root; tests: dry-run zero-write, apply deletes exactly those files and nothing else, non-divergent id refused, alias root refused.
- [ ] 5.2 Runbook §8.11 gains the command as the preferred recovery over hand deletion.

## Risk packs

- **File IO/path safety** (selected): D1 seam, alias/loop roots, `--output` containment, no-follow anchor listing.
- **Legacy compatibility** (selected): the census reads every on-disk shape the live tree carries (flat, by-cycle, latest, segments, anchors, `.tmp` residue, legacy `active-reconcile`) without a writer change.
- **Error handling/rollback/partial outputs** (selected): fail-loud on any non-scope reader error; zero-write invariant; exit codes.
- **Security/authz** (selected, light): a new CLI over the production root — no write action, typed errors without tracebacks.
- Not selected: Concurrency/shared state (the census holds no repository lock and runs between timer ticks; a torn read fails loud and is rerun); Resource limits/discovery (existing `max_files` / `max_records` budgets reused, overridable).

## Non-goals

- Any edit to `file_orchestration_journal.py`, `safe_fs.py`, `db_free.py`.
- Rewriting journal segments; skip semantics of any kind.
- node-22 checkout sync or `uv` use there.
