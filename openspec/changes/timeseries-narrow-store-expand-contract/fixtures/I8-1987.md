# Fixture I8 (#1987): runbook and executable rollback rehearsal

## Scope and split

Upstream fixture suggestion expanded: agreed. High repair intensity because the deliverable
prescribes production schema rename, shutdown, rollback and destructive DROP. Effective review tier
high; round 1 four seats correctness, invariant-state, test-evidence+spec-compliance,
security-perf+integration. Later rounds at most three, exactly one full-PR owner.

Consume proposal/design D7/D11/D12 and both narrow-store/expand-contract specs unchanged.
Issue #1986 is closed by PR #2274 (merge e0ecf99beeef5deb7efc20fb737de3352c7a745c).

Use the issue's declared minimal mergeable slice: PR A delivers task 5.1 (runbook plus actual
throwaway-cluster rollback rehearsal); PR B delivers task 5.2 (authorized production window plus all
live receipts). Do not close #1987 or check 5.2 after PR A. Do not archive the shared change. Every
PR retains its own review/accountability record.

PR A changes only `docs/runbooks/tier-node27-timeseries-storage.md`, this fixture/tasks completion
evidence, and rehearsal receipts under
`openspec/changes/timeseries-narrow-store-expand-contract/receipts/`. Parent governance selfrow for
PR #2278 rides this push separately. No permanent runtime code, migrations, tests, env files or
units change. A temporary executable rehearsal driver may live under `.workplans/issue-1987/`;
parent runs it on node-27, archives its exact commands/digest and output, then removes it. No
placeholder receipts or simulated production PASS.

## Authoritative operational facts and boundaries

User confirmed host PGDATA `/data/GHDC/nhms-primary/pgdata`; container bind
`/home/postgres/pgdata/data`. No need to reconfirm the migration. Parent read-only observation on
2026-09-12 found `/home/nwm/NWM` clean at a8db554d6402bec642e9a05627eae64b2b79aec3 on
hotfix/node27-rollback-pre-2073; display and autopipe WorkingDirectory point instead to
`/home/nwm/NWM-reslice-original-5a86841c`, via respective `60-reslice-pin-original-5a86841c.conf`.
Actual pinned code is 5a86841c5496f56b6c2e6d725f6ecbecb9d49f2c, with untracked `.venv` retained. API
and autopipe are active. These observations are evidence, not permission to alter the pins.

PR merge authorization is not production window GO. The #2162 restore/pin decision and any foreign
CAPACITY HOLD/60/90/91 drop-ins need their owners' explicit disposition. PR A neither changes nor
removes them. No node-22 mutation; no shared environment rebuild. Treat #2273 as a known
production-governance prerequisite: `/home` free bytes cannot certify capacity of the current PGDATA
destination. Do not silently accept an invalid headroom receipt or fix that code in this docs slice.

References: latest
`docs/runbooks/receipts/2026-09-12-issue-2145-geometry-generation-migration-node27.md`; tier runbook
quiescence, bound-1 override and manual deployment sections; `packages/common/migrate.py`;
`db/migrations/000059_river_timeseries_narrow_expand.sql`; actual old/new parser entrypoints; I7
tests/helpers.

## Invariant Matrix

Governing invariant: at every serving/writing instant, code and canonical schema agree; rollback
preserves old facts and makes new narrow runs recoverable by actual pre-change parsing, without
dropping the only copy of data or releasing a foreign operational fence.

Source of truth: frozen old/new Git SHAs, actual systemd/PID runtime directory,
public.schema_migrations and physical table OIDs, authoritative run identity and timeseries_store,
recorded operator approval.

Surfaces:

- Producers: old/new actual parser, migration 000059, runbook operator commands.
- Validators/preflight: exact unit/lock/PID/catalog checks; ff-only clean-tree gate; role audit;
  timeouts; explicit pin/hold/window authorization.
- Storage/query: canonical text or narrow hypertable, `_legacy`, `_narrow_rollback`, migration
  ledger and route column.
- Entrypoints: API, autopipe/parser and compression/retention/governance timers; no restart against
  mismatched schema.
- Downstream consumers: old reader sees canonical text facts after reverse; new consumers require
  both expanded tables and authoritative routes.
- Failure/rollback/stale state: held locks or partial rename remain stopped; retained narrow rows
  not read by old code; old parser reconstitutes facts before narrow rollback DROP.
- Evidence: exact-image disposable node-27 transcript/JSON, nonproduction DSN identity without
  password/DSN strings, assertions tied to old/new SHAs. Live receipt remains task 5.2, not
  rehearsal.

Boundary-surface checklist:

- Shared helper roots unchanged: migration splitter, parser authority/value/QC helpers and role
  bootstrap reused.
- Public entrypoints: document every effective API/parser launch source, not merely checkout path.
- Read/write/delete: before/after query rows and real parse behavior; no production DDL/DML.
- Staging/publish/rollback: stop timers then drain API/parser; update checkout and effective runtime
  under fence; migrate then validate before restart; timers last, preserving original enable/mask
  state.
- Producer/consumer evidence: commands, tool/source SHA, catalog/result identities and timestamps
  match; no invented migration-count totals.
- Stale-state/idempotency: migration ledger stays applied through D12; automatic migrate does not
  re-expand. Runbook must state explicit reattempt prerequisites rather than falsely promising
  automatic re-expand.
- Unchanged consumers: legacy fact rows/readability, forcing schema/settings and foreign fences
  remain intact.

## Required runbook behavior (5.1)

1. Record GO inputs and abort criteria before mutation. Inventory actual code/pins/units/hold state,
   backups and original unit states. Do not infer current capacity, active SHA or timer state from
   historical absolute numbers. Capture baseline SQL/API/probe plans before expand. #2162/cumulative
   master rollout is an explicit preflight gate, not a silent side effect of pull.
2. Preserve pinned order: timers stop -> API/parser stop and drain -> clean-tree restore decision
   then `git pull --ff-only` -> migration -> validated code/schema start -> timers start. Never use
   automatic stash pop. Hotfix without upstream needs approved branch restore first. No requests
   served during rename/restart interval; abort if quiescence not proven. Exact state comparison,
   never substring `active` matching `inactive`.
3. Reuse bounded-lock/statement PGOPTIONS precedent around actual `packages.common.migrate`
   execution. 000059 is one top-level DO statement, so record its one-statement wall time rather
   than inventing internal DDL timings. Enumerate pending migrations and refuse unexpected files; do
   not blindly apply accumulated master migrations. Required owner must be provisioned/audited
   before apply, no PUBLIC fallback.
4. Pre-expand backlog recipe references existing bound-1 override lifecycle and two-lane timeout
   budget checks; remeasure current chunk geometry, do not transplant old seven-day timing
   constants. Preserve capacity/pin fences and restore the exact env/drop-in/unit state on abort. Do
   not invoke cold move/replay lanes as a shortcut.
5. D12 exact reverse: stop/drain -> rename narrow canonical to `river_timeseries_narrow_rollback` ->
   rename legacy to canonical -> deploy frozen pre-change code -> set all run stores legacy -> start
   only after schema/code validation. Allowed intermediate state: legacy route with retained narrow
   rows. Existing legacy run remains readable/reparseable; previously narrow run is reparsed into
   canonical text before narrow rollback table is dropped. Never DROP CASCADE or drop the only copy.
   Contract removes this rollback option.
6. Reattempt warning: D12 retains route column and migration-ledger row. A normal `migrate`
   skips 000059. A rollback-window new run can inherit default narrow even though old code writes
   text; an approved explicit reattempt must classify rollback-window parse facts correctly and
   clear the owned rollback table before re-executing expand. No automatic ledger deletion or
   unreviewed recovery shortcut.
7. State verbatim that transitional cold tier does not cover `_legacy`; canonical-only cold
   allowlist is unchanged.
8. Lay out task 5.2 evidence acquisition and abort gates, without claiming execution: full-cycle
   firstchunk; two network pins and three storage states; SQL/API bounds; probe miss before/after
   losses; registry active/runnable/selected/excluded provenance; compression/retention include both
   tables (zero eligible new chunks is not proof of compressed-state performance); actual
   working-set governance; three basin screenshots, C1-C4 denywrite, /ops; any fact Seq Scan or
   order-of-magnitude shape regression blocks contract. Browser click P95 belongs to #1970; fourteen
   real daily receipts plus zero legacy chunks belongs only to #1988. No invented immediate
   availability of naturally compressed narrow chunks.

## Rehearsal regression rows (actual node-27 disposable oracle)

R1. Exact production-image dedicated container, no live mount or production DSN; apply historical
schema through 000058 with required owner provisioned. Seed actual authority and old fact fixtures.
Parse/read one legacy run using actual frozen pre-change code, not a hand-coded INSERT standing in
for parsing. Record source SHAs, image/version, relation identities and observable rows. R2. Apply
actual 000059 through existing splitter/runner; parse a distinct new run with current narrow writer.
Prove canonical narrow data and route, legacy facts preserved, exact key identities and values. A
repeated expand does not reclassify that new parse. R3. Execute recorded D12 rename/code/route
sequence in the disposable database. Read the legacy run and reparse it with actual old code; read
old canonical and show prior narrow-only run is absent there while retained narrow rows still exist.
Reparse that prior narrow run with old code; canonical text rows now carry the expected
identities/values. R4. Drop only the owned `river_timeseries_narrow_rollback` without CASCADE after
reparse proof. Both runs remain readable from canonical text, no `_legacy`/`_narrow_rollback`
remains, and untouched forcing shape/settings stay equal. Record migration ledger retained and
ordinary migration runner's skip behavior without falsely labeling it re-expanded. R5.
Failure/boundary check on disposable database: stop before dropping rollback rows when reparse proof
is absent; no claim that dropped data is recoverable without original SHUD artifacts. Existing I7
induced/missing-owner rollback evidence may be referenced for forward atomicity; do not duplicate
all I7 suites.

The rehearsal harness must exercise real code and SQL, fail on mismatched schema/route/data, keep
credentials only in env, and emit DSN-safe receipt. Parent inspects output and independently
executes; implementer does not run project-wide checks. Permanent regression tests are not added to
make a docs PR look tested.

## Verification and completion

Before authoring: read-only fixture review and strict OpenSpec. Parent runs actual R1-R5 rehearsal
on node-27 disposable production-image container with TMPDIR=/home/nwm/tmp. Archive readable
transcript plus structured evidence/digests under the shared change. Markdown/strict OpenSpec and CI
document checks; no backend suite for docs-only. Full-tree Ruff is currently blocked by unrelated
installed skill projection E501; do not suppress or modify user tooling. Task 5.1 checked only after
actual rehearsal is green and runbook matches executed sequence. Task 5.2 and #1987 remain open
until all live proof is collected. Cleanup only owned disposable resources, preserve public receipt
and foreign work.

## Risk packs

Core selected: CLI/script entry (operator execution); config/setup (effective runtime/unit ordering
described, not changed); file overwrite (rollback DROP); schema/columns (physical reverse sequence);
permissions/secrets (exact owner, private DSN); concurrency/order (drain before DDL, timers last);
resource limits (bounded migration/backlog timing); legacy compatibility (actual old parser);
rollback/partial outputs (R1-R5); documentation/migration notes (primary deliverable). Core not
selected: release/dependencies (no new package or build-system change). Domain selected: hydro-met
time windows (legacy/narrow facts); Timescale/PostGIS behavior (hypertable rename, compression
preservation); run provenance (exact run/artifact pairing); published/display identity (code/schema
boundary). Domain not selected: CRS/geometry edits, SHUD numerics, Slurm lifecycle, external
provider fetches (unchanged and no node-22 mutation).
