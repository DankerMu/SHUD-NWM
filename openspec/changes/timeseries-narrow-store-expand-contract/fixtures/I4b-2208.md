# Fixture I4b (#2208): coverage river-row store union

Task 2.3b only; source base `b7cdce635a62dd30badbbf17a8ed625157224ad4`, plus governance-only PR2251 accountability carried by `d57d7b80`. Shared change remains active. Existing user authorization permits per-test disposable post-expand catalogs, never production transition, deployment or early I7.

## Scope and calibration

Effective fixture compact, agreeing with upstream: one authored river-row read and one unchanged coverage upsert; no schema/public signature/connection-policy change. One serial implementer owns production and dependent tests; parent owns baseline, specification, validation and Git. Cross-review seats: correctness+test-evidence and integration.

Selected packs: API/CLI compatibility (existing refresh and CLI suites); schema/columns/units (same seven river output columns and coverage fields, no DDL); concurrency/shared state and error/rollback (unchanged atomic overwrite guard, per-run transactions and parallel refusal/failure accounting); resource limits/discovery (unchanged candidate/window/NULL scan predicates and timeout); legacy compatibility (three aids, NULL-key exclusion, two stores); documentation/migration notes (activation gate). Domain time-series identity/window and PostgreSQL query behavior selected; production Timescale plans explicitly I7/I8. Config, filesystem publication/path safety, auth/secrets, dependencies/packaging, geospatial geometry, SHUD/numerics, Slurm, provider ingestion and artifact provenance are not selected: no changes to those surfaces.

## Implementation boundary

Add `h.timeseries_store` to `_CANDIDATE_RUNS_SQL`; retain every existing projection, join, filter, window expression and resource-profile fallback. `_SCAN_HEADER_SQL` still shares this candidate body but its final seven-column projection and `_SCAN_PARAM_KEYS` remain unchanged. Do not add store lookup/validation to `_refresh` or its public wrappers.

Extract the existing `river_sample_rows` body into one caller-owned `_river_sample_rows_template(store)` following the existing forecast-store/MVT raw-source pattern. Preserve its seven-column order (`run_key`, `basin_version_key`, `river_network_version_key`, `river_segment_key`, `expected_segment_count`, `valid_time`, `lead_time_hours`), joins, enum predicate, three sanctioned aids and complete scan/window guards. Add only the corresponding `cr.timeseries_store = 'legacy'/'narrow'` predicate. Render both raw branches with `render_river_ts_sql` and assemble their `UNION ALL` only inside `river_sample_rows`, once at module initialization. Both branches reuse named parameters and touch only their matching physical table. No whole-DML rendering/union, shared binder, parameter suffix, runtime SQL generation per refresh, fallback store or extra statement.

Station CTEs, everything from `river_identity_coverage` through `coverage`, and the outer `INSERT ... ON CONFLICT DO UPDATE ... WHERE ... RETURNING` must remain byte-identical to baseline. Preserve all ten existing top-level function/class ASTs, including `_refresh` header early return, NULL all-run scans, refusal classification, force, commit/rollback, timeout and parallel connection ownership. Protected source files: renderer, forecast_store, MVT and hydro_display. `tests/integration_helpers.py` stays unchanged; use its existing per-test transition helper.

## Baseline and registry

Parent baseline capture has run: `.workplans/issue-2208/baseline/manifest.json` contains 13 raw inputs in both stores, candidate/header/CTE/upsert SQL, ten function/class ASTs and five protected-source hashes. Preserve the other twelve raw inputs byte-for-byte and immutable golden SHA `d104d1c69cea55cdb86bd8a44d90d8f93c621580b43f74744f6b5cca7cd5a453` (20 historical keys, 215 chains).

Retain registry key `display_coverage:refresh`, but its source becomes the new raw branch factory, never `_REFRESH_SQL`. It has three aids and one canonical mention. Add this key to the existing changed/store-qualified raw set: 13 live raw inputs, eight store-qualified and five historical-comparable inputs. Preserve exact census/closure sets; update dependent shape/golden classifications and prose. Freeze the parent's original full `_REFRESH_SQL` bytes as `tests/fixtures/display_coverage_pre_store_b7cdce63.sql` with a checked SHA, not a regenerated post-change oracle. Keep the HAVING-deletion counterexample meaningful against this historical full statement and/or actual executed coverage SQL; no passing composed unions to the renderer. Keep historical 20-key/215-chain provenance intact.

The frozen statement also supplies pre-transition coverage seed data in `test_mvt_national_identity_probe_integration.py::_refresh_coverage`, whose callers deliberately seed/query the old catalog before selecting their test's store mapping. This helper may execute the hash-checked frozen DML with complete existing named parameters; label it historical fixture preparation, not coverage-runtime verification. Preserve every national route/query assertion and transition ordering. Do not monkeypatch the production coverage SQL globally or use a frozen query in the new coverage behavior owners.

## Observable owners and proof

Extend existing capture-cursor tests in `test_display_coverage_refresh.py` to exercise real `_refresh` for a named run and `None`. Inspect the executed main statement: the same `_REFRESH_SQL`, one river-local union with exactly two branch targets/predicates, identical ordered projections and scan names, one outer upsert/RETURNING, station/rollup/guard unchanged. Named mode prefetches then binds all seven original header-derived scan values; all-runs mode performs no header query and binds all scan values NULL. Keep noncandidate early exit, refusal, force and parallel/CLI behavior. Assertions must fail for an unrendered old river read, a wrong branch store/table, or a union placed outside the river CTE; no mock-echo-only additions.

Upgrade `test_display_coverage_river_rollup_counts_keyed_rows_and_skips_null_key_rows` in the existing surrogate integration suite to legacy/narrow and named/all-runs coverage, using one explicit disposable transition per test after seed and rollback. The keyed run's authoritative facts remain at original times; opposite-store +30-minute decoys must not alter exact counts/windows/lead times. In all-runs mode assert both mapped keyed runs' coverage results, not just the first; keep the all-NULL-key legacy run protected. Preserve NULL-key rows in legacy and every existing overwrite-guard owner (refused whole row/timestamp, force zeroing, already-zero rewrite, first insert, nonempty refresh). Update only their catalog preparation and physical-table observation when required.

Adapt the three existing residual-debt tests that call real coverage refresh to prepare their post-expand catalog before the first refresh, once per test. Preserve fast-vs-fallback equality, status-only publication freshness, stale-rescan and out-of-band write behavior. For the last test, target its new data write to the run's authoritative physical store using explicit fixture SQL if the old dual-write helper cannot write that shape; do not change production writers or shared seed helpers. Other residual-debt tests retain their existing setup.

Reuse current selector rules and add only missing dependency edges for actual new registry/frozen-fixture importers and owners. No duplicate rule or closure relaxation. Run full coverage refresh/parallel/CLI, SQL_SHAPE_ORACLE_TESTS, selector and selected transitive pure suites on node27; run full surrogate, national and residual-debt integration suites in disposable databases with zero skips (exclude only timescaledb_210). Local Ruff, strict OpenSpec and pure preservation/capture smoke are hygiene, not production receipts.

Parent proof: run new capture owners against baseline-unrendered SQL and wrong-store/table mutations, requiring assertion failures rather than import/SQL/fixture errors, then restored green. Run the real coverage owner with a valid-SQL swapped route predicate or decoy table mapping: counts/windows must fail by assertion and restore green. Preserve station/rollup/upsert bytes and protected-source hashes. All evidence names the tested SHA or an explicit evidence-only bridge; archive executed harness/receipts, then remove throwaways/private environments.

Production mixed-store coverage, Timescale EXPLAIN and live deployment remain #1986/#1987. This fixture's per-test catalogs prove reader semantics, not rollout readiness. No API/MVT/forecast/source-parser behavior changes; no station forcing changes (#1990). User `.gitignore` remains excluded.
