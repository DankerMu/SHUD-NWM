# I9 / #1988 — River contract 受票（fixture level: expanded）

范围：`tasks.md` 第 6 节。本受票把 6.3 的文字扩到 `spec.md:54` 的实际外延，更正任务清单的定序，并记录一次只读生产门控实测。

> 修订记录：第一稿的 C3 断言“contract 之后覆盖刷新拒绝不可能再发生”，**该前提为假，已整条撤回**（见下方 C3）。第一稿的 M2 改法与 C5 基数亦经审查证伪，已改。撤回保留在原位，不删除。

---

## D0 —— 定序：6.3 被 6.1 的开门检查门控

`tasks.md` 把 6.1/6.2/6.3 按编号排列，但**那不是执行序**：

- `spec.md:54`：“The contract window SHALL deploy the code that no longer references `timeseries_store` before applying the migration” → **6.3 先于 6.2**。
- 6.1 的开门条件“保留窗口内 legacy 路由 run = 0” → **6.1 先于 6.3**。

**执行序：6.1 → 6.3 → 6.2 → 6.4。**

我在本轮之前称 cut (a) = 6.3“纯代码、无需生产 GO、现在就能做”，收回。node-27 跟随 master，合并即部署到活跃主库。

**危害机理（第一稿写错，此处更正）**：第一稿说危害是“一次 recompute 给 legacy 路由 run 写窄表事实”。错——**写窄表事实恰恰是治愈**。真正的危害在**读侧**：6.3 同时拆掉读方的两表路由，部署瞬间所有 in-window legacy run 的事实在窄表里根本不存在 → **立即不可见，不需要任何 recompute 触发**。按第一稿的措辞，读者可能推出“窗口内不跑 recompute ⇒ 可以早合”这个错误豁免。`spec.md:54` 只管住 6.3→6.2，管不住 6.1→6.3，所以这条约束必须靠本受票承载。

### D0 判别结果（已执行只读实测，2026-09-18）

在 node-27 以 `PGOPTIONS='-c default_transaction_read_only=on'` 强制会话只读跑 runner 的 `plan`，收据归档于 `receipts/2026-09-18-i9-entry-gate/`：

| 指标 | 值 |
|---|---|
| **窗口内** legacy 路由 run | **0** |
| 窗口外 legacy 路由 run | 2919（published 2685 + superseded 234） |
| `candidates`（待 reparse） | **0** |
| `compressed_narrow_overlap` | `[]` |
| `artifact_missing` | 0 |
| `window_days` | 21 |

21 天保留窗口已把全部 legacy 路由 run 熬出窗外，正是 runner docstring 预期的终局。推论：

1. **6.3 可实现并合并，全程零生产写**，不需要写侧 GO。
2. 2919 与 C4 所述冻结覆盖行总数独立吻合，C4 的处置对象由此确证。
3. `candidates: 0` ⇒ reparse 通道自建成起在生产上从未有可做之事，其删除（C1a）不欠“最后一次调用”的收据。

**6.1 的门有三项，三项现已全部满足。** `tasks.md:293/295` 要求“reparse receipts + zero in-window legacy-routed runs + **no shape regression**”。第一稿在此处自相矛盾（一边断言开门条件已满足，一边把 shape 检查推给 6.1），现按实据结清：

| 6.1 门项 | 状态 | 依据 |
|---|---|---|
| reparse 收据已归档 | ✅ | `receipts/2026-09-18-i9-entry-gate/`（`candidates: 0`，通道从无待办） |
| 窗口内 legacy 路由 run = 0 | ✅ | 同上，实测 0 |
| no shape regression | ✅ | `receipts/2026-09-18-i8-task52/` §10 |

第三项无需新测：该判据的定义原文即“any pinned curve / MVT / display shape that regresses by an order of magnitude against the legacy baseline, or any Seq Scan on a fact table, **blocks the contract (I9)**”，而 §10 的结论是最差比 **2.19×**（远在一个数量级内）、**fact-table Seq Scan 为零**（`gate0918.py` 判了 58 个 fact plan 节点）、逐字记为 “**Criterion not tripped; it does not block I9.**”。其实测对象是活进程 `258b06ec`，**正是 node-27 此刻仍在运行的 sha**（D0 只读检查时复核过），故该证据对本 cut 直接有效。

**结论：6.1 完成，全程零生产写，6.3 的门已开。**

---

## D1 —— 渲染器签名：由 `spec.md:66` 裁定

`tasks.md:297` 说“remove the legacy path and routing code”，`spec.md:66` 说“the renderer SHALL accept only the narrow store”。spec 是契约 → **按 spec**。

`render_river_ts_sql` **保留 `store` 形参**，取值校验收缩为单值，传 `legacy` 抛错。不解散回调用点：forcing 渲染器是它的镜像，`assert_structurally_intact` / `_assert_no_fact_text_identity` 是存活的 oracle。渲染器降级为窄表校验器。

`_strip_aids` 在 marker 归零后不再移除任何东西，`_assert_key_predicates_retained` 变成模板与自身比较——**那是该删的测试，不是重构生产代码的理由**。

---

## D2 —— 三个冻结 fixture：整体删除，verify grep 保持原样

6.3 的验证 grep 命中三个冻结快照，占 64 行 marker 中的 17 行（11 + 3 + 3）：`hydro_national_mvt_pre_store_c21bacf9.sql`、`display_coverage_pre_store_b7cdce63.sql`、`hydro_mvt_pre_store_f33441a2.sql`。其中第二个被 `tests/river_ts_template_registry.py:115-118` 以 sha256 钉住，**只能删不能改**。

这不是 grep 写错，是 grep 在干它该干的事——这三个快照的存在理由是证明 legacy↔narrow 等价，contract 之后没有主语。**不得**给 grep 加 `tests/fixtures/` 例外，那会让下一个冻结的 legacy 快照藏进去。

全部消费方（缺一即红）：

| 消费方 | 行 |
|---|---|
| `tests/river_ts_template_registry.py` | `:115-118`（sha256 钉） |
| `tests/test_hydro_display_mvt_scaling.py` | `:3915`、`:3946` |
| `tests/test_river_ts_read_path_surrogate_keys_integration.py` | `:208`、`:522` |
| `tests/test_mvt_national_identity_probe_integration.py` | `:2077` |
| `scripts/select_ci_tests.py`（selector 规则） | `:2682`、`:2689`、`:2693` |
| `tests/test_select_ci_tests.py`（元测试） | `:1251`、`:1267`、`:1282`、`:1288`、`:1302`、`:1313` |
| `.github/workflows/ci.yml`（路径过滤） | `:117`、`:118`、`:122`（三个 fixture 路径全在内） |

上表的 `tests/river_ts_template_registry.py:115-118` 只是**加载器**，不是消费者。真正会因删除而死的调用方（第二稿全漏，缺一即红）：`historical_display_coverage_sql()` 的三个消费模块——

| 模块 | 行 |
|---|---|
| `tests/test_river_ts_template_golden.py` | `:39`（import）、`:383`、`:417` |
| `tests/test_display_coverage_refresh.py` | `:31`、`:124` |
| `tests/test_mvt_national_identity_probe_integration.py` | `:84`、`:505`（另 `:2077` 为 national fixture，已在上表） |

最后一行尤其要紧：`tests/test_select_ci_tests.py` 是 selector 元守卫，**本改动族已两次被它抓到 CI 红**。删 fixture 不同步删其 selector 规则必然转红。

`tests/fixtures/forcing_read_path_pre_wiring_0c92093e7.json` 不在此列，其处置见 **M3**（第一稿此处误引“D6”，无此条目）。实测它含 **2 行 / 6 处** marker 文本（`display_coverage.refresh_statement` 3 处 + `forecast_store.latest_product_statement` 3 处），非第一稿所写的 3 处；因是 `.json`，逃过 `--include` 限定的 grep。

---

## C1 —— 删除面：任务点名 2 处，`spec.md:54` 要求 11 处，且有一处必须**存活**

`grep -rln "timeseries_store" --include='*.py' packages workers services scripts apps db` 实测 12 个生产文件：

| 文件 | 处置 |
|---|---|
| `workers/output_parser/parser.py` | 改（M1） |
| `scripts/node27_autopipeline.py` | 改（M1a、M2） |
| `packages/common/display_coverage.py` | 改 |
| `packages/common/forecast_store.py` | 改 |
| `apps/api/routes/hydro_display.py` | 改 |
| `services/tiles/mvt.py` | 改 |
| `services/tile_publisher/publisher.py` | 改 |
| `services/tile_publisher/forcing_copyback_backfill.py` | 改 |
| `scripts/reset_qhh_smoke_db.py` | 改 |
| `scripts/summarize_qhh_smoke_results.py` | 改 |
| `scripts/node27_river_narrow_reparse_backfill.py` | 删（C1a） |
| **`packages/common/forcing_ts_render.py:83`** | **不动** |

最后一行是陷阱：该处讲的是 **`met.forcing_version.timeseries_store`**——forcing 的列，7.3 才创建，与 river 的路由列同名而不同物，**必须存活**。第一稿把它漏了，同时误列了 `services/production_closure/scale_validation.py`（实测**不含**该字面量，只有 `:201-204` 的 `HYDRO_MAP_PLAN_LINES_BY_STORE`）。

**故 `spec.md:54` 不能按字面执行**：“引用了 `timeseries_store` 的代码”既多（含归档证据、冻结收据、forcing 同名列）又少（漏掉按 store 分支但不含该字面量的 `scale_validation.py`）。判据须带排除域，见“验证”。

### C1a —— reparse runner 随列而死，不因“引用了列”被扫入

`scripts/node27_river_narrow_reparse_backfill.py` 引用该列 11 处（`:74,85,111,377,392,465,481,482,670,677,681`），因为那就是它的工作——它是 6.1 开门收据的产出者。它在 D0 定的序里落于 6.3（此时已完成使命，且 D0 实测 `candidates: 0` 证明它无待办），**不是**因为字面命中被扫进来。

**其清算账（第二稿漏，与 C2 为同一危害）**：两个测试模块在模块层导入它——`tests/test_node27_river_narrow_reparse_backfill.py:12`、`tests/test_node27_river_narrow_reparse_backfill_integration.py:26`——只删脚本即**收集期 `ImportError`**。另 `tests/river_ts_stats_matrix_seed.py:423` 在 docstring 里引用其集成测试路径，须一并更新。同时注意 `tasks.md:297` 点名的是 `scripts/node27_river_identity_backfill.py`，**与本文件不是同一个**；C1a 是受票据 `spec.md:54` 扩出的范围，故其清算账由本受票承载。

### C2 —— 任务 glob 漏掉导入方与其 selector 钉

`tests/river_identity_backfill_fakes.py` 不被 `tests/test_node27_river_identity_backfill*.py` 匹配，但 `:18` 导入被删的 runner → 只删点名的两个文件会在**收集期** `ImportError`。

同时须删其 selector 记账（与 D2 对三个 fixture 所做的清算同类，第一稿漏了）：`scripts/select_ci_tests.py:1074-1075`、`tests/test_select_ci_tests.py:10404` 的 `("tests/river_identity_backfill_fakes.py", "tests/test_node27_river_identity_backfill.py")` 配对钉。

---

## ~~C3~~ —— **整条撤回**：该拒绝在 contract 之后照常发生

> **第一稿主张**：`_REFUSAL_ADVICE`（`packages/common/display_coverage.py:786-793`）指名被删脚本，且“contract 之后该拒绝不可能再发生（路由列已不存在）”，故连同拒绝分支一起删除。
>
> **该前提为假。** `refresh_run_display_coverage`（`:858-873`）的唯一 raise 条件是 `run_id in outcome.refused`（`:869`）——即 #1446 守卫跳过了 `DO UPDATE`（新扫描 0 段 + 存量行非零 + 非 force）。**没有任何路由条件参与。**
>
> 更糟的是它与 C4 直接冲突：C4 论证的那 2919 个冻结覆盖行，恰恰意味着 contract 之后对它们的每次 refresh 都落在这个分支。按第一稿执行，要么删掉 C4 所依赖的运维信号，要么删掉守卫本身从而清零冻结行——**当场破 M5 #3**。
>
> 这与 I11 的 C5 是同一失败类：断言了一个未去读判据的行为事实。撤回留痕，不删除。

**正确处置（取代 C3）**：陈旧的只有 `_REFUSAL_ADVICE` 文案里“the identity back-fill (#1408) heals that on its own”**这一句**——它指名 6.3 删除的脚本。同一文案 `:790-791`“Rows that were legitimately removed (retention, a re-parse) produce exactly the same refusal”仍然成立。

→ **只改那一句**（`:789`），保留拒绝分支、保留守卫、保留退出码 3 的运维通道（通道确认于 `tests/test_node27_refresh_coverage_cli.py:93`）。

注记：`tests/test_display_coverage_refresh.py:288`（`== _REFUSAL_ADVICE`）与 `:291`（无换行）钉的都**不是**文案内容，全仓也没有任何测试钉住 `#1408` 那句话——故“随文案更新”在测试侧是空操作，改完不会有测试转红。这意味着**该句的正确性无自动守卫**，只能靠本受票记账。

---

## C4 —— `run_display_coverage` 的 spec 缺口

2919 个窗口外 legacy run 的覆盖行是**冻结**，不是损坏。`display_coverage.py:699-701` 的 #1446 覆写守卫

```sql
WHERE %(force)s OR EXCLUDED.segment_count > 0 OR hydro.run_display_coverage.segment_count = 0
```

原子地跳过 `DO UPDATE`，run 被分类 `refused`；`_stale_run_ids` 仅在 stale 时重扫；retention 从不触碰覆盖表。唯一可观测后果：对历史 `:valid_time` 的请求返回 424。

`spec.md:54` 只覆盖事实表，**未覆盖覆盖表**——真缺口。以 MODIFIED 子句补：

> …their remaining legacy chunks are dropped with the table; their `run_display_coverage` rows stay frozen by the #1446 guard and are never zeroed or deleted.

---

## C5 —— marker 账目：64 ≠ 可成对删除数

`packages/common/river_ts_render.py:2453` 的 `_strip_aids` docstring 写明它删的是 “every marker line **and the single aid conjunct beneath it**”——模板内每个 marker 是 **2 行一组**。这条成对论证正确，但第一稿据此推出“64 个 marker → 128 行”，**把三个不同的量混成一个**。实测账目：

| 类别 | 行数 | 处置 |
|---|---|---|
| 生产模板内的 marker | **30** | 成对删（见下） |
| 三个冻结 fixture | 17 | 随文件整体删除（D2） |
| `river_ts_render.py:106,113` 的 `PUSHDOWN_AID_MARKER` / `_MARKER_TAG` 常量 | 2 | 随机制删除 |
| 测试内的字符串字面量 | 15 | 逐处删（非模板，无配对） |
| **合计** | **64** | |

生产 30 行里，**只有 28 行有 REGISTRY 背书**：`tests/test_sql_shape_helpers.py:845` 钉 `sum(entry.expected_aids for entry in REGISTRY) == 28`，`tests/test_river_ts_read_path_surrogate_keys.py:931` 钉 display 三家合计 20（`hydro_display.py` 3 + `display_coverage.py` 3 + `mvt.py` 14）。

**剩下 2 行不在 REGISTRY 内**：`services/tile_publisher/publisher.py` 1 行、`services/tile_publisher/forcing_copyback_backfill.py` 1 行。实现方**必须逐一目视**这两处是否真是 marker + aid 合取项的 2 行块——它们从未被 `_strip_aids` 处理过，形状无保证。

**成对删除只适用于那 28 行（= 56 行）**；对其余 36 行套用“成对删”会撞上 `_strip_aids` 的 fail-closed 分支。

正确执行顺序：**先在改前的模板上跑一次全量渲染**，`_strip_aids` 的三条 fail-closed 分支（非逐字 marker、marker 下无行、下一行不是恰好一条 aid 合取项）是免费的形状校验，任何抛错即说明该处不是干净的 2 行块。

成对删除正确完成后，“窄表 SQL 逐字节不变”（M5 #1 的模板半边）**结构上必然成立**：模板逐字变成今天的 narrow 渲染结果，可机械自证。

---

## M1 —— parser 的行锁与缺行检测必须在去列后存活

`workers/output_parser/parser.py:872-880` 一条语句承担三件事：锁 `hydro_run` 行、拒绝 legacy、**检测缺行**。`_result_value`（`:47-52`）在 `row is None` 时返回 `None`，故 `store != "narrow"` 的 `DATABASE_ROW_MISSING` 分支同时是缺行检测。去列后两者都要留：

```python
cursor.execute("SELECT 1 FROM hydro.hydro_run WHERE run_key = %s FOR UPDATE", (run_key,))
if cursor.fetchone() is None:
    raise OutputParsingError("DATABASE_ROW_MISSING", "Narrow run identity is missing")
```

`FOR UPDATE` 必须保留：下方 replacement-window 计算在同一事务内，锁围的是 replace 链。

连带删除：`LegacyStoreWriteRefused` 类（`:43`，连同 `:40` 附近的定义上下文）、`:271` 的 re-raise、`workers/output_parser/cli.py` 的 **import 2 处**（`:15` 的 `LEGACY_STORE_REFUSED_EXIT_CODE`、`:16` 的异常类）与**捕获 4 处**（`:65,88,124,143`）。第一稿把 `:16` 误记为“五处捕获”之一。

`tests/test_river_ts_dual_write_integration.py` 的处置：`spec.md:66` 要求整体删除**或**改钉为窄写集成，第一稿只写“`:226,233` 调整”，未回应该选择。**本受票选“改钉为窄写集成”**——该文件还覆盖 `:608,733,763,766,781` 的 decline 竞态语义，那部分与路由无关且仍有效，整体删除会丢掉真实覆盖。

## M1a —— `_record_recompute_decline` 是同一个坑（第一稿漏掉）

**函数名更正**：第二稿把这段代码归给 `_decline_blocked_recompute`，错。它在 `_record_recompute_decline`（`scripts/node27_autopipeline.py:953`）里；`_decline_blocked_recompute`（`:2066`）自己不读该列，只调用前者。仓库自身的 docstring（`:1099`）也把名字记错了，我是从那儿抄错的——实现方请以行号为准。

`scripts/node27_autopipeline.py:973-981`：

```python
cur.execute("SELECT timeseries_store FROM hydro.hydro_run WHERE run_id=%s FOR SHARE", (run_id,))
authority = cur.fetchone()
if authority is None:
    raise RuntimeError(f"Cannot record decline without run authority: {run_id}")
store = authority[0]
if reason_code == "legacy_store_refused" and store != "legacy":
    raise RuntimeError(f"Legacy refusal is stale for narrow run: {run_id}")
```

与 `parser.py:872-880` **结构完全同构**：行存在性校验 + `FOR SHARE` 锁 + 取 store 三合一。列 DROP 后它对**所有** reason_code 的 decline 写都会 `UndefinedColumn`，不只是 legacy 那条。

处置：降为 `SELECT 1 … FOR SHARE`，**保留** `authority is None` 的存在性校验与锁；`:980-981` 的 staleness 断言随列而死。

`store` 还有第三个消费点：除 `:990`、`:993` 外，**`:997` 的参数元组** `(…, store, store)`。故整个 `ON CONFLICT … SET reason_code = CASE …`（`:988-994`）须坍缩为 `SET reason_code = EXCLUDED.reason_code, detail = EXCLUDED.detail`，参数元组同步收缩。漏改会 `UndefinedColumn` 或参数个数不匹配，属响亮失败，但清单在此记全。

同类遗漏（M1 只谈了 `:872-880` 与异常管线）：`workers/output_parser/parser.py:1115-1116` 的 `SET parsed_at = now(), timeseries_store = 'narrow' WHERE run_id = %s AND timeseries_store = 'narrow'` —— 写侧的路由自赋值，随列删除，谓词同步去掉。两处均被判据 2 覆盖。

---

## M2 —— decline 抑制：删 reason 过滤**并**删 `:1079` 的特判分支（第一稿的改法被证伪）

`_declined_runs` 返回的是**被抑制**集合。路由在 `:1078-1082`：

```python
if row[3] == "legacy_store_refused":
    declined.add(str(row[0]))          # 绕过键比对，直接抑制
else:
    recorded.setdefault(...).add(...)  # 进入 (init_state_id, product_mtime) 键治理
```

**第一稿主张**保留 `d.reason_code <> 'legacy_store_refused'`、只删析取项与 `LEFT JOIN`，并称这让 run“回落到常规键路径”。**该主张为假**：该过滤把这些行**整个排除出查询**，于是它们既不进 `declined` 也不进 `recorded`，`:1086-1091` 的 `_decline_key` 比对无从发生 → 该 run **每 tick 无条件重试**，与键变没变无关。第一稿“不是盲目重试”的自辩恰好被推翻。

**正确改法（第三选项）**：SQL 里**整条 reason 过滤删掉**（连同析取项与 `LEFT JOIN`），**同时删掉 `:1079-1080` 的特判分支**，让 legacy 行落进 `recorded`、按 `(init_state_id, product_mtime)` 键治理：

```sql
WHERE d.run_id = ANY(%s)
```

语义：contract 之后 `legacy_store_refused` 是陈旧词表，不再特判；这些 run 与其他 decline 一视同仁——键没变则抑制，键变了则重新入列。这才是第一稿想要而没拿到的语义。`:1079` 正是决定两种行为的开关，第一稿通篇未提。

`_decline_key` 定义在 `:1095`（第一稿误记 `:1099`）。

**两处边界变化需记**（第二稿只记了第一处）：

1. decline 行对应 run 无 `hydro_run` 行时，今天靠 `NULL IS DISTINCT FROM 'narrow'` 为真而被抑制；改后其归宿由键比对决定。
2. 今天 `legacy_store_refused` 行即使 `_decline_key` 返回 `None`（产物 mtime 不可知）也被抑制；改后落到 `:1088-1089` 的 `continue`，该 run 重试。contract 之后无人再写入该 reason_code，故实际影响限于历史行，但这是第二处真实语义变化。

两处各钉一道行为单测，外加一道查询文本钉。

另：`_declined_runs` 的 docstring（`:1036`）“Suppress legacy refusals until narrow”在本改法下变假，且不含任何可 grep 的字面量——判据 3 抓不到，须手改。生产总是传入真实 `object_store_root`（`:2495-2498`），故 `:1084` 的早返回是测试专用路径，不影响上述推理。

连带删除写侧：`:981`、`:990`、`:993`、`:2177`、`:2182`，以及 `:2168` 的 `if rc == LEGACY_STORE_REFUSED_EXIT_CODE`。`tests/test_node27_autopipeline_handoff.py:105-110,2694-2713` 相应调整。

---

## M3 —— forcing 的两个 M1 字节同一性金样按 delta 重录

8 条冻结 forcing 语句中 2 条转红——`forecast_store.latest_product_statement` 与 `display_coverage.refresh_statement` 内嵌 river SQL，携带 `hydro.river_timeseries_legacy`、marker 文本（各 3 处）与 `timeseries_store`。

**这是刻意造的耦合在正常工作**，不是故障。处置：从 6.3 基线工作树**重录这两条**，JSON 内以 `delta_from` 记本 commit 与原因（“river leg lost `_legacy`, markers, store column”），沿用 `GOLDEN_SHA256` 的 #2007/#2451 模式。

**不采**“把 reader #2 的 pin 拆成只覆盖 station leg”——那是为迁就一次合法 river 变更而削弱 forcing 的 M1。

---

## M4 —— 清理 oracle：逐条目断言**已存在**，真正会红的是别处

第一稿要“把逐条目 `fact_table_text_identity_columns(...) == set()` 移植到全部 13 个 REGISTRY 条目”。**该工作已经完成**：`tests/test_sql_shape_helpers.py:763-780` 已对全部 13 条参数化跑 `:778` 的同一断言。无需移植。

第一稿的行号亦错。实测 `tests/test_river_ts_read_path_surrogate_keys.py`：

| 行 | 内容 | 处置 |
|---|---|---|
| `:984` | `fact_table_text_identity_columns(narrow.sql) == set()` | **保留**（主断言） |
| `:988` | `legacy = render_river_ts_sql(..., "legacy", ...)` | 删（legacy 变体消失） |
| `:989` | `"_key = " in narrow.sql or "variable_e = " in narrow.sql` | **保留**（去 store 后仍成立的非真空性检查） |
| `:990` | `len(narrow) == len(legacy) - 2 * entry.expected_aids` | 删（算术依赖 legacy 与 `expected_aids`） |

第一稿写“保留 `:987`（实为注释行）、删除 `:988-989`”——`:989` 正是它上一句要保留的断言，照此执行会删掉自己要留的东西。

**真正会红而第一稿一字未提的**，在 `tests/test_sql_shape_helpers.py`：

- `:746-760` 整个 `test_every_registered_template_renders_for_the_legacy_store`（legacy 变体消失后整测死亡）
- `:758`、`:777` 的 `RIVER_TABLE_LEGACY`
- `:759`、`:780` 的 `expected_aids` 算术
- `:789-790` 的 `removed_placeholders` / `POSITIONAL_INDEX_PINS`
- `:809` 的闭合断言、`:834`、`:845`
- **`:813` 与 `:828` 两处 `@pytest.mark.parametrize("store", ("legacy", "narrow"))`**（在 `:822`/`:831` 调 `entry.source(store)`）——legacy 模板源消失后 legacy 半边必死。**两处的 `store` 参数化都要去掉**。这一条同时修正下面“保留：语句普查”的措辞：`:812-825` 那段普查**不能原样保留**，须去参数化后保留。

**保留**：语句普查 + REGISTRY 闭合（穷尽性来源）；`assert_marker_census` 作为**零值 tripwire**（marker 回归正是它该抓的）。
**删除**：所有读 `expected_aids` 的断言、5 个邻接反例。

---

## M6 —— river 模板金样：本 cut 最大的红面（前两稿均未覆盖）

`tests/fixtures/river_ts_templates_51f9d273.json`（42908 字节，20 条目 / 215 chains），由 `tests/river_ts_template_registry.py:105` 的 `GOLDEN_SHA256 = "d104d1c6…"` 逐字节钉住，被整个 418 行的 `tests/test_river_ts_template_golden.py` 消费。

**验证判据 1 抓不到它**（`--include='*.py' --include='*.sql'`，而它是 `.json` 且不含 marker 字符串），M3 只处理了 8 条 **forcing** 金样却引用了同一套 `GOLDEN_SHA256` 模式——即受票知道这类工件存在，却漏掉了 river 自己那份。

cut 后必红处：

- `:121-135` `test_legacy_renderer_preserves_every_current_template_predicate` —— 对**每个** REGISTRY 条目跑 `entry.source("legacy")` 并与金样 chains 比对。aid 合取项是这些 chain 里的**真实谓词**，故凡有 aid 的条目全红。legacy 变体消失后该测整体死亡。
- `:148`、`:161`、`:179`、`:201` —— 四处 `("legacy", "narrow")` 参数化，legacy 半边全死。
- `:412-417` —— 钉 20/215，并调用 D2 删除的 `historical_display_coverage_sql()`。

处置：**金样按 delta 重录**（与 M3 同法），删去 legacy 半边的参数化与 `test_legacy_renderer_preserves_every_current_template_predicate`，`GOLDEN_SHA256` 同步更新，并在金样 JSON 内以 `delta_from` 记本 commit 与原因。20/215 两个计数随之重钉——实现方须以**重录后的实测值**填写，不得沿用旧数。

---

## M5 —— 必须保持的行为

1. 窄表读路径的 SQL **文本**在本 cut 前后逐字节不变（C5：成对删除使其结构上必然成立）。
2. `hydro_run` 行锁与 `DATABASE_ROW_MISSING`（M1）；decline 写侧的行存在性校验与 `FOR SHARE` 锁（M1a）。
3. `run_display_coverage` 已冻结行不被清零或删除（C4），且其拒绝信号与退出码 3 通道存活（C3 撤回后的正确处置）。
4. 历史 `:valid_time` 的 424 行为不变。
5. **positional 参数契约**：读方以静态 `UNION ALL` 组合两个 leg（`services/tiles/mvt.py:942-943,1046-1050,2042`、`packages/common/display_coverage.py:191-197`），砍掉 legacy leg 后各读方的参数元组必然变化，`tests/test_sql_shape_helpers.py:790` 的 `POSITIONAL_INDEX_PINS` 是那份映射，须**逐读方手改**。生产无人消费 `RenderedSql.removed_placeholders`，故不是运行期自动坑，但漏改即红。

---

## 验证

第一稿的判据 2 不可达（`grep -rn "timeseries_store" --include='*.py' .` 实测命中 44 文件，含必须存活的 forcing 同名列、归档证据 `openspec/changes/archive/**`、冻结收据 `receipts/**`、`tests/test_migrations.py`、`tests/integration_helpers.py`；且迁移是 `.sql`，`--include='*.py'` 不可能“余下 6.2 迁移自身”）。改为带排除域的三条：

1. **marker 归零**
   `grep -rn "remove with #1342" --include='*.py' --include='*.sql' .` 为空（当前 64 行 / 17 文件）。
2. **river 路由列零引用**（限生产树，排除 **forcing 同名列**的全部四处）

   forcing 的 `met.forcing_version.timeseries_store` 由 7.3 创建，与 river 的路由列同名而不同物，**必须存活**。第二稿只排除了 `forcing_ts_render.py`，漏掉另外三处注释：

   | 文件:行 | 内容 |
   |---|---|
   | `packages/common/forcing_ts_render.py:83` | forcing 列说明 |
   | `packages/common/forecast_store.py:265` | “`met.forcing_version.timeseries_store` does not exist until task 7.3” |
   | `packages/common/display_coverage.py:299` | “…none of which exist until task 7.3” |
   | `scripts/reset_qhh_smoke_db.py:29` | “task 7.3 creates `met.forcing_version.timeseries_store`” |

   判据（**按内容排除，不写行号**）：

   ```
   grep -rn "timeseries_store" --include='*.py' packages workers services scripts apps \
     | grep -v "forcing_version.timeseries_store" \
     | grep -v "met\.forcing"
   ```

   > **实现期更正（第 4 处受票漂移）**：本判据初稿把行号写死为 `forecast_store.py:265:` / `display_coverage.py:299:`。判据是**改后**才跑的，而改动本身会移动行号（实测漂到 `:246` / `:286`），照字面跑会把必须存活的注释报成回归。**验收命令里不得出现行号。** 已改为按内容排除。

   **警告：这几处注释块里同时含 river 的陈旧引用**，它们**会**过时、必须改，而行级排除表达不了“同一注释块里改一半留一半”。故这些是**手改点**，实现方须逐块目视，判据只负责兜住其余部分。

   归档证据（`openspec/changes/archive/**`）、冻结收据（`receipts/**`）、`db/migrations/` 历史文件、`tests/test_migrations.py`、`tests/integration_helpers.py` **不在判据内**——它们记录的是历史事实，改动它们才是错。
3. **refusal 词表零引用**（第一稿完全缺失此判据）
   `grep -rn "legacy_store_refused\|LegacyStoreWriteRefused\|LEGACY_STORE_REFUSED_EXIT_CODE" --include='*.py' packages workers services scripts` 为空。
   M1/M2 的行清单已被证明非穷尽（漏过 `parser.py:40`、`cli.py:15`、`autopipeline.py:2168`、`:1079`），**故以判据收口，不以清单收口**。

外加：

- 坍缩后的清理 oracle、模板普查、`tests/test_select_ci_tests.py` 元测试全绿——**该套件已两次抓到本改动族的 CI 红，推前必跑**。
- forcing 的 8 条 M1 金样：6 条不变，2 条按 M3 带 `delta_from` 重录。

## 实现期发现（受票第 5 处覆盖缺口）

`packages/common/node27_pgdata_workload_query.py` 是被改动的 forecast SQL 的**生产消费方**，但它通篇不出现 `timeseries_store` 字样 → **判据 2 看不见它**，C1 的 12 文件表也没列。

6.3 删掉事实表上的文本身份谓词后，该捕获校验器原先强制的 `rt.river_segment_id = %(river_segment_id)s` 不复存在——身份改由权威表子查询绑定，列名无别名前缀。故 `_NAMED_EQUALS_RE` 的别名前缀改为可选（**这是放宽**），补偿是新增 `_REQUIRED_KEY_RESOLUTIONS` 事实表锚点（`rt.river_segment_key = (`、`rt.river_network_version_key IS NOT DISTINCT FROM (`），缺锚点即以 `QUERY_SEGMENT_UNBOUND` / `QUERY_IDENTITY_UNBOUND` 拒绝。咬合证据：更新 `tests/test_node27_pgdata_workload.py::_named_sql` 之前，4 个合成 SQL 用例先红。

**已知脆弱点（有意）**：锚点硬编码了 `IS NOT DISTINCT FROM` 的拼法，将来若出现 #2451 式重拼，此处会**故意**转红。

教训与判据 2 的行号问题同源：**纯文本判据只能兜住提到该名字的代码，兜不住依赖其语义的代码。** 这类消费方须靠人工识别，不能指望 grep。

---

## 未决（不在本 cut）

- ~~shape regression 检查~~ —— **已结清**，见 D0 判别结果表（`receipts/2026-09-18-i8-task52/` §10，判据逐字记为 “does not block I9”）。第二稿此处与 D0 自相矛盾，以 D0 为准。
- 2919 冻结覆盖行的 `refreshed_at` 新鲜度与残留 chunk —— 6.2 迁移前。
- `hydro.hydro_run.timeseries_store` 与两个函数的实际 DROP —— 6.2。
