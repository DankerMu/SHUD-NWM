# Fixture I3c (#2207): store-routed valid-time discovery

## Authority and calibration

Base: `b05422b6be3320209810d6d92b72f7b2921b6330`. Task 2.2c owns only `valid_times_for_layer`'s discharge discovery. The existing user authorization for remaining River readers permits per-test disposable post-expand catalogs, not production transition/deployment or early I7/I8. Shared OpenSpec remains active.

Effective fixture: compact, retaining the upstream suggestion. One callable selects two timestamp-only fact shapes beneath one outer semantic layer; no tile generation, geometry, production schema/state transition, public signature, cache or permission change. Selected risks: API/consumer compatibility, key/enum authority, bounded ordering/overflow, legacy aids and failure/empty behavior. Existing PostGIS test infrastructure is reused, but no spatial operator changes. Auth, file publication, concurrency/lifecycle, Slurm, providers and new dependencies are not selected. First review seats: correctness+test-evidence; integration. One serial implementer owns the coupled source/registry/test boundary; parent owns fixtures, immutable capture, verification and Git/PR operations.

## Immutable inputs and non-goals

Before editing, `.workplans/issue-2207/baseline/` captured all five complete tile SQL strings/bind sets, all thirteen raw entries in both stores, the original mvt.py/golden and actual named/any discovery SQL/response behavior. The initial prototype stopped after capture; `complete_baseline.py` verified every existing capture against the unchanged committed source and wrote only the missing manifest, without replacing any captured input. The historical golden SHA-256 stays `d104d1c69cea55cdb86bd8a44d90d8f93c621580b43f74744f6b5cca7cd5a453`.

Keep all five tile-generation SQL strings and binds byte-identical, including already-routed hydro/national. Keep the other eleven raw inputs byte-identical in both stores. Leave `national_discharge_valid_times`, cycles, digest/cache versions, API code, `_valid_time_discovery`, migrations, shared renderer and shared integration helpers unchanged. Do not add a shared union/binder/walker, fallback, store bind, per-store parameter suffix, permanent frozen-SQL fixture or new dependency.

## Raw factories and the single outer query

Add module-private required-store factories `_valid_times_named_source_template(store)` and `_valid_times_any_source_template(store)` alongside the existing raw factories before `postgis_tile_sql`. Do not insert them between `display_ready_run` and `valid_times_for_layer`, where an existing migration oracle owns a function-source slice. Validate the closed legacy/narrow vocabulary before interpolation.

Named raw source projects only `valid_time`, without DISTINCT/ORDER/LIMIT. Preserve its unaliased fact key/enum predicates, four-column key prefix and three separately marked aids (run_id, river_network_version_id, variable). Its run-key scalar lookup uses `SELECT h.run_key FROM hydro.hydro_run h WHERE h.run_id = :run_id AND h.timeseries_store = '<store>'`. Keep the basin/network lookups and safe enum-range lookup. Explicit metadata qualification is necessary: a real prototype with bare inner run_id plus the new conjunct hit the existing unaliased scalar-scope fail-closed check. Do not loosen that checker to accommodate this caller.

Any raw source projects only `ts.valid_time`, from the fact table aliased `ts`, retaining the enum predicate and the one marked variable aid with qualified fact columns. Add `EXISTS (SELECT 1 FROM hydro.hydro_run h WHERE h.run_key = ts.run_key AND h.timeseries_store = '<store>')`. This is run-key/store association, not a new user identity/status/coverage filter: do not add run_id/basin/network binds, active/status/latest-run selection or NULL-or-equals guards. Qualify metadata references. A local experiment verified named qualification and this correlated-EXISTS form through the unchanged renderer for both stores; no SQL grammar extension is needed.

Inside the existing discharge branch, select the appropriate factory once, render legacy/narrow, compose caller-local `legacy + UNION ALL + narrow`, and wrap that fact-row result in exactly one `SELECT DISTINCT valid_time ... ORDER BY valid_time DESC LIMIT :limit`. Each arm has the same projection and parameter vocabulary as its mate. Legacy keeps its aids/physical legacy table; narrow has no fact text identities. Raw templates contain no semantic operators. The public signature, validation-before-SQL, selected_identity_params, fixed q_down variable and execution/return path remain unchanged.

Named statement binds remain basin_version_id, limit, river_network_version_id, run_id, variable; any statement binds remain limit, variable. The existing unified execution-parameter dictionary may still carry unused identity keys for any form. `sample_limit = max(0, limit)`, query_limit=sample_limit+1 and response metadata stay unchanged. SQL returns newest instants first; Python retains the first sample_limit rows and only then sorts the retained values ascending. Sorting all rows before truncation would retain the wrong end of the window.

## Registry and static-oracle cutover

Keep the two live keys `mvt:valid_times_named_identity` / `mvt:valid_times_any_identity`, now pointing directly to the real raw factories. Remove the obsolete `_valid_times_branch` two-literal extraction helper. Entries remain statement/named, mentions 1/1 and aids 3/1. Census: thirteen live, seven routed, six historical-comparable; historical golden twenty keys/215 chains, raw aids thirty, mvt authored mentions five/markers fourteen and display markers twenty remain unchanged. Add the two old discovery keys to the historical reconstruction, not to a live compatibility alias. Keep exact sets and duplicate-key guards; never recapture the golden.

Update all owners of the former two-inline-literals convention: registry, `test_river_ts_read_path_surrogate_keys.py`, `test_migrations.py`'s selected-run discovery/index test, and `test_sql_shape_helpers.py`'s extraction non-vacuity guard. Preserve each index column tuple, all four named key predicates and forbidden fact-column assertions, checking rendered branches independently. Outer DISTINCT/order/limit assertions move to the actual emitted statement, not raw templates. The any branch's obsolete no-run_key assertion is superseded only by the explicit run-key/store association; keep prohibitions on accidental user-identity filters and unsafe text predicates. Update source-region extraction and relevant explanatory prose without weakening generic lexer tests.

The existing services/tiles/mvt.py selector already includes the migration, surrogate and MVT consumer owners. Verify existing selector/importer-closure tests; do not edit working configuration or add duplicate ownership merely to produce a diff. No new file edge is required.

## Required observable tests and isolation

Add exactly the requested named owner `test_valid_times_for_layer_capture_session_preserves_outer_limit_semantics_for_named_and_any_identity` in the surrogate-key unit file. It invokes both callable branches with a minimal capture session following the existing `_Session` protocol, without adding a cross-test helper import dependency. Return three fixed descending instants T2,T1,T0 with limit=2; assert one actual execution per form, one outer DISTINCT/order/limit with the UNION beneath it, limit bind=3, returned values T1,T2 ascending, limit=2, observed_count=3 and truncated=True. It must reject a missing overflow row and sorting-before-truncation, not just echo mock inputs.

Reuse `post_expand_forecast_database` unchanged and transition each affected test once. Existing `_prepare_hydro_stores` deliberately restores decoy TIMES for MVT value tests; do not use that retiming path for discovery-only authority discriminants, where DISTINCT would hide wrong-store reads. The dedicated named field-identical test becomes both-store parameterized: capture its existing text-era oracle first, rollback the session, call the shared post-expand fixture with explicit mappings, leave +30min decoys shifted, then run current discovery and retain every existing assertion. Exercise its active raw branch with an out-of-vocabulary variable and require an empty real-DB result, using the unchanged enum lookup rather than a direct enum cast.

In the existing unknown-identity/OOV and NULL-key tests, finish all historical queries before the existing `_prepare_hydro_stores` call and move current discovery after that single transition. Preserve their existing MVT assertions and retiming behavior; dedicated discovery tests own store discrimination. No global upgrade of seeded fixtures or SQL-rewriting connection wrapper. Coverage/existence/national/per-basin siblings stay otherwise unchanged.

Add one both-store-assignment any-identity integration owner using the existing seeded runs and `insert_river_timeseries_dual_written` for test-owned additions, not a new seeding framework. Seed KEYED times T0,T1,T4 and LEGACY times T0,T3,T4 (the existing rows supply T0/T1; add the remaining instants on an existing segment). Explicitly assign the two runs opposite stores and ALL_LEGACY to legacy; rollback before the shared fixture, retaining shifted decoy times. Verify non-vacuous authoritative and opposite-store time sets. With sample_limit=3 the real callable returns T1,T3,T4, observed_count=4 and truncated=True from the four distinct authoritative instants. Shared T4 plus per-run duplicates discriminate missing DISTINCT; unique T1/T3 discriminate either missing store; unbounded full-statement union changes cardinality/window. Both stores must contribute. Also prove the active any raw branches return empty for an unknown enum; no production parameter is added for this check.

For the new any-identity owner, Tn means `_T0 + timedelta(hours=n)`. Extend only that test's two keyed runs' end_time through T4 before inserting additions, keeping the test data within its declared run windows. Do not change shared seed defaults or introduce coverage/status filtering into discovery.

## Verification, red proof and exclusions

Parent runs the issue's five-file matrix plus affected migration, renderer/reference-lexer, cleanup/status-only and HHE owners on node-27, along with the committed-source benchmark/live-evidence suites (their mvt.py source hash changes even though tile SQL does not). Run the full surrogate integration file and the national integration sibling file in a private node-27 checkout/venv with integration and not timescaledb_210, no skips in the selected database owners. Local Ruff and strict OpenSpec remain hygiene; no active deployment pull/switch or shared environment mutation.

Required proof: the requested capture owner fails on the pre-change callable's missing union and on in-memory missing +1 / sorting-before-truncation mutants, then passes restored. Real discovery owners must reject an omitted narrow contribution and omitted store authority via shifted decoy times, with valid SQL and assertion failures, then pass restored. Keep one final source SHA for the published oracles/CI, or explicitly bridge an evidence-only checkpoint without implying a production receipt. Parent's immutable capture smoke proves five tile strings/binds, eleven other raw inputs and golden bytes unchanged.

Production mixed-store/Timescale EXPLAIN, expand, deployment and display receipts remain #1986/#1987, not claims made by these disposable catalogs. The prior PR2229 retrospective row rides this normal issue push; user .gitignore remains excluded.
