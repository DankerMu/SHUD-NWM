# Fixture: I1 (#1980) — river read-template normalisation + per-store renderer + cleanup oracles

Per-issue fixture note for `subagent-workflow` Phase 0.5. The change artifacts (`proposal.md`, `design.md` D5 / Sketch seams #1, `tasks.md` 1.1–1.3 and 1.5–1.12 (1.4 is the completed caller-contract Stage 5 re-entry, decisions 15/26 — not child A's), `specs/timeseries-narrow-store/spec.md` "Read paths SHALL be rendered…") are the contract; this file adds the risk triage, must-preserve set, Invariant Matrix, the measured baseline and the decisions the fixture review asked for. Revision 2 (after the read-only fixture review): baseline counts re-measured, marker/aid direction corrected, registry defined, oracle ownership per file, allowed-pin set enumerated.

## Risk triage

```text
Issue type: refactor (zero behaviour change) + new shared helper
Project profile: NHMS
Blast radius: high — SQL text of every river fact-table reader in production, plus the cleanup oracles that gate #1342
Fixture level: expanded
Upstream suggested level: compact (override: mandatory `reader` trigger + new shared helper root under packages/common + oracle re-pin; the zero-behaviour-change property is the must-preserve, not a reason to downgrade)
Repair intensity: high (shared helper behaviour; oracle integrity — a weakened oracle is the failure mode that would let #1342 contract land on a text predicate)
Why:
- six production files (forecast_store / mvt / hydro_display / display_coverage / publisher / copyback) are rewritten for layout; the 23 production marker lines today (distribution in "Measured baseline", including parser's 2 which stay untouched) become 34 markers / 34 aids; any semantic slip changes production query plans on compressed chunks
- the renderer is the single seam every later issue (I2–I5, I7, I9) depends on
- the oracles are re-pinned in the same PR: oracle integrity must be reviewed, not assumed
Selected risk packs:
- PostGIS / TimescaleDB domain behavior — selected: transitional aids exist to keep segmentby pushdown on compressed chunks; the legacy variant must keep every aid
- Published NHMS artifacts / display identity — selected: reader outputs (API payloads, tiles, coverage rows) must stay byte-identical; no wiring in this PR, so the proof is SQL-text equivalence
- Shared helper root (core) — selected: new `packages/common/river_ts_render.py` with a fail-closed contract
- Evidence chain / oracle integrity (core) — selected: census + adjacency + shape pins are rewritten
- Hydro-met time series / forcing windows — not selected: no window or timestamp logic changes
- Geospatial / CRS — not selected: no geometry, SRID or tile-envelope code is touched (the mvt SQL edits are WHERE/ON-chain layout only)
- SHUD numerical — not selected: no model input/output arithmetic is touched
- Slurm — not selected: no job submission, sbatch or compute-node code
- External providers — not selected: no forcing/provider fetch code
- Run manifest / QC — not selected: no manifest field, QC rule or run-state transition changes
- File IO / path safety — not selected: no file IO; Auth / RBAC — not selected: no route or role change
OpenSpec change: timeseries-narrow-store-expand-contract (existing)
Evidence floor:
- uv run ruff check .
- uv run pytest -q on the nine SQL_SHAPE_ORACLE_TESTS files (scripts/select_ci_tests.py): tests/test_sql_shape_helpers.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_river_ts_text_identity_cleanup.py tests/test_display_coverage_refresh.py tests/test_migrations.py tests/test_qhh_latest_fallback_pushdown.py tests/test_river_ts_render.py tests/test_river_ts_render_reference_lexer.py tests/test_river_ts_template_golden.py, plus tests/test_select_ci_tests.py
- uv run pytest -q on every other existing test module of the touched readers (forecast_store, mvt, hydro_display, display_coverage, tile_publisher, output_parser) — zero edits to those modules
- openspec validate timeseries-narrow-store-expand-contract --strict --no-interactive
```

### Issue #2086 risk-triage addendum

```text
Issue type: bug fix in the no-text-identity safety gate; no production caller yet
Suggested fixture level: none declared on #2086
Effective fixture level: expanded — inherited shared I1 fixture plus a new PostgreSQL quoted-identifier contract row
Repair intensity: high — shared helper behavior and evidence-chain/oracle integrity; first reader wiring trusts this gate
Selected risk packs:
- Shared helper root — selected: all future river readers use packages/common/river_ts_render.py
- PostgreSQL / Timescale query semantics — selected: quoted/unquoted identity equivalence decides whether a nonexistent narrow-table text column escapes
- Evidence chain / oracle integrity — selected: public-helper and narrow-refusal pins must independently kill three quoted-reference arms
- Published/display identity compatibility — selected: 20 registered reader templates must remain byte-compatible before wiring
- File/path, auth/RBAC, geospatial, hydro-met windows, SHUD, Slurm, providers, manifest/QC — not selected: untouched
Boundary checklist:
- Shared helper root: packages/common/river_ts_render.py matcher only
- Read/public/frontend/storage/write/publish/rollback/stale-state surfaces: none changed — no caller, DDL, migration or deployment
- Evidence boundary: tests/test_river_ts_render.py plus the existing seven-file selector-owned suite, post-#2053 20-entry differential and golden
- Unchanged consumers: all registry templates and shape-oracle callers
Issue-body divergence: exact quoted qualifier "rt" is in scope only when it references an alias declared bare as RT/rt; this corrects the issue's contradictory negative, with rationale in decision 20 and Non-goals
```

### Issue #2092 risk-triage addendum

```text
Issue type: bug fix in the no-text-identity safety gate; no production caller yet
Suggested fixture level: absent on #2092
Effective fixture level: expanded — mandatory column/parser semantics trigger plus inherited shared I1 helper fixture
Repair intensity: high — every future reader trusts this matcher and mutation/evidence truthfulness is the failure boundary
Selected risk packs:
- Shared helper root — selected: the three qualified matcher branches covering four token combinations share one separator grammar in packages/common/river_ts_render.py
- PostgreSQL / Timescale query semantics — selected: whitespace and comments are legal qualified-name token separators around dot
- Evidence chain / oracle integrity — selected: both separator sides and comment-normalised public behavior must be mutation-visible without claiming redundant-layer mutations
- Published/display identity compatibility — selected: all 20 registered reader templates must remain unchanged before wiring
- Schema / columns / field names — selected: failure is attribution of narrow-table-forbidden text columns
- Public API/CLI, config, file/path, auth, concurrency, resources, rollback, packaging, docs migration, geospatial, hydro-met windows, SHUD, Slurm, providers, manifest/QC — not selected: no caller, state, IO, route, migration or deployment changes
Boundary checklist:
- Shared helper root: packages/common/river_ts_render.py qualified-reference separator and three qualified matcher branches covering four token combinations
- Read/public/frontend/storage/write/publish/rollback/stale-state surfaces: none changed — no production caller, DDL, migration or deployment
- Evidence boundary: tests/test_river_ts_render.py plus the existing seven-file selector-owned suite, post-#2086 20-entry differential and golden
- Unchanged consumers: direct-dot #2053/#2086 behavior, alias-scoped/guarded helper responsibilities, all registry templates and shape-oracle callers
Issue-body evidence correction: comments are already removed by both outer_predicates and _blank_non_code. The fixture requires block/line-comment separator public behavior and mutations that remove load-bearing left/right separator spacing or bypass all comment normalization; it does not claim deleting one redundant comment-removal layer alone must turn tests red.
```

### Issue #2112 risk-triage addendum

```text
Issue type: bug fix in the no-text-identity safety gate; no production caller yet
Suggested fixture level: absent on #2112
Effective fixture level: expanded — mandatory PostgreSQL field/column semantics trigger plus the inherited shared I1 helper fixture
Repair intensity: high — every future reader trusts this shared matcher/preflight pair; a false clean answer ships a missing narrow-table column
Selected risk packs:
- Shared helper root — selected: packages/common/river_ts_render.py must give the exact parenthesized qualifier grammar one owner shared by both public helpers and the render preflight
- PostgreSQL / Timescale query semantics — selected: a table alias denotes the row composite and expression.fieldname is PostgreSQL field selection
- Evidence chain / oracle integrity — selected: exact positives, conservative unsupported refusals and matcher/guard mutation ownership must be independently observable
- Published/display identity compatibility — selected: all 20 registered reader templates must keep attribution, guarded/per-alias sets and both rendered bytes unchanged
- Schema / columns / field names — selected: the bypass retains a text identity column absent from the narrow table
- Public API/CLI, config, file/path, auth, concurrency, resources, rollback, packaging, docs migration, geospatial, hydro-met windows, SHUD, Slurm, providers, manifest/QC — not selected: no caller, state, IO, route, migration or deployment changes
Boundary checklist:
- Shared helper root: parenthesized field-selection classifier, _text_identity_columns_for_references and _assert_modelled_reference_forms in packages/common/river_ts_render.py
- Read/public/frontend/storage/write/publish/rollback/stale-state surfaces: none changed — no production caller, DDL, migration or deployment
- Evidence boundary: tests/test_river_ts_render.py plus the existing seven-file selector-owned suite, post-#2092 20-entry five-surface differential and golden
- Unchanged consumers: direct #2053/#2086 references, #2092 separator/left-boundary behavior, unaliased helper responsibility, all registry templates and shape-oracle callers
Adjacent deferral: comparison-position scalar subqueries are stripped before column attribution, so a correlated outer fact-alias text reference (direct or parenthesized) is a separate visibility defect tracked by #2114. It is not claimed safe here and also blocks #1981; #2112 changes only parenthesized field selection in code that remains visible after outer_predicates.
```

### Issue #2114 risk-triage addendum

```text
Issue type: bug fix in the guarded no-text-identity preflight; no production caller yet
Suggested fixture level: absent on #2114
Effective fixture level: expanded — mandatory reader/field semantics trigger plus the inherited shared I1 helper fixture
Repair intensity: high — every future reader trusts the scalar-authority stripping boundary; a false clean answer ships a correlated reference to a text column absent from the narrow table
Selected risk packs:
- Shared helper root — selected: packages/common/river_ts_render.py owns scalar traversal, attribution, matcher grammar and all guarded/render entrypoints
- PostgreSQL / Timescale query semantics — selected: comparison-position scalar subqueries may correlate to surrounding aliases while authority-local columns must remain legal
- Evidence chain / oracle integrity — selected: scalar-body visibility, nested/later-body traversal, error precedence and mutation ownership must be independently observable
- Published/display identity compatibility — selected: all 20 registered templates must keep attribution, guarded/per-alias answers and both rendered bytes unchanged
- Schema / columns / field names — selected: the bypass retains a text identity column absent from the narrow table
- Error handling — selected: guarded helper plus legacy/narrow render must fail closed with one entry-named correlated-scalar reason; inner fact-table rereads retain the older count-delta reason
- Public API/CLI, config, file/path, auth, concurrency, resources, rollback/partial output, packaging, docs migration, geospatial, hydro-met windows, SHUD, Slurm, providers, manifest/QC — not selected: no caller, state, IO, route, migration or deployment changes
Boundary checklist:
- Shared helper root: comparison-position scalar traversal and _assert_modelled_reference_forms; existing direct/quoted/separator and parenthesized matcher grammar is consumed, not forked
- Read/public/frontend/storage/write/publish/rollback/stale-state surfaces: none changed — no production caller, DDL, migration or deployment
- Evidence boundary: tests/test_river_ts_render.py plus the existing eight-file verification set, 20-entry five-surface differential and golden
- Unchanged consumers: outer_predicates and text_fact_columns remain outer-query-only; authority-local subqueries, inner fact-read delta refusal, direct #2053/#2086/#2092 and #2112 outer-code behavior, registry templates and shape-oracle callers
Issue-body refinement: #2114 does not redefine parenthesized field-selection grammar, but scalar visibility must feed decision 22's existing exact/unsupported classifier so `(rt).member` cannot remain invisible merely because it occurs inside the scalar body. Unaliased outer-fact + unqualified scalar-body names require scope resolution and are routed separately to #2148; #2148 does not directly block #1981 because no current registry/#1981 template has that shape.
```

### Issue #2082 risk-triage addendum

```text
Issue type: test/evidence repair for an unchanged shared structural helper
Suggested fixture level: absent on #2082
Effective fixture level: compact — a helper-level direct test plus evidence wording; no source behavior or caller changes
Repair intensity: medium — the renderer is shared, but this slice changes only the oracle and fixture responsibility boundary
Selected risk packs:
- Shared helper root — selected: assert_structurally_intact's quoted-identifier paren-skip branch needs direct observable ownership
- Evidence chain / oracle integrity — selected: the commutation oracle is structurally unable to kill m25, so the new pin must prove the exact branch independently
- PostgreSQL lexical/query semantics — selected: parentheses inside a complete double-quoted identifier are identifier bytes, not structural grouping
- Legacy compatibility — selected: helper behavior and every production renderer output remain byte-identical; the existing commutation oracle stays intact
- Public API/CLI, schema/columns, config, file/path, auth, concurrency, resources, error/rollback, packaging, docs migration, geospatial, hydro-met windows, SHUD, Slurm, providers, manifest/QC, storage/routes/frontend/deployment — not selected: no production source or runtime surface changes
Must preserve:
- assert_structurally_intact accepts complete quoted identifiers containing unmatched `)` or `(` while still rejecting the same parentheses in scanner-visible code
- test_every_traversal_commutes_with_the_scanner_over_the_corpus remains unchanged in behavior and retains sole t1/m26 traversal-family responsibility
Must add:
- one helper-level direct parametrized pin for unmatched close/open bytes inside complete double-quoted identifiers
- one isolated m25 proof that changes only assert_structurally_intact's double-quote skip arm; the direct pin fails with the intended unbalanced-parentheses assertions while the commutation node remains green
Evidence floor:
- uv run pytest -q tests/test_river_ts_render.py
- uv run ruff check .
- openspec validate timeseries-narrow-store-expand-contract --strict --no-interactive
- final source blob byte-identical to base; no production caller, registry/golden, DB or node claim
```

### Issue #2141 risk-triage addendum

```text
Issue type: bug fix in the no-text-identity matcher/preflight; no production caller yet
Suggested fixture level: absent on #2141
Effective fixture level: expanded — mandatory PostgreSQL field/column semantics trigger plus the inherited shared I1 helper fixture
Repair intensity: high — first reader wiring trusts one shared alias-member analysis; an unseen functional spelling ships a text column absent from the narrow table
Selected risk packs:
- Shared helper root — selected: packages/common/river_ts_render.py must give exact and unsupported functional notation one owner consumed by both public helpers, render preflight and decision 23 scalar bodies
- PostgreSQL / Timescale query semantics — selected: PostgreSQL documents unqualified field(alias) and alias.field as interchangeable, while schema qualification forces function interpretation
- Evidence chain / oracle integrity — selected: 35 exact fail-open rows, scanner separators, unsupported boundaries and three independent mutation owners must be public-seam observable
- Published/display identity compatibility — selected: all 20 registered templates must keep attribution, guarded/per-alias sets and both rendered bytes unchanged
- Schema / columns / field names — selected: the functional spelling can name any of seven text columns absent from the narrow table
- Error handling — selected: exact forms reuse the existing text-identity narrow refusal; unsupported alias-rooted forms get one stable entry-named refusal before either store returns
- Public API/CLI, config, file/path, auth, concurrency, resources, rollback/partial output, packaging, docs migration, geospatial, hydro-met windows, SHUD, Slurm, providers, manifest/QC — not selected: no caller, state, IO, route, migration or deployment changes
Boundary checklist:
- Shared helper root: _alias_member_analysis plus one scanner-owned functional classifier; _assert_modelled_reference_forms consumes unsupported state, decision 23 consumes the same analysis over scalar bodies
- Read/public/frontend/storage/write/publish/rollback/stale-state surfaces: none changed — no production caller, DDL, migration or deployment
- Evidence boundary: tests/test_river_ts_render.py plus the existing eight-file verification set, latest-master 20-entry five-surface differential and golden
- Unchanged consumers: direct #2053/#2086, #2092 separators, #2112 parenthesized forms, #2114 scalar visibility/precedence, outer-only helper scope, all registry templates and shape-oracle callers
Measured latest-master baseline: all seven members x five exact functional spellings = 35/35 alias-helper empty, guarded-helper empty and narrow RETURN; variable whitespace/block/line-comment variants and comparison-scalar variable(rt) also RETURN. Schema-qualified calls, other aliases and non-exact quoted controls share that empty/RETURN baseline but must remain controls, not positives.
```

## Measured baseline (base 51f9d273, `grep -rn "remove with #1342" --include='*.py'`, .venv excluded)

- Production marker lines: **23** — hydro_display 3, forecast_store 6, forcing_copyback_backfill 1, publisher 1, mvt 10, parser 2. Three further grep hits are oracle-side constants/prose (`tests/test_sql_shape_helpers.py`, `tests/test_river_ts_text_identity_cleanup.py` ×2) and are not aid markers.
- Of the 23: **20 verbatim**, **3 non-verbatim** in mvt (`:661` one-line "aids" form covering 3 aids; `:793` and `:840` two-line "compressed-chunk / text-pkey pushdown / aids" form each covering **4** aids: run_id, river_network_version_id, river_segment_id, variable).
- Marked aids: **31**. Unmarked aids: **3** in display_coverage (`:406` WHERE-line aid under the prose comment `:383-391`, the `run_id` disjunction `:410-411`, the `river_network_version_id` disjunction `:417-418`).
- Normalisation target: **34 markers / 34 aids**, strictly 1:1; the 1:N expansion in mvt alone yields 3 + 4 + 4 = 11 markers.
- `RIVER_TABLE_CENSUS` (`tests/test_river_ts_text_identity_cleanup.py:171-181`) counts `hydro.river_timeseries` string mentions per file, not markers; a layout-only normalisation does not change it. The re-pin this issue owes is a **new marker/aid census** (per file, 1:1), while the statement census is asserted unchanged for every registered production file.
- Five WHERE-line sites (`hydro_display.py:773-774`, `forecast_store.py:1897-1898`, `mvt.py:511-512`, `:1496-1497`, `:1524-1525`): the **marker already sits on its own line**; it is the **aid** that is the first conjunct on the `WHERE` line. Direction of the edit: move the aid off the `WHERE` line into its own `AND` line under the marker; the `WHERE` line takes the next non-aid conjunct. (`design.md` D5 and `tasks.md` 1.1 are corrected in this PR to say the same.)
- `workers/output_parser/parser.py:949-950`, `:966-967` already conform (marker on its own line, one aid on the next, pinned by `_PARSER_AID_ADJACENCY`): **no parser diff is expected**; a parser diff is a deviation to report.
- `services/tile_publisher/publisher.py:2665` carries its aid inside a `JOIN … ON` chain, not a `WHERE`; the same three rules (one conjunct per line, remove the next line, key predicates retained) apply to ON-chains.

## Decisions the review asked for

1. **What a "registered template" is.** The renderer takes SQL text; it has no registry. The registry is test-side: one new module `tests/river_ts_template_registry.py` (keyed `<module>:<name>`, one block per reader module in stable file order so the four wave-2 PRs append without colliding) whose entries carry `source` (a callable returning the template text — runtime-assembled forecast_store blocks come through the existing capture-cursor harness at `tests/test_river_ts_text_identity_cleanup.py:437,472`), `kind` (`statement` | `fragment`), `params` (`positional` | `named`) and `expected_aids`. `mvt.py:1493-1530`'s conditional valid_times SQL registers as two entries. Fragments (`_SEGMENT_IDENTITY_PREDICATE_SQL`, `forecast_store.py:92-113`) have no table name: table rename is a no-op on them and the structural check applies to the fragment alone.
2. **Text identity column authority.** The whole constant group moves as one from `tests/test_sql_shape_helpers.py:121-162` into `packages/common/river_ts_render.py`: `SANCTIONED_TEXT_PUSHDOWN_COLUMNS` (:121), `FORBIDDEN_TEXT_FACT_COLUMNS` (:126), `LATERAL_PROBE_TEXT_PUSHDOWN_COLUMNS` (:143), `TEXT_IDENTITY_COLUMNS` (:145, derived = sanctioned + forbidden — the derivation moves with it) and `TEXT_AID_COUNTERPARTS`; the tests re-import them so `test_sanctioned_and_forbidden_column_sets_are_disjoint_and_complete` (:877), `test_every_text_identity_column_has_a_declared_counterpart` (:871) and `test_the_sanctioned_ceiling_is_the_shared_one_not_a_private_copy` keep their meaning against the shared module (no private copy anywhere).
3. **"Parses" means the structural check**, because the repo has no SQL parser and three placeholder dialects coexist (`%s`, `%(name)s`, `:name`): balanced parentheses; no `WHERE AND` / `FROM AND` / `ON AND` / `WHERE\n)`; no `AND`/`OR` left dangling before `)`, before end of text, **before a clause keyword** (`LIMIT`, `ORDER`, `GROUP`, `HAVING`, `UNION`, `EXCEPT`, `INTERSECT`, `WINDOW`, `OFFSET`, `FETCH`, `RETURNING` — and, round-2 ruling, the same join/from family the region-stop regex already enumerates: `JOIN`, `INNER`, `CROSS`, `NATURAL`, `WHERE`, `ON`, `FROM`, `SELECT`, `WITH`, `VALUES`, `SET`, and the outer-join arm spelled `(LEFT|RIGHT|FULL)(\s+OUTER)?\s+JOIN` — bare `LEFT`/`RIGHT` would false-positive on `LEFT(col, n)` function syntax; one keyword family, shared by both checks; plus a new `ON <keyword>` fault so an ON chain whose only conjunct was an aid is refused — verifier H6 measured the widened family refuses 0 of 20 registry entries and catches the four `AND JOIN` / `ON JOIN` / `AND LEFT JOIN` / `AND WHERE` shapes) **or before another connective** (`AND AND`, `OR OR`, `AND OR`); no `WHERE` immediately followed by a clause keyword; no marker string; no `SELECT … FROM x AND …` (round-1 review C4 widened the enumeration).
4. **Positional parameters.** Removing a `%s` aid changes the caller's tuple arity. `render_river_ts_sql` returns a `RenderedSql` with `sql` and `removed_placeholders` (ordered indices of positional placeholders that were removed; empty for named templates). `test_forecast_store_segment_blocks_bind_every_placeholder_they_grew` (`tests/test_river_ts_text_identity_cleanup.py:599`) stays green because no caller is wired.
5. **Renderer module and the statement census.** `packages/common/river_ts_render.py` holds the two table-name constants and no statement; it is **registered** in `REGISTERED_SOURCES` + `RIVER_TABLE_CENSUS` with its measured mention count (expected **2**: `_river_table_mentions` counts substrings and the `_legacy` literal contains the canonical name) and wired in `scripts/select_ci_tests.py`, so a statement sneaking into the shared helper is red. Registry closure: for every registered production file, the sum of canonical-name mentions over that file's registry entries' source text equals `RIVER_TABLE_CENSUS[file]` minus the file's declared `NON_TEMPLATE_MENTIONS[file]` (comment/prose/identifier mentions that are not read statements — a declared-offset dict a reader must know exists: bumping it is the only way to hide an unregistered read site, so it is reviewed line by line) (fragments contribute 0; both mvt valid_times branches count), so an unregistered zero-marker read site is red too.
6. **Equivalence oracle (golden; predicate chains only — see must-preserve).** Before any template edit, capture every registry entry's base-SHA text (`git show 51f9d273:<file>` for constants; capture cursor for assembled blocks) and commit the normalised form — `outer_predicates`-folded, split on top-level `AND` per chain, sorted — as `tests/fixtures/river_ts_templates_51f9d273.json`. A test asserts `render(entry, legacy)` with the table name restored has the same conjunct multiset per chain as the golden. **Chain** = one AND-chain delimited by its own opening keyword or bracket: each `WHERE` chain, each `JOIN … ON` chain, the body of each subquery / `EXISTS` / `IN (SELECT …)` / `CROSS JOIN LATERAL (…)` (its inner `WHERE` is its own chain, never merged with the outer one — `outer_predicates` keeps lateral bodies, `tests/test_sql_shape_helpers.py:374-384,566-584`), and the inside of each `OR (…)` disjunct; a fragment is one chain. Chains are compared positionally (same count, same order of chains, same multiset within each), so a conjunct migrating from a lateral body to the outer WHERE is red. Conjunct order inside one chain is explicitly NOT preserved (see must-preserve); everything else is.
7. **Oracle ownership per file** (the two registers are mutually exclusive by design, `tests/test_river_ts_text_identity_cleanup.py:114-124` and `:1461`): cleanup oracle owns forecast_store, publisher, forcing_copyback_backfill, parser (and the non-reader files already in `REGISTERED_SOURCES`); `tests/test_river_ts_read_path_surrogate_keys.py` owns mvt, hydro_display, display_coverage. Each file's marker/aid census pin lives in its owning oracle.
8. **Allowed re-pins** (the only test edits permitted): the `SQL_SHAPE_ORACLE_TESTS` files (nine after this PR — #2018 round 5 added `tests/test_river_ts_render_reference_lexer.py`) — in particular `tests/test_display_coverage_refresh.py:302` (`WHERE rt.variable = 'q_down' AND rt.variable_e = …` substring) and `tests/test_migrations.py:441-448` (whole-string `outer_predicates` equality for the mvt valid_times branch, which any conjunct re-order turns red) — plus `tests/test_select_ci_tests.py` and the new registry/golden/renderer tests. `tests/test_qhh_latest_fallback_pushdown.py:191-196` is also a re-pin (implementation finding: the prescribed `OR (` + marker-line layout leaves a comment inside the bracket, and `strip_comments` folds it to `OR ( rt.run_id …`, so the two substrings at `:191/:194` change while the protected shape does not).
9. **Selector rules** map `packages/common/river_ts_render.py`, `tests/river_ts_template_registry.py` and every touched reader to the whole `SQL_SHAPE_ORACLE_TESTS` group — now nine files: the six existing plus `tests/test_river_ts_render.py`, `tests/test_river_ts_render_reference_lexer.py` and `tests/test_river_ts_template_golden.py`, which consume the same machinery (implementation finding, accepted; round-5 decision 18 added the independent reference-lexer differential) — not a new three-file tuple.
10. **[Superseded by decision 15 — combinator split out] `render_union_all` parameters (orchestrator ruling on implementer deviation D1).** Parameter names are shared across branches, not suffixed per store: both dialects allow a named placeholder to repeat, the branches are the same query against two stores, and a renamed-parameter contract would leak into every I3 caller. `params` is returned unchanged.
11. **Independent golden verification (Phase 2).** The orchestrator regenerated the golden from a pristine detached worktree at 51f9d273 (copying only `river_ts_render.py` and the registry in) and diffed it against the committed fixture: 20 entries / 216 chains, no difference.
12. **Golden chain boundary re-pin (round-3 L4).** `_REGION_START` gains `HAVING` and excludes `DISTINCT ON (…)` SELECT-list brackets (`(?<!DISTINCT ON)` guard or an explicit `DISTINCT ON` skip), so `HAVING COUNT(…) = %(…)s` chains (`display_coverage:refresh`, `forecast_store:latest_product_fallback`) are collected and `DISTINCT ON (…)` lists (`forecast_store:analysis_segment_rows`, `mvt:postgis_tile_sql_hydro_national`) are not. The golden is re-captured from the pristine base by the same procedure as decision 11 — the new `sql_chains` run over a `git archive 51f9d273` copy of the reader files — and committed with `GOLDEN_SHA256` re-pinned in the registry as a reviewed one-line diff; `GOLDEN_BASE_SHA` stays 51f9d273. Two counter-examples are pinned: deleting a whole `HAVING` line reddens the golden test; editing a `DISTINCT ON` SELECT list leaves it green (the golden is a predicate-chain oracle, decision 6). Phase 2's check becomes "golden reproducible from the pristine base with the new regex", not "golden unchanged".
13. **[Superseded by decision 15 — combinator split out; kept as the record of what the round-3 redesign attempted] Union combinator contract (round-3 L1/L2 + retro).** `render_union_all` implemented a whitelist (record now folded into the superseded clause of the Failure-paths row, :91; the split record is :101). `TemplateEntry` gains `union_safe: bool` and `union_unsafe_reason: str | None`; the sweep partitions the named statement entries into `union_safe=True` (rendered, placement pinned by a literal per-entry / per-occurrence table like `POSITIONAL_INDEX_PINS`, never by re-running the binder's own walk) and `union_safe=False` (each asserted to refuse with its reason). `display_coverage:refresh` leaves the union set by statement kind (DML) — its `kind` stays `statement` for the census. Measured at the redesign: 12 entries `union_safe=True` (eight `forecast_store` segment blocks, `forcing_copyback_backfill:discover_backfill_runs`, `publisher:qdown_discovery`, the two parser probes), 7 `union_safe=False` with reasons (`hydro_display:mvt_source_identity_probe` LIMIT 1 + `.first()`; `display_coverage:refresh` DML; `forecast_store:latest_product_fallback` fact table is not the driving relation — the correct union sits inside its CTE; `mvt:postgis_tile_sql_hydro` / `_national` one aggregate row per branch; `mvt:valid_times_named_identity` / `_any_identity` DISTINCT + ORDER BY + LIMIT with `formatted[:limit]`), one fragment; a `union_safe=True` entry that also carries a reason is refused as a contradiction. Statement-level union is all I1 ships; the flag is declared, never inferred, and a future CTE-level combinator is designed by the wave-2 PR that has a caller for it.
14. **Literal-aware rename (round-3 L2-3).** `_rename_table` substitutes the canonical name only outside single-quoted literals, dollar-quoted bodies and comments (spans from `_blank_comments_and_literals`); a name inside a literal is left verbatim in both branches. Structural faults, the occurrence counter and (round-4 H-6) `fact_table_attribution` scan the same blanked text.
15. **PR split at the second gate entry (round 4 @7637dcd2, `.workplans/epic-1979/retro-1996-2.md`, shape breadth).** PR #1996 is superseded; child A (this fixture, new PR from master 1b56648b, fresh round counter) ships surface 1 only: `render_river_ts_sql`, aid stripping, structural check, text-identity guard, registry + golden (`GOLDEN_SHA256` unchanged, `GOLDEN_BASE_SHA` 51f9d273), `_REGION_START` HAVING / DISTINCT ON (decision 12), literal-aware rename (decision 14), positional pins, census, selector rules, plus the two renderer-side round-4 findings (H-2 consequence 1 quoted alias refusal, H-6 literal-blanked attribution). Surface 2 (the UNION ALL combinator, decisions 10 and 13) is removed from the issue and routed upstream as the pipeline's sizing-retro (`slice-error`: cut as a text-level helper with no caller); this historical handoff to tasks 2.2 (#1982) / 2.3 (#1983) is superseded by decision 26's five caller-owned Stage-5 slices. Issue #1980's declared `Minimal mergeable slice` ("先合渲染器模块 + 其单测（不接任何模板）") is exactly child A.
16. **Counter-vs-walk invariant (#2018 round 2, Phase 6.2 — the class round 1 and round 2 both hit).** `_assert_modelled_reference_forms` delivers: no statement reaches a render or a text-identity answer unless the independent occurrence counter and the FROM/JOIN walk agree AND the counter is blind to no spelling of the fact table. Round 1 taught the counter two quoted spellings, round 2 found four more plus the unqualified and re-qualified forms — one spelling per review round is the enumeration failure that sank the combinator. Ruling: the counter counts the table's bare NAME as a whole token (`river_timeseries` / `river_timeseries_legacy`, any case, over comment/literal-blanked text; a column qualified by the table name is a counted mention the walk does not model, so the statement is refused — alias the table (round 3 G2, by deletion of the qualifier lookahead; round-4 J1 record correction)) and needs no model of schemas, quoting or whitespace; the walk stays strict (canonical `hydro.river_timeseries[_legacy]` + bare alias only); disagreement refuses. Widening is counter-side ONLY — widening the walk (`_FACT_REFERENCE`, `_FACT_QUOTED_ALIAS`, `_CANONICAL_NAME`'s form) makes an unmodelled form "modelled" while the rename still needs the literal, which is a new fail-open (verifier E, fixC). The one SPELLING of the name a token counter cannot see is PostgreSQL's Unicode-escape syntax, refused wholesale on `U&` (either case). **Round 3 correction:** that is a closure over spellings only — the counter and the walk read the SAME blanked text, so an error in the lexer that produces it is common-mode (both sides 0, equality satisfied by mutual blindness); the lexer is audited separately against PostgreSQL §4.1 in decision 17. Accepted over-matches (all fail-closed, six): other schema prefixes, the unqualified search_path spelling, quoted-uppercase (a different table), a column qualified by the table name, a CTE named `river_timeseries`, a column alias `AS river_timeseries` / `AS "river_timeseries"`. Round-3 reviewers are asked to ATTACK the invariant (construct a spelling both sides miss; mutate the token pattern) rather than enumerate.
17. **Lexical audit against PostgreSQL §4.1 (#2018 round 3 hard gate, retro-2018.md, shape depth).** The scanner `non_code_spans` is the common-mode surface every counter, guard and the rename read; its hole space is PostgreSQL's lexical structure, which is finite and externally specified. Each rule is either implemented faithfully or REFUSED — never reasoned about; every row below carries a pin, and no fail-closed direction claim enters this fixture or a docstring without a red/green probe next to it (the round-2 residual row was written from reasoning and was wrong).
    | §4.1 rule | scanner behaviour | pin |
    |---|---|---|
    | plain identifier; `$` allowed after the first character (PG extension); non-ASCII bytes allowed (`scan.l` byte classes) | a `$` or a non-ASCII byte in code is REFUSED — decision 18 (round 4 I1: the ASCII tag class of this row's former `(?<![\w$])` guard was fail-open on `$备注$` / `$€$`) | `test_a_dollar_sign_in_code_is_refused_as_outside_the_lexical_subset[identifier_dollar]`, `test_a_non_ascii_byte_in_code_is_refused_as_outside_the_lexical_subset[identifier|symbol_before_escape_prefix]` |
    | quoted identifier `"…"` with `""` escape | traversed, NOT blanked (identifiers are code) | `test_an_apostrophe_inside_a_quoted_identifier_does_not_desynchronise_the_scan` (round 5; round-4 J2 — the round-2 `quoted_schema*` ids pin the counter, not this arm; deleting the arm survived 461/461 at 7be3e273) |
    | Unicode-escaped identifier `U&"…"` (either case) | REFUSED wholesale | `unicode_escape_*[U&|u&]` |
    | string constant `'…'` with `''` escape; backslash is data (`standard_conforming_strings = on` — recorded assumption) | blanked body, quotes kept for the structural check | `'C:\'`, `'a''b'` |
    | escape string `E'…'` / `e'…'` with backslash escapes; `E` must be a token start and immediately adjacent to the quote | blanked with backslash awareness; an identifier character before `E` makes it an identifier tail, while a digit immediately before `E'` / `e'` is outside the declared subset and is refused by decision 18 | `E`/`e` params, `LIKE'C:\'`, `x_e'C:\'`, `escape_prefix_needs_adjacency`, `numeric_literal_glued_to_an_escape_prefix[select_list|predicate|lowercase_e]` |
    | `U&'…'` Unicode-escaped constant | REFUSED wholesale (same `U&` guard) | `test_a_unicode_escaped_string_constant_is_refused_like_the_identifier_form` (round 5; round-4 J2 — the `U&'abc'` cell cited a test that did not exist) |
    | bit/hex constants `B'…'` / `X'…'` | plain literals | `B'1010'` / `X'FF'` control |
    | dollar-quoted `$$…$$` / `$tag$…$tag$` (tag = identifier byte classes, PostgreSQL `dolq_start [A-Za-z\200-\377_]`) | NOT modelled — the dollar arm is DELETED; any `$` in code is refused (decision 18), so a dollar body, `$1`, `a$b` and a tag after a numeric constant are all refused before any counter or traversal runs | `test_a_dollar_sign_in_code_is_refused_as_outside_the_lexical_subset[dollar_dollar|tagged|non_ascii_tag|symbol_tag|positional|identifier_dollar|tag_after_number]`, `test_a_dollar_body_hiding_a_comment_or_paren_is_refused_before_any_traversal[line_comment|block_comment|unbalanced_paren]`; control `test_a_dollar_sign_inside_a_literal_or_comment_stays_inside_the_subset` |
    | positional parameter `$n` | outside the subset (0/20; the readers use `%s` / `%(name)s` / `:name`) — refused by the same rule | `[positional]` id above |
    | `--` line comment to end of line (`\n` or `\r`) | blanked, newline kept | T4 `\r` |
    | `/* … */` block comment, NESTED | depth-counted | T1 nested, `/* a /* b */ c */` one-span pin |
    | unterminated literal / comment / dollar quote | REFUSED at `_assert_modelled_reference_forms` (belt for the unterminated sub-case only — T3 proves a phantom span can re-synchronise before EOF, so this is not the closure) | EOF-belt pins, T3 |
    Named exit (recorded at round 3): an execution oracle that shares nothing with the scanner — `PREPARE` every narrow render against the narrow table in the real-db CI job. **Round-4 disposition (retro-2018-2.md):** routed to task 4.1 (I7, the expand migration), not built here — no narrow DDL exists in this PR, the real narrow table carries `variable_e`-style key columns rather than `variable`, so a fabricated schema would test a phantom, and `PREPARE` over the registry is green today and discriminates none of the round-1..4 findings.
18. **Declared lexical subset (#2018 round 4 second gate entry, retro-2018-2.md, shape depth — closure by DELETION; #2050 round-5 closure).** Round 4 found the §4.1 audit fail-open at two SITES of one rule (the dollar-quote tag class is a byte class the scanner spelled as ASCII; the paren/sub-select/comment traversal family re-lexed without a dollar arm). Measured over the 20 registry templates: 0 block comments, 0 `E'` strings, 0 backslashes, 0 `$` in code (10 `$` occurrences across 2 entries, all inside `'^[0-9]+$'` literals), 0 non-ASCII bytes in code (6 `—` occurrences across 4 entries, all inside line comments). Ruling: the renderer accepts a declared subset of §4.1 — `'…'` with `''`, `E'…'`/`e'…'` with backslash escapes, `"…"` with `""`, `--` to `\n`/`\r`, nested `/* */`, `%s` / `%(name)s` / `:name` parameters — and `_assert_modelled_reference_forms` refuses, before any counter, traversal or structural check and through all three doors (legacy render, narrow render and `fact_table_text_identity_columns`), any `$`, any non-ASCII byte in comment/literal-blanked code text, and the numeric-glued escape-prefix shape below. Every refusal message says `outside the modelled lexical subset`, names the entry, offset and complete offending text rather than merely an offending character, and never names the fact table. `_DOLLAR_QUOTE_OPEN`, `_scan_dollar_quoted_span` and the scanner's dollar arm are deleted. Widening the subset later is a fixture decision with its own pins, never a silent regex change.
    - **L1 — independent reference-lexer adjacency.** PostgreSQL requires the `E` / `e` escape prefix immediately before the opening quote. The reference lexer therefore treats a quote as escaped only when `previous_token in ("E", "e")` **and** `previous_end == index`; `E ␠'…'` and `e ␠'…'` remain plain literals. Independent known-case pins cover adjacent and whitespace-separated forms in **both cases**. The seeded alphabet must generate accepted token-adjacent `E'…'` and `e'…'` samples, with an anti-vacuity count for each; the suite must turn red both when the module walks backward across whitespace (m6) and when the reference drops only its lowercase `e` branch. `1E` / `1e` fragments enter the alphabet only after L2's refusal exists.
    - **L2 — numeric-glued escape prefix is outside the subset.** A PostgreSQL numeric constant immediately followed by `E'` / `e'` is refused by a token-aware `_lexical_subset_violation` arm before any scanner/traversal answer. The numeric prefix includes digit-started integer/decimal forms and optional exponent `e` / `E` with optional sign and digits; the recognizer also handles PostgreSQL's leading-dot numeric form rather than matching a `[0-9.]*` substring. It reports the complete numeric prefix plus adjacent `E'` / `e'` from the numeric token's true start, so `1e+2E'` is not truncated to `2E'`. The arm consumes an offset-preserving view that blanks comments and string literals while retaining literal quote boundaries, and separately masks complete double-quoted identifiers (including `""` escapes) **for this arm only**; quoted identifiers remain code for table-name counting, attribution, `$` / non-ASCII subset checks and all existing traversals. `1E'`, `1e'`, `1e2E'`, `1.2e3E'`, `1e+2E'` and leading-dot equivalents in select-list and predicate positions must be refused through legacy render, narrow render and `fact_table_text_identity_columns`; every message carries entry, exact offset and complete offending text and omits `river_timeseries`. Controls keep `col1E'x'`, `1.5e3'…'`, `1EE'…'`, `1E'` text inside a literal/comment, and quoted identifiers whose bodies contain `1E'` / `1e'` / decimal text (including `""` escapes) at their base outcomes. Removing exponent/sign/leading-dot handling, masking raw SQL incorrectly, removing the quoted-identifier mask, or broadening the terminal quote to `[Ee]["']` must turn focused tests red. The independent reference's numeric-token walk is implemented separately from the production recognizer so the differential cannot certify a shared `[0-9.]*` mistake. The 20-entry registry remains 0/20 refused by the new arm.
    - **L3 — traversal commutation and its declared limit.** The second traversal family must lex exactly the code/non-code runs reported by `non_code_spans`; `test_every_traversal_commutes_with_the_scanner_over_the_corpus` compares all 20 registry sources plus both renders (60 statements, `registry_excluded == 0`) and a **16-statement** in-subset adversarial corpus against the spans-derived reference. Its sixteenth row places an unbalanced parenthesis inside a quoted identifier **inside the stripped scalar subquery**: `SELECT rt.value FROM hydro.river_timeseries rt WHERE rt.run_key = (SELECT run_key AS "a)b" FROM hydro.hydro_run WHERE run_id = :r) AND rt.valid_time = :t`. This row kills t1 (remove the quoted-identifier arm from `_opaque_runs`) and m26 (remove it from `_skip_balanced`); putting `AS "a(b"` in the outer SELECT list does not. At the #2050 boundary, m25 — deleting the quoted-identifier arm from `assert_structurally_intact` — deliberately remained green and was deferred: that outcome comparison feeds both sides `_blank_non_code(..., keep_literal_quotes=True)`, which leaves quoted identifiers intact, so the structural branch is unreachable through this pin. Decision 25 / task 1.12 now closes m25 with a direct structural test; this commutation pin intentionally remains green under m25 and does not claim responsibility for killing it.
    The round-4 sketch `_blank_non_code(T(sql)) == T(_blank_non_code(sql, keep_literal_quotes=True))` was measured FALSE at head on `SELECT 'a' -- c\nFROM x` (the two sides disagree on whether the literal's quotes survive), and its repaired form was vacuous against the block-comment mutant; the spans-derived reference is retained instead (implementer deviation D1, `implementer-2018-r4.md` §9). The scanner's agreement with PostgreSQL on the subset remains pinned by the committed seeded differential `test_the_scanner_agrees_with_a_reference_lexer_or_refuses`, with a non-vacuity floor on accepted samples. Mechanical record rule: every pin cell in this fixture names a node id present in `pytest --collect-only -q` at the commit that cites it.
19. **Unquoted alias / column case folding (#2053, C-ALIAS-01).** PostgreSQL lower-folds every unquoted identifier before name resolution, but the renderer currently applies that rule only to fact-table-name patterns. `_FACT_REFERENCE` captures the source spelling (`RT`), while `fact_table_text_identity_columns` and `text_fact_columns` compare that spelling and each `TEXT_IDENTITY_COLUMNS` member case-sensitively. Therefore `FROM hydro.river_timeseries RT` plus an unmarked `rt.variable` predicate is reported only as `{'run_id'}` after the marked `RT.run_id` aid is removed, and the narrow SQL returns with `rt.variable`; `RT.VARIABLE` and an unaliased `VARIABLE` also escape. Ruling: one code-only matcher owns the rule for both public attribution helpers — unquoted alias and column tokens are compared case-insensitively while retaining the trailing `\b` that excludes `variable_e`, `unit_e` and `quality_flag_e`; aliases returned by `fact_table_attribution` are canonical lower-folded so two source spellings cannot become two set members. The matcher still attributes only the outer query: `strip_scalar_subqueries` / `outer_predicates` remains load-bearing, so an authority `hydro_run WHERE run_id` sub-select is not a fact-table text column. On that outer text it reuses the existing `non_code_spans` / `_blank_non_code` scanner rather than implementing a second lexer: both comment forms (`--` and nested `/* … */`) and both single-quoted forms (`'…'` with `''`, `E'…'` / `e'…'` with backslash escapes) are blanked. Complete double-quoted identifier runs, including `""` escapes, are masked for this comparison only (the same local scope as L2 / `_skip_quoted_identifier`) and remain code for counting, the `FROM` / `JOIN` walk, lexical-subset checks and traversals. At task 1.6's merge boundary, three quoted shapes stayed distinct and were pinned separately: (1) a complete quoted identifier whose body equals `rt.variable` (`SELECT "rt.variable"`) was not an alias-qualified token pair and did not attribute `variable`; (2) a qualified quoted column `rt."variable"` / `"rt"."variable"` remained unattributed and was explicitly deferred to decision 20 / task 1.7; (3) a quoted fact alias (`AS "r"` / bare `"r"`) remained the existing `_FACT_QUOTED_ALIAS` unmodelled refusal. A predicate qualified by another relation remains unattributed. `_AID_PREDICATE` remains the deliberately narrow normalised-layout grammar: upper-case `RT.RUN_ID` under a marker continues to make `_strip_aids` fail closed as a mis-shaped aid rather than silently expanding accepted marker syntax. Public red/green proof covers `fact_table_text_identity_columns`, `text_fact_columns` with both `RT` and `rt`, and `render_river_ts_sql(..., 'narrow')`; the two `text_fact_columns` answers on the defect probe must be equal and non-empty with `variable`, so equality cannot pass vacuously. Deleting case folding from either qualified or unaliased arm must turn focused tests red. All 20 registry attribution sets/renders and the golden remain unchanged.
20. **Exact lower-case quoted fact references below a bare-declared alias (#2086).** PostgreSQL folds bare alias/column tokens to lower case, so `FROM … RT` declares alias `rt`: both `rt` and exact quoted qualifier `"rt"` refer to it. Likewise bare `variable` and complete quoted identifier `"variable"` denote the same fact column; `"RT"`, `"VARIABLE"`, `"Variable"` and any identifier containing a doubled-quote escape denote different names. Decision 19 intentionally masked every quoted identifier and therefore leaves four valid combinations invisible whenever at least one component is quoted: `rt."variable"`, `"rt".variable`, `"rt"."variable"`, and an unaliased fact read with `"variable"`; narrow returns SQL containing a fact-table text identity column. A quoted qualifier **reference** to an alias declared bare is distinct from a quoted alias **declaration** (`FROM … AS "rt"`), which remains refused.

    Ruling: keep the decision-19 unquoted arm unchanged and derive both comparison representations from the same `_blank_non_code(outer_predicates(sql))` scanner-owned text. The unquoted representation masks every complete quoted-identifier span as today. A second quoted-reference representation preserves a span only when the scanner proves the **entire** token is exactly either a canonical lower-case bare fact alias or one lower-case `TEXT_IDENTITY_COLUMNS` member; every other complete quoted span is opaque. This token classification, rather than a raw prefix regex, is load-bearing for `"variable""suffix"`, `"rt.variable"`, mixed-case quoted names and identifiers containing `""`. On that representation, the qualified arm accepts a qualifier only when it is either the existing bare ASCII alias (case-folded as in decision 19) or its exact lower-case quoted spelling, and accepts a column only when it is either the existing unquoted spelling (ASCII case-folded) or its exact lower-case quoted spelling; at least one side must be quoted so the decision-19 arm retains sole ownership of bare/bare. The unaliased arm accepts the exact quoted member only in the existing comparison-position operator family. Both arms feed the same `_text_identity_columns_for_references` answer used by `text_fact_columns`, `fact_table_text_identity_columns` and the narrow guard.

    The new grammar is exact, not an arbitrary quoted-identifier allow-list: `"RT"."variable"`, `"rt.variable"`, and another relation's `rs."variable"` / `"rs"."variable"` remain unattributed; quoted fact-alias declarations remain `_FACT_QUOTED_ALIAS` refusals. Exactness is pinned at the actual load-bearing complete-span classifier plus its consuming matcher arm. Qualified comparison controls `rt."VARIABLE"`, `RT."VARIABLE"`, `rt."Variable"`, `"rt"."VARIABLE"` and `"rt"."Variable"` remain negative; `rt."variable""suffix"` and `"rt"."variable""suffix"` remain negative. Their mutation proof changes the complete-span classifier and the relevant quoted consumer together (case-fold admission or prefix/early-stop admission), because loosening a redundant downstream regex alone is behavior-preserving after the classifier has already masked that token and is not claimed as a killed mutation. The unaliased arm has its own `"VARIABLE"`, `"Variable"` and `"variable""suffix"` comparison controls and kills the analogous classifier + unaliased-consumer mutations independently of the qualified consumers.

    Authority scalar sub-selects and ordinary/E/e literals plus line/nested-block comments are removed before matching. The authority pin includes both aliased and unaliased fact reads; only the unaliased exact-quoted form reaches the unaliased quoted consumer, so a quoted-view-only mutation that bypasses `outer_predicates` must turn that pin red. `_AID_PREDICATE` remains byte-for-byte strict. Red proof on post-#2053 master must show both helpers empty and narrow DID NOT RAISE for every qualified probe; for the unaliased probe, guarded `fact_table_text_identity_columns` is empty and narrow DID NOT RAISE while alias-scoped `text_fact_columns(sql, alias)` remains empty by contract. After the fix, both helpers report each qualified quoted reference and narrow refuses it; only `fact_table_text_identity_columns` reports the unaliased quoted column, narrow refuses it, and `text_fact_columns` stays empty rather than silently becoming a whole-statement helper. Independent deletion of the bare-qualifier quoted-column arm, the exact-quoted-qualifier arm, or the unaliased quoted-column arm must turn its responsible helper and exact narrow-refusal pins red. Every lower-case quoted member in `TEXT_IDENTITY_COLUMNS` is enumerated; enum-suffix quoted controls remain negative. All 20 registry attribution sets/renders and the golden remain unchanged with 0 new refusals.
21. **Qualified-name separator grammar (#2092).** PostgreSQL permits whitespace around a qualified-name dot and treats comments as whitespace. The scanner-owned outer text already collapses `rt /*x*/ . "variable"` and `rt -- x\n . "variable"` to the same code shape as `rt . "variable"`, but decisions 19/20 hard-code literal `\.` in every qualified matcher arm. Consequently bare/bare, bare/quoted, exact-quoted/bare and exact-quoted/exact-quoted references with legal separator whitespace return empty from both helpers and pass narrow rendering.

    Ruling: one named qualified-reference separator fragment owns the normalised grammar `scanner-code whitespace* + dot + scanner-code whitespace*`; the existing bare/bare branch and the two quoted-reference branches (three regex branches covering four token combinations) consume it. The fragment admits only ASCII whitespace produced or retained by the existing scanner/`outer_predicates` path and does not broaden alias or column token policy: bare tokens still ASCII lower-fold, quoted tokens still require complete exact lower-case bodies admitted by decision 20, and at least one quoted token remains decision 20's ownership distinction. Every qualified branch also rejects an alias candidate whose preceding code token ends in a dot, so all four suffix combinations — `other . rt . variable`, `other . rt . "variable"`, `other . "rt" . VARIABLE`, `other . "rt" . "variable"` — cannot be truncated to their last two components. Both public helpers and the narrow guard receive the same result through `_text_identity_columns_for_references`.

    Positive proof enumerates every `TEXT_IDENTITY_COLUMNS` member for `rt . column`, `rt . "column"`, `"rt" . COLUMN` and `"rt" . "column"`, asserting both public helpers and the exact narrow refusal. A separator matrix independently varies the left and right side across no whitespace, spaces/tabs/newlines, nested block comments and newline-terminated line comments; comments must be recognised as separators only after scanner/outer normalisation, never by teaching the regex comment syntax. Negative proof retains direct-dot decisions 19/20, exact quoted case/whole-token rules, quoted alias-declaration refusal, other-relation isolation, enum/key suffix boundaries and literal/comment data blanking. Multi-part/table-name shapes such as `hydro . river_timeseries . variable` and every bare/quoted form of `other . rt . variable` must not be truncated into a fact-alias match: existing modelled-reference guards keep table-name forms fail-closed, while all four unrelated-prefix suffix combinations are pinned as no attribution even when the actual fact reference separately declares alias `rt`.

    Load-bearing mutation proof removes optional spacing from the separator's left side and right side independently; each must turn its side-specific public-seam nodes red. Comment behaviour is pinned publicly with block/line separators and literal/comment data controls. Because `outer_predicates` and `_blank_non_code` both remove comments today, deleting only one layer is behavior-preserving and is not claimed as a killed mutation; a comment-normalisation mutation counts only if it bypasses every active normalisation path and demonstrably changes the public answer. All 20 post-#2086 registry attribution/helper/render surfaces remain identical with zero new refusals and unchanged golden; no production caller or node-27 claim is added.
22. **Parenthesized whole-row alias field selection (#2112).** PostgreSQL treats a table alias as the composite value of its current row and defines a qualified column reference as a special case of `expression.fieldname`. Therefore a fact alias declared bare as `RT`/`rt` denotes the same row in `(rt).variable`, `(RT).VARIABLE`, `("rt").variable` and `("rt")."variable"`; decision 20's exact quoted-token policy still makes `("RT")`, `("Variable")` and doubled-quote suffix identifiers different names. Today all four canonical lower-case bare/quoted combinations return an empty set from both helpers and let narrow rendering return SQL with a forbidden text column.

    Ruling: one scanner-owned classifier walks every balanced parenthesis group in the outer-query code view and classifies a field selection ending in a known `TEXT_IDENTITY_COLUMNS` member. The supported form is deliberately finite: exactly one standalone parenthesis pair whose body, after scanner-normalised ASCII whitespace/comments, is exactly one canonical fact-alias token — bare with decision-19 ASCII lower-folding or a complete exact lower-case quoted spelling admitted by decision 20 — followed by decision 21's shared qualified-reference separator and a bare lower-folded or exact lower-case quoted text member. The opening group must be the start of that row expression, not a suffix cut from an identifier/function call, another dot component or another enclosing expression. Internal spaces/tabs/newlines and block/line comments are accepted only through the existing `outer_predicates`/`_blank_non_code` pipeline; the classifier does not parse comment syntax independently.

    Exact supported references feed `_text_identity_columns_for_references`, so `text_fact_columns(sql, 'RT')`, `text_fact_columns(sql, 'rt')` and guarded `fact_table_text_identity_columns` report the same canonical member and narrow rendering raises the existing exact `text identity column(s)` error. Legacy rendering accepts the supported syntax and changes only the table name. `text_fact_columns` remains deliberately unguarded and alias-scoped: it does not become a whole-statement validator.

    The same classifier separately reports an **unsupported alias-rooted parenthesized field selection** when a balanced group immediately field-selects a known bare/exact-quoted text member, contains a canonical fact-alias token in scanner-visible code, but is not the exact one-token group above. Alias containment is whole-token bounded: a longer bare identifier such as `art` or `rt2` that merely contains the canonical spelling `rt` is another relation, never the fact alias. `_assert_modelled_reference_forms` consumes that report before both render variants or the guarded helper can return and raises `RiverTemplateError` naming the entry and `unmodelled parenthesized fact-alias field selection`. This closes nested `((rt)).variable`, cast `(rt::record).variable` / `CAST(rt AS record).variable`, function/row `f(rt).variable` / `ROW(rt).variable`, and multipart `other.(rt).variable` suffixes without pretending to parse arbitrary row expressions. The detector is conservative over this no-caller subset; it must inspect every balanced group, not stop at the first non-field-selection group. A group rooted only in another relation, including an alias superstring such as `(art).variable` or `(rt2).unit`, a non-exact quoted alias, or a field outside `TEXT_IDENTITY_COLUMNS` retains its prior no-attribution answer.

    Exact positives enumerate every text member across the four bare/quoted qualifier-column combinations and the internal/outer whitespace-comment matrix, asserting both helpers and exact narrow refusal. Negatives preserve all direct #2053/#2086 references, #2092 separator/left-boundary behavior, quoted declaration refusal, other-relation isolation (including `(art).variable` and `(rt2).unit` alias-superstring rows), non-exact quoted names, enum/key siblings, complete quoted bodies, literals/comments and authority-table local columns. Unsupported alias-rooted examples assert the dedicated refusal through guarded helper plus legacy and narrow render, while the unguarded alias helper is not used as a refusal oracle. Independent mutations (1) remove the exact parenthesized-alias positive classification, (2) suppress the unsupported report and (3) replace whole-token alias containment with substring containment; each must make its owner nodes red without collection/import/syntax errors, and mutation 3 is owned specifically by the alias-superstring no-attribution/render-unchanged rows. All 20 post-#2092 registry entries retain attribution, guarded/per-alias sets and both render bytes with zero new refusals and unchanged golden.

    `outer_predicates` deliberately removes comparison-position scalar subqueries before this classifier. A direct or parenthesized correlated reference to the outer fact alias inside such a subquery is therefore **not** made safe by decision 22; that independent visibility defect is #2114 and also blocks #1981. No #2112 evidence may count a scalar-subquery case as covered, and no #2114 fix is folded into this slice.

23. **Comparison-position scalar-body visibility for an already-attributed outer fact alias (#2114).** A scalar subquery is an ordinary `SELECT` that may refer to surrounding-query variables. Today `strip_scalar_subqueries` removes the complete body before `_text_identity_columns_for_references` and the #2112 classifier run; the final count-delta guard notices only another fact-table name. Therefore every direct/exact-quoted spelling of all seven text members through outer alias `rt` (28/28), #2092 separator/comment variants, #2112 exact `(rt).member` forms and #2112 unsupported alias-rooted forms all produce guarded empty plus legacy/narrow RETURN when placed inside an authority scalar body. Nested scalar bodies are equally invisible. An inner fact-table reread still correctly gets the existing comparison-position sub-select refusal.

    Ruling: keep `outer_predicates` and unguarded `text_fact_columns` byte-for-byte outer-query-only. Add one scanner-owned enumerator for **every** comparison-position scalar body (including nested and later independent bodies), and have guarded `_assert_modelled_reference_forms` ask each body through the existing alias/member analysis for aliases already attributed in the surrounding statement. The analysis is one owner: decision-19/20 direct forms, decision-21 separators and decision-22 exact/unsupported parenthesized forms are reused rather than re-spelled. A direct or exact parenthesized known-member hit, or decision 22's unsupported alias-rooted report, raises one entry-named `correlated outer fact-alias reference inside comparison-position scalar subquery` refusal through guarded `fact_table_text_identity_columns` and both stores; it does not return a member set because the guarded boundary is rejecting an unsafe complete statement. A body-local relation that declares the same bare alias is conservatively refused: resolving shadowing would require SQL scope resolution, and guessing that the token is local would recreate the fail-open. The current registered authority shapes use different aliases and remain accepted.

    Error order remains stable: U& / lexical subset / unterminated / quoted-alias / outer parenthesized/functional checks / table-name counts / inner fact-read count delta run before the correlated-body check. Thus a scalar body that rereads the fact table retains the existing `comparison-position sub-select` reason even if it also references `rt.variable`; the new reason owns only statements whose table-read accounting otherwise passes. Authority/local aliases such as `hr.run_id` or `hr.variable`, unrelated aliases, `other.rt.variable`, enum/key siblings, non-exact quoted identifiers and literal/comment data do not become candidates. Unaliased outer fact reads with unqualified scalar-body names cannot be safely distinguished from body-local columns by this lexical subset and are not guessed at; #2148 owns that independent contract gap and does not directly block #1981 because no current registry/#1981 template uses it. Decision 24 now fulfils this hook for functional notation: exact and unsupported `member(alias)` shapes enter the same shared analysis, so this scalar-body seam applies the correlated reason without a private functional matcher. The existing `functional-notation-2141` clean pin is retired/inverted in task 1.11; only the paired whole-row-star #2115 row stays routed and clean until its own grammar lands.

    Proof enumerates all seven members across four direct bare/exact-quoted combinations plus #2092 scanner-normalised separators, all four #2112 exact combinations, representative unsupported forms `f(rt).variable` and `((rt)).variable`, local-shadow ambiguity, and these two distinct every-body owners through all three guarded doors:

    - later-independent (kills `bodies[0]`): `SELECT rt.value FROM hydro.river_timeseries rt WHERE rt.run_key = (SELECT max(hr.run_key) FROM hydro.hydro_run hr WHERE hr.run_id = :r) AND rt.basin_version_key = (SELECT max(hr2.basin_version_key) FROM hydro.hydro_run hr2 WHERE hr2.run_id = :r2 AND rt.variable = :v)` — first scalar is authority-clean; only the second carries the candidate;
    - nested-only (kills top-level-only enumeration): `SELECT rt.value FROM hydro.river_timeseries rt WHERE rt.run_key = (SELECT max(hr.run_key) FROM hydro.hydro_run hr WHERE hr.run_key = (SELECT max(hr2.run_key) FROM hydro.hydro_run hr2 WHERE rt.variable = :v))` — only the inner scalar carries the candidate.

    Both currently yield guarded empty plus legacy/narrow RETURN with `rt.variable` retained; the fixture-repair probe records that exact I/O. Public controls pin `outer_predicates` and `text_fact_columns` unchanged, authority/other/enum/non-code clean, and inner fact reread precedence. Mutations independently bypass all scalar bodies, stop after the first body, and skip nested-body recursion; the direct matrix, later-independent owner and nested-only owner respectively turn red without changing controls. An exact mutation that calls `outer_predicates` on each already-enumerated body remains green because nested bodies are separately yielded and inspected; that transform is behaviorally equivalent under this design and is explicitly not claimed as a killed mutation. All 20 registry entries remain identical on attribution, guarded/per-alias sets and legacy/narrow renders, with zero new refusal and unchanged golden.

24. **Unqualified one-argument functional composite field notation (#2141).** PostgreSQL documents `field(table)` and `table.field` as interchangeable when `table` (or its alias) denotes the row composite. The function-call spelling is not merely punctuation sugar in every catalog state: an unqualified single-composite function with the same name takes precedence in functional notation, while `schema.field(alias)` forces function interpretation. The renderer does not inspect a live catalog, so its safety rule is lexical and conservative: an unqualified known text-member call with the fact alias as its row argument can denote a narrow-table-forbidden field and cannot return clean.

    Ruling: extend the single `_alias_member_analysis` owner with one scanner-owned functional classifier over the same raw outer-code view as decision 22. An exact form consists of an unqualified known `TEXT_IDENTITY_COLUMNS` member — bare ASCII lower-folded or a complete exact lower-case quoted identifier — followed through scanner-normalised ASCII whitespace/comments by exactly one canonical fact alias argument — bare ASCII lower-folded or complete exact lower-case quoted — and no suffix field-selection dot. All seven members across `member(rt)`, `"member"(rt)`, `MEMBER(rt)`, `member("rt")`, `"member"("rt")` add the canonical member to `exact_columns`; both public helpers report it, legacy remains rename-only, and narrow reuses the existing entry-named `text identity column(s)` refusal. The member's previous significant scanner-code character must not be `.`: `hydro.variable(rt)`, `hydro . variable(rt)`, `pg_catalog /*c*/ . variable(rt)` and `"hydro" . variable(rt)` remain schema-qualified functions and are clean through both helpers and both renders.

    The same classifier separately reports a known-member call as unsupported when its balanced or unclosed argument body contains a whole-token fact alias in an expression-value role but is outside the exact grammar: `member(rt.*)`, `member(rt, 1)`, `member(f(rt))`, `member(rt::record)`, `member(ROW(rt))`, an unclosed `member(rt`, or `member(rt).value`. It must call `_skip_balanced_span` and trust its `closed` flag: an unclosed body is consumed through EOF, never discarded because its final byte is not `)`, and `variable(f(rt)` must not mistake the inner close for the outer one. Guarded `fact_table_text_identity_columns` and both render stores refuse these before structural checking as `unmodelled functional fact-alias field notation`; unguarded alias-scoped `text_fact_columns` remains a matcher and reports only exact forms. Whole-token alias containment reuses decision 22's expression-role filter: `variable(art)` and `variable(rt2)` remain clean and specifically kill any substring-containment mutant, while placeholder `variable(:rt)`, type `variable(CAST(x AS rt))`, named-argument `variable(rt => :x)` / `variable(rt := :x)`, dotted function-name and quoted-label roles cannot manufacture an alias value. A call whose only row argument is another relation, a non-exact quoted alias/member, an enum/key sibling, a complete quoted identifier, or literal/comment data retains its prior clean answer.

    Decision 23 enumerates scalar bodies and invokes `_alias_member_analysis`; therefore exact and unsupported functional forms inside comparison-position scalar bodies automatically receive the existing correlated-scalar guarded refusal after inner fact-read count-delta precedence, with no scalar-only functional matcher. The existing `test_correlated_scalar_functional_notation_and_star_remain_routed` is split: the functional id is inverted into exact and unsupported correlated-refusal owners, while `rt.*` remains the #2115 clean control. Public proof owns 35 exact outer rows; whitespace/comment variants before `(`, around the alias and on both token gaps (including `variable(/*c*/rt/*d*/)`); tight/spaced/comment/quoted schema qualification; other relation and `art`/`rt2` superstrings; non-exact/data/role controls; all unsupported shapes through the three guarded doors; and exact/unsupported scalar owners. Independent mutations (1) delete exact classification, (2) suppress unsupported reporting, (3) remove guarded/render consumption of functional unsupported state while parenthesized consumption stays green, and (4) replace whole-token alias containment with substring containment; each must fail its responsible public nodes without collection/import/syntax errors. Existing direct and parenthesized owners, decision-23 direct scalar owners, authority/enum/data scalar controls and #2115 `rt.*` stay green; the retired #2141 scalar-clean row is not a green control. All 20 registry entries remain identical across attribution, guarded/per-alias sets and legacy/narrow renders, with zero new refusal and unchanged golden.
25. **Direct structural quoted-identifier evidence (#2082).** PostgreSQL double-quoted identifier bodies are identifier data, so an unmatched `)` or `(` inside a complete quoted identifier cannot alter statement grouping. `assert_structurally_intact` already implements this by sending both single- and double-quote openers through `_scan_quoted`, but decision 18's commutation outcome cannot prove the double-quote arm: both sides retain quoted identifiers after `_blank_non_code(..., keep_literal_quotes=True)`, so removing only the structural helper's double-quote skip makes both sides count the same phantom parenthesis and the commutation node remains green.

    Ruling: keep production code byte-identical and add one helper-level direct parametrized pin over `SELECT 1 AS "a)b" FROM t WHERE a = :a` and `SELECT 1 AS "a(b" FROM t WHERE a = :a`. The current helper must accept both. An isolated m25 mutation changes only the `assert_structurally_intact` branch from single-or-double quote to single quote; the two direct cases must then fail at their own call with the corresponding `closed one that was never opened` / `1 unclosed` structural reason, without import, syntax or collection failure. The existing commutation node must still pass under that exact mutation, proving the two tests own different seams rather than allowing the broad oracle to claim coverage it cannot provide. No renderer output, public signature, registry entry, golden fixture or production caller changes.
26. **Caller-owned route and occurrence-local UNION contract (task 1.4 Stage 5 re-entry).** PR #1996 proved that a statement-level `render_union_all` without a production caller was the wrong ownership boundary: every affected MVT/coverage statement contains an aggregate, limit, result-decision or DML layer that cannot be duplicated per store. The rejected helper and its binder/walker remain absent. The shared renderer produces one fact-row variant only; each reader owns both routing and placement.

    Ruling: a caller with one known run obtains that run's `timeseries_store` through its existing metadata seam and executes one rendered variant. A spanning caller composes two occurrence-local fact-row branches before the enclosing aggregate, `DISTINCT`/`ORDER BY`/`LIMIT`, HTTP result decision or DML; both branches reuse the same named parameters, project identical columns in identical order, bind the corresponding `hydro_run.timeseries_store`, and touch only `hydro.river_timeseries_legacy` or `hydro.river_timeseries`. The precise owners are: MVT per-basin `source_rows` as a local union whose run-key authority lookup binds branch store; all three national LATERAL probes as local unions, with store projected by both candidate-run queries; both valid-time sources as unions under one outer distinct/order/limit, with the named branch binding store in its run-key authority lookup; the hydro-display identity probe as a known-run single variant; and display coverage's `river_sample_rows` CTE as a union under one unchanged upsert. Existing zoom-leg unions are not store unions. The pre-expand catalog has neither route column nor `_legacy` table, so wave-2 reader commits may merge but must not be deployed or executed until I7 atomically deploys them with expand. Real mixed-store execution and EXPLAIN belong to I7/I8, not this Stage 5 re-entry.

    Width ruling: #1982 keeps only the per-basin hydro statement; national MVT (#2206) and valid-time discovery (#2207) are two independent issues. The three national LATERAL occurrences remain one atomic slice because identity-existence and both zoom data legs must choose the same candidate/run-store semantics. #1983 keeps only the hydro-display probe; display coverage (#2208) is an independent issue. The five slices share no artificial dependency: each depends on I1, and I7 depends on all five. Each slice has one statement/entrypoint behavior and one verification path.

## Must-preserve

- For every registry entry, the `legacy` variant with `hydro.river_timeseries_legacy` substituted back to `hydro.river_timeseries` has the same conjunct multiset per predicate chain as the base-SHA text (golden: WHERE / ON / subquery / lateral / OR-disjunct chains compared positionally), the same parameter names/placeholders inside those chains and the same disjunction truth table — i.e. equal **modulo whitespace, marker placement and conjunct order within one chain**. The golden is blind outside predicate chains (SELECT list, FROM/JOIN targets, LIMIT/ORDER BY): those are covered by the reviewer's per-block diff (evidence mapping 1.1) and the sibling substring pins, and this PR's production diff touches nothing outside WHERE/ON chains and comments (round-1 review C11).
- No reader is wired to the renderer; no executed production SQL changes meaning. The rewritten disjunctions (`forecast_store.py:1903` shape, `display_coverage.py:410/417`) keep the same truth table: `OR (` + marker + `rt.run_id = %(scan_run_id)s AND` + `rt.run_key = (...))`.
- Every pre-existing test of the touched readers outside the allowed re-pin set passes with zero edits.
- After the PR the only marker form in production code is (apart from the two non-aid tag lines inside `packages/common/river_ts_render.py` declared in `NON_AID_MARKER_TAG_LINES`) the verbatim `-- transitional compressed-chunk pushdown aid, remove with #1342`, one per aid, on the line immediately above the aid; a marker never sits on a `WHERE`/`ON`/keyword line.
- Statement census (`RIVER_TABLE_CENSUS`) unchanged for every pre-existing registered file; parser has no diff.
- #2053 case-folding addendum: task 1.6 changes only unquoted identifier comparison. The matcher retains outer-query-only attribution and reuses the existing scanner; literal/comment text and the complete quoted identifier `"rt.variable"` never create a fact-column predicate. At the #2053 merge boundary, qualified quoted references remained unmodelled; decision 20 / task 1.7 subsequently closes references through a bare-declared fact alias when the qualifier and/or known text column uses its exact lower-case quoted spelling, plus the exact lower-case unaliased column. A quoted fact alias declaration (`AS "r"` / bare `"r"`) remains refused. Enum/key siblings such as `variable_e`, `unit_e`, `quality_flag_e` remain excluded; another relation's identically named column remains unattributed; `_AID_PREDICATE` remains case-sensitive and an uppercase marked aid fails closed instead of widening the normalised marker grammar.
- #2086 quoted-reference addendum: `rt."variable"`, `RT."variable"`, `"rt".variable`, `"rt"."variable"` and an unaliased `"variable"` in comparison position are attributed exactly like their unquoted lower-case fact-column spelling, for every member of `TEXT_IDENTITY_COLUMNS`, because the fact alias was declared bare and lower-folds to `rt`. Quoted-token comparison is case-sensitive and whole-token: `"RT"`, `"VARIABLE"`, `"Variable"`, `"rt.variable"`, quoted enum/key siblings and another relation's quoted member stay negative. Qualified comparison controls cover `rt."VARIABLE"` / `RT."VARIABLE"` / `rt."Variable"` / `"rt"."VARIABLE"` / `"rt"."Variable"` and both qualified `"variable""suffix"` forms; unaliased controls separately cover `"VARIABLE"`, `"Variable"` and `"variable""suffix"`. Mutation proofs target the load-bearing complete-span classifier together with the corresponding qualified or unaliased quoted consumer; they do not claim a behavior-preserving regex-only change must fail. All quoted arms consume the same scanner-owned outer code as the unquoted matcher; aliased and unaliased authority-subquery controls make a quoted-view bypass of `outer_predicates` observable. No literal, comment or authority scalar sub-select can create attribution. Quoted fact-alias declarations and `_AID_PREDICATE` retain their existing fail-closed contracts.
- #2092 separator addendum: the three qualified matcher branches covering four token combinations share one normalised whitespace-dot-whitespace fragment. Spaces, tabs, newlines and block/line comments around the dot are accepted only through scanner-owned code normalisation; direct-dot behavior, bare lower-folding, exact quoted token classification, helper responsibility and narrow error text stay unchanged. Left/right optional spacing are independently mutation-owned. Comment separator behavior is publicly pinned, but no evidence claims that removing one of two behaviorally redundant comment-stripping layers must fail.
- #2112 parenthesized field-selection addendum: exactly one scanner-visible parenthesis pair containing only a canonical bare fact alias or its exact lower-case quoted reference may qualify a bare-folded or exact lower-case quoted text member. Internal and separator whitespace/comments use the existing scanner/outer normalisation. Both public helpers report exact supported forms; narrow raises the existing text-identity refusal and legacy remains rename-only. Any balanced parenthesized field selection of a known text member that contains the fact alias but is not that exact form is rejected by the guarded preflight through both renders and `fact_table_text_identity_columns`; the unguarded alias helper remains a matcher, not a validator. Another relation, non-exact quoted names, enum/key siblings and non-code bytes retain their prior answers. Comparison-position scalar-subquery correlation remains #2114 and is not claimed covered by task 1.9.
- #2114 correlated-scalar addendum: every comparison-position scalar body is scanner-enumerated only for guarded preflight. A known text member through an already-attributed outer fact alias reuses the shared direct/exact-quoted/#2092 separator, #2112 exact/unsupported parenthesized and #2141 exact/unsupported functional grammar, then makes guarded helper plus both stores fail closed with one entry-named correlated-scalar reason. The former #2141 functional clean pin is inverted while #2115 star remains routed. Local alias shadow is conservatively refused; inner fact-table rereads retain the older count-delta reason. `outer_predicates` and `text_fact_columns` stay outer-only, authority/other/enum/data controls stay clean, and unaliased/unqualified scope ambiguity remains #2148.
- #2141 functional-notation addendum: an unqualified known text member applied to exactly one canonical bare-declared fact alias is the PostgreSQL `member(alias)` spelling of `alias.member`; both helpers report the canonical member, legacy remains rename-only and narrow uses the existing text-identity refusal. Bare tokens lower-fold; quoted tokens are complete and exact lower-case; whitespace/comments enter only through scanner normalisation. A schema-qualified call is a function and remains unattributed. A known-member call that contains the fact alias but is outside the exact one-argument grammar is rejected by guarded helper and both stores as unmodelled functional fact-alias field notation. Decision 23 consumes the same shared analysis for scalar bodies; no scalar-only matcher or catalog lookup is introduced.
- #2082 structural-evidence addendum: `assert_structurally_intact` continues to ignore every parenthesis byte inside a complete double-quoted identifier. A helper-level direct test, not the scanner/traversal commutation oracle, owns this guarantee. The commutation test stays present and keeps t1/m26 responsibility; under m25 it is expected to remain green while the new direct pin alone turns red.
- Task 1.4 caller addendum: no complete aggregate/limited/DML statement is duplicated per store. A known-run read executes exactly one variant; a spanning read has exactly one local two-branch fact-row union beneath one outer semantic layer. Each branch is store-bound, table-exclusive and projection-compatible. No shared `render_union_all`, binder, SQL-scope walker or store-suffixed parameter surface is introduced. Wave-2 code is not deployed before I7 creates the route column and legacy table name.

## Seams under test

Consumed as declared in `design.md` → Sketch seams under test #1 (SQL shape oracle) plus the existing capture-cursor seam for runtime-assembled blocks. The golden fixture is a new artefact of the same seam, not a new seam. A needed-but-missing seam is a reported deviation, not a renegotiation.

## Invariant Matrix

```text
Invariant Matrix
Governing invariant: For every registry entry, render(legacy) is the normalised template with the table renamed and nothing else; render(narrow) removes exactly each marker line plus the single aid conjunct immediately below it, contains no fact-table text identity column under any modelled PostgreSQL direct, qualified-name, exact one-token parenthesized whole-row, or unqualified one-argument functional field spelling, refuses unsupported parenthesized/functional alias-rooted forms and known text-member references through an already-attributed outer fact alias inside any comparison-position scalar body before guarded/render output, contains no marker, keeps every key/enum predicate of the legacy variant, passes the structural check, and reports removed positional placeholders; the renderer raises naming the entry before returning SQL when a marker or reference form is unsafe.
Source-of-truth identity/contract: the verbatim marker string; TEXT_IDENTITY_COLUMNS / TEXT_AID_COUNTERPARTS, the shared direct/exact-quoted/separator, exact/unsupported parenthesized and exact/unsupported functional alias-member analysis, plus the scanner-owned comparison-position scalar-body enumerator in packages/common/river_ts_render.py (fact-table columns run_id, basin_version_id, river_network_version_id, river_segment_id, variable, unit, quality_flag); the test-side registry; the golden fixture at base 51f9d273.
Surfaces:
- Producers (owning oracle): packages/common/forecast_store.py (cleanup), services/tile_publisher/publisher.py (cleanup), services/tile_publisher/forcing_copyback_backfill.py (cleanup), workers/output_parser/parser.py (cleanup; already conforming, no diff), services/tiles/mvt.py (surrogate-keys), apps/api/routes/hydro_display.py (surrogate-keys), packages/common/display_coverage.py (surrogate-keys)
- Validators/preflight: packages/common/river_ts_render.py (structural check; table-scoped no-text-identity assert over `TEXT_IDENTITY_COLUMNS`, attributed to the fact table through a canonical lower-folded bare alias — or through unqualified comparison position when the reference is unaliased — on scanner-owned outer code after `outer_predicates` strips authority sub-selects and comments/literal bodies are blanked; the unquoted arm masks complete quoted identifiers and compares ASCII case-insensitively (decision 19), while the quoted-reference arm preserves only whole-token exact lower-case spellings of the bare-declared alias or a `TEXT_IDENTITY_COLUMNS` member and accepts their four PostgreSQL-equivalent qualifier/column combinations (decision 20); one named scanner-normalised separator fragment admits optional code whitespace around the dot for all three qualified matcher branches (four token combinations), so spaces/newlines and comments-as-whitespace cannot bypass attribution (decision 21); one balanced-group classifier recognises the exact direct parenthesized whole-row alias qualifier for both helpers and separately makes guarded/render paths refuse other alias-rooted parenthesized text-member selections (decision 22); one comparison-position scalar-body enumerator reuses that shared analysis for already-attributed surrounding aliases in guarded preflight (decision 23); and one unqualified functional-field classifier recognises exact `member(alias)` forms for both helpers while reporting other known-member calls containing the alias to guarded/render preflight, excluding schema-qualified functions (decision 24). Unaliased/unqualified scalar scope remains #2148; arbitrary/mixed-case quoted identifiers and quoted alias declarations stay outside the grammar; both public helpers and the narrow guard share the exact matcher while only guarded/render paths consume unsupported reports; round-3 L4 corrected the earlier constant attribution); key-predicate-retention assert (a conjunct is exempt only when it EQUALS a removed aid; a containing conjunct — `OR (…)` guard or `EXISTS (…)` body — must reappear in its aid-deleted form, computed by the renderer, so the authority `run_key` predicate can never vanish silently; round-1 C8, implemented stronger than the prescribed `OR (`-only rule because two registry entries carry the aid inside `EXISTS`); mis-shaped-marker refusal); tests/river_ts_template_registry.py; tests/fixtures/river_ts_templates_51f9d273.json; the nine SQL_SHAPE_ORACLE_TESTS files
- Storage/cache/query: none changed — no caller is wired
- Public routes/entrypoints: none changed
- Frontend/downstream consumers: none
- Failure paths/rollback/stale state: renderer fail-closed (`RiverTemplateError`, message names the entry and the reason) on a mis-shaped marker, on a marker whose next line is a keyword, on a scanner-visible structural fault, or on unsupported parenthesized/functional fact-alias field notation; complete double-quoted identifier bodies are data to the structural parenthesis counter and are directly pinned rather than delegated to the commutation oracle; an unclosed outer functional call reaches the dedicated `unmodelled functional fact-alias field notation` refusal before the render-only `unbalanced parentheses` structural check, while an exact/unsupported functional shape inside a comparison scalar reaches the decision-23 correlated reason after inner-fact count-delta precedence; `_rename_table` never rewrites the table name inside a string literal or comment; **[Superseded by decision 15 — combinator split out; historical record only]** `render_union_all` refused — never bound — every shape outside the whitelist that was recorded at decision 13: positional template, `union_safe=False` entry (message carries the declared reason), empty store list / unknown store / mixed placeholder dialects, missing named parameter, data-modifying statement (masked top-level DML), fact read under an outer join / negation / EXCEPT-INTERSECT right operand anywhere in its ancestor opener chain, reference form the walk does not model (comma join, `FROM ONLY`, `USING`, quoted identifier, fact table as the preserved side of an outer join), a scope with no own WHERE/ON chain, an occurrence count disagreeing with the walk, a statement terminator or row-locking clause, a top-level disjunction as the governing chain, and a RIGHT/FULL join in any ancestor segment; the join form binds `hydro_run.timeseries_store` per branch only when the scope correlates the `hydro_run` alias on `run_key`, otherwise a correlated EXISTS qualified by the reference's alias (or the branch's physical table name where it has none); one shared parameter mapping returned unchanged
- Evidence/audit/readiness: marker/aid census per file in the owning oracle; golden equivalence test; scripts/select_ci_tests.py rules to SQL_SHAPE_ORACLE_TESTS
Regression rows:
- every registry entry + legacy -> conjunct multiset per chain equals the golden; table name is the only textual difference from the normalised source
- every registry entry + narrow -> no fact-table text identity column (table-scoped: display_coverage's `hydro_run WHERE run_id` subquery is legitimate; unaliased single-table statements hydro_display :773 / mvt :1496 / :1524 are attributed to the fact table), no marker, every key/enum predicate present, structural check passes
- already-attributed outer alias + known text member in any comparison-position scalar body -> guarded helper and both stores refuse with the correlated-scalar reason; nested/later bodies and local alias shadow are included
- unqualified known text member + exact one-argument canonical fact alias (`member(rt)` family, including comments/whitespace around the alias) -> both helpers report the canonical member, legacy rename-only, narrow text-identity refusal; the same shape inside a comparison scalar body -> decision-23 correlated refusal through the shared analysis, replacing only the former #2141 routed pin while #2115 star stays clean
- unqualified known-member call + fact alias in an unsupported argument/result shape -> guarded helper and both stores refuse as entry-named unmodelled functional fact-alias field notation; `variable(rt` and `variable(f(rt)` prove actual unclosed state and inner-close ownership before structural refusal
- tight/spaced/comment/quoted schema-qualified functions, other relations, alias-superstrings `art`/`rt2`, non-exact quoted/enum/key/non-code and placeholder/type/named-argument roles -> unchanged clean answer; substring and previous-significant-dot mutants turn those owners red
- complete double-quoted identifier containing unmatched `)` or `(` + direct `assert_structurally_intact` call -> accepted; m25 makes only these direct owners fail with the corresponding unbalanced-parentheses reason while the traversal commutation node remains green
- authority/other/enum/non-code scalar-body controls -> unchanged clean answer; inner fact-table reread -> existing count-delta refusal; unaliased/unqualified ambiguity -> #2148, not guessed
- marker above WHERE / above a two-aid line / above a non-aid line / as the last line -> renderer raises naming the entry; adjacency oracle fails naming the file:line
- consecutive marker/aid/marker/aid runs (mvt :661 ×3, :793 ×4, :840 ×4 after expansion) -> narrow removes all of them, legacy keeps all
- zero-marker entry -> narrow == legacy modulo table name
- ON-chain aid (publisher :2665) -> same rules; narrow leaves `ON r.run_key = h.run_key` + the enum predicate
- positional template + narrow -> removed_placeholders lists the removed %s indices, and for every positional registry entry `len(removed_placeholders) == sum('%s' in aid for aid in the removed aids)` (an independent formula — the `count('%s') before − after` form is an identity of `_strip_aids` and cannot go red) plus a literal index pin per real positional entry (measured at 515a3947: eight `forecast_store` entries → `(3, 4)`, `forecast_store:forecast_segment_rows_selected_cycles` → `(5, 6)`, the two `parser:replace_chain_*` entries → `(1,)`; a mutant reporting `p + 1` must go red); the selector glob self-comparison at `tests/test_select_ci_tests.py:911-915` stays as is — the rule itself is pinned literally by the sibling test (H7-b refuted)
- **Cross-store union ownership (round-4 second gate entry, `retro-1996-2.md`, decisions 15 and 26).** `render_union_all` and its binder (`store_binding_plan`/`BoundScope`/`store_predicate`, `union_safe`/`union_unsafe_reason`, `UNION_PLACEMENT_PINS`, probes A–U) are absent. Stage 5 emits five implementation slices: per-basin MVT, national MVT's three coupled LATERAL occurrences, MVT valid-time discovery, hydro-display known-run probe and display-coverage river CTE. Shape oracles must show one outer semantic layer, occurrence-local store branches, identical branch projections/parameters and branch-exclusive physical tables; deployment remains gated on I7.
- quoted fact alias (`FROM hydro.river_timeseries AS "r"` / bare `"r"`, round-4 H-2 consequence 1) -> refused by `fact_table_text_identity_columns` and by `render_river_ts_sql` for both stores (`RiverTemplateError`, unmodelled reference form) — never a silent empty text-identity set; `AS r` (unquoted) yields alias `r` and its text-identity columns are detected; unquoted-alias control still renders
- literal carrying `FROM hydro.river_timeseries` (round-4 H-6) -> `fact_table_attribution` counts over the comment/literal-blanked text like its sibling counters: `reference_count == 1`, `has_unaliased_reference` False when the real reference is aliased, narrow render leaves the literal verbatim (probe F extended with a FROM-bearing literal)
- **unquoted alias / column case folding (#2053, decision 19)** -> `FROM hydro.river_timeseries RT` with a marked `RT.run_id` aid and an unmarked `rt.variable = 'q_down'` predicate: `fact_table_attribution(...).aliases == frozenset({'rt'})`; `fact_table_text_identity_columns` includes `variable`; both `text_fact_columns(sql, 'RT')` and `text_fact_columns(sql, 'rt')` return the same non-empty set containing `variable`; after aid removal the narrow render refuses with `text identity column(s) ['variable']`, not `unmodelled`. Equivalent `RT.VARIABLE` and an unaliased `VARIABLE` are attributed/refused. Controls: upper/lower same-spelling aliases retain existing results; `RT.VARIABLE_E` / `RT.UNIT_E` / `RT.QUALITY_FLAG_E` and `RS.VARIABLE` on another relation do not add text columns; `'rt.variable = x'`, `E'rt.variable'` / `e'rt.variable'`, `-- rt.variable`, nested `/* rt.variable */`, and the authority-subquery `hydro_run WHERE run_id` stay unattributed. At the task-1.6 boundary three quoted controls were distinct: complete body `SELECT "rt.variable"` was not a token pair; qualified quoted column was deliberately deferred to decision 20; quoted fact alias `AS "r"` / bare `"r"` retained the existing `unmodelled` refusal. `_AID_PREDICATE` is explicitly not widened: `AND RT.RUN_ID = …` under a marker is not an aid and `_strip_aids` refuses it. Compare all 20 registry entries' attribution sets plus both renders to pre-change; 0 new refusals and golden unchanged.
- **exact lower-case quoted fact references (#2086, decision 20)** -> for every `column in TEXT_IDENTITY_COLUMNS`, all four references through a bare-declared fact alias — `rt."<column>"`, `RT."<column>"`, `"rt".<COLUMN>` and `"rt"."<column>"` — make both public helpers report that canonical lower-case member; an unaliased fact read with `"<column>"` in comparison position is reported by guarded `fact_table_text_identity_columns` only. Narrow raises naming the entry with `text identity column(s)` and the member, not `unmodelled`, for all five shapes; `text_fact_columns` remains empty on the unaliased shape because it answers only for the supplied alias. Independent bare-qualifier/quoted-column and exact-quoted-qualifier arm deletions turn both helper and narrow pins red; deleting the unaliased quoted-column arm turns the guarded-helper and narrow pins red. Controls: quoted enum/key siblings `"variable_e"` / `"unit_e"` / `"quality_flag_e"`; `rs."variable"` / `"rs"."variable"`; ordinary/E/e literals and line/nested block comments containing `rt."variable"`; `"RT"."variable"`, `rt."VARIABLE"`, `RT."VARIABLE"`, `rt."Variable"`, `"rt"."VARIABLE"`, `"rt"."Variable"`, `rt."variable""suffix"`, `"rt"."variable""suffix"` and complete `"rt.variable"` all stay unattributed. Unaliased `"VARIABLE"`, `"Variable"` and `"variable""suffix"` also stay unattributed; classifier + unaliased-consumer case/prefix mutants must turn these exact nodes red while qualified consumers remain unchanged. Both an aliased and an unaliased fact read resolving `run_key` through `hydro_run WHERE "run_id"` remain unattributed and render unchanged; a quoted-view-only bypass of `outer_predicates` must turn only the unaliased authority node red. Qualified case/suffix nodes kill the analogous classifier + qualified-consumer mutations; no regex-only equivalent mutation is claimed. `AS "r"` / bare `"r"` remains the existing unmodelled fact-alias-declaration refusal; `_AID_PREDICATE` is unchanged. Compare all 20 post-#2053 registry entries' attribution sets and legacy/narrow renders; 0 new refusals and golden unchanged.
- **qualified-name separator whitespace/comments (#2092, decision 21)** -> for every text identity member, `rt . column`, `rt . "column"`, `"rt" . COLUMN` and `"rt" . "column"` report that member from both public helpers and narrow refuses with the exact text-identity error. A side matrix covers no whitespace, space/tab/newline, nested block comments and newline-terminated line comments around the dot; left-only and right-only optional-spacing mutants each make their owner rows red. Direct-dot rows keep their #2053/#2086 answers. Non-exact quoted tokens, enum/key suffixes, other aliases, literal/comment bodies, authority subqueries, quoted alias declarations and table-name qualification retain their prior negative or fail-closed outcomes. The four `other . <alias-token> . <column-token>` suffix combinations change from conservative substring attribution/refusal to no fact-column attribution, proving the new alias-left-boundary guard rather than claiming prior behavior. Comment behavior is pinned at public seams without claiming either redundant stripping layer alone is mutation-visible; compare all 20 post-#2086 entries across attribution, guarded/per-alias text sets and both renders with zero new refusal and unchanged golden.
- **parenthesized whole-row alias field selection (#2112, decision 22)** -> for every text member and each `(bare-alias|exact-lowercase-quoted-alias)` × `(bare-column|exact-lowercase-quoted-column)` combination, both public helpers report the canonical member and narrow raises the exact text-identity error. The exact group accepts scanner-normalised spaces/tabs/newlines and block/line comments inside its parentheses plus decision-21 separator forms outside; direct controls remain unchanged. `((rt)).variable`, `(rt::record).variable`, `CAST(rt AS record).variable`, `ROW(rt).variable`, `f(rt).variable`, `other.(rt).variable` and equivalent known-member spellings are rejected as `unmodelled parenthesized fact-alias field selection` by guarded helper plus legacy/narrow render, because the balanced expression contains the attributed alias but is not the finite exact grammar; a mutation suppressing this unsupported report makes those owner nodes red. `(rs).variable`, alias-superstring controls `(art).variable` / `(rt2).unit`, `("RT").variable`, `("rt""suffix").variable`, `(rt).variable_e`, complete `"rt.variable"`, literal/comment bytes and authority-table local columns retain their prior no-attribution answers and render unchanged. A mutation deleting the exact-positive classifier makes both helper and narrow-positive nodes red while direct-dot and #2092 nodes stay green; a mutation replacing whole-token alias containment with substring containment is killed specifically by the alias-superstring rows. Registry comparison remains 20 entries × attribution/guarded/per-alias/legacy/narrow with zero new refusals and stable golden. Correlated outer-alias references inside comparison-position scalar subqueries are deliberately absent from this row and remain #2114.
- quoted fact alias WITHOUT whitespace (`hydro.river_timeseries"r"` / `AS"r"`, #2018 round-1 B/P2-1) -> same refusal as the whitespace spellings (`_FACT_QUOTED_ALIAS` uses `\s*` in both gaps; accepted over-match: `FROM "hydro.river_timeseries" r` is refused too) — `tests/test_river_ts_render.py::test_a_double_quoted_fact_alias_is_refused_instead_of_reported_clean[no_ws_bare_quoted|no_ws_as_quoted|legacy_no_ws_quoted]`; non-vacuity `test_an_unquoted_fact_alias_survives_the_no_whitespace_widening`
- escape-string literal `E'a\'b'` before a real fact read (#2018 round-1 B/P2-2: the H-6 move to blanked text made a backslash-escaped quote swallow the FROM clause) -> `_scan_quoted` is backslash-aware for `E'`/`e'` strings only; attribution still sees the alias, narrow refuses, legacy renames — `test_an_escape_string_literal_does_not_swallow_the_statement_after_it`; controls `E'abc'` / `'a''b'` render both stores (`test_an_escape_string_with_no_backslash_and_a_doubled_quote_still_render`)
- reference form the FROM/JOIN walk cannot count (`"hydro"."river_timeseries" r`, `hydro."river_timeseries" r`, `FROM ONLY`, comma join, `IN (TABLE …)`; #2018 round-1 C/P2-1 — the combinator's refusal for these forms left with the split) -> `_assert_modelled_reference_forms` refuses when `fact_table_name_occurrences(sql) != fact_table_attribution(sql).reference_count` (message `unmodelled fact-table reference form`, both counts named), the counter counts the table's bare NAME as a whole token (decision 16; round 1's `_FACT_NAME_QUOTED` enumeration is retired) so every quoted / spaced / cased / re-qualified spelling is counted and refused — accepted over-matches: any other schema prefix (`otherhydro.river_timeseries x`, #2018 round-2 D/F7), a CTE named `river_timeseries` and a column alias `AS river_timeseries` / `AS "river_timeseries"` (#2018 round-3 H2b) are refused too; both stores + `fact_table_text_identity_columns` — `test_a_reference_the_from_join_walk_cannot_count_is_refused[fully_quoted|half_quoted|from_only|comma_join|in_table]`; positive control `test_the_reference_count_guard_does_not_fire_on_a_plainly_aliased_read`; 0/20 registry entries fire, golden unmoved
- row-locking clause (`FOR UPDATE` / `FOR NO KEY UPDATE` / `FOR SHARE` / `FOR KEY SHARE`, #2018 round-1 C/P2-2: the arm's only killing test left with the combinator) -> chain ends before the clause and `AND FOR UPDATE` is a dangling-connective structural fault — `test_a_row_locking_clause_ends_the_chain_and_is_never_a_conjunct` (red when `_KEYWORD_FAMILY`'s lock arm is deleted)
- census scope (#2018 round-1 C/P2-3): the 34/34 marker/aid census and the table-name census are pinned over the REGISTERED files (`REGISTERED_SOURCES` + `DISPLAY_MARKER_AID_CENSUS`), not the tree; an unregistered reader is the I11 discovery-set census's job (tasks 7.2a) — docstrings at the three oracle sites say so; no sweep in child A
- spelling both counters miss (#2018 round-2 E/P2-2, lane-1 F1, orchestrator probes: `"hydro".river_timeseries`, `hydro . river_timeseries`, newline / block-comment around the dot, unqualified `FROM river_timeseries rt` (search_path spelling), `FROM RIVER_TIMESERIES rt`, `otherhydro.river_timeseries x`, `hydro."River_Timeseries" r` — all but the last RENDERED at b397f8a4 with the text predicate shipped; the quoted-uppercase one was already refused by round 1's IGNORECASE `_FACT_NAME_QUOTED` and stays a pin of the token counter) -> the counter is the bare-name token `\briver_timeseries(?:_legacy)?\b` (IGNORECASE, over blanked text; decision 16; the column-qualifier lookahead was deleted in round 3 G2) so occ ≥ 1 and the walk's 0 refuses every one of them; column qualifiers (`hydro.river_timeseries_legacy.run_key`, `hydro."river_timeseries"."run_key"`) count 1 per mention (2 in the `_legacy` control statement) and are refused — `test_a_reference_the_from_join_walk_cannot_count_is_refused[spaced_dot|newline_dot|comment_dot|quoted_schema|quoted_schema_quoted_alias|unqualified|unqualified_upper|other_schema|quoted_upper]`; control: a `_legacy` read qualifying a column by the table name is REFUSED for both stores (occ 2, walk 1); the bare-alias spelling renders; 0/20 registry entries change count, golden unmoved
- Unicode-escaped identifier `U&"riv\0065r_timeseries"` (the one spelling that hides the NAME from a token counter) -> `_assert_modelled_reference_forms` refuses any `U&` (either case) in the blanked text (entry named; over-refusal of `U&'…'` literals accepted, 0/20 registry) — pinned for `U&"river_timeseries"` and the escaped form, both stores + `fact_table_text_identity_columns`
- uppercase canonical spelling `FROM HYDRO.RIVER_TIMESERIES rt` (#2018 round-2 F2: counters agree, guard silent, legacy rendered UN-renamed) -> `_CANONICAL_NAME` carries `re.IGNORECASE` like every other fact-name pattern (one case policy per module); pin: legacy render of `HYDRO.RIVER_TIMESERIES` / `Hydro.River_Timeseries` contains `hydro.river_timeseries_legacy` and not the un-renamed spelling (literal compare); `_legacy_legacy` idempotency control
- comparison-position scalar sub-select fact read (`rt.run_key = (SELECT r2.run_key FROM hydro.river_timeseries r2 WHERE r2.variable = %s LIMIT 1)`, #2018 round-2 F4: `outer_predicates` strips it before the alias scan, narrow shipped `r2.variable`) -> refused when `fact_table_name_occurrences(sql) != fact_table_name_occurrences(strip_scalar_subqueries(sql))` (message `comparison-position sub-select`); refusal, not scan extension — extending the scan false-refuses the two authority-sub-select entries (`hydro_display:mvt_source_identity_probe`, `mvt:valid_times_named_identity`); pins over both shapes × both stores + the authority sub-select positive control; 0/20 fire
- `E'…'` scanner scoping (#2018 round-2 E/P2-1: 4/7 scanner mutants survived) -> plain `'C:\'` literal is a `(7, 12)` span and the read after it is attributed; `LIKE'C:\'` and `x_e'C:\'` are NOT escape strings; `e'` lowercase is — pins next to `test_an_escape_string_literal_does_not_swallow_the_statement_after_it` (parametrised over `E'`/`e'`)
- registry-closure census (`_river_table_mentions`, #2018 round-2 lane-1 F1 sibling; decision 8 allowed set) -> counts schema-qualified spellings tolerant of quoting / whitespace / case (`(?:"hydro"|\bhydro)\s*\.\s*"?river_timeseries`), same 10 per-file counts as the substring it replaces; the unqualified search_path spelling is refused at render time but cannot be forced into the register by this census (recorded limit)
- scanner/lexer divergence hides a read from BOTH counters (#2018 round-3 G1, P1; found by all three lanes; the round-2 record here claimed the opposite direction WITHOUT a probe — retro-2018.md): nested block comment `/* outer /* inner */ don't stop here */` (PostgreSQL nests; the re-tokenised tail's apostrophe opened a phantom literal over the real FROM), `$` inside an identifier `a$b$c` (opened a phantom dollar-quote to EOF), and the re-synchronising case `/* a /* b */ don't */ … E'q\'x'` (phantom literal closes on a later odd-quote literal — proves an EOF check alone is NOT the class) -> `_scan_block_comment` depth-counts, `_DOLLAR_QUOTE_OPEN` never opens after `[\w$]`, `--` ends at `\r` too, and an unterminated span at EOF is refused as a BELT only (decision 17) — pins T1/T2/T3/T4 + EOF belt + controls (non-nested apostrophe comment, real `$tag$`/`$$` bodies, `$1`, `B''`/`X''`); 0/20 registry entries change spans, counts or renders
- table-name-qualified predicate on an unaliased read (`WHERE hydro.river_timeseries.variable = %s`, bare `river_timeseries.variable`; #2018 round-3 G2, P2: neither alias arm of the text-identity scan saw it, narrow shipped `variable`, the bare spelling's legacy render was emitted with an un-renamed qualifier) -> closed by DELETION: `_FACT_NAME_TOKEN` no longer excludes column qualifiers, so the qualifier is a counted mention the walk does not model and the equality guard refuses it for both stores (the former `…still_renders` control is retired; pin `test_a_column_qualified_by_the_table_name_is_refused_so_the_qualifier_is_never_unseen[schema_qualified|bare|schema_qualified_legacy|bare_legacy|quoted]`; alias the table); deviation from verifier G's detection-arm shape recorded in retro-2018.md; 0/20 registry
- `U&` guard case policy (#2018 round-3 H1, test-only: dropping IGNORECASE survived every suite and let `u&"riv\0065r_timeseries"` render) -> the two `U&` pins parametrised over `U&` / `u&`
- lexer/PostgreSQL disagreement at a SECOND site of one rule (#2018 round-4 I1 P1 non-ASCII dollar tag `$备注$don't$备注$ … E'q\'x'`, `$é$note -- x$é$`; I2 P1 the traversal family `strip_comments` → `outer_predicates` truncating at a `--` inside `$q$…$q$` so narrow shipped `rt.variable`; I3 P2 `_FACT_NAME_TOKEN` trailing/leading `\b` unpinned; J2 P2 the quoted-identifier traversal arm unpinned and fail-open under deletion) -> decision 18: any `$` or non-ASCII byte in blanked code is refused before any traversal, the dollar arm is deleted, the family commutes with the scanner (pinned), the scanner is differentially pinned against a reference lexer; `\b` pins `test_the_token_counter_keeps_its_trailing_boundary` / `test_the_token_counter_keeps_its_leading_boundary` (mutants dropping either boundary die); J2 pins named in decision 17; 0/20 registry entries change spans, counts or renders; round-3's `a$b$c`, `x$e'C:\'`, `$1`, real-dollar-body and unterminated-`$tag$` controls flip to subset refusals
- **round-5 lexical closure L1/L2 (#2050, PR #2057 Round-1 strengthened)** -> `E ␠'…'` / `e ␠'…'` are plain-literal syntax in the independent reference; adjacent escape and whitespace-adjacency known-case pins cover both cases. The seeded differential has non-zero accepted adjacent counts for uppercase and lowercase, kills m6 (module prefix lookbehind crossing whitespace), and turns red when only the reference's lowercase branch is deleted. True code-position `1E'a\'x'` / `1e'a\'x'` plus exponent/sign/leading-dot numeric variants in select-list and predicate positions are refused before scanning by legacy render, narrow render and `fact_table_text_identity_columns`; every message contains entry, exact numeric-token start and complete offending text and omits `river_timeseries`. Quoted identifiers containing the same byte sequences — including `""` escapes — keep their base three-door outcome because the L2-only mask skips identifier bodies without changing their code status elsewhere. Removing any numeric-token arm, feeding raw SQL, removing quoted-identifier masking, or broadening the terminal quote to `[Ee]["']` turns focused tests red; controls for identifier-contained text, valid exponent literals, literals/comments and quoted identifiers preserve their prior outcomes. The reference numeric-token walk is structurally independent from the production recognizer. Registry comparison stays 20 × {occurrences, spans, legacy render, narrow render} byte-identical; 0/20 registry entries are newly refused; golden SHA-256 stays unchanged.
- **round-5 lexical closure L3 (#2050)** -> the 16-statement adversarial corpus includes the scalar-subquery-local `AS "a)b"` row from decision 18. Removing quoted-identifier handling from `_opaque_runs` (t1) or `_skip_balanced` (m26) makes the commutation test red. Removing it from `assert_structurally_intact` (m25) remains green at this commutation seam for the structural reason recorded in decision 18; #2050 did not claim or implement m25 closure, and task 1.12 now owns the separate direct pin.
- marker/aid census -> 34/34 after normalisation, per-file counts pinned in the owning oracle; statement census unchanged for pre-existing files; renderer module registered with its own count
- adjacency oracle -> `test_a_marker_on_the_aids_own_line_turns_the_oracle_red` (the former `…_is_accepted` inverted); _assert_aids_are_marked (:348) requires exactly the previous line and 1:1
- allowed re-pins -> tests/test_display_coverage_refresh.py:302, tests/test_migrations.py:441-448 and tests/test_qhh_latest_fallback_pushdown.py:191-196 re-pinned to the normalised layout (decision 8)
- scripts/select_ci_tests.py + a diff touching river_ts_render.py, the registry, the golden JSON (tests/fixtures/river_ts_templates_*.json), or any normalised reader -> the nine SQL_SHAPE_ORACLE_TESTS files are selected; the golden's sha256 is pinned in the registry so a regeneration is a visible one-line diff (round-1 C10)
- unchanged siblings (tile publisher `_has_table`, copyback `required_columns`, QHH smoke scripts, parser DELETE/probe SQL) -> untouched, tests pass
```

## Boundary-surface checklist

- Shared helper roots: new `packages/common/river_ts_render.py` — single owner (the implementer); the two text-identity constants move there from the test module.
- Public entrypoints: none changed.
- Read surfaces: the six production files above — SQL text layout only.
- Write/delete/overwrite surfaces: none (parser untouched).
- Staging/publish/rollback: none.
- Producer/consumer evidence boundaries: golden fixture + marker/aid census + shape pins re-pinned in the same PR with measured counts.
- Stale-state/idempotency: none.
- Unchanged downstream consumers: API contract tests, mocked tile tests, display coverage tests outside :302 — zero edits.

## Evidence mapping (tasks.md → proof)

- 1.1 normalisation → golden equivalence test (mechanical) + marker/aid census 34/34 + adjacency oracle + the two allowed re-pins; reviewer additionally diffs every rewritten block
- 1.2 renderer → unit tests: legacy variant, narrow variant, structural check, mis-shaped-marker refusal, consecutive blocks, zero-marker, ON-chain, positional removal, quoted-alias refusal, literal-blanked attribution (union combinator split out — decision 15, task 1.4)
- 1.3 oracle tightening + selector rules → the nine SQL_SHAPE_ORACLE_TESTS green on both variants for every registry entry; `tests/test_select_ci_tests.py` covers the new rules
- 1.4 Stage 5 re-entry → decisions 15/26 + design D5 per-entry matrix; #1982/#1983 narrowed to atomic first slices, three independent follow-on issues created, Epic/I7 dependencies updated; one lightweight Stage 5.5 review has no P0/P1 residual; tracked sizing-retro records PR #1996 as `slice-error`; AST/token search proves production/test Python has no `render_union_all` definition, import or call; only superseded OpenSpec history retains the name
- 1.5 round-5 lexical closure (#2050) → red proof on the #2049 base for all three L2 doors; L1 reference self-pin + m6 differential kill; L3 t1/m26 commutation kills with m25 explicitly still green at that seam and routed to 1.12; the nine SQL_SHAPE_ORACLE_TESTS plus `tests/test_select_ci_tests.py`; strict OpenSpec validation; 20 × 4 byte-identical registry comparison, 0/20 new refusals, unchanged golden SHA-256
- 1.6 alias/column case folding (#2053) → latest-master red proof showing `RT` + lower `rt.variable`, `RT.VARIABLE` and bare `VARIABLE` absent from attribution and allowed through narrow; after the change, exact public-helper and narrow-error assertions plus non-vacuous `text_fact_columns` equality; preserve outer-subquery stripping and reuse `non_code_spans` / `_blank_non_code` rather than a second lexer; negative controls cover ordinary and E/e literals, line/nested-block comments, enum suffixes, other aliases, complete-body `"rt.variable"`, qualified quoted columns and quoted fact aliases as three distinct shapes at the #2053 boundary; mutation proof for qualified and unaliased case-fold arms; `_AID_PREDICATE` uppercase marked-aid refusal remains fail-closed; seven-file local suite + full Ruff + strict OpenSpec; 20-entry attribution/render comparison and unchanged golden SHA-256
- 1.7 exact lower-case quoted fact references (#2086) → post-#2053 red proof for `rt."variable"`, `"rt".variable`, `"rt"."variable"` and unaliased `"variable"`: both helpers are empty and narrow DID NOT RAISE on the qualified shapes; on the unaliased shape `fact_table_text_identity_columns` is empty, narrow DID NOT RAISE, and alias-scoped `text_fact_columns` remains empty before and after. Enumerate every `TEXT_IDENTITY_COLUMNS` member: both helpers + narrow on bare/quoted qualifier shapes, guarded helper + narrow on the unaliased arm, with an explicit `text_fact_columns` empty control. Add scanner-owned whole-token classification; qualified-comparison and unaliased negative controls for mixed/upper quoted columns and `""`-escaped suffixes; complete-body identifier, enum suffixes, other relation, both aliased/unaliased quoted authority subqueries, both literal forms and both comment forms; quoted fact-alias-declaration and strict `_AID_PREDICATE` controls. Mutation proof: independent deletion of each of the three positive matcher arms; a quoted-view-only bypass of `outer_predicates` killed by the unaliased authority node; complete-span classifier + corresponding qualified/unaliased consumer case-fold and prefix/early-stop mutations killed by their own controls. Regex-only changes rendered behavior-preserving by the exact classifier are not claimed as killed mutations. Run the seven-file local suite + full Ruff + strict OpenSpec; compare 20 post-#2053 entries' attribution/render surfaces, 0 new refusals and unchanged golden SHA-256
- 1.8 qualified-name separator grammar (#2092) → post-#2086 red proof for four spaced bare/quoted qualifier-column combinations and left/right block/line-comment separators, with both helpers empty and narrow DID NOT RAISE. Add one named scanner-normalised whitespace-dot-whitespace fragment used by all three qualified matcher branches covering the four token combinations; enumerate every text member across the four token combinations and a separator-side matrix; retain direct-dot and all decision-19/20 negatives. Independently delete left and right optional spacing and make side-owned public-seam rows fail. Publicly pin comment separators and data controls; no claim that deleting one redundant comment-removal layer alone must fail. Run the seven-file suite, full Ruff and strict OpenSpec; compare 20 post-#2086 entries across attribution, guarded/per-alias sets and legacy/narrow render, 0 new refusals, unchanged golden.
- 1.9 parenthesized whole-row field selection (#2112) → post-#2092 red proof for all four direct one-pair bare/quoted qualifier-column combinations plus internal and separator whitespace/comments, with both helpers empty and narrow DID NOT RAISE. Implement one scanner-owned balanced-group classifier that reports exact one-token alias groups to both helpers and separately reports known-member selections containing the fact alias but outside that grammar to `_assert_modelled_reference_forms`; enumerate every text member across the four exact combinations and a whitespace/comment matrix. Pin guarded + legacy/narrow `unmodelled parenthesized fact-alias field selection` refusals for nested/cast/function/row/multipart alias-rooted shapes; pin another relation, non-exact quoted names, enum/key siblings, complete quoted body, authority/local columns and literal/comment data as unchanged. Independently delete exact-positive classification and suppress the unsupported report; each mutation must fail its own public owner nodes while direct-dot/#2092 controls stay green. Run the seven-file suite, full Ruff and strict OpenSpec; compare all 20 post-#2092 entries across attribution, guarded/per-alias sets and both renders, 0 new refusals, unchanged golden. Do not count or claim comparison-position scalar-subquery correlation; #2114 owns it.
- 1.10 correlated scalar-body visibility (#2114) → post-#2112 red proof: all seven text members × four direct bare/exact-quoted token combinations, #2092 separator/comment forms, four #2112 exact forms, representative unsupported forms `f(rt).variable` / `((rt)).variable`, the quoted nested-only and later-independent scalar owners, and local alias shadow produce guarded empty plus both renders RETURN. Add one scanner-owned every-body enumerator and reuse the existing alias-member analysis only in `_assert_modelled_reference_forms`; all three guarded doors then refuse with entry + correlated-scalar reason, while `outer_predicates` / `text_fact_columns` bytes remain unchanged and inner fact rereads retain their older reason. Pin authority/other/enum/non-exact/multipart/non-code controls and #2148 unaliased boundary. Mutations bypass all bodies, stop after the first body and skip nested recursion; each has a distinct direct/later/nested owner. Per-body `outer_predicates` is recorded as equivalent rather than falsely claimed load-bearing. Run the eight-file suite, Ruff and strict OpenSpec; compare all 20 entries across five surfaces with 0 new refusals and unchanged golden.
- 1.11 functional composite field notation (#2141) → latest-master public-seam red proof: all seven members × five exact `member(alias)` bare/exact-quoted combinations = 35/35 alias-helper empty + guarded-helper empty + narrow RETURN, with whitespace/block/line-comment and comparison-scalar exact probes also returning. Add one scanner-owned exact/unsupported functional classifier to `_alias_member_analysis`; exact forms report the canonical member to both helpers, narrow reuses text-identity refusal and legacy remains rename-only. Unsupported unqualified known-member calls containing the fact alias (unclosed `variable(rt` and `variable(f(rt)`, star, multi-arg, nested, cast, ROW, result-field suffix) are refused by guarded helper plus both stores before structural checking using `_skip_balanced_span.closed`. Tight/spaced/comment/quoted schema-qualified functions, other aliases and `art`/`rt2` superstrings, non-exact quoted identifiers, enum/key siblings, placeholder/type/named-argument roles and non-code bytes stay clean. Decision 23 consumes the same analysis in scalar bodies: invert the old #2141 scalar-clean id into exact/unsupported correlated owners and leave the #2115 star id clean. Independent mutations delete exact classification, suppress unsupported reporting, remove functional-unsupported guarded consumption while parenthesized remains green, and replace whole-token alias containment with substring containment; each reddens its own public owner without syntax/import/collection failure. Run the eight-file suite, Ruff and strict OpenSpec; compare 20 entries × five surfaces with `DIFFS=0`, `NEW_REFUSALS=0`, unchanged golden.
- 1.12 direct structural quoted-identifier evidence (#2082) → latest-master baseline proves `assert_structurally_intact` accepts both `SELECT 1 AS "a)b" FROM t WHERE a = :a` and its unmatched-open twin; an AST-bounded m25 changes only the helper's double-quote skip arm and makes the two direct probes refuse with their own unbalanced-parentheses reasons, while `test_every_traversal_commutes_with_the_scanner_over_the_corpus` remains green. Add one parametrized helper-level test for both directions, keep the commutation test behavior intact, and update decision 18/docstrings to state direct pin = m25 owner and commutation = t1/m26 owner. Run the renderer test file, Ruff and strict OpenSpec; source blob, production outputs, registry/golden and caller set remain unchanged.

## Non-goals

- No reader wiring, no store lookup helper (I2–I5). No `hydro_run.timeseries_store` column (I7). No forcing readers (I11). No marker deletion (I9). No parser edit.
- #2053 does not revisit #2050 lexical scanning, implement m25 (#2082), model quoted aliases, widen `_AID_PREDICATE`, add a `render_union_all` helper, or claim a production/node-27 result. Its historical qualified-quoted-column deferral is consumed only by #2086 / decision 20.
- #2086 models only references to a fact alias **declared bare**: a bare or exact lower-case quoted spelling of that canonical alias, combined with a bare or exact lower-case quoted `TEXT_IDENTITY_COLUMNS` member (at least one quoted), plus the exact unaliased quoted-column comparison-position form. It does not model a quoted fact-alias declaration, arbitrary/mixed-case quoted identifiers, new aid grammar, production callers, schema/migration/UNION, m25, DB/display/frontend behavior, or node-27 evidence.
- #2086 issue-body corrections (fixture authority): (1) its original out-of-scope bullet listed `"rt"."variable"` as negative. That contradicts PostgreSQL §4.1.1 and the same issue's governing invariant: when the alias is declared bare as `RT`/`rt`, exact quoted qualifier `"rt"` denotes that alias, and exact quoted `"variable"` denotes the text column. Decision 20 therefore covers `"rt".variable` and `"rt"."variable"`; only a quoted alias **declaration** or non-exact quoted spelling remains outside the model. No separate owner exists, and leaving either combination negative would preserve the same narrow fail-open under another equivalent spelling. (2) the body says the unaliased probe gets the same answer from both helpers, but `text_fact_columns(sql, alias)` deliberately answers only for the caller-supplied alias and fixes `has_unaliased_reference=False` (decision 19's public contract). Task 1.7 therefore assigns unaliased detection to guarded `fact_table_text_identity_columns` + narrow refusal and pins `text_fact_columns` empty, rather than broadening that helper into a whole-statement answer.
- At task 1.7's merge boundary, #2086 deliberately did not widen the qualified-name separator grammar; #2092 / decision 21 / task 1.8 now owns that closure without revisiting #2086's exact quoted-token classification. #2092 does not add a general SQL parser, model quoted fact-alias declarations, accept arbitrary multi-part/table-name references, widen `_AID_PREDICATE`, wire readers, or claim schema/migration/DB/display/frontend/node-27 behavior.
- #2112 models only scanner-visible outer-code field selection from one direct parenthesized whole-row alias token and conservatively refuses known text-member selections containing that fact alias outside the exact grammar. It does not parse arbitrary row/composite expressions, attribute another relation, accept non-exact quoted tokens, change the unguarded alias helper into a validator, wire callers, change schema/migration/UNION/m25, or claim DB/display/frontend/node-27 behavior. It specifically does not fix or certify a direct/parenthesized correlated outer-alias reference inside a comparison-position scalar subquery; #2114 owns that visibility boundary and independently blocks #1981.
- #2114 only restores guarded visibility for known-member references through an already-attributed surrounding fact alias. It does not change `outer_predicates`, broaden alias-scoped `text_fact_columns`, resolve PostgreSQL scopes, guess whether an unqualified scalar-body name belongs to an unaliased outer fact read (#2148), implement functional notation (#2141) or whole-row star (#2115), wire callers, alter schema/migrations/UNION/m25, or claim DB/display/frontend/node-27 behavior.
- #2141 models only an **unqualified** known text member applied to a bare-declared fact alias through one exact bare/exact-quoted argument, and conservatively refuses other unqualified known-member calls that contain that alias. It does not inspect function catalogs, treat a schema-qualified function as a field, parse arbitrary expressions/functions, implement whole-row star (#2115), resolve unaliased scalar scope (#2148), revisit m25 (#2082), wire callers, change schema/migration/UNION, or claim DB/display/frontend/node-27 behavior.

## Issue #2115: whole-row star closure (task 1.13)

This addendum governs only #2115; earlier decisions' star deferrals are historical for outer output expansion and remain current for comparison-position scalar bodies. Shared change stays active after this issue.

### Risk triage and boundary

- Issue type: bug fix; upstream suggested fixture level absent; effective fixture expanded, repair intensity high (shared helper and output/evidence contract).
- Minimal mergeable slice: one classifier plus its helper/guard consumption and public-seam proof; no independent reader wiring.
- Public API / CLI / script entry: selected — existing public helper behavior changes; no signature or runtime caller change.
- Schema / columns / units / field names: selected — legacy whole-row exposure maps to the seven canonical text identity members.
- Legacy compatibility / examples: selected — exact legacy projections stay byte-identical modulo table rename; all 20 registry entries remain identical.
- Error handling / rollback / partial outputs: selected — unsupported star grammar refuses at guarded entry before SQL return; no IO or rollback surface.
- Documentation / migration notes: selected — record output-schema drift semantics and the finite grammar here and in helper documentation; no migration.
- Config / project setup; File IO / path safety / overwrite; Auth / permissions / secrets; Concurrency / shared state / ordering; Resource limits / large input / discovery; Release / packaging / dependency compatibility: not selected — no new configuration, IO, state, discovery, dependency or release mechanism.
- PostGIS / TimescaleDB domain behavior: selected only for PostgreSQL composite-row semantics; no engine/catalog/DB verification.
- Published NHMS artifacts / display identity: selected only for pre-caller output compatibility, evidenced by registry bytes; no live display claim.
- Geospatial / CRS / basin geometry; Hydro-met time series / forcing windows; SHUD numerical runtime / conservation / NaN; Slurm production lifecycle / mock-vs-real parity; External hydro-met providers / snapshot reproducibility; Run manifest / QC provenance: not selected — unchanged.
- Shared helper/evidence boundary: `packages/common/river_ts_render.py`, `tests/test_river_ts_render.py`, `tests/test_sql_shape_helpers.py`; registry/golden and selector are compatibility consumers, not new runtime entrypoints.
- Storage/write/delete/publish/stale-state/frontend/deployment boundaries: none changed. Existing `.gitignore` work is unrelated and must not be committed here.

### Finite grammar and must-preserve behavior

PostgreSQL [8.16.5](https://www.postgresql.org/docs/current/rowtypes.html#ROWTYPES-USAGE) distinguishes direct row expansion `alias.*`, composite-expression expansion `(alias).*`, and named field selection `(alias).field` (decision 22). At SELECT output-list top level, the first two expose the physical row's columns. Legacy includes all seven text identity members; narrow has a different row schema. Refusal prevents cross-store payload/row-mapping/UNION shape drift, not an inevitable missing-column SQL exception. Bare case folding and exact lower-case quoted reference semantics follow PostgreSQL 4.1.1 and decisions 19/20.

One scanner-owned classifier recognises complete output items `alias.*` and `(alias).*`, including first/later output items, SELECT ALL/DISTINCT/DISTINCT ON prefixes and whitespace/comments inside the one-token group or around the shared dot separator. Traverse only scanner-visible outer code and balanced query groups; do not turn scalar-body inspection into a second whole-row visibility feature. Existing token-role and complete-quoted-token rules remain authoritative.

Required output-position owners include `SELECT rt.value, rt.* FROM hydro.river_timeseries rt` and its `(rt).*` twin, plus `SELECT DISTINCT ON (rt.run_key) rt.* FROM hydro.river_timeseries rt` and its `(rt).*` twin. Each has the same full-set/legacy-return/narrow-refusal public outcomes as a first item; a first-item-only classifier must fail these rows.

Both `text_fact_columns(sql, alias)` and guarded `fact_table_text_identity_columns(sql, entry=...)` return exactly `set(TEXT_IDENTITY_COLUMNS)` for exact fact-alias expansions. Legacy remains rename-only; narrow uses the existing entry-named text-identity refusal, never the generic fact-table-reference-count error for these exact forms. Unguarded `text_fact_columns` remains a matcher, not a validator.

For scanner-visible fact-alias-rooted stars outside the exact output grammar, guarded helper and both stores refuse with entry plus a dedicated unmodelled whole-row-star reason. Required examples: `((rt)).*`, `(rt::record).*`, `f(rt).*`, `ROW(rt).*`, a multipart alias-rooted star, and a direct star in a function argument or ORDER BY. These are conservative refusal cases, not claims to parse arbitrary composite expressions or non-output semantics. The classifier must not silently accept a known candidate merely because its projection shape is unsupported.

Refusal precedence preserves existing named-member owners: unqualified `variable(rt.*)` remains decision-24 functional-unmodelled in outer code and decision-23 correlated refusal in scalar code. The new function-argument refusal owner is `foo(rt.*)` (not a known member); `ORDER BY rt.*` separately owns non-output clause refusal. The new star guard runs after the existing functional-unsupported guard.

Non-fact `cr.*`, `source_rows.*`, `eligible.*`, other relations, alias-superstrings, complete identifier `"rt.*"`, non-exact quoted `"RT".*` / `("RT").*`, ordinary/E literals, line/nested-block comments, and key/enum projections retain their old attribution/render outcomes. Quoted fact-alias declarations retain the prior quoted-declaration refusal and are not mistaken for admitted references. All seven direct named members, decision-22 named field selection, decision-24 functional notation and decision-23 correlated named-member behavior remain intact.

Concrete alias-superstring controls are `SELECT art.* FROM hydro.river_timeseries rt JOIN hydro.hydro_run art ON true` and `SELECT (rt2).* FROM hydro.river_timeseries rt JOIN hydro.hydro_run rt2 ON true`: fact-alias helper and guarded identity helper remain empty and both renders return. A substring-containment mutation must turn these public owners red.

The existing comparison-scalar predicate-star routed control stays unchanged: star classification is not implicitly enabled when decision 23 reuses `_alias_member_analysis` for a scalar body. `outer_predicates` and the alias helper stay outer-only. Neither this task nor that control certifies scalar-star safety; #2148 continues to own only unaliased/unqualified scalar scope.

Keep star analysis as a separate outer-code classifier, not a new column/unsupported flag in `_alias_member_analysis`. The two public helper paths combine its exact result with named-member analysis; guarded preflight consumes unsupported stars only for the surrounding outer-code view. Decision 23 continues to consume named-member analysis only, so scalar-body star cannot leak into its column set or refusal flags.

### Invariant Matrix

Governing invariant: exact scanner-visible outer fact-row output stars are never reported clean or returned by narrow; unsupported visible fact-alias-rooted stars never escape guarded preflight; existing registry outputs and neighboring named-member contracts do not change.

Source of truth: `TEXT_IDENTITY_COLUMNS`, PostgreSQL composite semantics, scanner-owned code/output-item boundaries, existing alias/token classification.

- Producers: test templates; production templates remain unchanged and contain no fact-alias stars.
- Validators/preflight: one whole-row-star classifier, public helpers and `_assert_modelled_reference_forms`; reuse existing scanner/top-level/balanced-group infrastructure rather than a new lexer.
- Storage/cache/query/public routes/frontend: no changes, no runtime caller added.
- Failure paths: supported form -> narrow text-identity refusal only; unsupported visible candidate -> guarded helper and both renders refuse before returning output; lexical and quoted-alias refusal precedence remains intact.
- Evidence/readiness: public seam tests, three isolated mutation owners, five-surface registry comparison and golden digest.
- Regression rows: six exact spellings -> both helpers full set, legacy rename-only, narrow entry-named refusal; unsupported forms listed above -> three guarded doors refuse; non-fact/data/key controls -> unchanged; named/scalar sibling matrices -> unchanged.

### Evidence Floor

- Baseline evidence: preserve the issue's post-#2092 six-form helper-empty plus both-store-RETURN record; capture the same six-form behavior on the current implementation base and seven named-member positive/refusal controls before editing source. Historical evidence is not relabelled as a new run.
- Exact and negative matrices run through both public helpers and renders in `tests/test_river_ts_render.py`. In `tests/test_sql_shape_helpers.py`, `SELECT rt.* FROM hydro.river_timeseries rt` and the `(rt).*` twin passed to its imported `text_fact_columns(sql, "rt")` must report all seven members rather than an empty set, proving the SQL-shape consumer sees projection exposure independently of render refusal.
- Isolated direct-star classifier deletion, parenthesized-star classifier deletion, and guarded/render consumption bypass each turn their own public-seam owner red by a behavior assertion, never collection/import/syntax failure. Report exact mutation and node IDs, with unmutated green evidence.
- Also mutate exact alias containment to substring containment and require the `art.*` / `(rt2).*` negative owners to fail; record first-item-only output classification against the later/DISTINCT ON owners as an independent output-position mutation.
- Run once locally: `uv run pytest -q tests/test_river_ts_render.py tests/test_sql_shape_helpers.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_display_coverage_refresh.py tests/test_river_ts_template_golden.py tests/test_river_ts_render_reference_lexer.py tests/test_select_ci_tests.py`.
- Run `uv run ruff check .` and `openspec validate timeseries-narrow-store-expand-contract --strict --no-interactive`.
- Compare current pre-change and final code over all 20 registry entries: attribution, guarded identity set, per-alias identity sets, legacy bytes, narrow bytes. Require `DIFFS=0`, `NEW_REFUSALS=0` and unchanged golden SHA-256; do not regenerate the golden.
- No reader, UNION implementation, schema, migration, parser, PostgreSQL connection, node-27, production incident or live receipt claim. This issue explicitly owns local pure-logic evidence only.
