# Fixture I4a (#1983): single-store MVT source identity

## Authority, scope and calibration

Task 2.3a only. Source base is `ab4d65d86f58b245f6f6ddfe7e4f4d8208a88cef`; the branch also carries the governance-only PR2241 accountability commit `16329e5e04671697ab4193f4d8715ae6a770f4b1`. Shared OpenSpec remains active. The existing user authorization for remaining River pre-expand readers permits per-test disposable post-expand catalogs, not production transition/deployment or early I7/I8.

Effective fixture compact, retaining the upstream suggestion. One known-run metadata lookup and one limited existence probe; no spatial, tile-generation, public-signature, schema, cache or permission change. Selected risks: metadata/store authority, API/consumer compatibility, error precedence, bounded single-query behavior, legacy aids and unknown/OOV behavior. Auth/tenant, publication/filesystem, concurrency, Slurm, providers, new dependencies and migration design are not selected. Reviewer seats: correctness+test-evidence; integration. One serial implementer owns the coupled route/test changes; parent owns specifications, immutable baseline, all validation/proofs and Git.

## Immutable baseline and non-goals

Parent executed `.workplans/issue-1983/capture_baseline.py` before implementation. `baseline/manifest.json` records the original metadata SQL/row, executed probe SQL and five bind names, one `.first()` call, full 404 payload, all thirteen raw registry inputs in both stores and all five tile SQL strings. It also records protected source hashes and the historical golden SHA `d104d1c69cea55cdb86bd8a44d90d8f93c621580b43f74744f6b5cca7cd5a453`.

Keep every raw registry input byte-identical, the immutable golden unchanged, and all `services/tiles/mvt.py`, forecast_store, display_coverage and shared renderer source bytes unchanged. Do not edit services/tiles/display_ready_run: the known-run route uses the local `_run_row` seam. No UNION, new SQL factory, shared route/binder/walker, fallback store, parameter suffix, new dependency, DDL or production action. Preserve `_run_source_version`, cache keys, the other hydro-display routes and their behavior.

## Runtime change and precedence

In `apps/api/routes/hydro_display.py`, add `h.timeseries_store` to `_run_row`'s existing explicit SELECT. Preserve the join, WHERE, LIMIT, single execution, `.mappings().first()`, unknown-run 404 and `dict(row)` result. Do not add a second metadata query or validate this new field globally in `_run_row`: that would change sibling endpoints and preempt the existing readiness/identity errors.

`hydro_mvt_tile` keeps its existing identifier/variable/XYZ/time validation before all SQL, then `_require_display_ready`, then `_require_run_source_identity`, in that order. Pass the metadata's route via required keyword `timeseries_store` to `_require_hydro_mvt_source_identity`; a missing field must reach explicit validation as missing, not raise KeyError or silently default to legacy. There is no new client parameter. Do not change `_require_display_ready` status checks or `_require_run_source_identity`.

At the start of `_require_hydro_mvt_source_identity`, validate the supplied route against exactly `legacy` and `narrow`, without coercion, fallback or SQL. Missing/unknown values raise `ApiError(status_code=500, code="TIMESERIES_STORE_INVALID", details={"run_id": run_id})`; use the existing forecast-store convention (`forecast_store.py:135-143`) without importing its private helper or exception class. Keep invalid route values out of error details. This is validation before the fact probe, not before the metadata query that discovers the route. Existing request-input validation remains before any SQL; not-ready and missing-source-identity errors still win before store validation.

Keep the existing raw SELECT literal inside this function byte-identical. Wrap it with `render_river_ts_sql(raw, timeseries_store, entry="hydro_display:mvt_source_identity_probe").sql` and `text`, executing just that variant once. Preserve all five named binds, original scalar key/enum lookups, `LIMIT 1`, `.first()`, successful return and complete existing `MVT_SOURCE_IDENTITY_NOT_FOUND` 404 payload. Legacy uses only the renamed legacy fact table and retains three aids; narrow uses only canonical narrow table and strips all fact-table text identities. Store comes from the earlier metadata decision; do not redundantly query it again or add routing predicates to the raw literal.

## Registry and static-oracle treatment

Unlike the preceding multi-store raw-source rewrites, this known-store probe's raw input does not change. Keep `hydro_display:mvt_source_identity_probe` and its inline-literal extractor; no new factory or rendered-SQL registry entry. Keep thirteen live raw inputs, seven store-qualified raw sources and six historical-comparable inputs, all exact sets/census assertions and the frozen twenty-key/215-chain golden. Document that `ROUTED_SOURCE_KEYS` distinguishes changed store-qualified raw source inputs, not every caller that invokes the renderer: this probe is now executed through the renderer but remains historical-comparable. Replace misleading "unrouted" prose where needed with "store-independent raw input", without changing the symbol or counts.

Add/extend a behavioral owner comparing the actual executed probe for each store to the renderer result from the registry's unchanged raw input. Preserve extraction non-vacuity, four key predicates, exact instant predicate, aids/forbidden-column checks and `.first()` semantics. Existing source-slice owners in surrogate, SQL-shape, migration and direct-grid history tests must still find the literal; no generic lexer weakening or broad new ownership entries. The existing hydro_display.py selector is reused; add an edge only if an actual required owner is absent.

## Observable tests and compatibility

Use the existing MVT scaling/API test infrastructure rather than a new helper framework. Update only run metadata stubs that now reach the changed probe to carry the required explicit store; test-only default data may use legacy, but production must have no default. Preserve all old assertions, especially malformed identifiers, unsupported variables, XYZ/out-of-range times before SQL, not-ready status, missing source identity, subsecond forwarding, SQL order/count and cache behavior.

Call-level owners in `tests/test_hydro_display_mvt_scaling.py`: `test_hydro_mvt_probe_routes_one_store_and_preserves_not_found` and `test_hydro_mvt_probe_rejects_invalid_store_before_sql`. For legacy and narrow, execute the real probe and assert exactly one rendered fact-table target, no UNION, one LIMIT, one `.first()`, the original five binds, expected aids, successful identity and structured 404 code/details on a miss. Do not add wording-only assertions. For missing/null metadata (passed as `None`) and unknown store, the direct probe raises 500/TIMESERIES_STORE_INVALID with details only `{run_id}` and **zero session.execute calls**; this is not an API metadata-query count.

API owner `test_hydro_mvt_route_preserves_store_error_precedence_and_sql_order` uses the real route and extends `_LegacyRunRouteSession`'s metadata row rather than validating in `_run_row`. Existing request-input 422 tests still require zero SQL. The new distinct state cases have these exact counts and precedence:

| Metadata state | Expected API result | Executions |
|---|---|---|
| Not-ready status, even with missing/invalid store | 409 `DISPLAY_PRODUCT_NOT_READY` | 1: metadata only |
| Ready but missing basin or network identity, even with missing/invalid store | Existing `_require_run_source_identity` 404 | 1: metadata only |
| Ready with source identity but missing/null/unknown store | 500 `TIMESERIES_STORE_INVALID`, details only run_id | 1: metadata only; no probe/tile |
| Ready with source identity and legacy or narrow | Existing successful tile response/headers; no client store parameter | 3: metadata, selected-store probe, tile |

In the successful API cases, the captured probe SQL must equal `render_river_ts_sql(registry raw, metadata store).sql` and name only that physical fact table. Preserve the response/header assertions and use an isolated cold cache so the three-statement order is a measured contract, not a mock assumption.

Upgrade the existing `test_existence_probe_accepts_the_seeded_identity_and_404s_on_unknown_ones` real-DB owner to both-store parameterization using unchanged `post_expand_forecast_database`. Roll back before its single transition, map KEYED run to the parameter store and other seeded runs explicitly, and leave opposite-store facts shifted +30min. Read `metadata = _run_row(session, _KEYED_RUN_ID)` and first assert `metadata.get("timeseries_store") == store`, then pass that same mapped store to the probe. The membership-safe comparison makes an omitted route column fail an assertion rather than raise KeyError. Keep the original success plus unknown run/basin/network/OOV 404 cases; each unknown-identity case still passes the valid mapped store so it cannot become a routing 500. Verify the not-found structured details. Do not use the MVT retiming helper for this dedicated probe: exact-time shifted decoys must reveal a wrong physical store. Existing discovery, coverage, national and per-basin tests remain unchanged except strictly necessary metadata protocol updates.

## Evidence floor and proof

Parent executes the issue's six-file matrix on node27, plus affected renderer/reference-lexer, migration, cleanup/status-only, direct-grid history and HHE owners. Run the complete surrogate integration file and national integration sibling file in a private node27 checkout/venv with integration enabled, excluding only the timescaledb_210 marker; no skips in selected database owners. Local Ruff/OpenSpec and a pure capture/preservation smoke are hygiene, not live receipts. No active checkout pull/switch or shared environment mutation.

Capture/HTTP proof must go red on the pre-change unrendered probe and a forced opposite-store render, then green restored, with assertion failures rather than import/SQL/harness errors. For the pre-change callable, a parent-only adapter may discard the newly required keyword so the red is its old query behavior, not a signature TypeError. The real metadata owner must reject omission of the selected route column with valid SQL and an assertion failure, then restore green. Record one final source SHA or an explicit evidence-only bridge; preserve the original raw/tile/golden baseline and existing 404 behavior. Parent removes throwaway scripts/private environments after archiving executed harness and receipts.

Production route-column rollout, mixed-store transition/Timescale EXPLAIN and deployment/display receipts remain #1986/#1987. These isolated catalogs do not authorize deployment. PR2241's retrospective line rides this normal issue push; user .gitignore is excluded.
