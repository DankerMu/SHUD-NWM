# Fixture I3b (#2206): national MVT occurrence-local store routing

## Authority and triage

Base: `c21bacf9b9d8c29699dffca56bb9776c1f49db61`. Task 2.2b owns only the hydro-national statement. The 2026-09-10 user authorization in design D5 applies: affected tests may use per-test disposable post-expand catalogs; unmigrated siblings keep pre-expand setup. Production deployment/transition SQL, Timescale EXPLAIN/live receipts and the #1986 gate remain deferred. Shared OpenSpec stays active.

Issue type: feature/refactor. Project profile: NHMS/SHUD. Blast radius: medium. Fixture level: expanded. Upstream suggested level: compact; overridden because this is a shared geospatial reader with two raw probe types, three coupled LATERAL occurrences and real HTTP/PostGIS fixture boundaries. No production writer, lifecycle, permission or evidence-wire protocol changes are included.

First review seats: correctness; invariant-state; test-evidence+spec-compliance. One serial implementer owns the source/test mutation boundary. Parent owns the fixture, immutable capture, verification and Git/PR operations.

| Risk pack | Selection / reason |
|---|---|
| Public API / CLI | Selected: national and source-less route binds, single result and unchanged consumer outcomes |
| Config / setup | Not selected: no runtime configuration change |
| File IO / path / overwrite | Not selected: no cache/file-publication code change; only repository-owned frozen test data |
| Schema / columns / units | Selected: candidate store projection, raw probe projections and public payload preservation |
| Auth / secrets | Not selected: no role or permission boundary change |
| Concurrency / shared state | Not selected: no production transaction/concurrency mechanism change; test setup ordering is specified below |
| Resource limits / large input | Selected: preserve correlated probes, all limit/rank/geometry/budget boundaries |
| Legacy / examples | Selected: physical wide/narrow variants, sanctioned aids, NULL-key exclusion |
| Error / partial output | Selected: exact-instant presence, 404/424 and empty/invalid/budget behavior remain owned by existing consumers |
| Release / dependencies | Not selected: reuse the already installed dev-only mapbox-vector-tile 2.2.0; no dependency change |
| Docs / migration notes | Selected: explicit pre-expand execution boundary and raw-registry cutover |
| Geospatial, time-series, PostGIS, published display identity | Selected: both zoom regimes, source/cycle/window selection, decoded geometry and identity |
| SHUD, Slurm, providers, manifest/QC | Not selected: no such runtime or contract change |

## Immutable baseline and unchanged surfaces

`.workplans/issue-2206/baseline/` was captured before editing from the committed base: all five complete tile SQL strings and bind-name sets, all twelve raw entries in both variants, original mvt.py, golden bytes and manifest. The full national SQL SHA-256 is `d18c89af633838df8be0d84e3bff02df3e01d35ef21935f9bb59de0dc1f8e8b4`. The historical 20-entry/215-chain golden SHA remains `d104d1c69cea55cdb86bd8a44d90d8f93c621580b43f74744f6b5cca7cd5a453`.

Keep all four other layer SQL strings/bindings byte-identical, including the already routed per-basin hydro. Keep the other eleven raw inputs byte-identical in both stores. No valid-time, digest, cache-version, API, parser, schema/migration or shared renderer change. `postgis_tile_sql(layer)` keeps its signature. The national bind vocabulary remains exactly collection_coordinate_limit, cycle, feature_coordinate_limit, feature_limit, max_coordinate_dimensions, simplification_tolerance_m, source, valid_time, variable, x, y, z; no store bind or suffix.

## Two authored raw probes, three runtime locations

Add two module-local required-store factories, following `_hydro_source_template`: `_hydro_national_identity_source_template(store)` and `_hydro_national_data_source_template(store)`. Each validates the closed legacy/narrow set before interpolating the literal and contains exactly one authored canonical fact-table mention. Neither raw body contains LIMIT, an outer aggregate, or a store UNION.

- Identity raw body retains `SELECT 1`, run/network key equalities, enum-range lookup, exact `valid_time`, and its three separately marked legacy aids: run_id, river_network_version_id, variable. Add only `lr.timeseries_store = '<store>'` as a conjunct. It has no segment correlation.
- Data raw body retains the six columns in order: basin_version_key, value, unit_e::text AS unit, quality_flag_e::text AS quality_flag, variable_e::text AS variable, valid_time. Preserve run/network/segment keys, enum-range lookup, exact time and four marked legacy aids: run_id, river_network_version_id, river_segment_id, variable. Add the same candidate-store conjunct. One data factory is reused by both zoom legs; do not duplicate it.

Inside the national branch, render each factory for both stores and compose its two fact-row results with the existing caller-local `legacy + UNION ALL + narrow` pattern. Reuse the combined data body in both existing LATERAL locations. Put the existing LIMIT 1 once after each combined body, not inside each store branch. No extra derived-table alias or generalized SQL binder/walker is needed.

The two existing DISTINCT ON candidate queries each add `h.timeseries_store` to their projection. Do not copy discovery per store or filter store before DISTINCT ON: that could pick an older run from the other store. All status, active-model, coverage-window, source/cycle predicates and ordering remain unchanged. The identity discovery stays inline in its sibling CTE; it cannot read the latest_runs CTE nested inside source_rows.

Keep the identity query's separate outer EXISTS LIMIT 1 and its CASE result. `source_identity_count` is a boolean-like 0/1 presence value, not a network count. Preserve the two existing non-store zoom unions, CTE landmarks, PERCENT_RANK ownership, downstream authority/geometry joins, national eligibility and the common one-row MVT/statistics/geometry/budget pipeline. Apart from replacing the three probe bodies and adding two candidate columns, compare the emitted national SQL against the frozen baseline without unrelated edits.

## Raw registry and exact census cutover

The former full-statement key cannot remain a renderer input once it contains both physical stores. Replace its live entry with two real raw-input entries, `mvt:hydro_national_identity_source` and `mvt:hydro_national_data_source`, both kind statement/named parameters. Their mentions/aids are 1/3 and 1/4. Do not synthesize a fake wrapper SQL merely to retain the old entry count, retain a live alias, or register the composed statement.

This is an explicit internal-oracle deviation from the upstream one-registry-statement description: there is still one national entrypoint behavior, but two different raw projections. The old `mvt:postgis_tile_sql_hydro_national` remains only in the immutable historical golden.

Pin exactly thirteen live raw entries, five routed raw keys (the existing three plus the two national sources), eight historical-comparable live keys and twenty historical golden keys. Reconstruct the historical set with those eight, both historical MVT keys, the existing eight forecast blocks and the existing two historical forecast fragments. Keep the duplicate-key guard and exact-set comparisons; do not relax to subsets or recapture the golden.

Sharing the data body removes four duplicate authored aids: total raw aids 34→30, mvt authored markers 18→14, display marker total 24→20. Display registry closure remains exact against the actual non-docstring SQL-literal counter (mvt authored fact mentions 6→5); do not add mvt to the separate cleanup oracle's RIVER_TABLE_CENSUS. Update explanatory counts alongside assertions. Render/predicate preservation still covers every current raw input, and each new raw input must equal the actual factory used by production.

## Shape and observable evidence

Within each already located LATERAL, split/check the two store branches separately. Same `ts` aliases must not let legacy columns mask a narrow branch in an alias-set census. Keep the outer `v` and `hit` landmarks; do not retarget the first `) v` slicer with a nested same-named alias. Assert one LIMIT in each combined probe and preserve the additional outer identity EXISTS limit. Both data probes remain identical; key/enum predicates and projection order are checked per branch. Keep the status-discovery count, HHE source-version/bind-set and source/cycle site guards.

Freeze the captured full national SQL verbatim as `tests/fixtures/hydro_national_mvt_pre_store_c21bacf9.sql`; pin its digest/provenance in its real-DB owner. Do not regenerate it from new code. Add a pure full-SQL preservation owner analogous to the per-basin owner, and an actual PostGIS owner that collects all mappings, requires exactly one row, compares every statistic and the decoded ordered tile against the pre-transition frozen query, then invokes the unchanged real consumer. No canned bytes may stand in for that proof.

For the full-tile owner, reuse `_seed_second_network` / `_seed_second_network_run` in `test_mvt_national_identity_probe_integration.py`, not another seeding framework. Give both networks the same requested source/cycle/window and opposite stores, testing both assignments. Their separate basin versions preserve the active-model uniqueness and avoid cross-inheriting runs. Exercise z=5 and z=9. Make at least two segments per network untyped so the low-zoom untyped ranking has a non-vacuous survivor; assert each network contributes at both zooms and compare decoded fields/geometry/order with its own frozen baseline. Restore opposite-store decoy times only for this exact-time positive oracle, retaining +10000 values.

Add an identity-miss discriminant using a covered half-hour instant absent from both authoritative stores but present in the opposite-store +30-minute decoys. Leave decoy times shifted for this case. Correct SQL has source_identity_count=0 and the existing probe-specific 424; omitting an identity store condition must not turn it into empty-tile 200. Positive existence with different values alone cannot detect that error because SELECT 1/LIMIT 1 discards the value.

Required negative proof after implementation: drop the narrow data branch and observe the actual full-tile owner fail in both zoom regimes; omit identity store routing and observe the wrong-store-only instant fail the actual presence/424 owner. Restore and pass the controls. Unit per-branch store/literal/table assertions also own independent branch-condition omissions. Do not claim compressed Timescale plan equivalence from ordinary temporary tables.

## Existing integration boundaries, including the evolving test

Reuse `post_expand_forecast_database` unchanged: explicit Mapping after D3 and before copy/poison, with rollback/closed baseline transactions before its separate connection performs DDL. Do not globally transition either shared seed fixture. The two national source-row tests in the surrogate integration file opt in after their original pre-transition observations and explicitly route `_LEGACY_RUN_ID`, the latest covered run; preserve their NULL-key, rank and geometry assertions for both stores. Per-basin and valid-time/coverage siblings stay as they are.

In `test_mvt_national_identity_probe_integration.py`, only these eight existing tests execute national tile SQL and need opt-in; keep metadata-only cycle/valid-time/digest/geometry cases pre-expand:

1. test_national_tile_is_424_when_no_display_ready_run_covers_the_instant
2. test_national_tile_is_424_on_an_interior_coverage_window_gap
3. test_national_tile_is_200_with_a_non_empty_mvt_when_the_instant_has_data
4. test_national_identity_tile_is_424_for_the_source_without_a_run_at_that_cycle
5. test_national_identity_tile_matches_an_uppercase_source_id_from_a_lowercase_path
6. test_national_identity_tile_serves_the_requested_cycle_not_the_newest_one
7. test_national_identity_tile_serves_the_requested_source_not_the_other_one_at_that_cycle
8. test_national_digest_binds_the_instant_so_a_rival_outside_the_window_moves_nothing

For the first seven, finish all real seeding, coverage refresh and fact-dependent non-vacuity queries before opt-in. Keep every existing assertion, including the 424 details discriminator and source/cycle controls. Parametrize the selected run over both stores; when rivals are requested too, give them the opposite store so both actual routes are exercised. Do not add a SQL-rewriting connection wrapper to make the unmigrated coverage writer run after transition.

The eighth case deliberately seeds/refreshed a rival AFTER its baseline tile and checks cache regeneration. Preserve that sequence and every assertion. Its first tile is an explicit historical reference: scope the frozen pre-transition national SQL to that one baseline request only, using the real API/DB path, not canned rows. Restore the normal SQL function immediately. Perform the original rival seed, production coverage refresh, fact non-vacuity/digest assertions and cache clear on the pre-expand catalog; only then opt in, before the final tile request, which must execute current routed SQL and remain a cache miss with identical bytes. This is not a production fallback: the current no-rival route is independently exercised by test 3 in both stores, and the new full-tile oracle also compares current SQL to the historical baseline. No other tile request may use the frozen SQL. Record this explicit baseline-oracle adaptation as a deviation; do not silently weaken which phase is current code.

## Verification and exclusions

Parent runs the issue's six-file command (shape helpers, surrogate shape, golden, MVT scaling, HHE binding, selector), plus affected renderer/reference-lexer/cleanup/status-only owners. The MVT source-file hash also belongs to benchmark/live-evidence consumers even though they execute per-basin SQL: run their full suites on a committed source checkpoint, preserving their reviewed-source guard. Run Ruff and strict OpenSpec.

On node-27, run both complete affected integration files with `integration and not timescaledb_210`, including all unchanged siblings, plus the actual route/data mutants and restores. Use a private checkout and its own environment; no active deployment pull/switch or environment mutation. CI remains an additional exact-head gate.

The new frozen SQL file must get its exact selector rule, exact database-filter literal and both permanent exact-selection/unrescued-deletion tests, following the merged #2225 pattern. Preserve the existing per-basin fixture's edges and all other selection rules.

No new dependency, cache-version bump, digest or API change, production schema operation, generic SQL wrapper/parser, compatibility alias, table-existence fallback or early #2207/#2208/#1984 implementation. The prior PR2226 retrospective governance line is carried on this normal issue push; user .gitignore remains excluded.

## Non-blocking clarifications consumed after fixture PASS

- The surrogate integration pair's pre-transition national source-row reference is the source_rows body extracted from the mandated frozen full SQL, not a second hand-authored national CTE.
- `core.river_segment.stream_type` is generated from `properties_json` (migration 000048). Make the new mixed-store oracle untyped by changing its test-owned Type properties before freezing the baseline; never UPDATE the generated column. Existing seed defaults and metadata-only sibling fixtures remain unchanged.
- The evolving #2031 case scopes its one historical baseline through `apps.api.routes.hydro_display.postgis_tile_sql`, the imported name the route executes, not through an unused services-module patch. Restore it immediately; final state explicitly assigns the base run legacy and the rival narrow before the current final request.
