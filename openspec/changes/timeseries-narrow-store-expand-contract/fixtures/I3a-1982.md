# Fixture I3a (#1982): per-basin hydro MVT routed fact source

## Authority, triage and boundary

Base: `f33441a2dafd910375aab087f61257327055611c`. Task 2.2a owns only the per-basin `postgis_tile_sql("hydro")` source. On 2026-09-10 the user extended isolated post-expand test-database authorization to all remaining River pre-expand readers: affected tests may opt in, original assertions and unmigrated sibling test isolation stay intact. Production migration/deployment/transition SQL, Timescale plans/live receipts and the #1986 start gate remain unchanged. This supersedes the issue's blanket deferral only for disposable test catalogs.

Fixture level: high (initial compact suggestion was raised to expanded for isolated database oracles, then to high for the user-authorized benchmark producer/verifier binding cutover below). One serial implementer owns each shared mutation boundary; parent owns this specification, baseline and final validation. First comprehensive review has all four high-tier seats.

Selected core packs: Public API/CLI (MVT public properties and one-row consumer), Schema/columns/fields (branch projections and named binds), Legacy compatibility (three aids and NULL-key exclusion), Error handling (empty/OOV/unknown identity and unchanged 424/413/500 consumer outcomes), shared helper/evidence integrity (raw registry versus composed SQL, frozen oracle and fixture consumers). Selected domain: geospatial/CRS (existing 4490 source / 3857 clipping), hydrological valid-time/identity, PostGIS semantics and published display identity. No production auth/config/path/publication/concurrency/Slurm/parser mechanism changes. No production performance claim from ordinary test tables.

## Source contract

Extract the existing hydro source SQL into one module-local authored raw-template factory with required `store`, following the existing forecast factory pattern. Preserve the source's exact twelve outputs, order and expressions: feature_id, segment_id, river_segment_id, river_network_version_id, basin_version_id, value, unit, quality_flag, run_id, variable, valid_time, geom. The network/basin/run strings still echo bound identities, segment identity/geometry still come from the authority join, and enum labels/time formatting stay unchanged.

Each raw variant adds only its literal `timeseries_store = 'legacy'` or `'narrow'` to the existing scalar authority lookup `SELECT run_key FROM hydro.hydro_run WHERE run_id = :run_id`. Do not move route ownership to an API argument or add a catalog/prefetch query. Render each raw variant with matching `render_river_ts_sql(raw, store)`, then join those fact-row sources with caller-owned `UNION ALL` inside the existing `source_rows AS NOT MATERIALIZED` relation. Branches use the same named parameters, no suffixes and no store bind. Legacy retains all three marked run/network/variable aids; narrow contains no fact-table text identity.

Do not copy the complete MVT statement, source_identity_stats, aggregate/statistics, budget/geometry CTEs, output ordering or final row. Preserve all outer SQL byte-for-byte after replacing only the source body. Keep `postgis_tile_sql(layer)` signature and all other layers byte-identical. No shared union helper, binder/walker, renderer change or compatibility stub.

The unchanged consumer is `apps/api/routes/hydro_display.py:_fetch_postgis_tile_bytes`: one execute and `.mappings().first()`, invalid properties -> 500 MVT_TILE_CONTRACT_INVALID, over budget -> 413 MVT_TILE_BUDGET_EXCEEDED, no source identity -> 424 MVT_LIVE_POSTGIS_UNAVAILABLE, otherwise existing bytes/empty bytes. Do not edit that API module or its identity preflight. Its full route becomes deployable only with the other pre-expand slices at I7.

## Raw versus executed oracle

Keep the single existing registry key `mvt:postgis_tile_sql_hydro`, with one fact mention and three authored aids, but make its required `source(store)` return the matching unrendered source factory. The composed statement is not a renderer input. National and valid-time entries remain byte-identical. Keep the historical 20-key golden bytes/digest unchanged; the same historical key denotes the former whole statement only in that immutable capture.

Use an explicit finite set of routed-source keys (the existing two forecast keys plus this MVT key) for the intentional historical-chain comparison cutover, rather than excluding arbitrary modules. Current-template predicate preservation still applies to every raw entry. Provide a full before/after mapping for the one MVT statement: remove only the two added route predicates/physical table changes and the source composition, showing each source's original projection/predicates and every original outer clause preserved. Exactly twelve raw entries remain; eleven unaffected raw entries and all four non-hydro layer statements retain their prior bytes. Separate tests exercise the actual composed `postgis_tile_sql("hydro")` and its `.first()` consumer.

Baseline is immutable `.workplans/issue-1982/baseline/`: five full layer SQL files, hydro-source.sql, twelve raw entries for both stores, typed binding, old mvt.py and real-DB oracle source, golden.original.json and SHA-256 manifest. A pre-edit static probe already rendered the proposed store predicate through both existing renderer variants successfully; no new parser grammar is needed. LSP has Ruff only, without references support; parent attempted references, inspected capabilities, then used explicit caller searches (including the API consumer and benchmark/live consumers).

## Disposable database seam and sibling isolation

Three existing per-basin tests in `test_river_ts_read_path_surrogate_keys_integration.py` execute this source: field/geometry parity, unknown identity/OOV, and NULL-key exclusion. Their `seeded` fixture also serves national/valid-time/coverage tests and must NOT globally transition. Finish frozen text-era and unmigrated sibling reads first, end the SQLAlchemy read transaction, then opt in on that test's own throwaway database. A held baseline transaction would block the separate-connection rename; do not keep it open across fixture preparation.

Reuse the existing `post_expand_forecast_database` physical-table fixture. Its current narrow-run-id-only input is insufficient for this seed, whose fact-bearing runs are `succeeded` with no parsed_at and therefore get D3's default narrow store. Cleanly replace that test-fixture input with an explicit `Mapping[run_id, store]`, applying overrides before facts are copied/poisoned, while preserving the D3 default/backfill itself. Migrate every existing forecast test caller and callable annotation to the mapping, with no compatibility alias or old sequence input. Existing forecast payload/parity assertions stay unchanged. The helper remains disposable test setup, not a production route setter or I7 migration.

The physical canonical narrow table must lack text aids; legacy remains physical wide. Preserve authoritative facts and opposite-store decoys. NULL-key historical rows stay on legacy and invisible to key-based routed reads; do not backfill/drop them to make parity pass. The original text-era SQL literals remain immutable. Per-basin store controls explicitly assign the relevant seeded runs so both legacy and narrow routes are really exercised. Unmigrated national, valid-time and coverage tests retain pre-expand catalogs; no global fixture/skip/gate change.

## Invariant and observable evidence matrix

Governing invariant: the requested run contributes facts only from its authoritative physical store, while one unchanged outer MVT query produces exactly one aggregate/statistics row and the same public tile behavior.

| Surface/input | Required observation |
|---|---|
| Legacy and narrow source variants | matching table and authority store literal, absent opposite table/route, same projection/bind vocabulary; legacy three aids, narrow none |
| Actual hydro statement | one local store UNION beneath source_rows; exactly one final ST_AsMVT aggregation and one outer statistics/budget/order pipeline |
| Equivalent keyed facts in each store | exact field and geometry equality with pre-transition frozen text oracle; decoded MVT features/properties/geometry and outer statistics equal |
| Opposite-store facts | distinct decoy values/times do not enter requested-run output; missing store predicate/forced branch must be distinguishable |
| NULL-key legacy fact | frozen oracle sees the known extra segment; routed source excludes exactly that segment, without deleting it |
| Unknown run / unknown enum label | empty rows, no cast error; retain independent rejected-cast contrast |
| Empty/off-tile or invalid/budget row | one aggregate row; unchanged consumer empty bytes / 424 / 500 / 413 behavior as applicable |
| Other four layers and eleven raw entries | unchanged bytes/bindings; national/valid-time/forecast behavior stays out of the runtime diff |

Keep focused mocked per-basin consumer cases, but do not call encoded canned rows proof of PostgreSQL aggregation. Add/run a disposable PostGIS tile scenario using the actual full SQL, require one result row, decode its tile and compare meaningful features/geometry/properties/statistics with the pre-transition baseline. Capture the old full SQL as frozen test data only where needed, with provenance; never regenerate it from new production code. A branch or route-omission mutation must fail an actual consumer/source oracle, not import/collection/syntax.

## Scope and verification

Allowed: mvt.py hydro source only; its registry/golden/shape/surrogate-key owners; per-basin cases in test_hydro_display_mvt_scaling.py and the real-DB surrogate-key integration module; the shared disposable fixture input and existing forecast test callers needed for that clean cutover; necessary CI selector ownership. Existing benchmark/live consumers must remain binding-compatible and their current tests run, but their runtime adaptation is #1984. No national/valid-time/API metadata/production schema/parser or shared renderer API edits.

Parent validation: issue's five-file command plus affected renderer/reference-lexer/cleanup and existing MVT/benchmark/live consumer suites as discovered, `uv run ruff check .`, strict OpenSpec. Real database assertion matrix runs on an isolated node-27 checkout/test catalog and CI, not the active deployment. Run existing forecast integration owners affected by the fixture mapping along with all surrogate-key integration tests to prove unmigrated siblings still work. I7/I8 retain production mixed-store/EXPLAIN/live receipts; report that explicit limitation, not a blanket claim that no test database executed.

## Fixture revision 1: exact cutover and bounded execution owners

### Registry replacement, not an additional entry

Replace the source callable of the existing `mvt:postgis_tile_sql_hydro` entry; do not append another entry or change its key. Define the finite routed-source set as exactly `{forecast_store:segment_rows_source, forecast_store:latest_product_river_source, mvt:postgis_tile_sql_hydro}`. In `test_the_golden_covers_exactly_the_registered_entries`, separate the nine still-historical-comparable live keys from these three routed sources; reconstruct the historical set from those nine, the historical MVT key, eight historical forecast blocks, segment_identity_predicates and latest_product_fallback. Thus current keys remain exactly twelve and historical keys exactly twenty, with one intentional overlapping MVT identifier whose payload contract changed. The two exact sets and duplicate-key guard remain strict.

`test_legacy_renderer_preserves_every_current_template_predicate` still checks all twelve raw entries against their own predicates; only those nine historical-comparable keys also compare with frozen whole-statement chains. Extend `test_routed_registry_render_matches_actual_store_source` or add one equivalent MVT-specific owner for both stores: matching literal/table after rendering, opposite literal absent, and equality with the production raw factory. A registry source that mistakenly returns composed postgis_tile_sql("hydro") must fail its one-mention/three-authored-aid input contract and narrow rendering. The actual full hydro statement is exercised by the per-basin consumer/SQL tests, never registered as raw input.

### Forecast fixture caller translation

The Mapping is applied AFTER unchanged D3 column/default/backfill SQL and BEFORE copy/poison. `{run_id: "narrow"}` therefore overrides a backfilled legacy value; it is not a no-op. Do not use a shared mutable default. The callable and any `_parity_pair` forwarding parameter become mapping-based with no sequence shim.

| Current forecast caller/case | New override mapping |
|---|---|
| residual-debt fast/fallback comparison: `()` | `{}` |
| `_parity_pair` default/empty-candidate/negative-control: `()` | `{}` |
| covered-candidate legacy case: `()` | `{}` |
| covered-candidate narrow case: `(FORECAST_RUN_ID,)` | `{FORECAST_RUN_ID: "narrow"}` |
| NULL-forcing candidate: `(null_forcing_run_id,)` | `{null_forcing_run_id: "narrow"}` |
| public latest/pinned test, newest_store narrow: `(newest_run_id,)` | `{newest_run_id: "narrow"}` |
| public latest/pinned test, newest_store legacy: `(FORECAST_RUN_ID,)` | `{FORECAST_RUN_ID: "narrow"}`; newest remains D3 legacy |
| real schema API smoke: `()` | `{}` |
| history-window legacy case: `()` | `{}` |
| history-window narrow-forecast: `(FORECAST_RUN_ID,)` | `{FORECAST_RUN_ID: "narrow"}` |
| history-window narrow-history: `(HINDCAST_RUN_ID,)` | `{HINDCAST_RUN_ID: "narrow"}` |
| history-window both narrow | `{FORECAST_RUN_ID: "narrow", HINDCAST_RUN_ID: "narrow"}` |

For per-basin field/full-tile/OOV cases, explicitly map `_KEYED_RUN_ID` to the tested store, `_LEGACY_RUN_ID` to the opposite store and `_ALL_LEGACY_RUN_ID` to legacy. For NULL-key cases, map `_LEGACY_RUN_ID` to the tested store and `_KEYED_RUN_ID` to the opposite store. This is necessary because those seeds use succeeded/no parsed_at. Do not alter their seed status or D3 backfill to achieve a store assignment. Existing forecast assertions alone cannot prove that an equivalent logical snapshot changed physical representation; retain route-discriminating source/SQL controls rather than claiming an omitted override necessarily changes every payload.

### Exact-time decoys

Keep the existing shared fixture's time AND value poison unchanged for forecast/A9 coverage owners. Only in the opt-in per-basin test preparation, restore the non-authoritative decoy timestamps by subtracting their 30-minute offset, retaining the +10000 values and limiting updates by authoritative run_key/store. Leave NULL-key rows unchanged. This makes decoys pass the MVT exact valid_time predicate: dropping a store condition now duplicates features or exposes poisoned values instead of being accidentally masked by the time filter. The unchanged shared copy/poison SQL census stays exact.

### Full PostGIS tile owner and decoder

Add `test_hydro_store_union_preserves_one_mvt_result_and_decoded_payload` in `tests/test_river_ts_read_path_surrogate_keys_integration.py`, parametrized for the two requested stores. Freeze `.workplans/issue-1982/baseline/hydro.sql` verbatim as `tests/fixtures/hydro_mvt_pre_store_f33441a2.sql`; its SHA-256 is `bf284b1f7d6532d5f45a26b490b8f23093b303c962a1adf524292557b169fd68`. Record the capture base and digest in the test, not by editing the frozen bytes. The existing text-era source/valid-time literals remain unchanged.

Use the standard [mapbox-vector-tile 2.2.0 decoder](https://pypi.org/project/mapbox-vector-tile/2.2.0/), `mapbox_vector_tile.decode(bytes(tile))`, with the same coordinate options on baseline/new tiles. Add exactly `mapbox-vector-tile==2.2.0` to the existing project.optional-dependencies.dev and update uv.lock. It is a test-only dependency, not a runtime decoder, handwritten protobuf parser or production API change. Release/dependencies is selected for this test setup. CI already installs `.[dev]`; local setup follows uv sync --all-extras --dev. Node-27 verification installs into the new isolated checkout's own environment, never the active deployment's .venv.

Use `_identity_params(_KEYED_RUN_ID, _T0)` and `_postgis_tile_params(..., z=9, x=398, y=197, layer="hydro")`, verified against `_tile_xy(100.0, 38.0, 9)`. Before transition, execute the frozen full SQL and retain its one mapping plus decoded tile. End the read transaction, prepare explicit stores/exact-time decoys, execute the actual current full SQL, collect all mappings and assert exactly one. `.first()` alone cannot prove cardinality. Compare all outer statistic fields and the full decoded layer/ordered features/geometry/properties against baseline; additionally assert segment order `seg-a, seg-b, seg-c`, values `30,20,10`, requested run/network/variable/unit/time and no route metadata. Source has twelve columns including geom; public properties are eleven, with geometry decoded separately. Call the unchanged `_fetch_postgis_tile_bytes` against the same real session and require the same bytes/payload, covering its actual `.first()` use rather than a canned result.

Keep existing mocked outcome owners and add a bounded per-basin consumer outcome owner in the same integration module where needed: unknown/OOV source produces one statistics row with source_identity_count=0 and consumer 424; an off-tile bounds request keeps positive identity and returns empty bytes; one seeded non-finite value yields consumer 500; three valid features with a test-local API feature-limit of two yield 413, using the same limit for binding and comparison. Do not modify API source or create a general test framework.

Required mutants at the public source/full-tile seam: drop the narrow branch, and omit a store predicate with exact-time decoys present. Both must fail assertions without collection/import failures; restore code and pass the owners. A duplicated top-level aggregate is caught by the one-row assertion, while a future `.first()`-to-`.all()` consumer change is covered by the real consumer invocation; this slice does not require changing API production code just to run that mutation. Benchmark/live mock consumers continue comparing their captured query to current postgis_tile_sql("hydro"), not to the frozen baseline.

The new SQL fixture, pyproject.toml and uv.lock are explicitly in scope for this bounded decoder/oracle. No other package/runtime dependency or production environment change is authorized.

## 2026-09-10 user-authorized minimal named-binding consumer repair

Parent's full matrix ran 2444 passes and 332 failures, all in the benchmark/live-evidence suites (seven and 325 respectively). The failure is the inherited curve binding seam, before MVT capture: `_RecordingCursor` converts the new Mapping to tuple keys, `_curve_query_and_binding` demands eleven `%s` values, and the live verifier checks that same retired positional protocol. The user explicitly chose to include the smallest capture/serialization/strict-verifier repair in #1982 rather than insert a separate prerequisite PR. This supersedes earlier benchmark-runtime exclusions only for binding consumption; #1984's store-dependent lifecycle/plan work remains in its original place.

Allowed additional source: `scripts/node27_timeseries_compression_benchmark.py` and the curve-binding arm of `scripts/node27_timeseries_compression_live_evidence.py`, with their two existing unit suites and necessary selector ownership. No new storage branches, plan gates, capture permissions, file publication/recovery, source-ref rules, runtime fallback, schema version or evidence envelope redesign.

### Producer and execution contract

`_RecordingCursor` preserves actual named parameter values as a Mapping instead of iterating its keys into a positional tuple. `_curve_query_and_binding` still returns `(query_text, parameter_names, parameter_values)`: the names are canonical sorted unique names, and the tuple values are paired with those names for the existing receipt representation, NOT positional driver arguments. Require the unique named `%(name)s` placeholders to match the captured Mapping keys exactly, with no missing/extra key and no positional `%s` protocol. Repeated named placeholders consume one recorded value.

The observed current names are exactly `basin_version_id, end_time, issue_time, river_network_version_id, river_segment_id, scenario_ids, scenario_tokens` in sorted order. For the parent's gfs probe, scenario_ids is `["forecast_gfs_deterministic","gfs"]`, scenario_tokens is `["gfs"]`, and the seven-day end remains issue_time + seven days. Do not invent start_time or source_or_scenario_tokens aliases.

The same current seven-name set is the producer's admissibility floor before opening a database connection, not merely the verifier's later check. A sparse but internally SQL/Mapping-matched shape (for example, empty scenario normalization removes both scenario binds) must be refused by the general binding-set guard; do not special-case that input string. The old positional shape guard also refused a missing-scenario-bind shape. Parent's probe exposed this producer/verifier predicate mismatch during Phase 2; the general guard is its required closure.

Keep the receipt wire shape `{parameter_names: [...], bound_parameters: [...]}` and existing JSON value conversion. At the actual psycopg execution seam, reconstruct/pass the named Mapping from the paired names/values; never execute pyformat-named SQL with the values tuple. Preserve readonly connections, deadlines, warmups, measurements, source hashes and all other capture behavior. The MVT binding path remains its existing named mapping.

### Independent strict verifier and regression owners

The verifier requires unique nonempty string names, equal name/value vector lengths, and exact coverage of unique named SQL placeholders. It retains exact current-production query, canonical name order and canonical value comparisons against the public owner's capture. Missing/extra/duplicate/misnamed names or altered values cannot qualify. Replace the obsolete required start_time with the actual issue_time lower bound when checking positive overlap with the selected chunk; keep the existing strict interval edges and seven-day request validation. Preserve current source-ref, plan and evidence-envelope checks unchanged.

No structural schema change is required: the existing two array fields already represent name/value pairs. This is a clean current-owner cutover, not support for both old positional and new named execution. Existing illustrative schema fixtures are not promoted to production-verified receipts or silently regenerated.

Extend `test_capture_uses_exact_production_queries_bindings_and_new_readonly_connections` to observe named mappings at real capture-driver calls, repeated issue_time using one name/value, and unchanged actual identity/scenario/window values. Adapt the old positional-count tampering owner into semantic missing/extra/duplicate/misnamed/value-drift refusal cases, retaining a passing baseline and typed EvidenceError rejection rather than re-pinning incidental prose. Preserve exclusive-end overlap and all live bundle/capture guards. A tuple-keys recording or tuple-driver mutation must fail these owners.

Run both full affected suites after the repair. Add them to the existing forecast_store production selector and its exact-set owner as well as preserving the already-existing MVT selector edges; otherwise a future forecast-only change can recreate this inherited failure without running its consumers. No broad shared-test-group expansion or removed selection edge. This repair adds an evidence producer/consumer invariant to the high-tier review and does not claim #1984 or production activation complete.
