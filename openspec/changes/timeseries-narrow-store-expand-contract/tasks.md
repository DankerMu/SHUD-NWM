## 1. Renderer, template normalisation, oracles (I1 — zero behaviour change)

- [x] 1.1 Normalise every transitional aid in `packages/common/forecast_store.py` (incl. the `:1903` disjunction rewritten as `OR (` + marker + `rt.run_id = %(scan_run_id)s AND` + `rt.run_key = (...))`), `services/tiles/mvt.py` (three non-verbatim / 1:N markers at `:661` (3 aids), `:793` and `:840` (two-line comments, 4 aids each) split to one verbatim marker per aid — 11 markers), `apps/api/routes/hydro_display.py:773`, `packages/common/forecast_store.py:1897` and `services/tiles/mvt.py:511`, `:1496`, `:1524` (the aid that is the first conjunct on the `WHERE` line moves to its own `AND` line under its marker; the `WHERE` line takes the next key conjunct), `packages/common/display_coverage.py:383-418` (prose comment `:383-391` replaced by one verbatim marker per aid; the `:406` aid moved off the `WHERE` line; the `run_id` disjunction `:410-411` and the `river_network_version_id` disjunction `:417-418` split the same way as forecast_store `:1903`): one aid conjunct per line, exactly one verbatim marker line immediately above it. Verify: a committed golden of every registered template at the base SHA proves the legacy variant equal modulo whitespace, marker placement and conjunct order within one AND-chain; every existing reader test outside the nine `SQL_SHAPE_ORACLE_TESTS` files passes unchanged; a new per-file marker/aid census (23 markers / 34 aids today → 34/34) is pinned in the owning oracle while the statement census `RIVER_TABLE_CENSUS` stays unchanged for pre-existing files (see `fixtures/I1-1980.md`).
- [x] 1.2 Add `render_river_ts_sql(template, store)` in `packages/common/` (legacy: table rename only; narrow: remove marker line + immediately following aid line, fail closed when that line is not exactly one aid conjunct or is a keyword line; structural check instead of a parser — balanced parentheses, no `WHERE AND`/`FROM AND`/`ON AND`, no dangling `AND`/`OR (`, no connective directly before a clause/set/join keyword (one keyword family shared with the region-stop regex), no doubled connective, no `WHERE` directly before a keyword, no marker left; table-scoped no-text-identity and key-predicate-retention assertions; `RenderedSql.removed_placeholders` lists removed positional `%s` indices; the text-identity constant group moves from the shape-oracle test into the module). Verify: unit tests for both variants, the structural check, the refusal on a mis-shaped marker and on a keyword line, consecutive marker blocks, a zero-marker template, an ON-chain aid, positional removal, quoted-alias refusal, literal-blanked attribution (fixture `fixtures/I1-1980.md` decisions 2–4, 15). Round-3/round-4 history: the union combinator built at round 3 was split out at the round-4 second gate entry (decision 15, task 1.4).
- [x] 1.4 (re-entry) Replace the removed cross-store text combinator with the caller-owned contract in design D5 / fixture decision 26: a known-run caller renders one store; a spanning caller unions legacy/narrow fact-row subrelations inside its own CTE/LATERAL/source relation before aggregate, `DISTINCT`/ordering/limit, response selection or DML. Branches share named parameters, have identical projections, bind `hydro_run.timeseries_store`, and touch only their matching table; no `render_union_all` or no-caller binder/walker may reappear. Run the Stage 5 width gate: split per-basin MVT, national MVT and valid-time discovery, and split hydro-display from display-coverage, because they are distinct statement/entrypoint verification paths. Tasks 2.2a–2.3b consume this design. Verify: re-issue #1982 and #1983 as atomic first slices, create the three independent follow-on issues, update Epic #1979 and I7/#1986 dependencies, complete one lightweight Stage 5.5 review with no P0/P1 residual, append the #1996 sizing-retro, and prove production/test Python defines, imports or calls no `render_union_all` symbol (its only remaining text is explicitly superseded OpenSpec history).

  **Suggested fixture level:** none - Stage 5 contract/accountability only; no runtime code, SQL template, schema or deployment changes.

  **Minimal mergeable slice:** atomic: publish the per-entry design, five implementation-ready reader slices, DAG/dependency updates and Stage 5.5 evidence together; publishing only one side would leave the shared task internally contradictory.
- [x] 1.3 Tighten `tests/test_river_ts_text_identity_cleanup.py` adjacency to "one aid, one verbatim marker on the previous line"; extend `tests/test_river_ts_read_path_surrogate_keys.py` and `tests/test_sql_shape_helpers.py` to render both variants for every registered template (legacy == normalised template modulo table name; narrow free of text identity columns and markers with key predicates intact); add `scripts/select_ci_tests.py` rules so the new renderer module, the test-side template registry and every touched reader select the whole `SQL_SHAPE_ORACLE_TESTS` group. Verify: `uv run pytest` on the nine `SQL_SHAPE_ORACLE_TESTS` files + selector test.
- [x] 1.5 (#2050) Close #2018 round-5 lexical-subset and oracle findings without wiring a production caller: make the independent reference's `E'` recognition adjacency-aware; refuse numeric-glued `1E'` / `1e'` through legacy render, narrow render and `fact_table_text_identity_columns`; add the scalar-subquery-local `AS "a)b"` row to the 16-statement traversal corpus; correct nine-suite and registry occurrence records. Verify: on the #2049 base, the three L2 doors reproduce DID-NOT-RAISE or the wrong refusal; after the change, messages contain entry + exact numeric-token-start offset + complete offending text and omit the fact-table name; three-door cases cover simple integer/decimal plus exponent, signed-exponent and leading-dot numeric prefixes, while quoted identifiers containing the same byte sequences (including `""` escapes) preserve base outcomes; reference known-case pins and seeded accepted-sample counts cover adjacent and whitespace-separated uppercase/lowercase `E/e`, kill m6 and an uppercase-only reference mutant; production/reference numeric tokenizers are independent and exponent/quoted-mask mutants turn red; the commutation pin kills t1 and m26 while m25 explicitly remains green at the #2050 boundary and is now owned by task 1.12; run `uv run pytest -q tests/test_river_ts_render.py tests/test_river_ts_render_reference_lexer.py tests/test_sql_shape_helpers.py tests/test_river_ts_template_golden.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_select_ci_tests.py tests/test_migrations.py tests/test_display_coverage_refresh.py tests/test_qhh_latest_fallback_pushdown.py`; run strict OpenSpec validation; compare all 20 registry entries byte-for-byte across occurrences, spans, legacy render and narrow render; assert 0/20 new refusals and unchanged golden SHA-256. Do not implement or claim m25 closure as part of task 1.5; task 1.12 owns its later direct-pin closure.
- [x] 1.6 (#2053) Make unquoted fact aliases and text-identity column matching follow PostgreSQL case folding before the first production reader is wired: `fact_table_attribution` returns canonical lower-folded aliases; `fact_table_text_identity_columns` detects alias-qualified and unaliased column spellings case-insensitively; `text_fact_columns(sql, alias)` gives the same non-empty answer containing `variable` for `RT` and `rt` on the defect probe. Matching stays outer-query-only and code-only: preserve `strip_scalar_subqueries` / `outer_predicates`, then reuse `non_code_spans` / `_blank_non_code` rather than adding a lexer, so ordinary and E/e string bodies plus line/nested-block comments cannot create a predicate; `variable_e` / `unit_e` / `quality_flag_e` remain excluded by the trailing token boundary; another relation's same-named column stays unattributed. Keep three quoted shapes distinct at this task's boundary: complete body `SELECT "rt.variable"` is not a token pair; qualified quoted column `rt."variable"` / `"rt"."variable"` keeps the #2053 unattributed answer and is not claimed fixed by task 1.6 (its independently reproduced narrow bypass is #2086 / task 1.7); quoted fact alias `AS "r"` / bare `"r"` remains the existing unmodelled refusal. `_AID_PREDICATE` is not widened — an uppercase marked aid remains a fail-closed mis-shape. Verify: reproduce the latest-master red probe (`FROM … RT`, marked `RT.run_id`, unmarked `rt.variable`) as DID-NOT-RAISE; add public-helper and exact narrow-refusal pins for that probe plus `RT.VARIABLE` and bare `VARIABLE`; pin ordinary and E/e literals, line/nested-block comments, authority-subquery `run_id`, enum suffixes, other alias and all three quoted controls; run mutation proofs independently deleting qualified and unaliased folding; run `uv run pytest -q tests/test_river_ts_render.py tests/test_sql_shape_helpers.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_display_coverage_refresh.py tests/test_river_ts_template_golden.py tests/test_select_ci_tests.py`, full Ruff and strict OpenSpec; compare all 20 registry entries' attribution sets, legacy/narrow renders and golden SHA-256 to pre-change. No caller, schema, migration, UNION, m25 or node-27 work.
- [x] 1.7 (#2086) Close exact lower-case quoted fact references after #2053 and before the first production reader. A fact alias declared bare as `RT`/`rt` canonicalises to `rt`, so all four qualified combinations `rt."<column>"`, `RT."<column>"`, `"rt".<COLUMN>` and `"rt"."<column>"` make both public helpers attribute each canonical `TEXT_IDENTITY_COLUMNS` member; an unaliased fact read using `"<column>"` in comparison position is attributed only by guarded `fact_table_text_identity_columns`. Narrow render refuses all five shapes as `text identity column(s)`, not `unmodelled`; alias-scoped `text_fact_columns(sql, alias)` remains empty on the unaliased shape. Both public helpers and the narrow guard share scanner-owned outer code (`outer_predicates` before `_blank_non_code`); preserve exact whole-token, case-sensitive quoted-token rules. Qualified comparison negatives on both alias arms must pin `rt."VARIABLE"`, `RT."VARIABLE"`, `rt."Variable"`, `"rt"."VARIABLE"`, `"rt"."Variable"`, `rt."variable""suffix"` and `"rt"."variable""suffix"`; unaliased comparison negatives separately pin `"VARIABLE"`, `"Variable"` and `"variable""suffix"`; together with `"RT"`, `"rt.variable"`, quoted enum/key siblings and arbitrary quoted identifiers, they remain unattributed. Preserve other-relation isolation, authority-subquery stripping for both aliased and unaliased fact reads, ordinary/E/e literal and line/nested-comment blanking, quoted fact-alias-declaration refusal and byte-identical `_AID_PREDICATE`. Verify: on post-#2053 master, red probes for the four quoted qualified forms show both helpers empty and narrow DID NOT RAISE; the unaliased `"variable"` probe shows guarded helper empty, narrow DID NOT RAISE and `text_fact_columns` empty. Enumerate all `TEXT_IDENTITY_COLUMNS`: both helpers/narrow through bare or exact-quoted qualifier, guarded helper/narrow plus alias-helper-negative on the unaliased arm. Independently delete the bare-qualifier/quoted-column, exact-quoted-qualifier and unaliased-quoted-column arms; bypass `outer_predicates` only for the quoted view; and mutate the complete-span classifier together with the corresponding qualified/unaliased consumer to admit quoted case-fold or prefix/early-stop tokens. Each load-bearing mutation must fail its responsible assertions without collection/import/syntax errors. Do not claim behavior-preserving regex-only changes are killed behind the exact classifier. Run `uv run pytest -q tests/test_river_ts_render.py tests/test_sql_shape_helpers.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_display_coverage_refresh.py tests/test_river_ts_template_golden.py tests/test_select_ci_tests.py`, full Ruff and strict OpenSpec; compare all 20 post-#2053 registry entries' attribution sets and legacy/narrow renders, assert 0 new refusals and unchanged golden SHA-256. A quoted alias declaration remains out of scope; no caller, schema, migration, UNION, m25 or node-27 work. This task intentionally corrects the issue body's contradictory `"rt"."variable"` negative: exact quoted `"rt"` refers to a bare-declared lower-case alias in PostgreSQL, so leaving it negative preserves the same fail-open. The PostgreSQL-qualified-name separator grammar remains outside this direct-dot slice: whitespace/comments around `.` still require closure by #2092 before #1981 wiring and are not claimed safe here.
- [x] 1.8 (#2092) Close PostgreSQL qualified-name separator whitespace/comment bypass before the first production reader: define one named scanner-normalised qualified-reference separator (`whitespace* . whitespace*`) and make the existing bare/bare, bare/exact-quoted, exact-quoted/bare and exact-quoted/exact-quoted fact-alias column arms consume it. For every `TEXT_IDENTITY_COLUMNS` member, spaces/tabs/newlines and block/line-comment separators around either side of the dot make both public helpers report the member and make narrow render refuse with entry + `text identity column(s)` + canonical member, never `unmodelled`; direct-dot behavior stays byte-compatible. Preserve bare ASCII lower-folding, exact case/whole-token quoted admission, quoted fact-alias-declaration refusal, other-relation isolation, enum/key suffix boundaries, authority-subquery stripping, literal/comment data blanking and strict `_AID_PREDICATE`. Multi-part/table-name and unrelated-prefix forms must not be truncated into a fact-alias match; every qualified branch rejects an alias candidate whose preceding code token ends in a dot, with all four bare/quoted alias-column suffix combinations pinned. Verify: post-#2086 red proof shows both helpers empty and narrow DID NOT RAISE for the four spaced qualifier/column combinations plus left/right block/line comments; enumerate all text members across the four token combinations and a separator-side matrix; independently remove left-side and right-side optional spacing and require their owner nodes to fail without collection/import/syntax errors. Publicly pin comment separators and data controls, but do not claim deleting one behaviorally redundant comment-removal layer is killed; a comment-normalisation mutation counts only when it bypasses every active normalisation path. Run `uv run pytest -q tests/test_river_ts_render.py tests/test_sql_shape_helpers.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_display_coverage_refresh.py tests/test_river_ts_template_golden.py tests/test_select_ci_tests.py`, full Ruff and strict OpenSpec; compare all 20 post-#2086 registry entries across attribution, guarded/per-alias text sets and legacy/narrow renders, assert 0 new refusals and unchanged golden SHA-256. No caller, schema, migration, UNION, m25, DB/display/frontend or node-27 work.
- [x] 1.9 (#2112) Close parenthesized whole-row fact-alias field selection before the first production reader. In scanner-visible outer code, recognise exactly one balanced parenthesis pair whose body is one canonical bare alias or its exact lower-case quoted reference, followed by the shared #2092 separator and a bare-folded or exact lower-case quoted `TEXT_IDENTITY_COLUMNS` member. Every member across the four alias/column token combinations makes both public helpers report the canonical member and makes narrow render refuse with entry + `text identity column(s)` + member; legacy remains rename-only. The same classifier separately reports known-member parenthesized selections that contain the attributed fact alias but are outside this finite grammar; guarded `fact_table_text_identity_columns` and both renders refuse them as `unmodelled parenthesized fact-alias field selection`, while unguarded alias-scoped `text_fact_columns` remains a matcher rather than a statement validator. Cover internal/separator spaces, tabs, newlines and block/line comments through scanner normalisation. Preserve direct #2053/#2086 and #2092 behavior, quoted declaration refusal, other-relation isolation, non-exact quoted names, enum/key siblings, authority-table local columns, literal/comment data and strict `_AID_PREDICATE`. Verify: post-#2092 red proof shows both helpers empty and narrow DID NOT RAISE for the four exact combinations and whitespace/comment variants; enumerate every text member and the exact matrix. Pin nested/cast/function/row/multipart alias-rooted forms as guarded + legacy/narrow refusals, but keep another relation and non-exact/enum/data controls at their prior no-attribution result; another-relation controls must include aliases that contain the fact-alias spelling as a strict superstring, such as `(art).variable` and `(rt2).unit`, with guarded helper empty and both renders unchanged. Independently remove exact-positive classification, suppress the unsupported report, and replace whole-token alias containment with substring containment; each mutation must fail its owner public-seam nodes without collection/import/syntax errors while direct-dot/#2092 controls stay green, and the substring mutant must be killed by the alias-superstring rows. Run `uv run pytest -q tests/test_river_ts_render.py tests/test_sql_shape_helpers.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_display_coverage_refresh.py tests/test_river_ts_template_golden.py tests/test_select_ci_tests.py`, full Ruff and strict OpenSpec; compare all 20 post-#2092 registry entries across attribution, guarded/per-alias text sets and both renders, assert 0 new refusals and unchanged golden SHA-256. Comparison-position scalar-subquery correlation is #2114, is not claimed safe by this task, and separately blocks #1981. No caller, schema, migration, UNION, m25, DB/display/frontend or node-27 work.
- [x] 1.10 (#2114) Close already-attributed outer fact-alias text references inside comparison-position scalar subqueries before the first production reader. Keep `outer_predicates` and unguarded alias-scoped `text_fact_columns` outer-query-only; add one scanner-owned scalar-body visibility seam consumed only by guarded `_assert_modelled_reference_forms`. Enumerate every comparison-position scalar body, including nested and later bodies, and feed each body through the existing direct bare/exact-quoted/#2092 separator and #2112 exact/unsupported parenthesized alias-member grammar for the surrounding fact aliases. Any known text-member candidate through such an alias — including a local inner relation that shadows the outer alias, whose binding cannot be safely resolved by this lexical subset — makes guarded `fact_table_text_identity_columns` plus legacy/narrow render fail closed with entry + correlated/scalar reason. Preserve error precedence: an inner fact-table reread still gets the existing comparison-position sub-select count-delta refusal before the correlated-alias refusal. Authority-local/other-relation members, enum/key siblings, non-exact quoted names, multipart prefixes and literal/comment bytes remain clean; unaliased outer-fact/unqualified scalar names are independently tracked by #2148 and do not block #1981. Verify: on the post-#2112 base, 28 direct member/token combinations plus exact #2092 separator forms, all four #2112 exact forms, representative #2112 unsupported forms `f(rt).variable` and `((rt)).variable`, the quoted nested-only and later-independent scalar owners and local-shadow rows reproduce guarded empty + both renders RETURN; after the change all three guarded doors refuse with the correlated-scalar reason except inner fact rereads, which retain their older reason. `text_fact_columns` and `outer_predicates` keep byte-identical answers. Isolated mutations that bypass scalar-body enumeration, stop after the first body, or skip nested-body enumeration must fail their direct, later-independent and nested-only owner nodes while authority/data controls stay green. Calling `outer_predicates` on each already-enumerated body is behaviorally equivalent when nested bodies are separately enumerated, so no test may claim that transform is load-bearing. Run `uv run pytest -q tests/test_river_ts_render.py tests/test_sql_shape_helpers.py tests/test_river_ts_text_identity_cleanup.py tests/test_river_ts_read_path_surrogate_keys.py tests/test_display_coverage_refresh.py tests/test_river_ts_template_golden.py tests/test_river_ts_render_reference_lexer.py tests/test_select_ci_tests.py`, full Ruff and strict OpenSpec; compare all 20 registry entries across attribution, guarded/per-alias sets and both renders, assert 0 new refusals and unchanged golden SHA-256. No caller, schema, migration, UNION, m25, functional notation (#2141), star expansion (#2115), DB/display/frontend or node-27 work.
- [x] 1.11 (#2141) Close PostgreSQL functional composite field notation before the first production reader. Extend the one shared alias-member analysis with a scanner-owned classifier for an unqualified known `TEXT_IDENTITY_COLUMNS` member called with exactly one canonical bare or exact lower-case quoted fact alias: all seven members × `member(rt)`, `"member"(rt)`, `MEMBER(rt)`, `member("rt")`, `"member"("rt")`, plus scanner-normalised whitespace/block/line-comment variants before `(`, around the alias and inside the argument, return the canonical member from both public helpers and make narrow fail with the existing entry-named `text identity column(s)` reason while legacy remains rename-only. A known-member call whose argument expression contains the fact alias but is not the exact one-token grammar — unclosed `member(rt` and outer-unclosed/inner-closed `member(f(rt)`, `member(rt.*)`, multiple/nested/cast/ROW arguments, or a selected field from the call result — is reported by the shared analysis and refused by guarded helper plus both stores as entry-named `unmodelled functional fact-alias field notation` before render-only structural refusal; its traversal must use `_skip_balanced_span`'s `closed` flag and never infer closure from the final byte. Unguarded `text_fact_columns` remains a matcher and reports only exact forms. A previous-significant-code-character dot disqualifies field notation: tight `hydro.member(rt)`, spaced `hydro . member(rt)`, comment-separated `pg_catalog /*c*/ . member(rt)` and quoted-schema `"hydro" . member(rt)` are functions and remain clean. Preserve other relation plus alias-superstring `member(art)` / `member(rt2)`, non-exact quoted member/alias, enum/key suffix, complete quoted identifier, placeholder/type/named-argument roles, literal/comment data, direct/#2092/#2112 behavior, #2114 precedence and authority/enum/data scalar controls, #2115 star boundary and #2148 scope boundary. Retire/invert only the existing #2141 functional scalar clean id: exact and unsupported functional forms in comparison scalar bodies now hit the correlated reason through the shared analysis, while the paired `rt.*` #2115 row stays clean. Red proof on latest master must record 35/35 exact helper-empty + narrow RETURN plus whitespace/comment and scalar-body exact bypasses. Mutations independently delete exact classification, suppress unsupported reporting, remove functional-unsupported guarded consumption while parenthesized consumption remains green, and replace whole-token alias containment with substring containment; exact, unsupported, unclosed and superstring public owners must turn red without collection/import/syntax errors, while direct/parenthesized and pre-existing non-functional scalar controls remain green. Run the same eight-file suite as 1.10, full Ruff and strict OpenSpec; compare all 20 registry entries across attribution, guarded/per-alias sets and both renders, assert `DIFFS=0`, `NEW_REFUSALS=0` and unchanged golden SHA-256. No caller, catalog lookup, schema/migration/UNION/m25/star, DB/display/frontend or node-27 work.
- [x] 1.12 (#2082) Give `assert_structurally_intact`'s existing double-quoted-identifier parenthesis skip a direct, mutation-visible owner without changing production code. Parametrize unmatched close/open bytes inside complete quoted identifiers (`SELECT 1 AS "a)b" FROM t WHERE a = :a`, `SELECT 1 AS "a(b" FROM t WHERE a = :a`) and assert direct helper acceptance. An AST-bounded m25 that changes only this helper's `if character in "'\""` arm to single-quote-only must make both direct cases fail at the target call with `closed one that was never opened` / `1 unclosed`, without import/syntax/collection failure; the existing commutation node must remain green under m25, proving it retains t1/m26 responsibility and does not falsely claim m25. Update decision 18 and the commutation docstring from historical deferred wording to the two-seam ownership; run `uv run pytest -q tests/test_river_ts_render.py`, full Ruff and strict OpenSpec. The production source blob, all renderer outputs/signatures, registry/golden and caller set remain unchanged; no DB/display/frontend/node evidence.
- [x] 1.13 (#2115) Close scanner-visible outer fact-alias whole-row star output exposure at the shared public-helper/render boundary. Both helpers map exact `rt.*`, `(rt).*`, `RT.*`, `(RT).*`, `"rt".*`, `("rt").*` SELECT output expansions to `set(TEXT_IDENTITY_COLUMNS)`; narrow refuses with entry-named text-identity error, legacy remains rename-only. Use scanner-owned output-list/balanced-group traversal; unsupported visible fact-alias-rooted stars fail closed in guarded helper and both renders rather than empty plus returned SQL. Preserve non-fact/quoted/data/key controls and existing named-field/scalar responsibilities. Evidence: fixture I1-1980 issue-2115 addendum; six-form baseline and seven named-member controls, independent direct-star/parenthesized-star/guard-consumption mutation kills, eight-file issue suite, Ruff, strict OpenSpec, 20-entry five-surface `DIFFS=0`, `NEW_REFUSALS=0`, unchanged golden SHA-256. No caller, DDL, database or live claim.
  Fixture-review owner additions: later-item and DISTINCT ON direct/parenthesized stars share exact six-form outcomes; first-item-only and substring-containment mutations kill those position and `art.*`/`(rt2).*` controls. Keep the star classifier separate from scalar-shared `_alias_member_analysis`; existing `variable(rt.*)` functional/scalar refusal precedes the new outer `foo(rt.*)` and `ORDER BY rt.*` refusal owners. SQL-shape helpers independently assert full-set exposure for both direct and parenthesized projection inputs.
  Local evidence at source SHA-256 `e4d81d039d41b00196fb5230df108ce491bbe10ca05876b893ab290537a716d3`: eight-file suite 1759 passed; Ruff passed; all six exact forms and seven named controls pass public-API replay; 20 entries × five surfaces give `DIFFS=0`, `NEW_REFUSALS=0`; golden remains `d104d1c69cea55cdb86bd8a44d90d8f93c621580b43f74744f6b5cca7cd5a453`. Isolated pre-change replay yields 53 behavior failures; direct/parenthesized/guard/substring/first-item mutations yield 2/2/27/2/4 owner failures, restored 72 focused cases pass.
- [x] 1.14 (#2148) Refuse ambiguous unqualified text-identity candidates inside every comparison-position scalar body when the surrounding fact read is unaliased and has no attributed alias. Preserve `outer_predicates` and alias-scoped `text_fact_columns`; guarded helper and both render stores raise entry-named unaliased/scalar-scope errors instead of guessing binding. Preserve the exact existing authority key-resolution shapes, qualified inner references, named/scalar/star sibling contracts and 20-entry bytes. See fixture issue-2148 addendum for finite authority grammar, candidate roles, nested/later owners, mutation and eight-file Evidence Floor.
  Local evidence at source SHA-256 `6430603d0e6eb92c9810ed650e5105eb16ce8ecf98f0d6a4e8c66f8119313891`: eight-file suite 2054 passed, Ruff/strict OpenSpec passed, 30-case public-API replay gives 23 ambiguous three-door refusals and 7 preserved controls. Pre-change replay 123 behavior failures/175 controls pass; consumption/stripped-view/authority/first-body/no-nested mutations yield 123/124/15/6/3 failures, restored focused298 pass. 20-entry five-surface `DIFFS=0`, `NEW_REFUSALS=0`, golden unchanged. Unaliased-only traversal normalises comments through existing `strip_comments` before enumeration; shared traversal and aliased behavior unchanged.

## 2. Reader conversion (I2–I5, merge before expand; transition SQL activates with I7)

- [x] 2.1 (I2, #1981) Real per-run routing in `packages/common/forecast_store.py` per user decision 2026-09-09: eight spanning segment blocks use caller-local legacy/narrow fact-row sources bound to authoritative run store beneath a single existing outer semantic layer; A9 reads actual store from its pinned header and renders only that river source. No constant legacy/catalog fallback. Preserve windows/cycle selection/response fields and lineage; validate missing/invalid A9 store before heavy SQL. Named branch bindings share parameters. Fixture `fixtures/I2-1981.md` owns the per-entry matrix and deliberate raw-template registry/golden provenance cutover; all eight callsites remain enumerated. Verify mocked forecast/fallback contracts plus SQL-shape suites/selector, real public smoke, red/mutation proof and unchanged non-forecast registry surfaces; do not deploy/execute transition SQL before I7.
  Evidence: initial full 13-file contract suite 2211 passed; after correcting the registry to required `source(store)` inputs, affected 10-file matrix 2096 passed. Ruff/strict OpenSpec passed. Nine exact before/after outer/predicate/binding mappings, ten unchanged sibling raw/five-surface comparisons, 12 raw/9 execution inventories and unchanged historical golden digest pass. Public registry probe verifies both store literals for both new sources; 16 routing owners kill old source (16 failures), missing narrow branch/route (11 each), forced A9 legacy (4); frozen-legacy registry mutant kills both narrow cases. No PostgreSQL mixed-store/EXPLAIN or deployment claim; I7 activation remains mandatory.
  Final merged evidence (PR #2220, reviewed `bf96cfccdf9cf26c7f293c363f317f34b3c1264e`): under the 2026-09-10 isolated-database authorization, node-27 disposable catalogs ran 28 tests successfully; CI ran 4971 targeted unit and 192 real-database tests successfully (29/1 skips respectively). Two verified P1s were repaired without widening candidate-result parity or selector/census guards; round 2 and independent final review were clean. The preceding no-PostgreSQL statement describes the initial evidence stage, not this final disposable-database proof. Production activation and Timescale/EXPLAIN/live receipts remain I7/I8.
- [x] 2.2a (I3a, #1982) `services/tiles/mvt.py` per-basin hydro tile template only: its fact-row source renders via `render_river_ts_sql` and owns a local legacy/narrow union whose branches bind store in the existing run-key authority lookup. The outer one-row MVT aggregate, statistics, ordering, budget and `.first()` contract remain unchanged. Verify: SQL shape/registry/golden oracles plus the mocked per-basin tile path through one verification command.

  **Suggested fixture level:** compact - one registry statement and one per-basin tile path; the local union sits below the aggregate boundary.

  **Execution fixture:** high (`fixtures/I3a-1982.md`). The compact suggestion was first raised to expanded for real-DB field/geometry/NULL-key consumers and shared fixture routing, then raised to high when the user authorized the minimal inherited benchmark capture/live-verifier named-binding cutover in this PR. Isolated assertions are authorized now; #1984's other store-dependent work and production activation remain deferred.

  Evidence at source checkpoint `df698cc73ec87da3d7f00338368ebe5deeba8c5e`: node-27 private checkout/environment ran 57 database tests without skips, including decoded full-MVT/statistics/cardinality, both stores, NULL-key/OOV and unchanged sibling/forecast fixtures. In-memory drop-narrow and omit-legacy-route mutants each fail only the narrow case while the legacy control passes; restored runs pass both, and source bytes stay unchanged. The route-omission mutant doubles feature_count from 3 to 6. Full committed benchmark/live-evidence/selector suites pass 1202 tests; the initial 2444-pass/332-fail matrix's inherited positional-binding failures are closed, without weakening reviewed-SHA provenance. Full hydro outer SQL, four other layer statements and eleven sibling raw entries remain unchanged; historical golden and frozen SQL digests remain pinned. Ruff/strict OpenSpec pass. Production activation and I7/I8 plan/live-receipt claims remain deferred.

  PR #2225 first-round review independently confirmed a missing regression guard for the frozen-SQL file's working CI edges. Two selector tests now require its exact unit owner and prove deletion of its dedicated database literal is unrescued by every remaining pattern. In-memory missing/widened selector and missing/rescued database-edge mutants each fail the intended assertion while the other owner passes; every restored control passes both. The complete selector suite passes 679 tests. Runtime SQL, workflow configuration and frozen oracle bytes remain unchanged by this coverage repair; the existing real-DB test remains the sole required digest owner.

  **Minimal mergeable slice:** atomic: one statement, one fact read and one aggregate result path.
- [x] 2.2b (I3b, #2206) `services/tiles/mvt.py` hydro-national tile template only: the identity-existence LATERAL and both zoom data LATERAL occurrences render via `render_river_ts_sql` and each owns a local legacy/narrow union; both candidate-run scopes carry `timeseries_store`, both data legs stay symmetric, and each combined LATERAL applies its existing `LIMIT 1` once. Preserve the outer identity decision, one-row MVT aggregate, 404/424 contracts and the existing non-store zoom unions. Verify: SQL shape/registry/golden oracles plus mocked national tile and HHE binding tests through one national-tile verification path.

  **Suggested fixture level:** compact - one registry statement and one national-tile path, with three coupled LATERAL occurrences below identity/zoom boundaries.

  **Execution fixture:** expanded (`fixtures/I3b-2206.md`): shared geospatial reader, two authored raw probe shapes and three coupled LATERAL sites, with per-test PostGIS/HTTP oracles under the existing user authorization. The live national statement entry is replaced by identity/data raw inputs (13 live entries; the historical 20-key golden remains frozen). Both full affected integration files run, but only actual tile cases opt in; metadata-only siblings remain pre-expand. Production EXPLAIN/deployment remains I7/I8.

  Evidence at source checkpoint `e979a9a17cc5817d8cae528461db4867a36c916b`: both complete affected integration files pass 61 tests without skips on node-27's private checkout/environment (Python 3.11.15). The full-MVT owner compares one result, all statistics and ordered decoded geometry/attributes against the frozen pre-transition SQL for both opposite-store assignments at z=5 and z=9, then executes the unchanged consumer. The half-hour owner proves zero authoritative facts, non-vacuous opposite-store decoys and the existing probe-specific 424. In-memory drop-narrow-data produces four intended failures plus two passing gap controls; omit-identity-route produces two intended failures plus four passing full-tile controls. Every restored run passes all six; source bytes remain unchanged.

  The complete pure matrix passes 2290 tests; committed-source benchmark/live-evidence consumers pass 525. All four sibling layer SQL strings/bind sets and eleven other raw inputs in both stores remain byte-identical; the historical golden and captured national SQL retain their pinned digests. Current census is 13 raw entries, 5 routed, 8 historical-comparable and 30 aids. Lexer traversal/reference tests retain complete four-variants-per-entry coverage without duplicating the registry's derived fixed total. Missing/widened selector and missing/rescued database-edge mutants each fail the intended permanent CI assertion with the sibling assertion passing; all restored controls pass both. Ruff and strict OpenSpec pass. No production deployment, transition, compressed-plan receipt or I7/I8 completion is claimed.

  **Minimal mergeable slice:** atomic: the identity-existence probe and both zoom data probes must use the same candidate/run-store semantics; splitting one occurrence would make one national tile internally inconsistent.
- [x] 2.2c (I3c, #2207) `services/tiles/mvt.py` valid-time discovery templates only: named and any-identity branches render per store; both put their legacy/narrow fact-row union below the single outer `DISTINCT valid_time ORDER BY valid_time DESC LIMIT :limit`; the named branches bind store in their run-key authority lookup, and the any-identity branches bind fact `run_key` to the matching `hydro_run` store. Preserve `sample_limit + 1` and Python truncation. Verify through one path: a call-level/capture-session behavior test in `tests/test_river_ts_read_path_surrogate_keys.py` invokes `valid_times_for_layer` for both named and any-identity branches. With `sample_limit = 2` and three fixed distinct captured times, each generated statement has exactly one outer `DISTINCT valid_time`, one outer `ORDER BY valid_time DESC`, and one outer `LIMIT :limit`, with `UNION ALL` in its fact-row source below that semantic layer; the captured bind has `limit = 3`, and the response asserts its chronologically ascending two-value result, `observed_count == 3`, and `truncated is True`. Run the SQL shape/registry/golden oracles and that capture-session behavior test in the same verification path.

  **Suggested fixture level:** compact - one callable with two branches and a limit-sensitive ordering contract.

  **Execution fixture:** compact (`fixtures/I3c-2207.md`), retaining the suggested level: one timestamp-discovery callable and one outer semantic layer, without tile-generation or production state changes. Raw named authority is explicitly qualified; any authority is a correlated EXISTS. Static extraction/index oracles migrate to real raw sources with exact census preserved. Dedicated discovery fixtures keep shifted decoy times; coupled MVT cases still transition once and retain their original assertions. Parent verifies the complete mapped backend matrices on node-27; no production activation or compressed-plan evidence is claimed.

  Evidence at source checkpoint `c938ff2b925e585ddb65ab7a156054a3020702ee`: node-27's private checkout/venv ran the complete mapped pure matrix (2327 passed), committed-source benchmark/live-evidence suites (525 passed), and both full surrogate/national integration files (64 passed), with no skips. The exact capture owner passes restored, fails against the frozen pre-change callable, and independently rejects missing +1 and sorting-before-truncation mutants. Dedicated physical-store owners retain shifted decoy times: suppressing narrow contribution yields three intended failures plus one legacy control; removing store authority yields four intended failures; every restored database run passes all four. Sanitized JUnit verifies assertion-only failures and rejects SQL/import/harness errors; source hashes remain unchanged.

  All five tile-generation SQL strings/binds, eleven other raw inputs in both stores, all preexisting mvt functions except the selected callable, and the historical golden remain unchanged. Census is 13 live /7 routed /6 historical-comparable, 20 historical keys/215 chains and 30 aids. The initial matrix's two failures were set-versus-tuple comparisons in new assertions; only their expected collection representation was corrected, with exact sets and the shared constant preserved. Ruff and strict OpenSpec pass. No production transition/deployment, compressed-plan receipt or early later-slice work is claimed.

  **Minimal mergeable slice:** atomic: both branches are selected by the same callable and must share one outer distinct/order/limit contract.
- [x] 2.3a (I4a, #1983) `apps/api/routes/hydro_display.py`: carry `timeseries_store` through the existing run metadata seam and render the MVT source-identity probe for that one known store; do not union its `SELECT 1 ... LIMIT 1`. Preserve `.first()`, validation-before-SQL, and `MVT_SOURCE_IDENTITY_NOT_FOUND` 404 behavior. Verify: SQL shape/registry/golden oracles plus MVT scaling/API contract tests through one API verification path.

  **Suggested fixture level:** compact - one known-run probe and one API contract path.

  **Minimal mergeable slice:** atomic: one callable, one fact read and one route lookup.

  **Execution fixture:** `fixtures/I4a-1983.md`. Keep the probe raw literal and all thirteen registry raw inputs unchanged; only the metadata projection and selected-store execution change. Preserve request-validation-before-any-SQL and readiness/identity precedence; validate the metadata route before the fact probe, with typed `TIMESERIES_STORE_INVALID` for missing/unknown store. Verify both-store call/HTTP behavior and the existing real-DB probe owner in explicitly authorized per-test post-expand catalogs; production transition/EXPLAIN remains I7/I8.

  **Completed checkpoint evidence:** `d0568912fb8e89371d72897ac8a529a8afa37485` passed the complete API-owned matrix plus required owners on node27: 3460 passed/0 skipped; complete surrogate, national and residual-debt real-DB suites: 76 passed/0 skipped. Capture baseline/restores pass 4; pre-change unrendered and opposite-store mutants each fail 4 assertions. Real metadata baseline/restore pass 2; omitted route-column mutant fails 2 assertions before the probe. All source hashes remain unchanged. The parent JUnit classifier was corrected against actual pytest `failure.message` evidence, retaining rejection of SQL/import/fixture errors; no product/test input changed between the full green matrix and continued proofs.

  **Preservation and deviations:** all thirteen raw inputs in both stores, five tile SQL strings, historical golden and forty-two other route-module functions remain identical. Direct invalid-store execution is zero SQL; API readiness/identity/store errors are one metadata SQL and success is three ordered statements. Initial new-test MIME expectation was corrected to the existing `application/x-protobuf`; mock-echo/duplicate-None assertions were removed without reducing routing/precedence coverage, and the newly required registry→MVT-scaling importer edge was added to its existing selector rule. Ruff, strict OpenSpec and preservation smoke pass. Production transition/deployment/EXPLAIN remains I7/I8.

- [ ] 2.3b (I4b, #2208) `packages/common/display_coverage.py` river leg only: project `timeseries_store` from `candidate_runs` and compose the two rendered fact-row branches only inside `river_sample_rows`; leave station CTEs, key aggregation/reconstruction, overwrite guard and outer `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` unchanged. Verify: SQL shape/registry/golden oracles plus `tests/test_display_coverage*`, parallel refresh and CLI tests through one coverage-refresh verification path.

  **Suggested fixture level:** compact - one module and one coverage-refresh path; DML boundary and forcing station leg are explicit invariants.

  **Minimal mergeable slice:** atomic: the single river CTE carries both store branches and feeds one unchanged upsert.
- [ ] 2.4 (I5) Non-template surfaces: `services/tile_publisher/publisher.py` `_has_table` accepts both names during the transition; `services/tile_publisher/forcing_copyback_backfill.py` `required_columns` branches on store; `scripts/reset_qhh_smoke_db.py` and `scripts/summarize_qhh_smoke_results.py` render per store (reset clears the legacy table for legacy runs); `services/production_closure/scale_validation.py` `QUERY_TARGETS` and `scripts/node27_timeseries_compression_live_evidence.py` plan-shape fixtures branch on store (the autopipeline statistics guard is 3.1's, not this task's). Verify: targeted tests for each surface with legacy present/absent.

## 3. Lifecycle lanes and governance (I6 — deploy before the expand migration)

- [ ] 3.1 Hypertable discovery ("canonical + existing `_legacy` sibling") shared by `scripts/node27_timeseries_compression.py`, `scripts/node27_timeseries_retention.py`, `scripts/node27_timeseries_compression_supervisor.py` (`validate_current_d3` asserts per table by catalog state, not by name: a canonical table **with** a `_legacy` sibling is asserted key-shaped and its sibling text-shaped; a canonical table **without** a sibling keeps its current text-shaped expectation — so river flips only at I7's expand and forcing only at I12's, and no expectation is flipped ahead of its migration; the capture tool's `HYPERTABLE_KEYS` follows the same three-state rule), `scripts/node27_timeseries_compression_capture.py` (`HYPERTABLE_KEYS` and its five hard-coded uses) and the autopipeline statistics guard. Verify: unit tests with legacy present and absent for each tool.
- [ ] 3.2 Receipt schemas: `schemas/timeseries_compression_receipt.schema.json` `per_table_totals` → `patternProperties` (canonical keys required, `_legacy` optional); `schemas/timeseries_retention_receipt.schema.json` adds `legacy_chunks`; committed historical receipts stay valid. Verify: schema tests for both shapes.
- [ ] 3.3 Per-tick derivation for one-day chunks: `infra/env/node27-timeseries-compression.example` keeps `PER_TICK_BOUND=4` with the re-derived comment, sets `LAG_SECONDS=172800`, deletes the "one chunk width" comment; runbook derivation section states both constraints with the legacy sibling as a finite backlog, the measured worst mixed tick, the pre-expand "compress the legacy backlog first under bound 1" recipe, the invalidation conditions and the cadence conclusion; template pin test updated (benchmark script's 168 h curve window is NOT touched). Verify: template pin test.
- [ ] 3.4 Governance: `packages/common/node27_cold_governance_collection.py` collects `uncompressed_bytes`, `daily_ingest_bytes`, `next_compressible_at`, `home_free_bytes`, `projection_status` (catalog-only, watermark via the compression runner's fetcher); `scripts/node27_resource_governance.py` computes `projected_peak_bytes` in days, emits `PROJECTED_PEAK_EXCEEDS_HOME_FREE` / `WORKING_SET_ABOVE_WARNING` / `WATERMARK_UNAVAILABLE`, demotes `DATABASE_SIZE_ABOVE_*` to info, adds CLI flags for margin and warning threshold; `schemas/node27_cold_governance_receipt.schema.json` and the OnFailure alert body updated. Verify: `tests/test_node27_resource_governance.py` scenarios fits / does-not-fit / no-uncompressed / watermark-unavailable / info-only; catalog-only SQL guard test.

## 4. Expand migration and parser (I7 — one issue, one PR)

- [ ] 4.1 River expand migration in the pinned DDL order (rename; `CREATE TABLE` with PK + two FKs inline; `create_hypertable(chunk_time_interval => interval '1 day', create_default_indexes => false)`; two secondary indexes; compression settings last; `OWNER TO nhms_ingest_rw`; `hydro_run.timeseries_store NOT NULL DEFAULT 'narrow'` + CHECK; backfill `legacy` where `parsed_at IS NOT NULL OR status IN ('parsed','published')`), idempotent guards, header recording why no chunk is decompressed and why `000047` is not re-run. Verify: CI dry run; order test; execution oracle routed from #2018 (`retro-2018-2.md`): an `integration`-marked test `PREPARE`s every registry entry's narrow render and legacy render against the migrated schema in the real-db CI job, so PostgreSQL's parser — not the text guards — is the last word on a shipped text-identity column.
- [ ] 4.2 `tests/test_migrations.py`: retained-index pin → the narrow three-index set on the canonical name (legacy exempt); "no text identity column may reappear" guard; DDL-order pin; `000047` unchanged pin; `tests/test_river_identity_normalization_integration.py` index expectations.
- [ ] 4.3 `workers/output_parser/parser.py`: narrow column set, `ON CONFLICT (run_key, river_segment_key, variable_e, valid_time)`, DELETE keyed by `run_key` + `river_network_version_key` + `variable_e` + closed `valid_time` window (both literals in one statement), keys derived from `hydro_run` / `core.river_segment`, `timeseries_store` kept `narrow` in the `mark_run_parsed` transaction, `LegacyStoreWriteRefused` before any DELETE with a dedicated CLI exit code. Verify: `tests/test_output_parser_dual_write.py` re-pinned as the narrow-write oracle; wire-site invariant test green; `tests/test_river_ts_text_identity_cleanup.py` re-pinned for the parser register entry in the same PR (statement census to the narrow chain's count, the one-marked-`run_id`-aid adjacency test deleted, parser marker count 2 → 0).
- [ ] 4.4 `scripts/node27_autopipeline.py`: map `LegacyStoreWriteRefused` to an `ops.ingest_recompute_decline` row with reason `legacy_store_refused`, tick `rc = 0`, permanent (not reopened by `product_mtime`); receipt schema updated. Verify: unit test for decline persistence and non-reopen.
- [ ] 4.5 Fixtures: `db/seeds/seed_demo.py`, `tests/integration_helpers.py` insert helpers and their twenty consumers, `tests/test_river_ts_dual_write_integration.py` (delete or re-pin as narrow-write integration). Verify: real-DB pytest on node-27 (expand idempotent twice; legacy chunks keep state; `time_interval = 1 day` on the canonical hypertable; parsed runs `legacy`, running runs `narrow`; first parse after expand lands narrow; mixed-store national-tile and coverage queries retain both-store result semantics by returning both runs; separate named-identity valid-times requests for the legacy and narrow run each return only that routed run's `valid_time`, while one any-identity request includes `valid_time` values from both stores before its single outer `DISTINCT valid_time ORDER BY valid_time DESC LIMIT :limit`; `EXPLAIN` for national-tile, coverage, and both valid-times forms proves every legacy/narrow fact branch references only its matching physical table and the non-matching named branch produces no rows).

## 5. River rollout on node-27 (I8)

- [ ] 5.1 Runbook: maintenance-window checklist in the pinned order (timers stop → API/parser stop → `git pull --ff-only` → `migrate.py` → start → timers start), the pre-expand legacy-backlog compression recipe, the D12 reverse sequence with the allowed intermediate state, and the "transitional cold tier does not cover `_legacy`" note. Verify: rollback rehearsal on a throwaway cluster recorded.
- [ ] 5.2 Execute and post the receipt: per-statement wall time; first narrow chunk size after one full cycle; curve EXPLAIN gate for SHJ-NJ and a small network on narrow uncompressed, narrow compressed and legacy (bounds: `river_segment_key` in Index Cond / segmentby pruning, `Rows Removed / returned ≤ 10`, `shared hit ≤ 5000`, SQL warm P95 ≤ 300 ms over ≥ 5 samples, local single-source `forecast-series` warm P95 ≤ 500 ms); identity-existence probe miss branch before/after with coverage-loss list; registry counts (active/runnable/selected/excluded); one compression tick and one retention tick covering both tables; governance receipt with the working-set fields; `/` clicks on SHJ-NJ, one medium and one small network with screenshots; display deny-write receipt (checklist C1–C4); `/ops` reachable; the regression criterion recorded.

## 6. River contract (I9 — entry gate: fourteen archived daily receipts + `legacy_chunks = 0`)

- [ ] 6.1 Archive the fourteen daily receipts under the change and assert the retention receipt shows `legacy_chunks = 0` and no shape regression; this is the issue's opening check (schedule note: the last legacy seven-day chunk may keep `legacy_chunks > 0` until about twenty-one days after the expand, so the conjunction gate opens later than the fourteen receipts alone).
- [ ] 6.2 Contract migration: refuse on non-zero legacy chunks; drop `hydro.river_timeseries_legacy`, `hydro.cutover_river_identity_normalization()`, `hydro.verify_river_identity_normalization()`, `hydro.hydro_run.timeseries_store`; contract window deploys routing-free code before the migration. Verify: real-DB test of the refusal and of the clean path.
- [ ] 6.3 Delete `scripts/node27_river_identity_backfill.py` and `tests/test_node27_river_identity_backfill*.py`; remove the **river** renderer's legacy path and routing code; physically delete every marker line and aid line; collapse the cleanup oracles to "no text identity column in fact-table SQL"; marker census zero (the marker set is river-only; forcing carries no markers). Verify: `grep -rn "remove with #1342" --include=*.py --include=*.sql .` empty; oracles green.
- [ ] 6.4 ADR 0002 amendment (Decision 4 list superseded by the narrow settings, 7-day chunk assumption retired, FK disposition), runbook table/capacity/cutover sections rewritten, `openspec/glossary.md` entries; move the browser click P95 acceptance text to #1970, then close #1342 and #1336 with the receipts.

## 7. Forcing expand (I10–I12, after the river contract)

- [ ] 7.1 (I10) Node-27 read-only receipt: `met.met_station` / `met.forcing_version` row counts and index footprint; distinct `variable` / `unit` / `quality_flag` / `native_resolution`; throwaway-cluster wall time of the IDENTITY column adds; measured cost of the legacy-table existence probe per `forcing_version` that the routing backfill relies on (D9 asymmetry with the river predicate); fill the design open questions.
- [ ] 7.2a (I11) Add `render_forcing_ts_sql(template_pair, store)` in `packages/common/`: the legacy variant is the text-column template against `met.forcing_station_timeseries_legacy`; the narrow variant is a separately registered key/enum template against the canonical name joining `met.forcing_version` (`source_id`) and `met.met_station` (`basin_version_id`). There is no `#1342` marker mechanism for forcing: the forcing legacy table has no key columns, so the two variants are two registered templates, not one template minus lines. A forcing template census in the shape oracle pins every SQL site on `met.forcing_station_timeseries` under `packages/`, `workers/`, `scripts/`, `services/`, `apps/` as either registered (rendered per store) or on an explicit exemption list with a named owner (name-only references: payload manifests, row-count keys, catalog-pin fixtures, lifecycle tools; and the two write-side post-write verification queries — the forcing-producer `verify_forcing_version_children` and the domain-handoff apply row-count check — which are converted together with the writers in 7.3). Verify: shape oracle renders both variants for every registered forcing template; census fails on an unregistered, unexempted site.
- [ ] 7.2 (I11) Every forcing reader through the forcing renderer with store lookup by forcing version: `packages/common/display_coverage.py` station leg; `packages/common/forecast_store.py` QHH latest-product fallback CTE, station-series rows helper, station-forcing membership validation, forcing-readiness overall and per-variable rows; `packages/common/best_available.py` forcing-inputs listing; `workers/model_registry/qhh_production_bootstrap.py` forcing-state count join; `scripts/reset_qhh_smoke_db.py` forcing DELETE rendered per store (legacy versions also clear `met.forcing_station_timeseries_legacy`); the live-evidence plan-shape fixtures for the forcing table branch on store. Coverage-count equality test with divergent basin provenance; `tests/test_forecast_api.py` index pins prepared for both stores. Verify: oracles, census and forecast API tests.
- [ ] 7.3 (I12) Forcing expand migration: IDENTITY keys on the two authority tables; `met.forcing_variable` / `met.forcing_unit` / `met.forcing_quality_flag` by the three-source union with excluded literals recorded; rename to `met.forcing_station_timeseries_legacy`; narrow table with key PK, successor index, compression last, one-day chunks, FKs, owner; `met.forcing_version.timeseries_store` default `narrow` with legacy backfill by row existence. Writers `workers/forcing_producer/store.py` and `packages/common/forcing_domain_handoff_apply.py` write only the narrow table through the guard, keep `narrow` in-transaction, refuse legacy versions with a distinct permanent code, and their post-write verification queries (`verify_forcing_version_children`, the apply row-count check) read the narrow table by `forcing_version_key`; `tests/test_forcing_domain_handoff_apply.py`, `tests/test_forcing_producer*.py`, `db/seeds/seed_demo.py` re-pinned. Verify: CI dry run; real-DB pytest on node-27; supervisor `validate_current_d3` and capture `HYPERTABLE_KEYS` accept the mixed forcing catalog (canonical key-shaped, `met.forcing_station_timeseries_legacy` text-shaped) with no code change, proven by a unit test on the post-expand catalog fixture.

## 8. Forcing rollout and contract (I13–I14)

- [ ] 8.1 (I13) Forcing rollout receipt: per-statement wall time, QHH fallback before/after EXPLAIN with coverage-loss list, station-series API field identity for a legacy and a narrow version, compression and retention ticks covering both tables, governance receipt.
- [ ] 8.2 (I14, entry gate: fourteen daily receipts + `legacy_chunks = 0`) Forcing contract migration (refuse on non-zero legacy chunks; drop legacy table and `met.forcing_version.timeseries_store` after routing-free code is deployed) with real-DB tests of both paths.
- [ ] 8.3 (I14) Delete the forcing renderer's legacy template and its tests so only the narrow template stays registered; forcing census shows no legacy-table reference; runbook and ADR follow-up.
