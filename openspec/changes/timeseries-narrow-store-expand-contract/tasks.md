## 1. Renderer, template normalisation, oracles (I1 — zero behaviour change)

- [ ] 1.1 Normalise every transitional aid in `packages/common/forecast_store.py` (incl. the `:1903` disjunction rewritten as `OR (` + marker + `rt.run_id = %(scan_run_id)s AND` + `rt.run_key = (...))`), `services/tiles/mvt.py` (three non-verbatim / 1:N markers at `:661`, `:793`, `:840` split to one verbatim marker per aid), `apps/api/routes/hydro_display.py:773`, `packages/common/forecast_store.py:1897` and `services/tiles/mvt.py:511`, `:1496`, `:1524` (markers moved off `WHERE` lines onto the aid line), `packages/common/display_coverage.py:383-418` (prose comment `:383-391` replaced by one verbatim marker per aid; the `:406` aid moved off the `WHERE` line; the `run_id` disjunction `:410-411` and the `river_network_version_id` disjunction `:417-418` split the same way as forecast_store `:1903`): one aid conjunct per line, exactly one verbatim marker line immediately above it. Verify: every existing test on those readers passes unchanged; the census and per-block shape pins in `tests/test_river_ts_text_identity_cleanup.py` are re-pinned in the same PR.
- [ ] 1.2 Add `render_river_ts_sql(template, store)` and `render_union_all(template, stores, params)` in `packages/common/` (legacy: table rename only; narrow: remove marker line + immediately following aid line, fail closed otherwise; parse-check; no-text-identity and key-predicate-retention assertions; per-branch `hydro_run.timeseries_store` binding and parameter copying). Verify: unit tests for both variants, the refusal on a mis-shaped marker, and the union shape.
- [ ] 1.3 Tighten `tests/test_river_ts_text_identity_cleanup.py` adjacency to "one aid, one verbatim marker on the previous line"; extend `tests/test_river_ts_read_path_surrogate_keys.py` and `tests/test_sql_shape_helpers.py` to render both variants for every registered template (legacy == normalised template modulo table name; narrow free of text identity columns and markers with key predicates intact); add `scripts/select_ci_tests.py` rules so the new renderer module and every touched reader select the three oracles. Verify: `uv run pytest` on the three oracles + selector test.

## 2. Reader conversion (I2–I5, store constant `legacy` until expand)

- [ ] 2.1 (I2) `packages/common/forecast_store.py`: the eight segment blocks and the A9 fallback render through the renderer; per-run store lookup helper. Verify: oracle green; contract-response field-identity test for a run of each store.
- [ ] 2.2 (I3) `services/tiles/mvt.py`: the hydro layer and the two hydro-national lateral legs plus the identity-existence probe render through the renderer; discovery queries that span runs use `render_union_all`. Verify: oracle green; mocked tile tests unchanged.
- [ ] 2.3 (I4) `apps/api/routes/hydro_display.py` and `packages/common/display_coverage.py` river leg render through the renderer; coverage batch refresh uses the union combinator. Verify: oracle green; `tests/test_display_coverage*` unchanged; API contract tests unchanged.
- [ ] 2.4 (I5) Non-template surfaces: `services/tile_publisher/publisher.py` `_has_table` accepts both names during the transition; `services/tile_publisher/forcing_copyback_backfill.py` `required_columns` branches on store; `scripts/node27_autopipeline.py` statistics-guard hypertable list uses the discovery set of 3.1; `scripts/reset_qhh_smoke_db.py` and `scripts/summarize_qhh_smoke_results.py` render per store (reset clears the legacy table for legacy runs); `services/production_closure/scale_validation.py` `QUERY_TARGETS` and `scripts/node27_timeseries_compression_live_evidence.py` plan-shape fixtures branch on store. Verify: targeted tests for each surface with legacy present/absent.

## 3. Lifecycle lanes and governance (I6 — deploy before the expand migration)

- [ ] 3.1 Hypertable discovery ("canonical + existing `_legacy` sibling") shared by `scripts/node27_timeseries_compression.py`, `scripts/node27_timeseries_retention.py`, `scripts/node27_timeseries_compression_supervisor.py` (`validate_current_d3` asserts per table: key-shaped canonical, text-shaped legacy), `scripts/node27_timeseries_compression_capture.py` (`HYPERTABLE_KEYS` and its five hard-coded uses) and the autopipeline statistics guard. Verify: unit tests with legacy present and absent for each tool.
- [ ] 3.2 Receipt schemas: `schemas/timeseries_compression_receipt.schema.json` `per_table_totals` → `patternProperties` (canonical keys required, `_legacy` optional); `schemas/timeseries_retention_receipt.schema.json` adds `legacy_chunks`; committed historical receipts stay valid. Verify: schema tests for both shapes.
- [ ] 3.3 Per-tick derivation for one-day chunks: `infra/env/node27-timeseries-compression.example` keeps `PER_TICK_BOUND=4` with the re-derived comment, sets `LAG_SECONDS=172800`, deletes the "one chunk width" comment; runbook derivation section states both constraints with the legacy sibling as a finite backlog, the measured worst mixed tick, the pre-expand "compress the legacy backlog first under bound 1" recipe, the invalidation conditions and the cadence conclusion; template pin test updated (benchmark script's 168 h curve window is NOT touched). Verify: template pin test.
- [ ] 3.4 Governance: `packages/common/node27_cold_governance_collection.py` collects `uncompressed_bytes`, `daily_ingest_bytes`, `next_compressible_at`, `home_free_bytes`, `projection_status` (catalog-only, watermark via the compression runner's fetcher); `scripts/node27_resource_governance.py` computes `projected_peak_bytes` in days, emits `PROJECTED_PEAK_EXCEEDS_HOME_FREE` / `WORKING_SET_ABOVE_WARNING` / `WATERMARK_UNAVAILABLE`, demotes `DATABASE_SIZE_ABOVE_*` to info, adds CLI flags for margin and warning threshold; `schemas/node27_cold_governance_receipt.schema.json` and the OnFailure alert body updated. Verify: `tests/test_node27_resource_governance.py` scenarios fits / does-not-fit / no-uncompressed / watermark-unavailable / info-only; catalog-only SQL guard test.

## 4. Expand migration and parser (I7 — one issue, one PR)

- [ ] 4.1 River expand migration in the pinned DDL order (rename; `CREATE TABLE` with PK + two FKs inline; `create_hypertable(chunk_time_interval => interval '1 day', create_default_indexes => false)`; two secondary indexes; compression settings last; `OWNER TO nhms_ingest_rw`; `hydro_run.timeseries_store NOT NULL DEFAULT 'narrow'` + CHECK; backfill `legacy` where `parsed_at IS NOT NULL OR status IN ('parsed','published')`), idempotent guards, header recording why no chunk is decompressed and why `000047` is not re-run. Verify: CI dry run; order test.
- [ ] 4.2 `tests/test_migrations.py`: retained-index pin → the narrow three-index set on the canonical name (legacy exempt); "no text identity column may reappear" guard; DDL-order pin; `000047` unchanged pin; `tests/test_river_identity_normalization_integration.py` index expectations.
- [ ] 4.3 `workers/output_parser/parser.py`: narrow column set, `ON CONFLICT (run_key, river_segment_key, variable_e, valid_time)`, DELETE keyed by `run_key` + `river_network_version_key` + `variable_e` + closed `valid_time` window (both literals in one statement), keys derived from `hydro_run` / `core.river_segment`, `timeseries_store` kept `narrow` in the `mark_run_parsed` transaction, `LegacyStoreWriteRefused` before any DELETE with a dedicated CLI exit code. Verify: `tests/test_output_parser_dual_write.py` re-pinned as the narrow-write oracle; wire-site invariant test green.
- [ ] 4.4 `scripts/node27_autopipeline.py`: map `LegacyStoreWriteRefused` to an `ops.ingest_recompute_decline` row with reason `legacy_store_refused`, tick `rc = 0`, permanent (not reopened by `product_mtime`); receipt schema updated. Verify: unit test for decline persistence and non-reopen.
- [ ] 4.5 Fixtures: `db/seeds/seed_demo.py`, `tests/integration_helpers.py` insert helpers and their twenty consumers, `tests/test_river_ts_dual_write_integration.py` (delete or re-pin as narrow-write integration). Verify: real-DB pytest on node-27 (expand idempotent twice; legacy chunks keep state; `time_interval = 1 day` on the canonical hypertable; parsed runs `legacy`, running runs `narrow`; first parse after expand lands narrow; mixed-store national-tile and coverage queries return both runs with each branch on its own table).

## 5. River rollout on node-27 (I8)

- [ ] 5.1 Runbook: maintenance-window checklist in the pinned order (timers stop → API/parser stop → `git pull --ff-only` → `migrate.py` → start → timers start), the pre-expand legacy-backlog compression recipe, the D12 reverse sequence with the allowed intermediate state, and the "transitional cold tier does not cover `_legacy`" note. Verify: rollback rehearsal on a throwaway cluster recorded.
- [ ] 5.2 Execute and post the receipt: per-statement wall time; first narrow chunk size after one full cycle; curve EXPLAIN gate for SHJ-NJ and a small network on narrow uncompressed, narrow compressed and legacy (bounds: `river_segment_key` in Index Cond / segmentby pruning, `Rows Removed / returned ≤ 10`, `shared hit ≤ 5000`, SQL warm P95 ≤ 300 ms over ≥ 5 samples, local single-source `forecast-series` warm P95 ≤ 500 ms); identity-existence probe miss branch before/after with coverage-loss list; registry counts (active/runnable/selected/excluded); one compression tick and one retention tick covering both tables; governance receipt with the working-set fields; `/` clicks on SHJ-NJ, one medium and one small network with screenshots; display deny-write receipt (checklist C1–C4); `/ops` reachable; the regression criterion recorded.

## 6. River contract (I9 — entry gate: fourteen archived daily receipts + `legacy_chunks = 0`)

- [ ] 6.1 Archive the fourteen daily receipts under the change and assert the retention receipt shows `legacy_chunks = 0` and no shape regression; this is the issue's opening check (schedule note: the last legacy seven-day chunk may keep `legacy_chunks > 0` until about twenty-one days after the expand, so the conjunction gate opens later than the fourteen receipts alone).
- [ ] 6.2 Contract migration: refuse on non-zero legacy chunks; drop `hydro.river_timeseries_legacy`, `hydro.cutover_river_identity_normalization()`, `hydro.verify_river_identity_normalization()`, `hydro.hydro_run.timeseries_store`; contract window deploys routing-free code before the migration. Verify: real-DB test of the refusal and of the clean path.
- [ ] 6.3 Delete `scripts/node27_river_identity_backfill.py` and `tests/test_node27_river_identity_backfill*.py`; remove the renderer's legacy path and routing code; physically delete every marker line and aid line; collapse the cleanup oracles to "no text identity column in fact-table SQL"; marker census zero. Verify: `grep -rn "remove with #1342"` empty; oracles green.
- [ ] 6.4 ADR 0002 amendment (Decision 4 list superseded by the narrow settings, 7-day chunk assumption retired, FK disposition), runbook table/capacity/cutover sections rewritten, `openspec/glossary.md` entries; move the browser click P95 acceptance text to #1970, then close #1342 and #1336 with the receipts.

## 7. Forcing expand (I10–I12, after the river contract)

- [ ] 7.1 (I10) Node-27 read-only receipt: `met.met_station` / `met.forcing_version` row counts and index footprint; distinct `variable` / `unit` / `quality_flag` / `native_resolution`; throwaway-cluster wall time of the IDENTITY column adds; measured cost of the legacy-table existence probe per `forcing_version` that the routing backfill relies on (D9 asymmetry with the river predicate); fill the design open questions.
- [ ] 7.2 (I11) Forcing readers through the renderer with store lookup by forcing version: display-coverage station leg, QHH latest-product fallback CTE (narrow joins `met.forcing_version` for `source_id` and `met.met_station` for `basin_version_id`), legacy `station_series()` helper (SQL only); coverage-count equality test with divergent basin provenance; `tests/test_forecast_api.py` index pins prepared for both stores. Verify: oracles and forecast API tests.
- [ ] 7.3 (I12) Forcing expand migration: IDENTITY keys on the two authority tables; `met.forcing_variable` / `met.forcing_unit` / `met.forcing_quality_flag` by the three-source union with excluded literals recorded; rename to `met.forcing_station_timeseries_legacy`; narrow table with key PK, successor index, compression last, one-day chunks, FKs, owner; `met.forcing_version.timeseries_store` default `narrow` with legacy backfill by row existence. Writers `workers/forcing_producer/store.py` and `packages/common/forcing_domain_handoff_apply.py` write only the narrow table through the guard, keep `narrow` in-transaction, refuse legacy versions with a distinct permanent code; `tests/test_forcing_domain_handoff_apply.py`, `tests/test_forcing_producer*.py`, `db/seeds/seed_demo.py` re-pinned. Verify: CI dry run; real-DB pytest on node-27.

## 8. Forcing rollout and contract (I13–I14)

- [ ] 8.1 (I13) Forcing rollout receipt: per-statement wall time, QHH fallback before/after EXPLAIN with coverage-loss list, station-series API field identity for a legacy and a narrow version, compression and retention ticks covering both tables, governance receipt.
- [ ] 8.2 (I14, entry gate: fourteen daily receipts + `legacy_chunks = 0`) Forcing contract migration (refuse on non-zero legacy chunks; drop legacy table and `met.forcing_version.timeseries_store` after routing-free code is deployed) with real-DB tests of both paths.
- [ ] 8.3 (I14) Remove the forcing renderer's legacy path and tests; forcing marker census zero; runbook and ADR follow-up.
