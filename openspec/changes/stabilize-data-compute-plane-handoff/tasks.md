## 1. Forcing-Domain Handoff

- [x] 1.1 Inventory the exact forcing-domain data required by node-27 display readiness, including `met.forcing_version`, `met.met_station`, `met.forcing_station_timeseries`, and `met.interp_weight`, and document the object-store handoff manifest fields/checksums needed to reconstruct them.
  Evidence: complete fixture input path validates required identity fields, `start_time`/`end_time`, payload checksums, station count, and per-table row-count output.
- [x] 1.2 Add fixture coverage for a complete object-store forcing-domain handoff package and at least one incomplete package with a stable unavailable reason.
  Evidence: missing payload/checksum/temporal fields produce named stable unavailable reasons, traversal/sibling package URIs are rejected without broad root scans, and validation output contains no credential-like values.
- [x] 1.3 Harden `scripts/node27_mirror_forcing.py` so transitional mirror mode requires `--node22-url` or `N22_DSN`, never reads `infra/env/display.env`, and returns credential-safe structured evidence.
  Evidence: `tests/test_node27_mirror_forcing.py` covers no-DSN rc 2 JSON skip without display-env fallback, explicit CLI/env `N22_DSN` source selection, transitional compatibility/read-only boundary evidence, and redaction of accidental credential-bearing DSN strings.
- [x] 1.4 Update `scripts/node27_autopipeline.py` to treat the explicit mirror as transitional compatibility, with skip/fail summaries that preserve run-level failure isolation.
  Evidence: `scripts/node27_autopipeline.py` now only invokes `scripts/node27_mirror_forcing.py` when a run has no declared object-store handoff and `N22_DSN` or `--node22-url` is explicitly configured; focused tests in `tests/test_node27_autopipeline_handoff.py` cover no-DSN precheck skip without mirror, env/CLI mirror fallback, rc=2 compatibility skip, nonzero mirror failure, four-table mirror row-count normalization, redacted DSN summary evidence, and one-run skip/fail isolation while unrelated runs continue. Verification passed: `uv run pytest -q tests/test_node27_autopipeline_handoff.py tests/test_node27_mirror_forcing.py tests/test_forcing_domain_handoff_apply.py` (32 passed), `uv run ruff check .`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, and `git diff --check`.
  Evidence floor: focused autopipeline tests cover no declared handoff + `N22_DSN` fallback, no declared handoff + new `--node22-url` passthrough fallback, no declared handoff + no explicit mirror DSN precheck skip without invoking mirror, explicitly configured mirror rc=2 compatibility skip, mirror nonzero failure, mirror boundary evidence with redacted DSN source, normalized four-table mirror row counts, and one-run isolation where an unrelated runnable run continues after a forcing-stage skip/fail.
- [x] 1.5 Implement the object-store forcing-domain contract parser/fixture reader that reads package manifests and station payloads, returns structured reconstruction evidence, and does not change autopipeline behavior.
  Evidence: `parse_forcing_domain_handoff_path(...)` added in `packages/common/forcing_domain_handoff.py`; focused tests in `tests/test_forcing_domain_handoff_contract.py` cover complete parsed table keys/counts/fields/checksums, station coordinate evidence as longitude/latitude or geometry, parser-only DB-shaped station coordinate rejection for invalid GeoJSON Point mappings, non-Point geometry, and partial longitude/latitude even when geometry is present, parser-only `direct_grid` rejection for missing/blank `grid_signature` and non-unit weight, non-direct optional `grid_signature` parsing, unavailable handoff checksum evidence when the handoff manifest is readable, missing required field, missing payload, malformed payload, checksum mismatch, credential-safe unavailable results with empty `parsed`, unsafe traversal/sibling-package rejection, oversized manifest/package/payload reads, lattice-too-large empty `parsed`, parser-only finite `elevation_m` station rows, successful parsed business-value preservation for nested grid signatures, and unchanged #641 validator test expectations. No `scripts/node27_autopipeline.py` changes were verified by orchestrator diff evidence, not by a focused parser test. Round-4 verification passed: `uv run pytest -q tests/test_forcing_domain_handoff_contract.py` (85 passed, 1 skipped), `uv run ruff check packages/common/forcing_domain_handoff.py tests/test_forcing_domain_handoff_contract.py`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, and `git diff --check`.
  Evidence floor: focused parser tests cover the complete fixture path input -> public parser envelope with `available/status/unavailable_reasons/evidence/parsed`, exact `parsed.met.forcing_version` row shape including `checksum` from `forcing_package_manifest_checksum_sha256`, exact `parsed.met.met_station` row keys with required business fields plus coordinate evidence (`longitude`+`latitude` or `geometry`) and optional geometry preservation, exact `parsed.met.forcing_station_timeseries`/`parsed.met.interp_weight` row keys, expected row counts and payload checksums, missing required field, missing payload, malformed payload, checksum mismatch, credential-safe unavailable reasons with no parsed rows, unsafe traversal/sibling-package URI rejection, no object-store escape, oversized/large-input behavior, bounded package/run discovery without broad object-store scans, unchanged #641 validation output, and no `scripts/node27_autopipeline.py` changes.
- [x] 1.6 Implement the object-store forcing-domain DB apply path that idempotently writes or verifies `met.forcing_version`, `met.met_station`, `met.forcing_station_timeseries`, and `met.interp_weight`, covering complete package, missing field, checksum mismatch, and credential-safe failure reasons.
  Evidence: `apply_forcing_domain_handoff(...)` and `apply_forcing_domain_handoff_path(...)` added in `packages/common/forcing_domain_handoff_apply.py`; focused tests in `tests/test_forcing_domain_handoff_apply.py` cover complete path apply -> four target table writes/row-count evidence in one owned transaction with canonical storage `source_id`, parser-envelope reapply idempotency, pre-registered `met.forcing_version` placeholder completion, parser unavailable for missing temporal field and payload checksum mismatch with no DB cursor writes, station geometry-only conversion through `ST_SetSRID(ST_MakePoint(...), 4490)`, caller-owned cursor savepoint success/failure semantics, lon/lat-vs-GeoJSON Point mismatch fail-closed before writes, existing global `station_id` conflict rollback without overwrite, station UPSERT `RETURNING` shortfall rollback, direct-grid rows preserving parser-proven weight/signature constraints and canonical method casing, interp-weight scope advisory lock evidence, credential-safe parser and SQL failure reports, rollback/no readiness when failure is injected after each apply stage, and unchanged `scripts/node27_autopipeline.py` diff against `origin/master...HEAD`. Focused verification passed: `uv run pytest -q tests/test_forcing_domain_handoff_apply.py` (19 passed), `uv run pytest -q tests/test_forcing_domain_handoff_contract.py` (85 passed, 1 skipped), `uv run ruff check .`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, `git diff --check`, and protected-path diff check for `scripts/node27_autopipeline.py`, `scripts/node27_mirror_forcing.py`, `scripts/node27_ingest_run.py`, and `db/migrations`.
  Evidence floor: focused DB-apply tests cover complete parser envelope -> four target table writes/row-count evidence in one transaction, duplicate/idempotent re-apply, parser unavailable due to missing field or checksum mismatch with no DB writes, station geometry conversion from parser coordinate evidence with SRID 4490 and ST_X/ST_Y assertions, lon/lat-vs-geometry mismatch fail-closed, existing station conflict fail-closed without overwrite, direct-grid row constraints preserved from parser output, credential-safe failure reports, transaction rollback when failure is injected after each apply stage, unchanged `scripts/node27_autopipeline.py` behavior, and `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`.
- [x] 1.7 Update `scripts/node27_autopipeline.py` to prefer object-store forcing-domain import for contracted packages before explicit transitional mirror fallback, covering skip/fail summaries and one-run failure isolation.
  Evidence: declared `runs/<run>/input/forcing_domain_handoff.json` packages are applied through the object-store DB apply boundary before parse/coverage, with no mirror fallback when the declared handoff is unavailable, failed, or raises at the DB apply boundary; `main()` now emits per-run `runs.details[]` forcing-stage evidence (`mode`, `status`, `ready`, normalized four-table `row_counts`, `reason_codes`) plus parse/coverage fields, and keeps publish as a single global phase after the run loop. Focused tests cover successful handoff bypassing mirror, declared unavailable/failed/exception handoff with mirror configured but not invoked, parse failure preserving forcing evidence, coverage refresh failure distinguished from forcing failure, credential-safe summaries, and unrelated run continuation. Verification passed: `uv run pytest -q tests/test_node27_autopipeline_handoff.py tests/test_node27_mirror_forcing.py tests/test_forcing_domain_handoff_apply.py` (32 passed), `uv run ruff check .`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, and `git diff --check`.
  Evidence floor: focused autopipeline tests cover complete declared object-store handoff apply -> no mirror subprocess -> parse/coverage path, declared handoff unavailable or failed + mirror configured -> no mirror fallback and `stage=forcing_handoff`, no declared handoff + no explicit mirror configured skip, final `main()` JSON `runs.details[]` with forcing mode/status/ready/normalized four-table row_counts/reason_codes plus parse/river/coverage fields, parse failure preserving forcing evidence, coverage failure distinguished from forcing/mirror unavailable with unrelated run continuation, global publish phase called once after the run loop and not represented as a per-run detail stage, credential-safe summary reasons, unchanged parser/apply internals, and `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`.
- [ ] 1.8 Verify qhh and heihe forcing/display readiness on node-27 without relying on an implicit node-22 DB or display-env mirror fallback, and record evidence paths without secrets.

## 2. Node-27 Ingest Boundary

- [x] 2.1 Add a committed node-27 ingest env template or wrapper contract and wire the actual cron/on-demand entry (`scripts/node27_autopipe_cron.sh`) to source or validate ingest-specific writer configuration instead of deriving it from `infra/env/display.env`.
  Evidence: `infra/env/node27-ingest.example` defines the node-27 data-plane ingest contract separately from display env; `scripts/node27_autopipe_cron.sh` sources the ingest env or requires explicit ambient override, exports `NHMS_NODE27_INGEST_CONFIG_SOURCE`, contains no writer DB default, does not pass writer `DATABASE_URL` in child argv, and blocks missing env, display env, symlink env, non-0600 env, source-failed env, missing ingest role, and env-file omissions that would otherwise be backfilled by ambient `DATABASE_URL`, `NHMS_NODE27_INGEST_ROLE`, transitional `N22_DSN`, or libpq credential env (`PGUSER`, `PGPASSWORD`, `PGPASSFILE`, `PGSERVICE`, `PGSERVICEFILE`) before Python/backstop. Focused tests cover missing env, strict env-file sourcing/role ownership, argv DSN non-exposure with `DATABASE_URL` still present in the child environment, strict-mode libpq credential clearing, and wrapper rc=2 backstop skip.
  Evidence floor: committed ingest template/contract is separate from display env, wrapper sources that contract or requires an explicit ambient override, wrapper exports data-plane ingest role/config-source evidence, wrapper contains no writer `DATABASE_URL` default and no `display.env` source path, child argv does not expose writer DSNs, and static/focused tests prove missing ingest env or role blocks before invoking Python or coverage backstop while strict env-file mode does not inherit libpq credential env.
- [x] 2.2 Add preflight checks for writer `DATABASE_URL`, `OBJECT_STORE_ROOT`, `BASINS_ROOT`, work/log roots, and secret redaction before `node27_autopipeline.py` starts seed/import/activate/backfill/register/mirror/parse/coverage/publish work.
  Evidence: `scripts/node27_autopipeline.py` emits rc=2 `preflight_blocked` JSON before run discovery/seed/run/publish when writer config is missing or unsafe; focused tests assert stable blockers for missing direct `NHMS_NODE27_INGEST_ROLE` (`INGEST_ROLE_REQUIRED`), missing DB URL, malformed DB URL, username-less DB URL, username-without-password DB URL (`DATABASE_URL_PASSWORD_MISSING`) despite ambient `PGPASSWORD`/`PGPASSFILE`, display-readonly role leakage, readonly/display-looking DB identity, forbidden libpq query overrides (`user`, `host`/`port`/`dbname`, `passfile`, `service`, `password`) with effective identity evidence from `parse_dsn`, missing object-store/Basins roots, unsafe work/log roots, and no calls into seed/run/publish helpers.
  Evidence floor: focused autopipeline tests prove missing direct ingest role, missing/unsafe DB URL, username-less DB URL that could otherwise borrow ambient libpq identity, username-without-password DB URL that could otherwise borrow ambient libpq credentials, display-readonly role leakage, readonly/display-looking DB identity, libpq query override blockers before work, missing object-store/Basins/work/log roots, and credential-bearing errors return rc=2 structured JSON with stable blocker codes before `_basin_seeded`, `_already_ingested_runs`, `_seed_basin`, `_process_run`, or `_publish_display_runs` are called; wrapper tests prove a preflight-blocked rc=2 exits without running coverage backstop/post-hooks.
- [x] 2.3 Add role/evidence fields to node-27 ingest summaries so operators can distinguish data-plane ingest health from display API health.
  Evidence: success/failure summaries now include `schema`, `status`, `return_code`, `ingest.role=node27_data_plane_ingest`, stage shape, config source, preflight DB host/port/database evidence with `password_present: true|false` but without password material, path evidence, and explicit display-health separation note; tests assert ready summary shape and redaction.
  Evidence floor: success and failure summaries contain data-plane ingest role, stage shape, config source, object-store root, Basins root, work/log roots, DB host/port/database/password-presence evidence without secrets, discovered/processed/skipped counts, final return code, and an explicit statement that display API health is separate.
- [x] 2.4 Add tests covering preflight-before-seed, missing env, basin seed failure isolation, one-run failure isolation, already-ingested skip behavior, and credential-safe logs.
  Evidence: `tests/test_node27_autopipeline_preflight.py` covers preflight-before-work, wrapper missing env, wrapper rc=2 no-backstop, strict env-file sourcing that ignores ambient `DATABASE_URL`, `NHMS_NODE27_INGEST_ROLE`, and libpq credential env unless explicitly overridden, wrapper argv DSN non-exposure, seed registry import subprocess argv non-exposure with `DATABASE_URL` supplied only via env, display env rejection before source, direct-entry missing ingest role, non-0600/symlink/source-failed ingest env blocking, username-less DB URL blocking before libpq ambient `PGUSER` can be used, username-without-password DB URL blocking before ambient `PGPASSWORD`/`PGPASSFILE` can be used, libpq query override blocking, basin seed failure isolation, one-run failure isolation, already-ingested skip, wrapper static contract, and credential-safe serialized output/logs. Verification passed: `uv run pytest -q tests/test_node27_autopipeline_preflight.py tests/test_node27_autopipeline_handoff.py tests/test_node27_mirror_forcing.py tests/test_redaction.py` (71 passed), `uv run ruff check .`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, `bash -n scripts/node27_autopipe_cron.sh`, and `git diff --check`.
  Evidence floor: focused tests cover preflight-before-seed no-op, wrapper/env missing block with no Python/backstop execution, strict env-file role ownership and libpq credential clearing, child argv with no writer DSN, seed subprocess argv with no writer DSN while env contains `DATABASE_URL`, basin seed failure isolation with unrelated basin continuation, one-run failure isolation after valid preflight, already-ingested skip without per-run work, and serialized stdout/stderr/log evidence that contains no DB password/token strings; blocker assertions include missing direct ingest role, missing `DATABASE_URL`, malformed/unsafe DB URL, username-less DB URL, username-without-password DB URL, forbidden libpq query overrides, display-readonly role leakage, readonly/display-looking DB identity, missing object-store root, missing Basins root, unsafe work root, and unsafe log root.
- [x] 2.5 Capture a node-27 live receipt showing ingest writer checks and display API `display_readonly` checks remain separate.
  Evidence: node-27 live receipt at `artifacts/stabilize-data-compute-plane-handoff/issue-646-node27-20260622T210205Z` on SHA `4f76eb148a4fcc23d380badd6696131fc27accbd` records wrapper missing-env fail-closed (`wrapper_missing_env_rc=2`, `INGEST_ENV_MISSING`, no Python/backstop), on-demand ingest preflight ready (`ingest_role=node27_data_plane_ingest`, DB host `127.0.0.1`, port `55432`, database `nhms`, username class `writer_candidate`, `password_present=true` without password material), display runtime `/api/v1/runtime/config` as `service_role=display_readonly`, `display_readonly=true`, `control_mutations_enabled=false`, `slurm_routes_enabled=false`, and `separation=true`; receipt secret scan for `nhms_dev|writer-secret|readonly-secret|receipt-redacted-secret|token=|password=` returned clean.
  Evidence floor: node-27 receipt records git SHA, command(s), redacted ingest preflight output from the actual wrapper or on-demand entry, display API `/health` or runtime evidence showing `display_readonly`, DB identity separation without secrets, and artifact/log paths under node-27 local evidence storage.

## 3. Production Topology Contract

- [x] 3.1 Update current operational docs so node-22 is compute/artifact producer, node-27 is active DB/ingest/display host, and historical node-22 DB material is explicitly non-current.
  Evidence floor: current docs/runbooks and role-boundary docs state node-22 is compute/Slurm/SHUD/artifact producer only; node-27 hosts active PostgreSQL `:55432`, data-plane ingest, display API, and frontend; any active mention of node-22 local PostgreSQL `:55433` or transitional node-22 mirror is labeled historical/do-not-connect/compatibility-only with explicit DSN and removal/sunset context; doc checks or grep evidence show no active node-22 DB writer instructions remain outside allowed historical/archive/compatibility context.
  Evidence: `docs/runbooks/current-production-ops.md`, `docs/governance/ROLE_BOUNDARY.md`, `docs/runbooks/two-node-deployment-overview.md`, `docs/runbooks/node-27-bringup-checklist.md`, `docs/runbooks/two-node-production-e2e-plan.md`, `infra/env/node27-ingest.example`, and `scripts/node27_autopipeline.py` now state node-22 is compute/Slurm/SHUD/artifact producer only, node-27 owns active PostgreSQL `:55432`, data-plane ingest, display API, and frontend, and node-22 local PostgreSQL `:55433` / transitional mirror use is historical, do-not-connect, compatibility-only, explicit-DSN, and sunset/removal-bound. `current-production-ops.md` also states missing node-27 ingest env blocks writer checks and must not fall back to `infra/env/display.env`.
- [x] 3.2 Add or extend static governance checks that flag active node-22 DB writer assumptions and display-env reuse for data-plane writer or mirror jobs.
  Evidence floor: static guard reports stable categories for active text that (a) describes node-22 as current NHMS DB writer, (b) mentions node-22 local PostgreSQL, `:55433`, or transitional mirror without historical/do-not-connect/compatibility-only/explicit-DSN/sunset wording, or (c) sources `infra/env/display.env` as data-plane writer or mirror authority; hard-gate mode exits nonzero for these categories; output contains no secret/token/password material.
  Evidence: `scripts/governance/audit_repo_entropy.py` now emits hard-gate families `production-topology-node22-db-writer`, `production-topology-node22-local-postgres`, and `production-topology-display-env-writer` with stable, credential-safe descriptions/recommendations. Local hard-gate verification reports `hard_gate_status=pass`, `hard_gate_failing_count=0`, `production_topology_findings=0`, and `exit_code=0` for the current repo.
- [x] 3.3 Update verification/oracle routing docs so local checks, node-27 live DB/display checks, and node-22 Slurm checks are not conflated.
  Evidence floor: `docs/runbooks/current-production-ops.md`, `docs/governance/ROLE_BOUNDARY.md`, `docs/runbooks/two-node-deployment-overview.md`, and `openspec/project-profile.md` distinguish local lint/unit/OpenSpec checks from node-27 live DB/display/frontend receipts and node-22 Slurm scheduling checks; node-27 remains the data/display oracle and node-22 remains the Slurm scheduling oracle; local-only checks are not presented as substitutes for required node-27 live receipts.
  Evidence: `docs/runbooks/current-production-ops.md`, `docs/governance/ROLE_BOUNDARY.md`, `docs/runbooks/two-node-deployment-overview.md`, `docs/runbooks/node-27-bringup-checklist.md`, and `openspec/project-profile.md` now route local lint/unit/OpenSpec and frontend build checks separately from node-27 live DB/ingest/display/frontend receipts and node-22 Slurm scheduling/SHUD compute receipts.
- [x] 3.4 Add focused tests or scripted checks for the new topology drift guardrails, including positive fixtures for active node-22 writer/display-env-writer drift and negative fixtures for historical/archive/compatibility-only text and display API readonly env use.
  Evidence floor: focused tests cover positive fixtures for current node-22 DB writer drift, unmarked `:55433`/node-22 local PG/transitional mirror drift, and `infra/env/display.env` writer/mirror drift; negative fixtures cover historical/archive/superseded/compatibility-only node-22 DB text, explicit transitional mirror DSN wording with sunset/removal context, display API readonly env usage, archive/generated scope exclusion, bounded active-surface scanning, deterministic finding categories, hard-gate exit code, and credential-safe output.
  Evidence: `tests/test_entropy_audit_script.py` covers active node-22 DB writer including terse/Chinese/bare-`22`/active-primary/wrapped Markdown idioms and node-27 reads node-22 active-primary false-negative regression, unmarked node-22 local PostgreSQL/`:55433`, unmarked transitional mirror, display-env writer/mirror prose authority, indirect source, normalized path variants, separated/far source-to-command association, and prohibition-then-source positive fixtures; it also covers historical/archive/non-archive superseded/compatibility-only, explicit negative node-22 DB access/querying wording, explicit transitional mirror DSN with sunset/removal context, display-readonly env with no writer credentials, active OpenSpec scanning with archive exclusion and no generic Requirement/Scenario allowlist, local allowlist boundaries, infra README scan/banner behavior, current-repo hard-gate regression, candidate-only context extraction, hard-gate exit, and credential-safe output. `tests/test_role_boundary_static.py` asserts current docs/profile state node-27 active DB/ingest/display/frontend ownership, node-22 compute/Slurm/SHUD/artifact-only routing, and local checks not replacing node-27 live receipts. Verification passed: `uv run pytest -q tests/test_entropy_audit_script.py tests/test_role_boundary_static.py` (235 passed), `uv run ruff check .`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, topology hard-gate summary (`hard_gate_status=pass`, `hard_gate_failing_count=0`, `production_topology_findings=0`, `exit_code=0`), and `git diff --check`.

## 4. End-to-End Evidence

- [ ] 4.1 Run focused local tests for the forcing handoff, node-27 ingest boundary, and topology guardrails.
- [ ] 4.2 Run `uv run ruff check .`, `openspec validate stabilize-data-compute-plane-handoff --strict --no-interactive`, and relevant docs lint.
- [ ] 4.3 Fast-forward node-27, run the live ingest/display receipt, and record qhh/heihe evidence paths without secrets.
