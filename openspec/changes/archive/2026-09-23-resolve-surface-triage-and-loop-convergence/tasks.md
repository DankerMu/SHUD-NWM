# Tasks — `.resolve()` surface triage + loop convergence (#2452 #2453 #2454)

## Risk Packs

- [x] File IO / path safety — **selected**: canonicalisation of scheduler roots and census output paths; symlink loops. Covered by 1.x, 2.x, 3.x.
- [x] Public API / CLI / script entry — **selected**: `census-job-id-scope` click + argparse exit contract. Covered by 2.x.
- [x] Error handling / partial outputs — **selected**: typed refusal before census (zero writes); dead handlers on the pin. Covered by 2.3, 3.2.
- [x] Schema / columns / field names — **selected**: scheduler root-check `check` key set and blocker code for loop roots. Covered by 1.2, 1.4.
- [x] Legacy compatibility — **selected**: non-loop root-check verdicts, existing census codes, realpath guard and its clause-2 exemption unchanged. Covered by 1.5, 2.5, 4.4.
- [x] Documentation / migration notes — **selected**: ADR 0009 已知限制 2 and 3. Covered by 4.5.
- [ ] Concurrency / shared state — not selected: no locking/ordering change (ADR 0009 已知限制 3's single-writer premise is untouched).
- [ ] Auth / permissions, Config / project setup, Resource limits, Release / packaging — not selected.

## 0. Setup

- [x] 0.1 Branch `feat/issue-2452-2453-2454-resolve-surface` from `origin/master`. Locate sites by symbol; issue line numbers are from the #1627 branch.
  - Worked in place on the pre-created branch, originally based on `17fa9f835`; the branch has since been rebased onto origin/master `327df2710`. `git diff 17fa9f835 327df2710 -- services workers packages apps` is empty, so the §Triage counts taken on `17fa9f835` stand.
- [x] 0.2 Large-file guard: any staged file over 1000 lines absent from `.large-file-guard.json` `exclude` (e.g. `scheduler_evidence.py` 1122, `chain_runtime_utils.py` 1163, `real_backend.py`, `file_orchestration_migration.py`, `slurm_validation.py`, `tests/test_production_scheduler.py`) is added to `exclude` before commit. A new test module is routed in `scripts/select_ci_tests.py` with a routing pin.
  - Added to `.large-file-guard.json` `exclude`: `services/orchestrator/file_orchestration_migration.py` (1817), `tests/test_file_orchestration_migration.py` (2161); every other modified file over 1000 lines was already excluded.
  - Routing (`scripts/select_ci_tests.py`): `RESOLVE_SURFACE_GUARD_TEST` rides the family guard's supplemental loop over `PATH_CANONICALIZATION_FAMILY_GUARD_ROOTS`; `tests/test_scheduler_root_check_loop_convergence.py` joins `SCHEDULER_IMPORTER_TESTS` and the `services/orchestrator/**` list (closes its `scheduler.py` / `__init__.py` / `scheduler_runtime_roots.py` / `path_modes.py` importer gaps). Pins: `tests/test_select_ci_tests.py::test_resolve_surface_guard_scans_the_family_guard_walk_and_rides_its_roots`, `::test_root_check_loop_convergence_suite_routes_from_every_module_it_pins`. The new rider is additive, so the 27 existing exact-selection pins that listed the family guard now list the new guard too (count pins: GitHub-output `count` 2 → 3, direct-grid `+ 3` → `+ 4`, broad-orchestrator 86 → 88 targets, copyback-guard 17 → 18, scheduler_runtime 28 → 29). The full `uv run pytest -q tests/test_select_ci_tests.py` gives **778 passed**.
- [x] 0.3 Every new-behaviour test is shown red on pre-change source (on 3.11), then green; record both inline below.

  - One red pass, 3.11.14, every production file of this change reverted to `HEAD` (`scheduler_runtime_roots.py`, `journal_scope_census.py`, `chain_runtime_utils.py`, `real_backend.py`, `file_orchestration_migration.py`, `slurm_validation.py`, `scripts/select_ci_tests.py`), new tests in place, then restored (diff md5 identical before/after): **57 failed, 36 passed**. The 36 are preservation/characterisation cells, not new behaviour: 6 pre-check cells (unchanged 7-key arm), 3 `[missing]` control cells of the four-site tests, the `[log_dir]` slurm cell (pre-refused on every interpreter), and in the new guard module 18 detector self-tests (test-side code) plus the 8 D5 pin cells (they measure the interpreter, not the repo). Excerpt:
    ```
    FAILED test_scheduler_root_check_loop_convergence.py (45 cells)  AssertionError: 'SCHEDULER_RO...T_UNSAFE_PATH' == 'SCHEDULER_RO..._ROOT_SYMLINK' / Extra items in the right set: 'symlink' 'allow_create' 'under_workspace'
    FAILED test_scheduler_journal_scope_census.py (5 cells)          FileNotFoundError: [Errno 2] / ValueError: embedded null byte / AttributeError (no OUTPUT_UNRESOLVABLE_MESSAGE)
    FAILED four-site tests [symlink_loop]/[log_file] (4 cells)       RuntimeError: Symlink loop from '.../bad-bin' | '.../workspace' | '.../7811_1.out'
    FAILED test_resolve_surface_guard.py::test_no_strict_resolve_sits_behind_an_oserror_only_handler
           - services/orchestrator/chain_runtime_utils.py::_python_runtime_export_lines line 1134
           - services/orchestrator/file_orchestration_migration.py::_prepare_rollback_retention_root line 501
           - services/production_closure/slurm_validation.py::_bind_slurm_log_path line 1583
           - services/slurm_gateway/real_backend.py::_python_runtime_export_lines line 2299
    FAILED test_select_ci_tests.py (2 pins)                          ImportError: cannot import name 'RESOLVE_SURFACE_GUARD_TEST' / does not select tests/test_scheduler_root_check_loop_convergence.py
    57 failed, 36 passed in 9.85s
    ```
  - Green, same set: 3.11.14 **93 passed**; 3.14.2 **91 passed** (the two selector pins are not part of the 3.14 set).
## 1. #2453 — `_scheduler_root_check` converges on loop input (design D1)

- [x] 1.1 Replace `path.resolve(strict=False)` and the `workspace_root` anchor `.resolve(strict=False)` with the strict-then-non-strict realpath helper; delete the `except RuntimeError` early-return arm; record the ADR 0009 disposition (marker + clause argument, incl. the `allow_create` arm) in the body.
  - Both canonicalisations go through the module's `_canonical_path` (strict `os.path.realpath`, one non-strict fallback); the `except RuntimeError` early return is deleted. The `except OSError` arm around the first call is **kept** with a one-line comment (reachable only by a readlink race inside the non-strict fallback, since `path` is absolute; fails closed with a typed blocker; corrected in 5.3: on ≤3.12 a deep symlink chain also reaches it, via `RecursionError`). The anchor's tuple keeps `RuntimeError` for `expanduser()`. Clause-1 argument (incl. `allow_create`) and the anchor's clause 3 are written in the body; `_canonical_path`'s reachability comment names `_scheduler_root_check` as its production caller. `_scheduler_root_check` itself calls no `realpath`, so the realpath guard's authority set and exemption list are unchanged.
- [x] 1.2 Parametrised matrix per design §Required evidence (4 shapes × 3 fields × raw + upstream-normalised string via the real upstream normaliser × both `evidence_safe_paths` settings) with the expected reason per cell stated there and the key set derived from the same field's non-loop symlink call, asserted by set equality. Plus the `allow_create` cells through `published_artifact_root` (blocked on both interpreters). Test comment cites ADR 0009 and the runtime-evidence spec for why `Path.resolve()` is not a loop predicate.
  - `tests/test_scheduler_root_check_loop_convergence.py::test_loop_root_check_reason_and_key_set_match_the_non_loop_assembly`: 4 shapes × 3 fields × {raw, normalised via `path_modes._config_path_preserve_final_component_for_mode`} × {db-backed, evidence_safe_paths} = 48 cells (24 per setting; the fixture's 36 counted three input strings, here each run sees raw + one normalised string and the normalised literal is pinned). Measured reasons equal the design's table in every cell: db-backed self-loop SYMLINK, `a/child` UNSAFE_PATH, raw `<missing>/../a` NOT_FOUND, raw `a/../a` UNSAFE_PATH, normalised `<missing>/../a` and `a/../a` → `<ws>/a` → SYMLINK; evidence_safe_paths raw `..` strings → pre-check UNSAFE_PATH (7 keys). Key sets derived from non-loop references (ordinary symlink; `<regular file>/child`; `real-dir/../real-dir` pre-check). `allow_create`: `::test_allow_create_published_root_loop_is_blocked_like_its_non_loop_sibling` (self-loop → SYMLINK, `a/child` → UNSAFE_PATH, key sets equal the non-loop sibling's, `allow_create` True).
- [x] 1.3 One end-to-end case through `ProductionSchedulerConfig` + `_scheduler_lock_evidence_root_preflight` with a self-loop `lock_root` → `SCHEDULER_ROOT_LOCK_ROOT_SYMLINK` and the 10-key set on the running interpreter.
  - `::test_self_loop_lock_root_reports_symlink_with_the_full_key_set`: blocked, `[SCHEDULER_ROOT_LOCK_ROOT_SYMLINK]`, key set == the issue's measured 3.13 set (10 keys incl. `symlink`, `allow_create`, `under_workspace`).
- [x] 1.4 Cross-interpreter receipt: run 1.2/1.3 under 3.11 and under 3.14 and paste both summaries; the key sets are identical. Do not rebuild the project `.venv` for this (use a separate `UV_PROJECT_ENVIRONMENT` or `--isolated`).
  - 3.11.14 (`uv run`): `tests/test_scheduler_root_check_loop_convergence.py` **51 passed**; 3.14.2 (`UV_PROJECT_ENVIRONMENT=<scratch>/venv314 uv run --frozen --python 3.14`, project `.venv` untouched and still 3.11.14): **51 passed**.
  - Receipt script over the same 48 cells printing `(setting, shape, field, spelling, normalised string with the tmp prefix masked, code, key count, sorted keys)`: the 3.11 and 3.14 outputs are **byte-identical** (`diff` empty, 48 lines each), so the normalised strings fed on both runs are byte-identical too (`<ws>/a` ×3 shapes, `<ws>/a/child`). Code tally per field: SYMLINK 8, UNSAFE_PATH 5 (+`unsafe_reason`, 10/11 keys), UNSAFE_PATH pre-check 2 (7 keys), NOT_FOUND 1.
- [x] 1.5 `uv run pytest -q tests/test_production_scheduler.py -k "root_preflight or runtime_roots or allowed_roots"` green; `realpath` family guard green.

  - `uv run pytest -q tests/test_production_scheduler.py -k "root_preflight or runtime_roots or allowed_roots"`: **66 passed, 2023 deselected** (3.11); same selection on 3.14: 66 passed. Realpath family guard green (see 4.4).
## 2. #2454 — census `--output` unresolvable is typed (design D2)

- [x] 2.1 Wrap both `os.path.realpath` calls in `_require_output_outside_root` in `except (OSError, ValueError)` → `CENSUS_OUTPUT_UNRESOLVABLE`; constant message; `error_type`/`output` details.
  - `CENSUS_OUTPUT_UNRESOLVABLE` / `OUTPUT_UNRESOLVABLE_MESSAGE` (constant, path-free); details `{error_type, output}`; the clause-2 comment marker stays in the body.
- [x] 2.2 Help: the code is listed in `CENSUS_JOB_ID_SCOPE_HELP` and `_OUTPUT_HELP` like the other three; the module's exit-contract docstring names it as a before-census failure.
- [x] 2.3 Test, parametrised over `_ENTRYPOINTS`: relative `--output` + deleted cwd → exit 1, stdout empty, stderr exactly one `CENSUS_OUTPUT_UNRESOLVABLE: <message>` line, no `Traceback` / `FileNotFoundError`; journal-root `_snapshot` unchanged. Restore cwd in a `finally`.
  - `tests/test_scheduler_journal_scope_census.py::test_relative_output_under_a_removed_working_directory_is_its_own_typed_code[click|argparse]`; cwd restored in `finally`. Red on 3.11 before: `FileNotFoundError: [Errno 2] No such file or directory` escapes both entrypoints.
- [x] 2.4 Same shape for an in-process NUL `--output`; no `ValueError` in stderr.
  - `::test_output_with_an_embedded_nul_is_the_same_typed_code[click|argparse]`; red before: `ValueError: embedded null byte`.
- [x] 2.5 Assert the new code differs from `CENSUS_OUTPUT_UNWRITABLE`; the clause-2 exemption entry and marker in the realpath guard are unmodified; `uv run pytest -q tests/test_scheduler_journal_scope_census.py` green.

  - `::test_unresolvable_output_code_is_listed_in_help_and_distinct_from_unwritable` (help lists, BEFORE-census sentence, message differs from UNWRITABLE's). `git diff tests/test_path_canonicalization_family_guard.py` is empty. Census suite: **40 passed** on 3.11 and on 3.14.
## 3. #2452 — dead handlers, guard, cross-version pin, triage (design D3–D5)

- [x] 3.1 Triage: AST census (same method as #2452: `ast.Call` with `func.attr == "resolve"`, `(module, qualified function)` dedup, four trees), counts on the day, filled into `design.md` §Triage with an ADR 0009 clause number or `无从句` and the 3.11 abort disposition per pair or per homogeneous group. Every `无从句` row dispositioned (changed, or accepted with a reason); if none, write "零错位站点". Include the 7 RuntimeError-wrapped sites' convergence verdict (#2453 lesson).
  - Filled in `design.md` §Triage: 152 pairs / 223 sites on the day, 25 row groups covering every pair (checked mechanically against a fresh census: 0 missing, 0 extra, 0 duplicates). 无从句 (after fix pass 1's relabel, 5.2): T8 (1 pair, render-only, **changed** in fix pass 1), T21 (7 pairs, render-only, accepted), T22 (3 pairs, remote-node log-URI audit, accepted) and T23 (2 pairs, URI-string policy classifier, accepted) — 13 pairs; T1 and T2 relabelled clause 1. The 7 `RuntimeError`-wrapped pairs: RT-3/4 diverged (fixed by #2453), the rest converge (RT-7 differs only in the `error_type` value).
- [x] 3.2 Fix the four dead handlers (`chain_runtime_utils.py::_python_runtime_export_lines`, `real_backend.py::_python_runtime_export_lines`, `file_orchestration_migration.py::_prepare_rollback_retention_root`, `slurm_validation.py::_bind_slurm_log_path`) to also catch `RuntimeError` with the same outcome as the `OSError` arm; one real-symlink-loop test per site (red on 3.11 before the fix); for `_bind_slurm_log_path` the loop is in `log_dir` and the non-strict pair sharing its `try` gets the D6 decision recorded.
  - Tests (each `[missing]` control + `[symlink_loop]` cell): `tests/test_orchestration_chain.py::test_template_export_lines_skip_an_unresolvable_venv_bin_candidate`, `tests/test_real_slurm_gateway.py::test_rendered_template_skips_an_unresolvable_venv_bin_candidate`, `tests/test_file_orchestration_migration.py::test_rollback_retention_root_refuses_an_unresolvable_workspace_typed` (private seam: the public launcher refuses a loop workspace earlier), `tests/test_production_slurm_validation.py::test_validate_slurm_submit_blocks_a_symlink_loop_log_on_every_interpreter[log_file|log_dir]` via `slurm_validation.main`. Loop cells red on 3.11 before (`RuntimeError`), green on 3.11 and 3.14. D6 = converge (design §Triage). Deviation: a loop **in `log_dir`** never reaches the strict call (lexically normalised dir + `_refuse_symlink_components` lstat walk refuse it first on every interpreter), so the red cell for this site is the loop **log file**, which exercises the same handler tuple.
- [x] 3.3 New `.resolve()`-surface guard per design D4 (strict calls only; non-strict excluded with the reason), asserting an empty violator set, with detector self-tests on synthetic sources; its docstring states why the realpath `strict=True` criterion is not reusable and what the guard cannot judge.
  - `tests/test_resolve_surface_guard.py::test_no_strict_resolve_sits_behind_an_oserror_only_handler` + 18 detector self-tests (strict in `except OSError` flagged; tuple / `IOError` / `EnvironmentError` / `builtins.OSError` / module-constant tuple flagged; non-literal and positional `strict` flagged; `(OSError, RuntimeError)` not flagged; enclosing `RuntimeError`/`Exception`/`BaseException`/bare coverage not flagged; subclass-only not flagged; non-strict and no-argument calls not flagged; nested `def` / handler-body calls not attributed to the outer `try`). Scan on pre-change source returns exactly the fixture's four sites (red excerpt in 0.3).
- [x] 3.4 Cross-version pin per design D5's table; receipts under 3.11 and 3.14 recorded here; any cell that disagrees with the table is a reported deviation.
  - `tests/test_resolve_surface_guard.py::test_resolve_and_realpath_loop_behaviour_matches_the_pinned_table` (8 cells, branching on `sys.version_info >= (3, 13)`): **8 passed on 3.11.14 and 8 passed on 3.14.2**. Measured beforehand on 3.11.14 / 3.12.12 / 3.13.3 / 3.14.2 with `uv run --no-project`: every cell matches design D5; the two `(measure)` cells are `FileNotFoundError`, `errno == ENOENT` on all four (an `OSError` subclass, as the table allows).
- [x] 3.5 Any `无从句` site changed by 3.1 gets its own test; if a production-scheduling-path site is fail-open (all clauses fail), flag it in the deviation report (#2452 re-pricing trigger).

  - One `无从句` pair changed: T8 `scheduler_evidence.py::root_evidence_item` (fix pass 1, see 5.1 for its test). No production-scheduling-path pair is fail-open. Reported, not changed: T9 `scheduler_lease.py::_open_lock_parent_directory` pre-empts its own `UnsafeSchedulerLockError` channel on 3.11 (follow-up #2582).
## 4. Verification

- [x] 4.1 `uv run ruff check .` clean.
  - `uv run ruff check .`: All checks passed.
- [x] 4.2 `openspec validate resolve-surface-triage-and-loop-convergence --strict --no-interactive` valid.
  - `Change 'resolve-surface-triage-and-loop-convergence' is valid`.
- [x] 4.3 Focused suites green locally: realpath guard, new guard, census, scheduler `-k` selection, tests of the four sites' modules, any module changed by the triage.
  - 3.11: realpath guard + new guard + root-check + census **128 passed**; four sites' modules (`test_real_slurm_gateway.py`, `test_file_orchestration_migration.py`, `test_production_slurm_validation.py`) **432 passed**, `test_orchestration_chain.py` **489 passed, 1 skipped**. 3.14: the same four focused modules 128 passed; the four site modules 921 passed, 1 skipped.
- [x] 4.4 `tests/test_path_canonicalization_family_guard.py` green and its exemption list unchanged except for what D1 strictly requires (stated in the deviation report).
  - Green; `git diff tests/test_path_canonicalization_family_guard.py` empty (criterion, exemption list and the census clause-2 entry byte-unchanged; D1 needed no exemption).
- [x] 4.5 ADR 0009: 已知限制 2 rewritten to point at #2452's triage and the new guard (closed); 已知限制 3's last sentence points at #2454 (closed).
  - 已知限制 2 rewritten (points at the §Triage and `tests/test_resolve_surface_guard.py`, closed); 已知限制 3's last sentence points at #2454 (closed); 子族定性 records #2453 closed; the four `scheduler_runtime_roots.py:<line>` anchors this change would have rotted are rewritten as symbol anchors.
- [x] 4.6 node-27 real-DB pytest receipt at the PR head for the focused suites (orchestrator-side oracle). — node-27 @7375960ed, isolated worktree, Python 3.11.15, disposable scratch PG (nhms-db image), NHMS_RUN_INTEGRATION=1, 11 suites: 4567 passed, rc=0 (`/home/nwm/tmp/oracle-batchF/run-7375960eda33bf078738ee48ff9f932e64ca8096.log`).
- [x] 4.7 CI green on the PR head (or red with the cause recorded). — CI run 35834186827 @7375960ed: Unit Tests, SQL Migration Dry Run, Markdown Lint success.
## 5. Fix pass 1 (PR #2581 review)

- [x] 5.1 [O1] `scheduler_evidence.py::root_evidence_item` (T8) renders through `scheduler_runtime_roots._canonical_path(_expanduser_or_verbatim(value))` (function-local import; `scheduler_runtime_roots` → `scheduler` → `scheduler_evidence` would cycle at module level). No direct `realpath` call, so the realpath authority set / exemption list are unchanged; the `_canonical_path` reachability comment names the new render-only caller.
  - Test: `tests/test_scheduler_root_check_loop_convergence.py::test_run_once_with_a_loop_lock_root_writes_preflight_blocked_evidence[self_loop-SYMLINK|under_loop-UNSAFE_PATH]` — `ProductionScheduler.from_env(ProductionSchedulerConfig()).run_once()`, real preflight, loop `lock_root` → no exception, `preflight_blocked`, artifact under the evidence root with the single `SCHEDULER_ROOT_LOCK_ROOT_<REASON>` blocker and `resolved_runtime_roots.lock_root.path` == the configured spelling.
  - Audit of the other `.resolve()` sites on this `run_once` path (T7 `reserve_pre_execution_evidence` / `write_evidence`, `artifact_path.parent.resolve()`), measured on 3.11.14 and 3.14.2 after the fix: loop `lock_root` (self / below) → `preflight_blocked` + artifact on both; loop `evidence_root` (self / below) → `ProductionSchedulerConfig()` raises the typed `ValueError("production scheduler evidence_dir must not resolve through a symlink loop: ...")` at construction on both, identical; a blocked `evidence_root` also makes `write_prelock_blocked_evidence` return before any write. Neither `artifact_path.parent.resolve()` is reached by a loop in this scenario; not changed.
- [x] 5.2 [S1] design.md §Triage: T1 (`Path(__file__)`) and T2 (`verify_directory_no_follow` product) → clause 1; T8 and T21 (render-only) → `无从句`; T8's disposition → changed (fix pass 1), T21's → accepted (render-only); `n/a` removed from the vocabulary; T9 links #2582; post-change counts 150 pairs / 220 sites. ADR 0009 已知限制 2: 5 → 13 pairs outside the clauses, one changed.
- [x] 5.3 [C-N1] `_scheduler_root_check`'s kept arm catches `(OSError, RecursionError)`; `RecursionError` → `UNSAFE_PATH` with the 8-key early-return `check`; comment corrected (the arm is reachable by input on ≤3.12). Measured first: a chain of 1200 links raises `RecursionError` from `os.path.realpath` (both forms) and `Path.resolve()` on 3.11.14; 600 links do not; 3.14.2 resolves 1200. Test: `::test_a_symlink_chain_deeper_than_the_recursion_limit_is_blocked_not_raised[workspace_root|lock_root|evidence_root]` (`sys.getrecursionlimit() + 100` links): blocked on both; `UNSAFE_PATH` + 8 keys on <3.13, `SYMLINK` + the ordinary-symlink key set on ≥3.13. Spec delta (runtime-evidence-and-operations) scopes the "no interpreter-specific early return" SHALL to loop roots and adds the chain and full-pass scenarios.
- [x] 5.4 [S-N1] 0.1 and §Triage wording: rebased onto `327df2710`, counts stand.
- [x] 5.5 Red → green. Red: 3.11.14, `scheduler_evidence.py` and `scheduler_runtime_roots.py` restored to `HEAD` 9df75b858, new tests in place, then restored:
    ```
    FAILED ...::test_a_symlink_chain_deeper_than_the_recursion_limit_is_blocked_not_raised[workspace_root|lock_root|evidence_root]
           E   RecursionError: maximum recursion depth exceeded   (<frozen posixpath>:63)
    FAILED ...::test_run_once_with_a_loop_lock_root_writes_preflight_blocked_evidence[self_loop-SYMLINK]
           E   RuntimeError: Symlink loop from '.../workspace/loop-locks'          (scheduler_evidence.py:878 root_evidence_item)
    FAILED ...::test_run_once_with_a_loop_lock_root_writes_preflight_blocked_evidence[under_loop-UNSAFE_PATH]
           E   RuntimeError: Symlink loop from '.../workspace/loop-locks/child'
    5 failed, 51 passed in 1.85s
    ```
    The same `run_once` self-loop test on 3.14.2 against `HEAD` passes (3.13+ folds): the divergence was version-only.
  - Green: `tests/test_scheduler_root_check_loop_convergence.py` **56 passed** on 3.11.14 and on 3.14.2 (`UV_PROJECT_ENVIRONMENT=<scratch>/venv314 uv run --frozen --python 3.14`; project `.venv` untouched, still 3.11.14).
- [x] 5.6 Verification: `uv run ruff check .` All checks passed; `openspec validate resolve-surface-triage-and-loop-convergence --strict --no-interactive` valid; realpath guard + `.resolve()` guard + root-check module **93 passed** (3.11 and 3.14); `tests/test_production_scheduler.py -k "root_preflight or runtime_roots or allowed_roots or evidence"` **297 passed, 1792 deselected** (3.11 and 3.14); full `tests/test_production_scheduler.py tests/test_operator_action_listing.py` **2210 passed** (3.11); `tests/test_select_ci_tests.py` **784 passed** (no routing change: both touched modules already select the root-check module). `scheduler_evidence.py` (1135 lines) was already in `.large-file-guard.json` `exclude`.
