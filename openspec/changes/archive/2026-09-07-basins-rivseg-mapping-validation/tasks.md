## 1. Fixture, Provenance, and Scope

- [x] 1.1 Classify issue #1903 as expanded/high, record the upstream suggested level and minimal mergeable slice, add the missing SHUD required-file content-integrity risk axis/triggers to `openspec/project-profile.md`, and author full proposal/design/spec/task artifacts.
- [x] 1.2 Complete one read-only fixture review covering every risk pack, the Invariant Matrix, the six-owner/snapshot design, controlled fixture/oracle transitions, and the explicit PR #1875 exclusions; repair at most twice and pass strict OpenSpec validation.
- [x] 1.3 Freeze merge-base `27dc6aab5a0772c5489b04049eb483a660cf60d8`, old PR mapping commits `f596c423` / `3e4f0b78` / `c9282ef6`, and saved-patch SHA-256 `7dc9f7ef5ca2b70110e600f2d2d07ec67c474f015c6ee4055ac5a30838b92120` as provenance only; prove the final diff contains none of PR #1875's scheduler, autopipeline, frontend, calibration-removal, direct stable-spec, DB/runtime, or Slurm changes.

## 2. Requirement-Driven Tests and Batched RED

- [x] 2.1 Add `tests/test_basins_package_publication_rivseg.py` at the two public package seams, importing the shared helper at module scope and covering valid multi-reach, non-contiguous Index, exact optional headers, comments/blanks, `.sp.riv` trailing blocks, and genuine single-reach compatibility with independent literals.
- [x] 2.2 Cover missing `iRiv`, duplicate reach Index, overlong signed/unsigned integer tokens, invalid UTF-8, over-limit and post-`fstat` growth, invalid/non-positive counts, invalid header, missing columns, truncated blocks, and extra `.sp.rivseg` rows; each input must produce bounded `BASINS_RIVSEG_MAPPING_INVALID` at source identity and publication without a raw exception or unbounded content.
- [x] 2.3 Cover multi-reach/multi-segment collapse at both seams with exact `BASINS_RIVSEG_MAPPING_DEGENERATE` details: reach count, segment count, mapped-reach count, and mapped reach ID.
- [x] 2.4 Prove mapping refusal precedes environment-backed store construction and leaves no injected-store child, package lock/temp/object, manifest, or local output; prove a mapping path replaced after validation cannot publish or identify replacement bytes and any written mapping object equals the validated snapshot.
- [x] 2.5 With all new tests and fixture compatibility changes present, run one batched source-only RED against merge-base production behavior. Every new-behavior node must fail for the intended missing-validator/snapshot reason; use a uniquely tagged source-only stash, capture/apply its SHA rather than `pop`, drop only that exact entry immediately, and leave no `red-proof` stash or debug instrumentation.

## 3. Shared Validator and Snapshot Binding

- [x] 3.1 Add fixed mapping byte, leading-skip, integer-digit, and reported-ID limits plus an internal immutable mapping-snapshot subtype without changing the frozen `SourceFile` constructor or any existing facade/public signature.
- [x] 3.2 In the existing `basins_package_source_io.py` owner, open each canonical mapping through `_open_verified_source_file`; use the same descriptor for `fstat`, `limit + 1` read, strict UTF-8 decode, declared-row parsing, actual unique reach Index authority, `iRiv` subset, and degeneracy checks; keep every error payload bounded.
- [x] 3.3 In the existing inventory/object-store owners, preserve all signatures while deriving mapping planned entries from snapshot size/SHA and writing/verifying mapping objects from snapshot bytes; leave ordinary non-mapping final-open/final-byte behavior unchanged.
- [x] 3.4 In `publish_basins_package`, enumerate canonical sources without a store, validate, then instantiate/bind the environment store before forcing/preflight/lock/write; in `basins_package_source_identity`, validate the same source list before identity. Both calls use one facade-global validator binding and the six production owners stay below 1,000 lines.

## 4. Controlled Fixture and Structural Transitions

- [x] 4.1 Change only `tests.basins_package_helpers._make_valid_model` mapping literals to a complete genuine one-reach mapping; compare the current member to the merge-base blob by one exact replacement and prove all other helper source is unchanged.
- [x] 4.2 Change only `tests.basins_registry_import_helpers._make_valid_model` `.sp.rivseg` row construction to emit exactly `sp_segment_count` rows distributed deterministically over actual reach IDs. Preserve its signature, `.sp.riv`, shapefiles, forcing, all other 23 helper members, all 94 definitions / 96 nodes / 17 integration nodes, and the 8-direct / 11-routed / 8-database authority.
- [x] 4.3 Update `tests/fixtures/basins_registry_partition_oracle.json` only for the controlled `_make_valid_model` source/AST row, helper aggregate/self digests, and transition metadata. Add independent merge-base blob/allowlist and unrelated-member constructed RED evidence so a whole-helper oracle recapture cannot pass.
- [x] 4.4 Change only the two mapping payloads in `object_store_validation_fixture.write_synthetic_basins_fixture` to complete `{1,2}` reach and segment blocks. Preserve all other stable text hashes and shapefile semantics; retain the old two hashes as before-provenance and add exact new hashes plus unrelated-byte RED evidence.
- [x] 4.5 Extend `BASINS_PACKAGE_PUBLICATION_TESTS` to the exact sorted seven-owner set and helper consumers to the exact eight-owner set. Update tree-derived count/collectibility/exact-set/per-edge RED meta-tests and the stable structural requirement without weakening the frozen 80-definition / 88-node six-owner baseline.
- [x] 4.6 Verify the normalized QHH 5-reach/18-segment staging path, all seven registry partitions, QHH A/B/C, publication consumers, forcing identity, migration report, object-store validation, and scheduler registry consumers remain compatible; do not modify committed real/sample Basins bytes.

## 5. Local Evidence Floor

- [x] 5.1 Run focused rivseg tests plus all seven publication partitions and `tests/test_basins_package.py`; record exact pass/skip counts and verify the new suite collects nonzero nodes while the helper collects zero.
- [x] 5.2 Run the registry helper's exact eleven routed consumers, object-store facade/production suites, QHH partition oracle/meta-tests, registry partition oracle/meta-tests, reingest, and calibration exact-seven-entry oracle; record exact counts with integration skips distinguished from execution.
- [x] 5.3 Run `uv run pytest -q tests/test_select_ci_tests.py` and constructively remove each new publication-owner/helper-route edge to prove 7/8 RED, then restore it; prove controlled-transition mutations outside each allowlist RED.
- [x] 5.4 Run `uv run ruff check` on every changed Python path, strict OpenSpec change/all validation, relevant current Markdown lint, `git diff --check`, and the ordinary large-file hook on the exact non-empty staged set. Every changed/new non-excluded source/test/helper remains below 1,000 lines; `.large-file-guard.json` remains absent from the diff.
- [x] 5.5 Run the project-profile Python/shared-helper matrix plus the repository full pytest suite on the frozen final SHA. No source/test/spec/selector/oracle/CI condition may be weakened to silence a failure.
- [x] 5.6 Update only current publication-validation commands to list the seventh suite and current registry owners; preserve historical results, archive bytes, old heading anchors, and unrelated docs.

## 6. Remote Evidence and Delivery

- [x] 6.1 On node-27, use a detached worktree at the frozen implementation SHA and `/home/nwm/tmp`; run focused backend/publication/registry/object-store tests without DB unless a selected existing integration suite requires node-27's local `:55432`. If DB is used, isolate it and prove cleanup plus production identity unchanged. Do not access node-22 for this proof. Final receipt `20260906T131415Z_2629f6f14fe7` at implementation SHA `15568122a023e4bdfef8fb1dc89cbcc45d51bd7a`: DB-free `483 passed, 34 skipped`; isolated 28-node local-DB oracle `27 passed, 1 skipped`; owned DB/role/worktree/ref/secrets/scratch removed; production checkout/catalog/roles/container/display identity unchanged; public sensitive scan zero hits.
- [x] 6.2 On node-22, use only `/scratch/frd_muziyao/NWM/.venv/bin/python` before the maintenance window. Copy real HHE `.sp.riv` / `.sp.rivseg` into a disposable root, run DB-free read-only validator cases, prove valid/non-contiguous/multiblock behavior and invalid/degenerate zero-write behavior, then remove only owned disposable artifacts. Do not connect any DB, run `uv sync` or bare `uv run`, submit Slurm, modify production Basins, or delete quarantine backups. Final receipt `20260906T131406Z_174444b342d2` at implementation SHA `15568122a023e4bdfef8fb1dc89cbcc45d51bd7a`: original 43,799-reach/96,362-segment HHE bytes mapped only to reach `1` and were rejected as exact degenerate at both seams with zero write; disposable non-contiguous multiblock bytes succeeded; disposable missing reference failed exactly with zero write; source metadata/SHA stayed unchanged; no DB/uv/Slurm/production write; owned worktree/ref/root/driver/launcher removed; quarantine remained 55,908 files; public sensitive scan zero hits.
- [x] 6.3 Rebuild existing PR #1875 from the latest-master branch using exact `--force-with-lease` against each freshly observed remote head; publish deviations, review/verifier evidence, Chinese work summary, frozen-SHA local/remote receipts, and final non-draft CI. Final feature head `6b8aa74b914930ca9d4b34ed8082db911cd6f0fd`; non-draft CI `34070988955` and Governance Audit `34070988949` both succeeded at that exact SHA.
- [x] 6.4 Pass completion/oracle/branch-tip/pre-merge hard gates, obtain or consume the user's merge authorization, merge with a merge commit, archive this OpenSpec change, close review-gate state, append per-PR review-loop accountability, run the tracked loop audit, and verify post-merge master CI. PR #1875 merged as `40ba05efb5e6ca198f0b2037de40b39feea4fbba`; master CI `34072190555` and Governance Audit `34072190576` succeeded; archive `2026-09-07-basins-rivseg-mapping-validation`, gate close, feature accountability append, and tracked loop audit all completed.
