# Tasks

## Implementation

- [x] T1 Add the canonical `description` (design D2, verbatim) to the five schema properties: `recovery.invocation` (`:90`), `migration.first_invocation` / `second_invocation` (`:128-129`), `receipts.dry_run_invocation` / `enforce_invocation` (`:157-158`) in `schemas/timeseries_compression_live_evidence.schema.json`.
- [x] T2 Add one comment line at each verifier overwrite point — `scripts/node27_timeseries_compression_live_evidence.py` — the comment lines landed at `:3535`, `:3567`, `:3621`, immediately above the overwrites now at `:3536`, `:3568-3569`, `:3622-3623` — pointing at `scripts/node27_timeseries_compression_bundle_author.py:21-25`. Comments only; no logic edits.
- [x] T3 Rewrite `docs/runbooks/tier-node27-timeseries-storage.md:770-771` so the five keys read as mandatory, naming them, per design D4. The top-level key list at `:755-763` stays a top-level list.
- [x] T4 Guard test G1 (design D3) in `tests/test_node27_timeseries_compression_live_evidence.py`, reusing the schema already loaded at `:45`. The slot list is **derived** from the schema — every property declared under a `properties` map whose name ends in `invocation`, with no dependence on how the artifact-ref shape is reached — and asserted to equal the five known slots.
- [x] T5 Oracles G2 and G3 (design D3) in the same module: the enforcement boundary (well-formed ref at a nonexistent path fails closed; four-key-with-scalar / string / `null` escapes closure and still qualifies; a non-ref mapping that *wraps* a well-formed ref still fails closed on that nested node) and manifest retention plus dedup (five distinct refs → five manifest nodes distinct from the ledger; five identical ledger refs → one node). Every new scenario in the spec delta has an oracle; none borrows credit from `test_legacy_authored_invocations_do_not_contribute_to_v3_truth`, which pins only the semantics half.

## Evidence Floor

- [x] E1 Local: `uv run ruff check .`
- [x] E2 Local: `uv run pytest -q tests/test_node27_timeseries_compression_live_evidence.py tests/test_node27_timeseries_compression_supervisor.py tests/test_node27_timeseries_compression_capture.py` green, including the new guard test and the untouched `test_legacy_authored_invocations_do_not_contribute_to_v3_truth`.
- [x] E3 Local: `uv run pytest -q tests/test_migrations.py -k terminal_state || true` is not the oracle — instead prove the annotations are inert for validation: `uv run python -c "from packages.common.compression_terminal_state import CANONICAL_SCHEMA; import json; json.loads(CANONICAL_SCHEMA.read_text())"` plus the committed-receipt/terminal-state tests that already exercise `validate_terminal_document` run green in E2.
- [x] E4 Local: `openspec validate compression-invocation-slot-truth-source --strict --no-interactive`.
- [x] E5 Fence self-evidence from `git diff master...HEAD`: `scripts/node27_timeseries_compression_live_evidence.py` diff is exactly three added comment lines; `:1148` (#1261 ruling) unchanged; `_validate_exact_command_argv` / `_concrete_argv` unchanged; schema `database_audit_proof` `{"const": false}` pins unchanged; `schemas/examples/timeseries_compression_live_evidence.example.json` not in the diff; no new launcher/interpreter gate.
- [x] E6 Carrier sweep, **by reading**, re-run after every fix commit. No carrier of this change may *assert* a formulation design D2 archives as refuted. D2 is the whole list — it is not restated here, so falsifying another draft never requires editing this item. Citations are not assertions: D2's refutation record (numbered and unnumbered), the proposal's refutation paragraph, this item, and any test docstring that names a draft as the thing its assertion refuted. **Paraphrases count**, and they are the hard part: a re-wording that says the same false thing is the same defect, and the two that got furthest — `design.md`'s "is never closure-checked" and `proposal.md`'s "no code interprets their content" — contain none of the archived strings and were caught only by a reviewer reading. An executable form of this item was attempted and withdrawn; see design D7 for why, and do not claim any mechanical sweep has run. Plus: every carrier design D2 lists as Tier 1 carries the canonical sentence byte-for-byte (whitespace-normalized where markdown wraps it), and each of the three verifier comments — Tier 2, because `pyproject.toml` sets `line-length = 120` with `E` selected and the 1032-character sentence would trip E501 while the D5 fence forbids wrapping the verifier diff past three lines — names `execution.ledger` as the source the slot is re-derived from and points at `scripts/node27_timeseries_compression_bundle_author.py:21-25`.
- [ ] E7 CI green on the final head.
- [ ] E8 PR description records 方案 a and its reason, and does not present the change as adding launcher/interpreter identity validation (#1261 fence).

## Not required

- No node-27 live receipt. 方案 a is pure-local by the issue's own "执行成本澄清"; a live receipt is a 方案-b cost only.
