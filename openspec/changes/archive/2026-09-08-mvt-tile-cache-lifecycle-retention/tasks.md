# Tasks: mvt-tile-cache-lifecycle-retention（issue #2032）

Fixture level: expanded（repair intensity: high）
Project profile: NHMS（`openspec/project-profile.md`）

## 0. 实测与裁定（orchestrator，已完成）

- [x] 0.1 node-27 实测 receipt `docs/runbooks/receipts/2026-09-08-issue-2032-mvt-cache-measurement-node27.md`：两条 SQL、`du`、`.pbf`/`.locks` 计数、`df -h / /home`、`df -i /home`、最新 vs ≥7 天（含压缩 chunk 内 08-25/08-27）冷耗时、424 耗时、稳态投影。
- [x] 0.2 裁定：文件侧在涨 → A 方案；Part B 锁 unlink 采纳；424 负缓存与入口收窄不采纳（理由见 design.md Non-Goals / receipt 第 8 节）。

## 1. 回收 runner（implementer）

- [x] 1.1 新建 `scripts/node27_mvt_cache_retention.py`（仅 stdlib；`from __future__ import annotations`；不 import `scripts.node27_raw_retention`）：`config_from_env`（`NHMS_MVT_FILE_CACHE_DIR`、`NODE27_MVT_CACHE_RETENTION_DAYS` 默认 14、`_ENABLED` 默认 true、`_PLAN_ONLY` 默认 false；CLI `--summary-path`、`--retention-days`、`--reference-time`）；`collect_targets(root, cutoff)` 只枚举 `<root>/<hh>/` 与 `<root>/.locks/<hh>/`（`hh` 匹配 `^[0-9a-f]{2}$`、非 symlink 目录），文件名匹配 `^[0-9a-f]{64}\.pbf$` / `^\.[0-9a-f]{64}\.pbf\.[0-9]+\.tmp$`（仅 `<hh>/`）/ `^[0-9a-f]{64}\.lock$`（仅 `.locks/<hh>/`），`lstat` 为常规文件，`st_mtime < cutoff`；`run_retention` 对 `.lock` 用 `os.open(path, os.O_RDONLY | os.O_NOFOLLOW | os.O_CLOEXEC | os.O_NONBLOCK)`（**禁止 `O_CREAT`**；`ENOENT` → `skipped[already_gone]`，`ELOOP`/`EMLINK` → `skipped[not_regular_file]`）取 fd 后**先 `fstat` 判 `S_ISREG`**（非常规 → `skipped[not_regular_file]`，在 flock 之前）再 `flock(LOCK_EX|LOCK_NB)`（busy → `skipped[lock_held]`），拿到后比较 `os.fstat(fd)` 与 `os.lstat(path)` 的 `(st_dev, st_ino)`（不等或 `ENOENT` → `skipped[already_gone]`，不 unlink——路径已被活 miss 重建），相等才 unlink 再 close；`.pbf`/`.tmp` 直接 `os.unlink`，`FileNotFoundError` → `skipped[already_gone]`，其它 `OSError` → `failed[]`（`path`、`kind`、`error`、`error_type`）。summary 字段按 spec Requirement 2：`skipped[]` 条目 `{path, kind, reason}`（lane 级 `kind: null`），`planned[]/deleted[]` 的 `mtime` 为 RFC3339 UTC 秒精度字符串，顶层 `precip_root_untouched: "<root>/precip"`；`--summary-path` 未给时 JSON 打到 stdout。preflight（**显式偏离兄弟**：不复用 `_env_int` 的静默回落，`args.retention_days` 用 `is None` 判断而不是 `or`；symlink 检查用 `Path(root).expanduser().is_symlink()`（lstat，**在任何 `resolve()` 之前**——兄弟 `_safe_resolved_dir` 先 `resolve()` 再 `is_symlink()` 的分支是死代码，不得照抄），枚举也用未 resolve 的路径，与显示进程 `Path(root).expanduser()` 同口径）：`NHMS_MVT_FILE_CACHE_DIR` 未设/空、`expanduser()` 后非绝对、等于 `/`、不存在、symlink、非目录，`NODE27_MVT_CACHE_RETENTION_DAYS`/`--retention-days` 不是整数或 `< 1`，或 `--reference-time` 不是 RFC3339（`field: reference_time`, `reason: not_rfc3339`）→ 写 `preflight_blocked` summary（`blockers[]` 含 `field`/`reason`；同一 sink：`--summary-path` 给了写文件，否则 stdout）rc 2、零删除；`<root>/.locks` 缺失 → 锁 lane 跳过 `reason: locks_root_missing`（新建缓存根的正常状态），symlink/非目录 → `reason: locks_root_unsafe` + `detail: path_is_symlink|path_not_directory`（均为 lane 级 `kind: null` 条目），其它 lane 照常；所有 `pathlib`/`os` 探针包 `OSError`（参考 `node27_raw_retention._resolve_lane_root` 的注释与理由，注释里引用它）；**枚举失败进 `failed[]`**：根/`.locks`/`<hh>` 的 `scandir` 抛 `OSError` → `failed[]` 条目 `{path, kind: null, reason: enumeration_unavailable, error, error_type}`、其余目录照常、rc 1（plan-only 同样），单条目 `stat` 失败静默跳过；`.locks` 自身 `lstat` 抛错 → `locks_root_unsafe` + `detail: path_unavailable`。`--summary-path` 是唯一 sink 参数：不读 env fallback、相对路径合法（wrapper 自己带 env override 并总是传 flag）。
- [x] 1.2 新建 `scripts/node27_mvt_cache_retention_once.sh`：env 文件存在/非 symlink/0600 检查、`set -a` source、`flock -n` 于 `${NODE27_MVT_CACHE_RETENTION_LOCK_PATH:-/tmp/node27-mvt-cache-retention.lock}`、log root `${NODE27_MVT_CACHE_RETENTION_LOG_ROOT:-/home/nwm/node27-mvt-cache-retention-logs}`、summary `mvt-cache-retention-<UTC stamp>.json`、`$REPO/.venv/bin/python` 缺失即 blocked rc 2、rc 原样透传。可直接裁剪 `node27_raw_retention_once.sh`，去掉 `scripts` namespace 的 importlib 检查（本脚本不 import 仓库包）。
- [x] 1.3 新建 `infra/systemd/nhms-node27-mvt-cache-retention.service`（`Type=oneshot`、`WorkingDirectory=/home/nwm/NWM`、`ExecStartPre=/usr/bin/mkdir -p /home/nwm/node27-mvt-cache-retention-logs` 在 `ExecStart` 之前、`StandardOutput=append:/home/nwm/node27-mvt-cache-retention-logs/systemd.log`、`StandardError=append:.../systemd.err`（对齐兄弟 unit 的形状，实现偏离记录 3）、`Environment=NODE27_MVT_CACHE_RETENTION_ENV_FILE=/home/nwm/NWM/infra/env/node27-mvt-cache-retention.env`）与 `.timer`（`OnCalendar=*-*-* 04:05:00 UTC`、`Persistent=true`、`WantedBy=timers.target`），形状对齐 `nhms-node27-raw-retention.{service,timer}`。
- [x] 1.4 新建 `infra/env/node27-mvt-cache-retention.example`：`NHMS_MVT_FILE_CACHE_DIR=/home/nwm/.cache/nhms/mvt`（逐字复制 `node27-raw-retention.example` 的「取显示进程实际值」警告）、`NODE27_MVT_CACHE_RETENTION_DAYS=14`、注释的 `_ENABLED`/`_PLAN_ONLY` 门、`LOG_ROOT`/`LOCK_PATH`，以及一条 jq 健康判据（`execution_mode == "production_execute"` 且 `finished_at` 新于 26 h 且 `failed` 为空）。

## 2. 锁生命周期（implementer）

- [x] 2.1 `services/tiles/mvt.py::tile_generation_lock`：新增模块级探针 `_lock_path_identity(path: Path) -> tuple[int, int] | None`（`os.stat` 的 `(st_dev, st_ino)`，`FileNotFoundError` → `None`）与 `_lock_file_identity(fd: int) -> tuple[int, int]`（`os.fstat`）；新增 `_open_live_lock_file(lock_path) -> IO | None`（每次尝试 = open `a+b` → `flock(LOCK_EX)` → 两探针比较；不等/`None` 则 close 后再试；`_TILE_LOCK_REACQUIRE_LIMIT = 8` 是**尝试总数**，即 `_lock_path_identity` 被调用的次数恰为 8 后返回 `None`，每条失败分支都已 close fd，比较阶段抛出的任何异常也 `except BaseException: close; raise`；docstring 写清耗尽的真实可达性（每次交接花掉等待者一次尝试、≥9 个同 key 跨进程竞争者、与 `NHMS_DISPLAY_WORKERS` 耦合，不是「外力持续替换」）；返回 `None` 时**不 unlink 路径**——它可能已是另一个进程的活锁，由 runner 按 mtime 兜底）；`finally` 顺序固定为 `unlink(lock_path)`（吞 `FileNotFoundError`，其它 `OSError` 记 warning）→ `LOCK_UN` → close；返回 `None` 时 `logger.warning` 后无锁 `yield`。**`_LOCAL_TILE_LOCKS` 不改**（已是 `WeakValueDictionary`）。`lock_path is None`（未配置缓存目录）分支行为不变。
- [x] 2.2 用注释把「先 unlink 后 LOCK_UN」的顺序理由钉在代码里（design D3），并在 `_file_cache_path` / `_file_cache_lock_path` / `_write_file_cache` 旁注明「路径形状被 `scripts/node27_mvt_cache_retention.py` 的正则依赖」。

## 3. 测试（implementer，与实现同 PR；新行为测试先对改前源码跑红一次，报告里贴红/绿输出）

- [x] 3.1 新建 `tests/test_node27_mvt_cache_retention.py`：spec 五个 Scenario 逐一落地——混合树只删三种 aged 形状（含 fresh `.pbf`、aged `precip/IFS/2026060100/x.png`、`zz/` 非 hex 目录、`ab/notes.txt`、symlink 冒充 `.pbf`、目录冒充 `.pbf`、更深一层的 `ab/cd/<sha>.pbf`、`.locks/` 下的 `.pbf`、`<hh>/` 下的 `.lock`、`.locks` 为 symlink 指向根外目录时其下 `.lock` 都不是目标）；plan-only 同集合零删除；`--reference-time` 非 RFC3339 → rc 2；wrapper 用例（subprocess 跑 `node27_mvt_cache_retention_once.sh`，参照 `tests/test_node27_frontier_stall_alert.py` 的 `ENV_FILE_MODE_UNSAFE`/`ENV_FILE_SYMLINK_FORBIDDEN` 断言）：env 文件缺失 / symlink / 0640 → rc 2 且日志含 blocked 原因；另一进程持 `flock` → rc 0 跳过；runner rc 1 与 rc 2 原样透传；**重建竞态**：先对 aged `.lock` 取 fd，再 `os.replace` 另一个文件到该路径，然后调用 runner 的单目标 unlink 步骤 → `skipped[already_gone]`、替换文件仍在；`.locks` 缺失 → `locks_root_missing` 且 `.pbf` lane 照常；`lock_held`（用 `multiprocessing` spawn 子进程持 flock，或同进程另一 fd 持 `LOCK_EX|LOCK_NB`——注意同进程内 flock 由 open file description 区分，独立 `open()` 即可）；`already_gone`；根缺失/symlink/非目录/`days=0` → rc 2 零删除；`ENABLED=false` → disabled；summary 字段与 rc 表；unit 文件断言（`ExecStartPre` < `ExecStart`、append 路径、timer `Persistent=true`、env 模板含 `NHMS_MVT_FILE_CACHE_DIR=/home/nwm/.cache/nhms/mvt`）；**形状同源测试**：用 `services.tiles.mvt._file_cache_path` / `_file_cache_lock_path` 与 `_write_file_cache` 的 tmp 命名生成真实路径，断言 runner 的三条正则恰好匹配它们（改形状即红）；**镜像排除测试**：同一根上同时放 aged `precip/IFS/2026060100/` 与 aged `ab/<sha>.pbf`，`node27_raw_retention.collect_targets` 只列前者、本 runner 只列后者；**round-1 补充**：不可读 `<hh>`（chmod 0o000，euid 0 时 skip）→ `failed[enumeration_unavailable]` 一条、同级 `.pbf` 照删、rc 1（plan-only 亦 rc 1）；不可读根 → `failed[]` 含根、`.locks` lane `locks_root_unsafe/path_unavailable`、rc 1；锁 lane 分支各一例（collect 后 unlink → `already_gone`；换成 symlink/目录/FIFO → `not_regular_file`，FIFO 用例带线程超时证明 `O_NONBLOCK`）+ summary 级 `not_regular_file` 条目；cutoff 恰等样本存活（`>=`→`>` 变异即红）；相对 `--summary-path` 可写、env 变量不改 sink。
- [x] 3.2 新建 `tests/test_mvt_tile_generation_lock.py`：(a) producer 正常返回与抛异常两种情况下退出后锁文件不存在（新行为，改前红）；同一用例在 `pytest.raises` 作用域**之外**断言 `_LOCAL_TILE_LOCKS` 无该 key（**不绑定 `as excinfo`**，或断言前 `del excinfo`——traceback 会钉住 generator frame 的 `local_lock` 引用，让弱引用条目多活到 `gc.collect()`），并在 docstring 注明这是钉住既有 `WeakValueDictionary` 行为、改前即绿；(b) 跨进程争锁：`multiprocessing.get_context("spawn")` + 模块级 target，A 进入锁后经文件/管道 barrier 通知父进程，父进程再启动 B；B 进入锁时断言 A 的完成标记已存在（无重叠，用标记文件而不是 sleep 作 oracle）；两者退出后 `.locks/**` 为空；(c) inode 复核：B 阻塞在 A 的 inode 上、A unlink+释放后，B 最终持有的 fd 身份与路径当下的 `(st_dev, st_ino)` 相等（**不**断言与 A 的 inode 号不等——ext4 会把刚释放的 inode 号在同目录下一次 create 时原样发回，该 oracle 在 Linux 上是假红；实现偏离记录 1）；重开分支的确定性证据由钉住探针恰好 2 次调用的单进程用例提供；(d) 重试耗尽：monkeypatch `services.tiles.mvt._lock_path_identity` 恒返回与 fd 不同的 `(st_dev, st_ino)`（不打全局 `os.stat`）→ 探针恰好被调用 `_TILE_LOCK_REACQUIRE_LIMIT` 次后返回、块仍执行、`caplog` 有 warning，且用例前后 `os.listdir("/proc/self/fd")`（Linux）/ `os.listdir("/dev/fd")`（macOS）的 fd 计数相等（无 fd 泄漏）；(e) `NHMS_MVT_FILE_CACHE_DIR` 未设置 → 无文件锁分支行为不变；**round-1 补充**：(f) 顺序 oracle——monkeypatch `mvt.fcntl.flock` 在 `LOCK_UN` 时记录 `lock_path.exists()`，断言 `[False]`（交换 unlink/LOCK_UN 即红）；(g) 探针抛 `PermissionError` → 异常传播、fd 计数不变、路径可被新 `flock(LOCK_NB)` 取到——用 `pytest.raises(...) as caught` 并在 `caught` 存活期间断言（不 bind 时 traceback 随 with 块释放、refcount 替你关 fd、断言永真）；(a) 的正常返回用例同样断言 `key not in _LOCAL_TILE_LOCKS`。
- [x] 3.3 `scripts/select_ci_tests.py`（**必做**，不是条件句——fixture review 实测：`infra/env/node27-*.example` 现有 glob 只路由到 `test_node27_write_roles.py`/docker-runtime 套件，`infra/systemd/**` 每个单元都要独立 `PathTestRule`，`scripts/*_once.sh` 无规则时只 arm core-smoke fallback）：新增三条 `PathTestRule`——`scripts/node27_mvt_cache_retention_once.sh` → `tests/test_node27_mvt_cache_retention.py`；`infra/systemd/nhms-node27-mvt-cache-retention.service` 与 `.timer` → 同上；`infra/env/node27-mvt-cache-retention.example` → 同上——形状对齐 `nhms-node27-timeseries-retention.service` 那条带注释的规则；**第四条必做**：`services/tiles/mvt.py` 是 `GUARDED_MODULE_CLOSURES` 里的受守模块，`tests/test_select_ci_tests.py::test_guarded_module_rules_cover_their_non_gated_importer_closure` 要求其生产规则的 tests 元组覆盖每个顶层 import 它的套件——元守卫只会变红、不会自动路由，所以把 `tests/test_mvt_tile_generation_lock.py` 与 `tests/test_node27_mvt_cache_retention.py`（3.1 的形状同源测试顶层 import `services.tiles.mvt`）都加进 `services/tiles/mvt.py` 那条 `PathTestRule` 的元组；若任一套件顶层 import `apps.api.routes.hydro_display`，同样加进它的规则（或改为函数体内 import，元守卫忽略函数体 import——二选一并在报告里说明）。`scripts/node27_mvt_cache_retention.py` 由同名套件规则覆盖。用合成 diff 跑 `uv run python scripts/select_ci_tests.py` 实证五类路径各自选中，把输出贴进报告；`uv run pytest tests/test_select_ci_tests.py -q` 必须绿。

## 4. 文档（implementer）

- [x] 4.1 `docs/runbooks/display-readonly-live-mvt.md`：在文件缓存段后加「缓存回收」小节——runner/unit/env 名、三种形状、`precip/**` 由 raw retention 负责、锁文件自清理、receipt 链接。
- [x] 4.2 `infra/env/node27-raw-retention.example` 的「the MVT tile cache is a sibling directory here」句后补一句指向新 runner（保持原句不变）。

## 5. 验证（orchestrator）

- [x] 5.1 本地：`uv run ruff check .`；`uv run pytest tests/test_node27_mvt_cache_retention.py tests/test_mvt_tile_generation_lock.py tests/test_hydro_display_mvt_scaling.py tests/test_node27_raw_retention.py tests/test_select_ci_tests.py -q`；`openspec validate mvt-tile-cache-lifecycle-retention --strict --no-interactive`。
- [x] 5.2 node-27（merge 前，一次性 worktree `git worktree add /home/nwm/tmp/wt-2032 <PR head>`，不动活动树）：`TMPDIR=/home/nwm/tmp uv run --no-sync`（或该 worktree 自建 venv）跑 5.1 的 pytest；`NHMS_MVT_FILE_CACHE_DIR=/home/nwm/.cache/nhms/mvt NODE27_MVT_CACHE_RETENTION_PLAN_ONLY=true` 对真实缓存目录跑两次 plan-only：生产口径 14 天（预期 planned 0，显式写明）与 `--retention-days 3`（预期选中 09-04 批次 `.pbf`+`.lock`，`planned[]` 无任何 `precip/` 路径）；隔离 uvicorn :8090（**`--workers 2`**，单 worker 碰不到 flock 路径）+ 空缓存目录：一次冷 miss 后 `.locks/**` 为 0、`.pbf` 为 1；`xargs -P2` 同一瓦片两并发冷 miss → 两个 200、字节相同、结束后 `.locks/**` 为 0；424 请求后 `.locks/**` 为 0；**双实例同刻对照**（issue 验收「新旧两条全国流量路由仍 200 且字节不变」）：并排起 master worktree 实例 :8091（空缓存），对新路由 gfs/ifs 各一张 z4 与旧 5 段路由一张 z4 在两实例上各冷打一次，HTTP 码与 `ETag`（等价 `X-Tile-Checksum`，即 body sha256）逐一相等，字节数作为次要记录。receipt `docs/runbooks/receipts/<date>-issue-2032-mvt-cache-retention-node27.md`。
- [ ] 5.3 node-27（merge 后部署，同一 receipt 追加节）：活动树 `git pull --ff-only`；安装 env（0600）+ unit + timer；plan-only 首跑；`bash scripts/ops/start-display-api.sh` 重启；下一次 prewarm 后 `.locks/**` 计数不增；timer `list-timers` 可见。 **（2026-09-08 merge 后 deferred：活动树停在外部回滚分支 `hotfix/node27-rollback-pre-2073`=`5a86841c`、无 upstream、落后 master 165 提交且含 #2031 migration，unit 指向的 `scripts/node27_mvt_cache_retention_once.sh` 在该树不存在；恢复到 master 是运维决策，见 receipt §6；跟踪 issue #2162（依赖 #2145 的迁移部署，同一维护窗口）。）**

## Evidence Floor

本地 `uv run ruff check .` + 5.1 的 pytest 集合 + `openspec validate` 严格通过；node-27 5.2 receipt（plan-only 两口径 + 隔离 uvicorn 锁计数 + 并发 single-flight）；merge 后 5.3 部署节。

## Risk packs considered

- Public API / CLI / script entry: **selected** — 新 CLI runner + wrapper + systemd unit；瓦片路由行为不变（用 `tests/test_hydro_display_mvt_scaling.py` 全绿为证）。
- Config / project setup: **selected** — 新 env 模板、unit 安装步骤、与显示进程共享 `NHMS_MVT_FILE_CACHE_DIR` 的根相等前提。
- File IO / path safety / overwrite: **selected** — 删除面；形状白名单、两级固定深度、symlink/非常规文件拒绝、`lstat`、`precip/**` 构造性排除、`already_gone`、`lock_held`。
- Schema / columns / units / field names: **selected（回执 schema）** — summary JSON 字段与 `execution_mode` 三值、`kind` 枚举。
- Auth / permissions / secrets: not selected — 无凭据、无 DB；env 文件 0600 检查沿用。
- Concurrency / shared state / ordering: **selected** — unlink-before-release、inode 复核、有界重试、进程内 `WeakValueDictionary`（不改，测试钉住）、runner 侧 `LOCK_NB`、混合版本窗口。
- Resource limits / large input / discovery: **selected** — 枚举深度固定两级、目录名白名单，不做全树 walk；有界重试。
- Legacy compatibility / examples: **selected** — 旧代码对被 unlink 的锁文件的行为（重建）；`yd-display-api` 独立目录；raw retention precip lane 不变。
- Error handling / rollback / partial outputs: **selected** — rc 0/1/2、`preflight_blocked` 零删除、producer 抛异常仍 unlink、`ENABLED=false` / `PLAN_ONLY` 回滚门。
- Release / packaging / dependency compatibility: not selected — 仅 stdlib，无新依赖。
- Documentation / migration notes: **selected** — runbook 小节、env 模板、部署与回滚步骤、receipt。
- Domain packs（profile）: Published NHMS artifacts / display identity — **selected**（缓存 key 身份不变，删除不改 key 空间，只改寿命）；其余（Geospatial、Hydro-met series、SHUD、PostGIS/Timescale、Slurm、Providers、Manifest/QC）not selected — 不触及。

## Seams under test（上游声明，实现者不重议）

- `scripts/node27_mvt_cache_retention.py::collect_targets` / `run_retention` / `main`（CLI + env）。
- `services/tiles/mvt.py::tile_generation_lock`（公开上下文管理器）、`_open_live_lock_file`，以及可注入探针 `_lock_path_identity` / `_lock_file_identity`（重试耗尽测试只打前者）。
- 文件系统形状（`_file_cache_path` / `_file_cache_lock_path` / tmp 命名）作为两侧共享契约。
- systemd/env 文本文件（unit 文件断言）。

## Review focus

1. `finally` 里 unlink 与 `LOCK_UN` 的顺序，以及 inode 复核的比较键 `(st_dev, st_ino)` 与 `ENOENT` 分支。
2. runner 的三条正则是否与 `mvt.py` 的真实路径构造同源，且 `precip/**`、symlink、目录冒充、非常规文件全部不可达。
3. 跨进程测试是否真的走到了 flock（spawn，不是线程），oracle 是否是标记文件而非 sleep。
4. `_LOCAL_TILE_LOCKS` 保持 `WeakValueDictionary` 未被改写；`_open_live_lock_file` 在每条失败分支都关闭了 fd（无 fd 泄漏）。
5. 回执/rc 语义是否与 raw retention 一致（`already_gone` 不是 failed；`PLAN_ONLY` 也报 `status: completed`），且 preflight 的显式偏离（无静默回落）已落地。
6. runner 打开 `.lock` 的 flags 无 `O_CREAT`、带 `O_NOFOLLOW`。
