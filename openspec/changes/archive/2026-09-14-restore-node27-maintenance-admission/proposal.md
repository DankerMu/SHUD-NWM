## Why

Incident #2349 blocks #2325 fresh prepare: the river107 compression statement timed out and its wrapper expired; retention refused overlapping lifecycle work. The user explicitly approved a one-incident temporary paired-budget exception after being told the current retirement recipe is withdrawn.

## What Changes

- Preserve the diagnosis and explicit exception; prepare private budget copies for one bounded compression-only recovery.
- Restore genuine service health and prove a new prepare without production cutover.
- Correct the two-lock recovery guidance in the incident receipt. The independent unsafe retention dry-run defect is #2355, not fixed or deployed here.

## Capabilities

### New Capabilities

- `node27-maintenance-admission-recovery`: incident-specific bounded recovery and preservation evidence.

### Modified Capabilities

None. Existing compression/retention policy, receipt schemas and admission predicates remain unchanged.

## Impact

Node-27 existing maintenance entrypoints and temporary private configuration only. No application/DB schema changes, cold activation, active checkout replacement, governance handoff, node-22 work or T0. Shared parent change remains active.
