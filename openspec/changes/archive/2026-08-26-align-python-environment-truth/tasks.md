## 1. Fixture and terminal-parent routing

- [x] 1.1 Record the full Python/environment invariant, expanded risk matrix, non-goals, and #1831 deferral.
- [x] 1.2 Preserve PR #1822 terminal ceiling history and superseded-by-split outcome in cross-PR gate memory.

## 2. Default and active environment

- [x] 2.1 Track `.python-version=3.11` without narrowing `requires-python` or migrating CI pip.
- [x] 2.2 Update source/generated instructions for default 3.11, explicit versions, maintenance gate, and safe observation.
- [x] 2.3 Replace active node-22 automatic/operator environment-updating commands with exact interpreter/wrapper forms.
- [x] 2.4 Preserve the isolated rollback checkout and defer active cutover to #1831.

## 3. Diagnostic and validation authorities

- [x] 3.1 Reject the canonical active physical root across all four QHH diagnostic entrypoints.
- [x] 3.2 Make backend-smoke direct Python use the detached exact interpreter and lock direct/uv command inventories.
- [x] 3.3 Route e2e/grib to node-27 with remote fail-fast ordering, executable 3.11 assertion, `uv run --no-sync pytest`, and tee status preservation.
- [x] 3.4 Close the terminal mixed-set finding by scanning all tokens in every `set` segment; add mixed `+e/+u/+o pipefail/+o errexit` red rows while preserving prior controls.

## 4. Historical topology authority

- [x] 4.1 Mark QHH bring-up as a complete historical baseline.
- [x] 4.2 Implement complete-marker classification with incomplete/current-authority deny-self-exemption.
- [x] 4.3 Record exact entropy grandfather exclusions and #1823/#1842 removal routing.

## 5. Evidence Floor

- [x] 5.1 Verify default 3.11, expected 3.13-only API failure, and explicit 3.14.
- [x] 5.2 Run node-22/QHH/Docker/static lane suites, including the repaired mixed-set matrix.
- [x] 5.3 Run entropy regressions and repository hard gate.
- [x] 5.4 Run full pytest, Ruff, strict target/all OpenSpec, markdown lint, and `git diff --check`.
- [x] 5.5 Confirm latest-master current-ops union, generated roots, exact child path inventory, and no #1634/#1619 implementation paths.
- [x] 5.6 Bind the existing disposable node-22 Python 3.11 receipt and explicitly perform no active cutover/Slurm/live-service operation.
