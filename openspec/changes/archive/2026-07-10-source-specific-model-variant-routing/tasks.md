## 1. Direct-Grid Variant Registration

- [x] 1.1 Add the variant-registration surface under `workers/model_registry/` (sibling to `basins_registry_import.py`) that inserts a NEW `core.model_instance` row for a built direct-grid variant: new `model_id`, the baseline's `basin_version_id`, the direct-grid contract under `resource_profile.direct_grid_forcing`, `active_flag=false`, and `lifecycle_state` left to its `'inactive'` default. The grain `(basin_version_id, canonical_grid_key)` is CROSS-SOURCE DEDUP, not lifetime uniqueness: variant identity and idempotency are keyed on the built mapping asset identity (`model_input_package_id` + `binding_checksum` from the built manifest) within the grain — two sources sharing one `canonical_grid_key` share the single variant (`applicable_source_ids` lists both normalized ids, `gfs`/`IFS`), while a DIFFERENT built asset for an occupied grain (fix-forward M1→M1′, §11.2) registers as a NEW row with the prior generation retained. The registration input carries the registered grid-snapshot identity (an explicit `grid_snapshot_id` or resolution via the built manifest's `grid_signature`/`grid_id` against `met.canonical_grid_snapshot`); the surface copies the snapshot row's `canonical_grid_key` verbatim — never recomputing it — and persists it at `resource_profile.canonical_grid_key` (top-level, alongside and not inside the parser-validated `direct_grid_forcing` block) so the grain/idempotency lookup runs over existing columns with no new migration.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "inactive_row or grain or shared_key or separate_variants or fix_forward_new_row"` proves a registered variant is a new row with `active_flag=false` / `lifecycle_state='inactive'` and the contract under `resource_profile.direct_grid_forcing`, that exactly one variant exists per `(basin_version_id, canonical_grid_key, built-asset identity)` with sources sharing the key sharing that one row, that IFS+GFS sharing one key yields a single variant with `applicable_source_ids` containing both normalized ids, that non-shared sources register as separate variants, and that registering a rebuilt asset (different `model_input_package_id`) for an occupied grain inserts a NEW `model_id` while the prior generation's row is unchanged.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "key_from_snapshot"` proves the persisted `resource_profile.canonical_grid_key` equals the registered snapshot's `met.canonical_grid_snapshot.canonical_grid_key` and is not computed by the registration surface (a snapshot with a sentinel key round-trips verbatim).
  - Required evidence: the emitted `resource_profile.direct_grid_forcing` round-trips through `workers.forcing_producer.direct_grid_contract.load_forcing_mapping_contract_from_manifest` without error, reusing the existing parser rather than a new contract shape.
  - Non-goal for 1.1: no lifecycle activation, no scheduler manifest re-publish, no `met.*` write (that is Task 1.2); registration never activates or routes any basin.
- [x] 1.2 Implement the `met.met_station` cell-station mirror write in the registration surface: one row per used cell with `station_id = "<mapping_asset_identity>::cell:<grid_cell_id>"` (from `workers/mapping_builder/binding.py:assign_station_id_from_mapping_asset_identity`), the shared `basin_version_id`, `geom = ST_SetSRID(ST_MakePoint(lon, lat), 4490)`, the `grid_snapshot_id` FK bound to the variant's registered canonical snapshot, `active_flag=false` set EXPLICITLY (the column defaults to `true` per `db/migrations/000005_met.sql:54`), `station_role='direct_grid_cache'` (NOT the `'forcing_proxy'` default per `db/migrations/000005_met.sql:53`), and the derived-cache identity `properties_json` matching the runtime producer's mirror contract (`workers/forcing_producer/store.py:ensure_direct_grid_met_stations`, lines 347-379): `derived_cache:true`, `forcing_mapping_mode:'direct_grid'`, `binding_checksum`, `binding_uri`, `model_input_package_id`, `sp_att_path`, `sp_att_checksum`, `grid_id`, `contract_grid_id`, `grid_cell_id`, `grid_signature`, `shud_forcing_index`, and `forcing_filename` — so the producer's conditional upsert (which requires `station_role='direct_grid_cache'` plus those identity keys, `store.py:400-418`) reconciles the rows instead of failing closed. The write is a CONDITIONAL `INSERT ... ON CONFLICT (station_id) DO UPDATE` with an affected-row-count check that fails closed when an existing `station_id` carries a different bound identity (different `binding_checksum`/`model_input_package_id`/`grid_signature`/`grid_id`/`grid_snapshot_id`/coordinates), mirroring the producer's collision policy (docs §7.4) — NOT the unconditional `qhh_production_bootstrap.py:_seed_station_rows` overwrite.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "mirror_inactive or station_id_form or grid_snapshot_binding or mvt_single_track or producer_shape"` proves every mirror row is written with `active_flag=false`, the `station_id` matches the `<mapping_asset_identity>::cell:<grid_cell_id>` form, `grid_snapshot_id` references the variant snapshot while legacy stations keep `grid_snapshot_id IS NULL`, the `met.met_station WHERE basin_version_id=… AND active_flag=true` MVT query returns none of the new mirror rows, and each row carries `station_role='direct_grid_cache'` plus the full derived-cache identity `properties_json` accepted by the producer's conditional-upsert predicate.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "mirror_collision_fails_closed"` proves a mirror write hitting an existing `station_id` with a different bound identity raises and mutates no row (fail-closed, affected-count check), and that the foreign row's contents are byte-identical afterwards.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "no_forbidden_runtime_rows"` proves registration writes no `met.interp_weight`, `met.forcing_version`, cycle-dated `.tsd.forc`, or station weather CSV (§8.1/§8.3), only the `core.model_instance` row and the `met.met_station` mirror.
  - Non-goal for 1.2: no `active_flag` flip to true (that is Change 8, cutover), no interp-weight/forcing-version rows, no cycle forcing generation.
- [x] 1.3 Enforce legacy-row retention and registration idempotency: the legacy IDW `core.model_instance` row is opened read-only and left unchanged (INV-1), the currently active legacy model stays active across registration, and re-registering the same built variant (same `basin_version_id`, `canonical_grid_key`, `model_input_package_id`, `binding_checksum`) returns the existing identity with no duplicate row or duplicate mirror — while a DIFFERENT built asset for the same grain is NOT swallowed by idempotency (it registers as a new row per Task 1.1's fix-forward rule).
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "legacy_retained or legacy_stays_active or idempotent"` proves the legacy row's `model_id`/`active_flag`/`lifecycle_state`/`model_package_uri`/`resource_profile` are unchanged, an active legacy model remains active after variant registration, a second registration of the SAME built asset inserts no duplicate `core.model_instance` or `met.met_station` row, and idempotency keys on the built-asset identity (a changed `model_input_package_id` is not returned as "existing").
  - Non-goal for 1.3: no deletion of any legacy asset; no change to legacy calibration/replay lineage.
- [x] 1.4 Reconcile the runtime producer's mirror `active_flag` ownership with registration (the scoped producer change; see the `fixed-station-forcing-production` delta): change `workers/forcing_producer/store.py:ensure_direct_grid_met_stations` so the insert template no longer hardcodes `active_flag=true` (fresh rows land `false`) and the conditional `DO UPDATE` preserves `met.met_station.active_flag` instead of `SET active_flag = true`; change the DB-free file plane the same way (`workers/forcing_producer/file_store.py:_handoff_station_rows` no longer emits `"active_flag": True`, and the `station_inventory.json` → `met.met_station` ingest preserves an existing row's flag). Mirror activation belongs exclusively to Change 8's cutover flip. Do NOT touch the producer's value generation, contract parsing, or the fail-closed collision predicate.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "producer_preserves_registration_flag or production_run_mirror_stays_inactive"` proves a direct-grid production run against a registered-but-inactive variant leaves every mirror row `active_flag=false` (registration-owned), that the producer upsert never escalates `false`→`true` while a row already `true` stays `true`, and that the file-plane handoff carries no forced-true flag; PLUS `uv run pytest -q tests/test_forcing_domain_handoff_apply.py -k "apply_preserves_existing_active_flag"` proves the ingest-apply plane (`packages/common/forcing_domain_handoff_apply._upsert_met_stations`) functionally preserves an existing `active_flag=true` row when the incoming payload carries `false` (no `STATION_CONFLICT` on flag divergence alone; DO UPDATE SET sentinel leaves the flag untouched).
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "producer_collision_still_fails_closed"` proves the producer's derived-cache collision predicate still fails closed on a non-matching `station_id` (the flag change relaxes nothing else).
  - Non-goal for 1.4: no change to forcing value generation, the contract parser, weights, timeseries, or the scheduler transport; no `active_flag` flip to `true` anywhere (that is Change 8).

## 2. Variant Activation Cutover

- [x] 2.1 Add the ordered pre-activation extension-point contract to `packages/common/model_registry.py:model_lifecycle_operation` so registered hooks run INSIDE the existing `with self._transaction() as cursor:` block, before the supersede+activate swap, each receiving the transaction cursor and an activation context (basin_version_id, previous active model, target model, source scope); register EMPTY/no-op hooks in this change and reserve ordered mount points for Change 5 (state clone) and Change 8 (station flag flip). Hook contract: hooks run ONLY when a real supersede+activate swap will occur — the idempotent already-current short-circuit skips them (no state change means nothing for a clone/flip hook to act on).
  - Required evidence: `uv run pytest -q tests/test_variant_activation_cutover.py -k "hooks_ordered or hooks_empty or hook_raises_aborts"` proves the empty hooks run in declared order before the swap and change no state, and that a deliberately raising test hook rolls back the whole transaction with no model activated, no model superseded, and no manifest re-published (fail-closed, no "activated but hooks not run" state).
  - Required evidence: the reuse of the existing single-transaction path is proven by asserting the swap still supersedes the prior active model and activates the target in the same cursor with one `ops.audit_log` row (no new routing table, no separate cutover state machine).
  - Non-goal for 2.1: do NOT implement the state-clone hook (Change 5) or the `met.met_station.active_flag` flip hook (Change 8); only the contract and empty hooks land here; no production basin is activated.
- [x] 2.2 Wire the scheduler registry manifest re-publish (`services/orchestrator/scheduler_file_providers.py:publish_scheduler_registry_manifest`) as an explicit step on the uniform post-commit tail of EVERY successful lifecycle transition that changes the dispatch candidate set — `activate`, `switch_version`, `rollback_version`, and a `deactivate` that removes the currently active model (the §11.2 step-4 pause-production lever, committed via the sys_admin missing-active override) — and prove it is skipped for a preflight-blocked, hook-aborted, or already-current operation. The uniform tail placement is defensive future-proofing: a standalone supersede-of-active never commits today (`packages/common/model_registry.py:2137-2149` appends the `MISSING_ACTIVE_RISK` blocker for supersede regardless of `override_missing_active` — the missing-active override is deactivate-only), so supersede-of-active is asserted as a blocked no-republish case, not a positive re-publish leg; if such a transition is ever admitted past preflight it re-publishes under the same trigger without new wiring.
  - Required evidence: `uv run pytest -q tests/test_variant_activation_cutover.py -k "manifest_republished or manifest_not_republished or republish_per_operation"` proves `publish_scheduler_registry_manifest` is invoked exactly once on a successful activation commit and exactly once for each of `switch_version`, `rollback_version`, and deactivate-of-active (committed via the sys_admin missing-active override); and is NOT invoked when the operation is blocked by preflight — including a standalone `supersede` addressed at the currently active model, which is preflight-blocked by `MISSING_ACTIVE_RISK`, mutates no state, and re-publishes nothing — aborted by a raising pre-activation hook, or returns already-current without a state change.
  - Non-goal for 2.2: no change to the manifest transport (NFS write) or the node-22 consumer; no periodic self-heal publisher (the manual `scripts/publish_scheduler_file_registry.py` path is unchanged); no production basin is activated.
- [x] 2.3 Add a regression test locking permanent retirement: a `superseded` (or otherwise non-`active`) model is excluded from the scheduler dispatch candidate set because the consumption face filters `str(row.get("lifecycle_state") or "active") == "active"` (`scheduler_file_providers.py:119`, consumed via `scheduler_models.py:41` `list_models(active=True)`), and the superseded row is retained as immutable lineage.
  - Required evidence: `uv run pytest -q tests/test_variant_activation_cutover.py -k "superseded_exits_dispatch or dispatch_filter_regression or retired_not_destroyed"` proves a superseded model is absent from the dispatch candidate list while the active model is present, that a synthetic non-`active` row is never a dispatch candidate (so a future filter refactor fails the test), and that the superseded `core.model_instance` row is retained (not deleted) after cutover.
  - Non-goal for 2.3: no change to the dispatch selection algorithm itself; the test locks existing behavior; no production basin is activated.
- [x] 2.4 Prove the cutover's concurrency safety and idempotency at the extension-point seam (the "Cutover is concurrency-safe and idempotent" scenario): two concurrent activation requests for the same `(basin_id, basin_version_id)` scope preserve exactly one active model with a stable conflict or already-current result for the loser, and repeating a completed activation returns a stable already-current result with NO duplicate supersede+activate transition, NO second hook execution, and NO second activation-success (`outcome IN ('allowed','rollback')`) audit row.
  - Required evidence: `uv run pytest -q tests/test_variant_activation_cutover.py -k "concurrent_activation or repeat_already_current"` proves the concurrent case (via the existing `FOR UPDATE` scope lock) leaves one active model and a stable losing result, and that the repeated case returns already-current, skips the pre-activation hooks (per Task 2.1's hook contract), re-publishes no manifest, and appends no second activation-success audit row.
  - Non-goal for 2.4: no new locking mechanism (the existing `FOR UPDATE` scope lock is the authority); no production basin is activated.

## 3. Legacy Reactivation Guard

- [x] 3.1 Add a legacy-reactivation blocker to `_build_model_operation_preflight` (`packages/common/model_registry.py:2090`) inside the activation-class path (`activation_class_operation`, `model_registry.py:2111`): refuse `activate`/`switch_version`/`rollback_version` when the TARGET — the model that would become active as a result of the operation, i.e. the addressed model for `activate`/`switch_version` and the RESTORED `previous_model` for `rollback_version` (the preflight's own `restored_model`, `model_registry.py:2105`) — classifies as legacy-mapping AND the `basin_version_id` has direct-grid activation history. Classification uses ONE classifier everywhere: a model is direct-grid iff its `resource_profile.direct_grid_forcing` parses through `workers/forcing_producer/direct_grid_contract.py:load_forcing_mapping_contract_from_manifest` with `forcing_mapping_mode='direct_grid'`; absent, malformed, or non-direct contracts classify as legacy-mapping (fail-closed), with a distinct blocker code for a present-but-invalid direct-grid contract. History is APPEND-ONLY, never mutable `lifecycle_state`: `EXISTS` an `ops.audit_log` record of a successful activation-class transition (`entity_type='model_instance'`, `action IN ('models.activate','models.switch_version','models.rollback_version')`, `details->>'outcome' IN ('allowed','rollback')`, `details->>'basin_version_id'` = the scope — the record shape already queried at `model_registry.py:1996-2009`) whose resulting-active model (`details->'updated_model'->>'model_id'` joined to `core.model_instance`) classifies as direct-grid, OR the scope's currently active model classifies as direct-grid. The blocker is fail-closed with NO override; a direct-grid target is allowed (fix-forward).
  - Required evidence: `uv run pytest -q tests/test_legacy_reactivation_guard.py -k "activate_legacy_refused or rollback_restored_legacy_refused or switch_legacy_refused or no_override"` proves `activate` and `switch_version` addressed at a legacy target are blocked, that `rollback_version` addressed at the ACTIVE direct-grid model whose `previous_model` is legacy-mapping is blocked (the guard classifies the restored model, not the addressed model), each with no state transition and no manifest re-publish on a basin with direct-grid history, and that supplying any override flag/reason/elevated role still refuses (no admitting path).
  - Required evidence: `uv run pytest -q tests/test_legacy_reactivation_guard.py -k "history_survives_deactivate or supersede_not_history"` proves the one-way channel is not escapable or fabricable with supported operations: after activate(direct M1) → deactivate(M1, sys_admin missing-active override) [→ optional deprecate], `activate` on legacy M0 is STILL refused (the audit record persists); and `supersede` on a never-activated inactive direct-grid variant does NOT arm the guard (legacy operations on that basin proceed unchanged).
  - Required evidence: `uv run pytest -q tests/test_legacy_reactivation_guard.py -k "direct_to_direct_allowed or malformed_direct_contract_refused"` proves activating a valid direct-grid variant on a basin with direct-grid history is NOT blocked, while a target that declares `forcing_mapping_mode='direct_grid'` but fails the parser is refused with the distinct invalid-direct-grid-contract blocker code.
  - Required evidence: `uv run pytest -q tests/test_legacy_reactivation_guard.py -k "inactive_only_not_armed or no_history_unaffected"` proves an `inactive`-only direct-grid variant (no activation audit record) does not arm the guard and that a basin with no direct-grid variant behaves exactly as before (13-live-basin safety).
  - Non-goal for 3.1: no new migration/column for the history marker (the predicate is derived from append-only `ops.audit_log` + immutable `core.model_instance` rows; `000046` stays reserved per design D8); no production basin is activated by these tests (they use synthetic basins/models).
- [x] 3.2 Confirm the guard scopes to lifecycle activation operations only and does not touch offline replay/calibration that uses the legacy package outside the lifecycle `activate` path.
  - Required evidence: `uv run pytest -q tests/test_legacy_reactivation_guard.py -k "offline_replay_not_blocked or scope_activation_only"` proves an offline replay/calibration path that uses the legacy package directly (not via a lifecycle activation-class operation) is not blocked by the guard.
  - Non-goal for 3.2: no change to the offline replay/calibration surface itself; only the guard's operation-scope is asserted.

## 4. Source-Scoped Dispatch Enforcement

- [x] 4.1 Add the source-scope fail-closed precondition at the dispatch/staging variant-selection boundary, which is PINNED to the orchestrator seam that pairs an active model with a `(source, cycle)`: the candidate-assembly path `services/orchestrator/scheduler_candidates.py:build_candidates` (the function that constructs `(model, source, cycle)` candidates, currently containing zero direct-grid awareness), upstream of `services/orchestrator/scheduler_execution.py:produce_forcing_for_candidates` (which calls `forcing_producer.produce(source_id=…, model_id=…)`). When the candidate's model carries a direct-grid contract, invoke the contract with the requested `source_id` — reading the variant's `resource_profile` from the manifest-carried model row and reusing `workers/forcing_producer/direct_grid_contract.py:_applicable_source_ids` (lines 246-293) — so an out-of-scope source raises `DirectGridContractError("Direct-grid contract does not apply to the current source.")`, the pairing becomes a blocked candidate with recorded evidence, and dispatch fails closed before forcing production, never falling back to legacy IDW or another source (INV-4/INV-5).
  - Required evidence: `uv run pytest -q tests/test_source_scoped_dispatch.py -k "in_scope_dispatches or out_of_scope_fails_closed or boundary_not_producer_only"` proves — by exercising the REAL dispatch path (`build_candidates` → `produce_forcing_for_candidates`), not a standalone helper — that an in-scope source dispatches, an out-of-scope source fails closed at the dispatch boundary with the parser's source-scope error and no fallback/forcing produced, and that the check runs at candidate assembly (before forcing production), not only inside the producer.
  - Non-goal for 4.1: no change to the parser's membership rule or `normalize_source_id`; no change to the manifest transport; no production basin is activated.
- [x] 4.2 Enforce zero cross-source substitution and the missing-source no-run rule. This task is split between NEW enforcement and REGRESSION LOCKS: (a) NEW at the dispatch seam — a source missing data for a cycle yields a recorded no-run for that `(source, cycle)` on the scheduler's EXISTING blocked-candidate evidence surface (`services/orchestrator/scheduler_execution.py:126-133`, `context.blocked_candidate(candidate, reason, state_evidence=…)`) under a named reason code that distinguishes missing source data from other `forcing_production_blocked` causes; no new DB table or ops row type. (b) REGRESSION LOCKS over the existing fail-closed compute-layer behavior in `workers/forcing_producer/` (landed by the archived `direct-grid-forcing` change, INV-4: `producer.py` direct-grid path + `direct_grid_contract.py`): one run stays single-source end to end, a missing required variable/cell fails the run closed rather than splicing another source, and no legacy-package compatibility layer substitutes sources — these lock current behavior so a refactor cannot re-admit substitution.
  - Required evidence: `uv run pytest -q tests/test_source_scoped_dispatch.py -k "missing_source_no_run"` proves a source with no cycle data produces a blocked-candidate evidence entry with the named missing-source reason code for that `(source, cycle)` and no substitution — the NEW dispatch-seam behavior.
  - Required evidence: `uv run pytest -q tests/test_source_scoped_dispatch.py -k "single_source_run or midrun_splice_forbidden or no_legacy_compat"` regression-locks the existing `workers/forcing_producer` behavior: a run stays single-source, a mid-run missing variable/cell fails closed (no splice), and no legacy/IDW compatibility layer is consulted.
  - Required evidence: `uv run pytest -q tests/test_source_scoped_dispatch.py -k "availability_is_display_concern"` proves cross-source availability is resolved by display best-available selection over already-produced per-source products, not by compute-layer source merging.
  - Non-goal for 4.2: no change to display `best-available-selection`; no change to the producer's already-landed fail-closed internals (they are locked, not modified); only the dispatch-seam no-run recording is new here.

## 5. Verification

- [x] 5.1 Add the mechanism-only invariant proof on the node-27 real DB (oracle per project convention): registering direct-grid variants for synthetic basins leaves the 13 live basins' active models and station sets unchanged, and no task in this change activates any production basin.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py -k "mechanism_only_live_basins_undisturbed"` (marked `pytest.mark.integration`, run on node-27 `docker exec nhms-db psql`) proves that after variant registration the pre-existing active model per live `basin_version_id` and its `active_flag=true` station set are byte-for-byte unchanged, and that every registered variant is `lifecycle_state='inactive'`.
  - Required evidence: a node-27 receipt records the before/after active-model and active-station-count per live basin, demonstrating zero production impact.
  - Non-goal for 5.1: no activation, cutover, state clone, or display flip on any live basin; this proof asserts the mechanism is inert.
- [x] 5.2 Run the full change verification suite and OpenSpec validation.
  - Required evidence: `uv run pytest -q tests/test_direct_grid_variant_registration.py tests/test_variant_activation_cutover.py tests/test_legacy_reactivation_guard.py tests/test_source_scoped_dispatch.py` passes.
  - Required evidence: `uv run ruff check .` reports `All checks passed!`.
  - Required evidence: `openspec validate source-specific-model-variant-routing --strict --no-interactive` reports `Change 'source-specific-model-variant-routing' is valid`.
  - Non-goal for 5.2: no basin activation and no display/frontend change; this change lands inert mechanism only and is exercised by a later pilot (Change 7).
