# fix-national-discharge-coverage-snapshot-order

Order the national discharge coverage helper's two statements so the active-network denominator is read after the coverage numerator, closing the zero-coverage and partial-coverage activation fail-open branches
