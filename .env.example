# PostgreSQL connection (TimescaleDB + PostGIS)
DATABASE_URL=postgresql://nhms:nhms_dev@localhost:5432/nhms

# S3-compatible object storage (MinIO in dev)
S3_ENDPOINT_URL=http://localhost:9000
S3_BUCKET_NAME=nhms
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
OBJECT_STORE_PREFIX=s3://nhms
OBJECT_STORE_ROOT=/work/nhms/object-store

# Slurm Gateway backend: mock (default) or slurm
SLURM_GATEWAY_BACKEND=mock

# M1 GFS forecast loop
GFS_NOMADS_BASE_URL=https://nomads.ncep.noaa.gov/pub/data/nccf/com/gfs/prod
WORKSPACE_ROOT=/work/nhms
SHUD_EXECUTABLE=shud_omp
MODEL_OUTPUT_INTERVAL=1440
SHUD_TIMEOUT_SECONDS=3600

# FastAPI port
API_PORT=8000
