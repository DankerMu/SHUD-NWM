# Legacy Tile Publisher Placeholder (Deprecated)

There is no active Python package under this hyphenated path.

Tile publication work is represented by `infra/sbatch/publish_tiles.sbatch`, API routes, and map/flood alert frontend code. Use underscore package names for importable Python modules.
