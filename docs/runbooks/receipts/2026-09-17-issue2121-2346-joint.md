# #2121-A / #2346 step two — joint node-27 receipt

**Historical status on 2026-09-17: BLOCKED on #2420; joint production deployment rolled back.**
The user explicitly chose “保持回滚，等待 #2420”, rejecting a scoped C4 waiver. PR #2450 was kept
draft/unmerged; the measurements below did not constitute overall deployment PASS.

**2026-09-20 resumption:** #2420 closed through #2508 after its own dual-source readonly and formal public C4 PASS;
PR #2521 archived its specification. The user authorized this same joint batch to resume. The rebased joint candidate
must independently pass current-head live acceptance; the historical failures and measurements below remain unchanged.

## Identity, ordering and retained settings

B merged first in #2449 (`d3d09d9be9f52f02de45cf1727bbe5d2f8077feb`), then the genuine cold baseline was committed as
`af61cccc7a0c9fae7cb341e9c6fd5399aeaff406`; see [baseline receipt](2026-09-17-issue2121-cold-prewarm.md). Only then were
A and step two implemented together, PR #2450. Candidate source: `c9891c1419b1c2e9470a0302e058369034534643`, node-27
`/home/nwm/tmp/nwm-2121-b`. Production remained at `7ecc46bed18cc8b6f20030f49aa3bf43a77b6858`, MainPID 3023964
throughout isolated tests.

Both sources independently discovered default cycle `2026-09-16T00:00:00Z`, 56 published times from
`2026-09-16T00:00:00Z` to `2026-09-22T21:00:00Z`. The tested source preserves the catalogue's original order, strings
and multiplicity; no 12-hour truncation remains.

Decision: retain prewarm workers=8, request timeout=30s, deadline=540s, API workers=2 and pool=8+8. Add cold-generation
cap=8 per worker, strictly below pool capacity. Candidate sessions used `PGOPTIONS` statement_timeout=30000 and
max_parallel_workers_per_gather=2; the global role was not changed during isolated measurements. These settings and code
are one production apply/rollback batch, not separate fixes. The bounded pre-change EXPLAIN for GFS z4/12/6 took
2032.815ms and had no parallel nodes; parallel=2 is a defensive resource ceiling, not a claimed speedup.

The pre-window node had max_connections=100, superuser reserve=3 and 19 client backends. Two API instances can consume
64 connections; the previously measured allowance includes other demand and explicit headroom. The load oracle
independently stops its exact candidate unit on production `/layers` failure or client count >=89. Disk before tests:
`/` 18G free, `/home` 1.1T, `/data/GHDC` 12T. TMPDIR stayed under `/home/nwm/tmp`.

## Standalone full-cycle cold prewarm

A fresh empty application cache at `/home/nwm/tmp/issue2121-joint-20260917/full-cold/cache` was used by an isolated
two-worker API on 127.0.0.1:8092. This is application/file-cache cold, not PostgreSQL/OS-cache cold. The genuine
scheduler/HTTP warmer ran with the retained 8/30/540 settings.

| Observation                            |                                                                    Result |
| -------------------------------------- | ------------------------------------------------------------------------: |
| Elapsed                                |                                                                  189.693s |
| Issued requests                        |                                                                      1611 |
| Discharge                              |                                       GFS 728/728, IFS 728/728 successful |
| River requests                         |                                                             43 successful |
| PNG                                    | GFS 56 successful; IFS 49 successful + 7 expected `png_window_incomplete` |
| Unexpected failures / deadline skipped |                                                                     0 / 0 |
| Cache hits                             |                                                                         0 |
| Initial / final cache files            |                                                                  0 / 1604 |
| Cache tree `du -sb` after run          |                                                           548123627 bytes |
| All-request p95 / max                  |                                                     2.651411s / 8.647424s |
| GFS discharge p95 / max                |                                                     2.516688s / 2.875407s |
| IFS discharge p95 / max                |                                                     3.340902s / 8.647424s |

Percentiles use nearest rank `ceil(.95*n)` and include non-2xx requests; the raw latency `failed=7` means HTTP non-2xx,
while scheduler `failed_count=0` excludes the seven explicitly classified expected IFS PNG gaps. No claim of 1611
HTTP200 or complete IFS precipitation is made. The 1456 discharge requests all returned 200. The v4 summary records
full_cycle, both sources available=warmed=56, zero discharge_failed, zero out-of-contract PNG, and rc=0.

Retention dry-run on this exact fresh cache used the production 14-day setting: execution_mode=plan_only,
planned=deleted=failed=0, freed_bytes=0. Its precipitation subtree was untouched by the PBF retention owner. This
demonstrates fresh files are not expired, not that long-term PNG pruning or unrelated canonical-lock issues have been
repaired. The footprint is one measured cycle, not a projection of unlimited retained cycles.

Compared with B's 183-request/18.771s baseline, the larger scope is 1611 requests/189.693s; these are different
workloads, not an apples-to-apples speedup. Both remain within the retained deadline with zero discharge failures/skips.
The cold run, not the later recovery fill, establishes the full-cycle budget.

## Same-worker contention and recovery

A second initially empty cache namespace (`contention/cache`) avoided contamination by the standalone run. The same two
candidate workers served a full prewarm, eight same-key followers, a 64-request dense cold spike, and a paced
four-ticks-per-second full-axis workload. Each tick requested six z4 viewport tiles plus one z10 discharge tile at
797/397. Three separately seeded fast probes continuously queried a hot discharge tile, z10 river tile and
`/api/v1/layers`.

| Phase                                | Observed responses                         |
| ------------------------------------ | ------------------------------------------ |
| 64 distinct dense spike requests     | 8×200, 56×503                              |
| 672 paced z4 cold requests           | 218×200, 454×503                           |
| 112 additional z10 requests          | 34×200, 78×503                             |
| 8 same-key followers                 | 8×200 (one miss, seven hits)               |
| Concurrent prewarm                   | 1213×200, 391×503, 7 expected PNG404; rc=1 |
| Hot discharge probes                 | 309/309 HTTP200 and hits                   |
| River probes                         | 309/309 HTTP200 and hits                   |
| Catalog probes                       | 309/309 HTTP200                            |
| Independent production safety probes | 401/401 HTTP200                            |

All 588 foreground overload responses carried code `MVT_COLD_GENERATION_BUSY`, Retry-After:1, no-store and matching
envelope/header request-id. Foreground admitted cold successes: 250 misses, p95=3.068914s, max=3.480419s. Overload
response p95=0.958216s, max=1.338756s. Hot-discharge p95/max=0.260402/0.827033s; river=0.255206/0.841693s;
catalog=0.111504/1.126431s. There were no foreground statuses outside 200/503 and no fast-surface 500s.

The concurrent prewarm was correctly incomplete, not silently retried or called successful. An explicitly separate fill
afterwards returned rc=0, all 1456 discharge successes and zero deadline skips. Then 672 full-axis z4 requests at four
ticks/sec returned 672 HTTP200/cache hits, p95=0.059184s and max=0.068105s. This HTTP playback covers six z4 viewport
tiles per time, a subset within the thirteen-tile z3/z4 prewarm envelope; it is not a browser claim or a replay of every
prewarmed tile.

Resource samples: 401 observations, peak 49 client connections and 36.45369% host CPU busy, no safety-stop errors. These
are sampled PostgreSQL session counts and host CPU, not exact QueuePool checkout counts or guaranteed peaks. Candidate
log inspection independently found no `QueuePool`, `TimeoutError`, `Traceback` or HTTP500 entries; absence of 500 access
lines alone was not used as timeout proof.

## Actual browser surface

Real Chromium drove the built candidate frontend with HTTP cache disabled, no API mocks, 1440×900 viewport,
precipitation off (MVT scope), source-specific default cycles and playback speed 4. Each source reached slider index55
and requested all56 catalogue times. Actual map zoom was z3. Every observed tile/river response was200, all API origins
matched the candidate, and no page errors occurred.

GFS: 262 discharge requests, 218 hits (83.2%); IFS: 240 requests, 211 hits (87.9%). Both requested six river tiles. The
harness's deliberately strict >=95% hit-rate predicate failed; that failure is retained, not relabeled PASS. Every miss
was z3 x7/y2 or x7/y3, beyond the existing China prewarm east bound134.8° (x<=6). The default wide browser viewport
therefore includes tiles outside the agreed prewarm envelope. This is predominantly cached playback with zero errors,
but not >=95% first-play hits across the entire wide viewport. Product scope was not expanded to prewarm unrelated world
tiles. Screenshots and complete response records are preserved with the raw evidence.

A falsifiable China-framed cross-check used a separate 640×600 browser context and the actual map zoom-in control.
Observed z4 playback requested all56 instants for each source, with GFS223/223 and IFS222/222 discharge hits, all
tile/river responses200 and no page errors; the unchanged >=95% predicate passed. Raw records/screenshots:
`browser-framed/`. Startup z3 traffic remains in raw rows; the framed discharge metric explicitly selects z4, while
all-tile/river status checks retain startup traffic. The framed service was stopped afterwards.

The wide viewport's x7 tiles span135–180°E; on-demand cold generation outside the China envelope succeeded with200.
Source-scoped cache keys remain intact. Source request totals differ; browser scheduling/cancellation is a plausible
explanation [INFERENCE], not independently instrumented here. Neither fixed per-time request cardinalities, equal source
counts nor 4Hz actual rendering is claimed. Existing CHINA_BOUNDS was not changed by this diff. No follow-up defect is
filed for that intentional envelope; the documented keep decision preserves it.

## Deterministic and build verification

On actual pre-change source, the new public-route real-QueuePool regression failed with hot HTTP500 and the full-cycle
regression failed by omitting18h entries. Corrected candidate node-27 durable log `targeted-pytest.log` reports **1258
passed in264.85s** across cold admission, MVT scaling, prewarm, locks, errors, OpenAPI drift/3.1/API contracts, CI
selector and connection attribution. The surrounding SSH capture later timed out at650s despite the completed test log
and absent pytest process; this is not represented as a successful SSH exit. The earlier 1257-pass/one stale selector
expectation run and subsequent67-pass focused correction remain separately recorded.

Local Ruff, both strict OpenSpec changes, generated API types and TypeScript checks passed. Candidate frontend was
freshly built on node-27: Vite6.4.2,2542 modules,16.26s; no old production dist was substituted. There are no
dependency-lock or DB-migration changes between the running production SHA and candidate. Three source review seats are
correctness, test-evidence+spec-compliance and invariant-state.

## Isolation cleanup and evidence

The candidate unit was stopped after both runs, the previously active `nhms-node27-autopipe.timer` restored active and
the temporary auto-restore timer stopped. Production MainPID stayed3023964. `map.tile_cache` remained0 rows
before/after; no production cache was purged and no node-22 operation occurred.

Raw evidence root: `/home/nwm/tmp/issue2121-joint-20260917/`:
`full-cold/{cold-receipt,prewarm-summary,request-samples,retention}.json`,
`contention/{joint-load-summary,joint-load-samples,joint-resource-samples,capture-completion}.json`,
`browser/{browser-summary,browser-responses}.json`, four browser screenshots and `combined-api.log`. Local invocation
logs and reviews are under `.workplans/2121/`. Source changes reached node-27 via GitHub; no
virtualenv/node_modules/private env was copied from macOS.

## Production and final acceptance

Three independent source seats returned no blocking findings at `c9891c1419b1c2e9470a0302e058369034534643`; round1 is
clean. Review reports and adjudication are in `.workplans/2121/joint-review/`. Production apply waited for the
already-running GFS/IFS ingest to finish naturally; no parser was interrupted. A separately guarded pause prevented
future timer activations during the cutover, then the previously active timer was restored.

The joint apply fast-forwarded the clean production checkout from `7ecc46bed18cc8b6f20030f49aa3bf43a77b6858` to reviewed
`c9891c14`, retained pool8+8 and two workers, set cold cap8, changed the two role defaults together, installed the newly
built frontend dist and restarted only `nhms-display-api.service`. Applied PID4146813 started at2026-09-17 15:13:59 CST.
Both role targets were previously unset (effective statement_timeout0, parallel20); fresh readonly connections after
restart show30s and2, read committed unchanged. The role is ACL-readonly, not transaction_read_only=on.

Apply-time connection budget: max100/reserve3/other3; conservative
`32 + max(other,16) + 8 headroom + 3 reserve = 59 <= 100`. Effective workers=2 was verified from process argv;
`NHMS_DISPLAY_WORKERS` is absent in the private env, so the unit's existing default applies. The three pool/cold env
values were read from the running parent process and are8/8/8.

One operational staging attempt aborted before any active code/env/role/dist replacement: placing the candidate dist
next to the checkout's dist made the clean-tree guard see the script's own untracked directory. The old service was
immediately restarted and health verified200; own staging was moved out, the aborted attempt retained under
`deploy-preflight-aborted/`, and the corrected staging path under the private deployment directory passed. This was not
a product-source fix or a hidden successful first attempt.

Private rollback state is `/home/nwm/tmp/issue2121-joint-20260917/deploy/` (0700): exact prior env (0600), before/after
env hashes, old SHA/branch, prior role settings/absence of database-role overrides, and original frontend dist. Rollback
restores the old code/env/dist together and RESETs only the two formerly-unset role targets, after rejecting foreign
changes; never reset unrelated role settings, purge cache or use the broad startup wrapper.

Production C1 smoke through **https://test.nwm.ac.cn** passed: root bytes matched the deployed dist, runtime is
display_readonly with control mutations and Slurm disabled, layers/river and both source-specific discharge tiles200,
Slurm health404. A real readonly zero-row DELETE attempt returned SQLSTATE42501 and was rolled back. The naturally
completed ingest advanced both default catalogues to `2026-09-16T12:00:00Z`,56 times each through
`2026-09-23T09:00:00Z`; these production identities are intentionally distinct from the earlier00Z candidate
measurements. `deploy/c1-approved.json` binds the reviewed SHA and this scoped C1 result, not blanket C2/C3 closure.

Canonical readonly baseline before role cutover: GFS23/23 write denials across12targets, no mutating/reachable/unsafe
role findings. Overall validator status remains **BLOCKED**, not PASS: jobs/status/stages lack required response
identity and job_logs lacks job_id. Existing open [#2420](https://github.com/DankerMu/SHUD-NWM/issues/2420) owns this
unchanged topology/response-projection gap; issue-scribe dedup confirmed no new issue was needed. The broad limitation
is neither suppressed nor repaired by cold admission.

Production prewarm of the new12Z default cycles was attempted with the original timer active. It overlapped
the15:17:40–15:19:22 autopipe tick:1611 requests,136.066s,207 discharge failures and rc=1; the bounded first20 failure
samples were all typed503. This unsuccessful attempt is retained as `production-prewarm.json`, not called complete.
After a guarded timer pause, a separately labeled recovery fill issued1611 requests in14.813s,1563 cache hits, both
sources728/728 discharge successes, zero unexpected failures/deadline skips, and the same seven expected IFS PNG gaps.
It returned rc=0. This warm/recovery result does not replace standalone cold-budget evidence.

Post-apply canonical readonly validation bound both sources to actual QHH00Z runs: GFS
`fcst_gfs_2026091600_dg_0883c7e9c1006c6fd347df500315e9df`; IFS
`fcst_ifs_2026091600_dg_9ccb261a39d51c24f4de9173fb4461b6`. Each passed23/23 write denials across12targets;
health/runtime/models/stations/latest-product passed. The same four #2420 job/pipeline routes remained BLOCKED. Evidence
is under
`/home/nwm/NWM/artifacts/issue2121-joint-readonly-after/{joint-after-gfs,joint-after-ifs-qhh}/db/readonly-db-boundary/`.
An earlier IFS invocation accidentally selected a different basin and yielded strict-latest404; it is retained
separately, not used as QHH evidence.

Formal public C4 first refused an incorrectly named output path before browser execution. After correcting only the
invocation to the canonical filename in a fresh private directory, freeze passed and the real browser lane produced
**FAIL / JOB_LOG_MISSING / stage=ops** at2026-09-17T07:24:24.518Z. Both captured identities were QHH12Z, `/` map/current
read and readonly runtime passed, no Slurm or non-GET control request occurred; `/ops` could not bind a current-identity
job. The failed terminal receipt is `deploy/c4-bound/nhms-frontend-c4-live-evidence-joint-2121-2346.json`. The PASS-only
bind/verify stages were not run and no acceptance.json was manufactured.

The exact applied API log window, from parent4146813 to rollback parent4161678, contained4595×200,23×404,399×503
and1×304; **zero500, QueuePool, TimeoutError or Traceback**. See `production-api-window.log` and
`production-api-log-audit.json`. Thus the deployment hold is the independently named C4 job-identity gate, not evidence
of renewed MVT pool starvation.

Following the fixture's rollback rule, the entire batch was restored: checkout and master branch at
`7ecc46bed18cc8b6f20030f49aa3bf43a77b6858`, exact prior env bytes, exact prior role settings, and exact prior frontend
file tree. Only the two newly set role targets were RESET. Fresh readonly connection values are again statement_timeout0
/ parallel20. Rollback API PID4161678 served public root/layers200; autopipe timer active; all temporary API/watch
processes and restore timers stopped. Caches and raw evidence were not purged.

The user chose to retain this rollback and wait for #2420 rather than weaken the gate. Tasks2.7/2.8 remain unchecked,
both issues remain open, and the PR must not be merged or redeployed until the blocker is resolved and the joint
acceptance is rerun. Source round1 clean does not mean deployment approval or final-head CI completion. No OpenSpec
archive is performed for this unfinished change.

### Evidence digests

All paths below are relative to `/home/nwm/tmp/issue2121-joint-20260917/`; SHA256:

| Artifact                                                              | SHA256                                                             |
| --------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `full-cold/cold-receipt.json`                                         | `9013e86ef62ddc744e8ee43ab3086e20217f87a5cacd95fefeec853fa419ef7f` |
| `full-cold/request-samples.json`                                      | `f9ce612010b44e98c556155cdf29f5315c03c5b5b84df37aa97cf148a4edba04` |
| `contention/joint-load-summary.json`                                  | `9adbd2ab316b36fb770eef9f3383efa9f4ce679d11962263604adbb2e10d574b` |
| `contention/joint-load-samples.json`                                  | `ae723298b792125dde847d93ae4b9bc41b1b1b9b2c5c0e9ac8a768c8c3200725` |
| `browser/browser-responses.json`                                      | `96bfcb41c574509dde149ee9878f0281912b8435cfc12df389c56bb3a5cfc5d2` |
| `browser-framed/browser-responses.json`                               | `35c1a43aef9192df4bfe1b5c5004abdfb631ec9a65590071bd1b20cf22f40652` |
| `targeted-pytest.log`                                                 | `f9fb1c526079d1057a8a314eaa916ee3f325a5b17407f8b965a8b6a3c57001cc` |
| `production-prewarm.json`                                             | `0a33be2aaaab9ae833f91a157ce969b0b84bba4213078b1078a52902cafddf80` |
| `production-recovery-fill.json`                                       | `e541598690ca1e864c42bde1eef36526b0623f26ae7d78f6dec7f9b413fda522` |
| `deploy/c4-bound/nhms-frontend-c4-live-evidence-joint-2121-2346.json` | `5ee935715fa6a4e7fd95cadbd08ca93ea4a2ba40e622d455512473d35b35cf7f` |
| `deploy/rolled-back.json`                                             | `ede9cf90c69d233a27fa319a4e6d343024246d7f524dfc40069a1426838019ea` |

Throwaway measurement/deployment scripts were removed after evidence capture and rollback; raw samples, logs,
screenshots, private rollback state and source-review reports were retained. No new changelog convention was introduced.
Both affected OpenSpec changes validate strictly in this blocked state.
