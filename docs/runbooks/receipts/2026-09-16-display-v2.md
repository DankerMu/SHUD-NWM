# node-27 receipt — display-v2 综合部署（issue #2017 / I15，epic #2003）

- 日期：2026-09-16，窗口 UTC `08:15:07Z → 08:15:23Z`（node-27 本机 `+08:00`，即 16:15）；瓦片预算与浏览器证据在窗口后续采。
- 节点：node-27（`210.77.77.27`），活动树 `/home/nwm/NWM`，分支 `master`。
- 树：窗口前 `f2476c8c29774d40b300d4a8a13657b5d6d05139` → 窗口后 `7ecc46bed18cc8b6f20030f49aa3bf43a77b6858`（`git pull --ff-only`，23 个文件全在 `docs/`，非文档改动 0）。
- OpenSpec change：`display-v2-national-timeline-precip-overlay`，本 receipt 交付 tasks 7.2 与 7.3。
- runner：`2026-09-16-display-v2/stage.sh`（md5 见 §10），日志 `window/stage.log`；证据目录 `window/`。
- 本窗口对节点只做三件事：`infra/env/display.env` **追加一行** `NHMS_PRECIP_MIRROR_ROOT`、`git pull --ff-only`、display API 重启一次。零 unit 改动、零 DB 写、零前端重建、活动树内零 `uv run`/`uv sync`。

## 1. 决策与配置改动

`NHMS_PRECIP_MIRROR_ROOT=/home/ghdc/nwm/object-store`（镜像根口径由 #2162 target 1 的 Non-Goals 路由到本单裁定）。三条依据：

1. `services/precip/mirror.py::precip_directory_key` 自己拼 `canonical/<S>/<K>/prcp_rate_or_amount`，故 env 取 **object-store 根**，不是 `canonical/` 子目录；
2. 与入库模板 `infra/env/display.example:60` 的注释值逐字相同，该值被 `tests/test_precip_overlay.py:1840` 钉住；
3. 与 `infra/env/node27-raw-retention.env` 的 `NODE27_RAW_RETENTION_OBJECT_STORE_ROOT` 同值，满足 `infra/env/node27-raw-retention.example:53-55` 要求的「两者同一棵树」。

写入前 `cp --parents` 备份到 `/home/nwm/nhms-display-v2-backup-2017-20260916T081507Z/`，
写入后 `grep -c '^NHMS_PRECIP_MIRROR_ROOT=' = 1`；`display.env` 41 → 45 行（3 行注释/空行 + 1 行赋值）。
仅键名 diff 记在 `window/envdiff-keys.txt`，**值全程未打印、未入库**。该 key 不在 `apps/api/runtime_mode.py::_DISPLAY_FORBIDDEN_COMPUTE_PATH_ENVS`（被禁的是 `NHMS_OBJECT_STORE_COPYBACK_ROOT`），重启后 `/api/v1/runtime/config` 仍报 `service_role=display_readonly`、`display_readonly=true`。

## 2. 窗口结论

- display MainPID → **2901698**，重启 3 s，`/health` 200；`yd` 实例未触碰。
- 判别器：同一 `GET /api/v1/precip/gfs/2026-09-14T00:00:00Z/index` 在配置前是 404 `PRECIP_CYCLE_NOT_MIRRORED / mirror_root_unconfigured`（#2162 两份 receipt 各记过一次），配置后 **200**。
- 9 个 user unit 的生效配置窗口前后 **逐字节相同**（`window/units-diff.txt` 为空）。
- timer 停/启：`autopipe`/`download`/`frontier-alert` 在冷测量期间停用，`trap` 恢复，`window/timers-{before,after}.txt` 对照。
- 失败 unit 12 个，全部是既有的外部一次性 unit（#1987/#2349/#2374/#2382/pgdata/reslice 系列），本窗口未新增。

## 3. 读面实测（`window/` 内逐项有原始输出）

| 项 | 实测 | 验收口径 | 结论 |
|---|---|---|---|
| `GET /api/v1/layers` 冷 p95（n=12，带 refresh token 头，串行） | **0.1119 s**（中位 0.0757 s，min 0.0682 s） | ≤ 200 ms | PASS |
| 首屏落定（`m11-overview-loading` 消失且全屏地图在位），本机 `127.0.0.1:8080` | 中位 **549 ms**（3 次：535/549/777） | < 1 s | PASS |
| 首屏落定，公网 `https://test.nwm.ac.cn` | 中位 **554 ms**（546/554/864） | < 1 s | PASS |
| 降水 index 冷 / 热 | gfs 0.0019 s / 0.0331 s；ifs 0.0174 s / 0.0028 s，均 200 | — | — |
| 降水 PNG 冷 | gfs lead0 0.2415 s（45742 B）、lead24 0.2099 s；ifs lead0 0.3397 s（51686 B）、lead24 0.1686 s，`X-Tile-Cache: miss` | — | — |
| 降水 PNG 热 / `If-None-Match` | 0.0037–0.0052 s / 四张全 **304**，0 字节 | — | — |
| 河网瓦片 z3–z7 冷 / 热 | 0.0102–0.0162 s / 0.0128–0.0194 s，全 200 非空（z4/12/6 149416 B） | — | — |
| 流量瓦片 z4/12/6 冷 / 热 | gfs 0.0386 s / 0.0405 s（1374311 B）；ifs 0.0384 s / 0.0379 s（1374336 B） | — | — |
| 公网入口 | `/` 200、`/ops` 200、`/api/v1/layers` 200、降水 index 200 | — | PASS |

`/api/v1/layers` 的 `precip` 条目存在，`tile_format: png`、`image_url_template: /api/v1/precip/{source}/{cycle}/{valid_time}.png`。

**改前 p95 取不到**：本 change 的代码早已在活动树上，节点上不存在「改前」进程可采样。改前值引用 #2009 receipt §2 的 before：`:8080` 生产侧 12 次 **0.091 s**。本次 0.1119 s 与之同量级，两者都远在 200 ms 预算内；因为不是背靠背同会话采样，**不据此宣称回归或改善**。

## 4. 河网坐标预算（tasks 2.4/2.5 的入库复算）

`scripts/node27_river_tile_coordinate_evidence.py`（本 PR 入库）对 `xyz_tiles(CHINA_BOUNDS, [3,4,5,6,7])` = **516 张**瓦片各跑两遍绑定：生产 `collection_coordinate_limit=120000` 与 `1e9`。只读角色、`SET TRANSACTION READ ONLY`、逐瓦片 `rollback`。

| zoom | 瓦片 | max `feature_coordinate_count`（瓦片） | overflow | max `coordinate_count`（瓦片） | max bytes | max 秒 | 截断 |
|---|---|---|---|---|---|---|---|
| 3 | 4 | 5828（3/6/2） | 0 | 86008（3/6/3） | 248347 | 3.087 | 0 |
| 4 | 9 | 5828（4/13/5） | 0 | 52331（4/12/6） | 149416 | 1.477 | 0 |
| 5 | 30 | 5454（5/26/11） | 0 | 52824（5/25/12） | 148784 | 1.425 | 0 |
| 6 | 99 | 14395（6/53/23） | 0 | 56336（6/52/24） | 164552 | 1.185 | 0 |
| 7 | 374 | 19528（7/105/51） | 0 | 52381（7/107/46） | 119266 | 0.922 | 0 |

四条 go 条件同时成立：`feature_coordinate_count < 50000`（最大 19528）、`overflow == 0`、`coordinate_count ≤ 120000`（最大 86008）、两遍绑定的 `coordinate_count` **逐瓦片相等**（截断 0 张）。
逐瓦片表 `window/river-tile-budget.csv`（516 行），汇总 `window/river-tile-budget.json`。与 #2005 的 2.4 receipt（max feature 19528、z3 max 86160）同结论；`86160 → 86008` 的差来自其后河网数据本身的变化，不是预算行为变化。

z≥9 确定性抽查按 2.5 的口径**无有效样本**：本次 516 张瓦片没有一张被 `collection_coordinate_limit` 截断，中国境内不存在自然截断样本，本 receipt 不为此记 PASS。

## 5. prewarm（cron 调用原文）

`\$REPO/.venv/bin/python scripts/node27_mvt_prewarm.py --zooms 3,4,5 --workers 8`，rc=**0**，墙钟 2 s。

```
requests_total=183  elapsed_seconds=1.891  cache_hits=175  bytes=50009174
river_tile_count=43  discharge_zooms=[3,4]  zooms=[3,4,5]  workers=8  failed_count=0  failures=[]
gfs: cycle=2026-09-14T00:00:00Z discharge_requests=65 png_ok=5 png_failed=0 png_not_mirrored=0
     valid_times_available=56 valid_times_warmed=5
ifs: cycle=2026-09-14T12:00:00Z discharge_requests=65 png_ok=5 png_failed=0 png_not_mirrored=0
     valid_times_available=56 valid_times_warmed=5
```

Evidence Floor 追加三条：

1. **workers**：`AUTOPIPE_MVT_PREWARM_WORKERS` 在节点上**未设**（`~/.config/systemd/user/*.service` 与 `infra/env/*.env` 均无），`scripts/node27_autopipe_cron.sh:244` 的回退值 8 生效；汇总顶层 `workers: 8` 与之一致。`nproc=40`，故按 `8/2=4` 核的成本估算前提成立（实机远超）。
2. **逐源 clamp 与降级幅度**：两源的 `/valid-times` 首项都等于各自 cycle（差值 **0 h**，无 clamp）；`valid_times_available=56`、`valid_times_warmed=5`，即 `PREWARM_LEAD_HOURS=12` 的窗口只覆盖 56 个时次里的 5 个（**8.9%**）。降水 index 自己列 58（gfs）/ 50（ifs）个时次。
   `window/valid-times.txt` 里那一行写的是 112，那是脚本用正则数 body 里全部 RFC3339 字面量的产物——响应体同时含 `valid_times` 与 `items` 两个 56 元数组（`observed_count=56`、`truncated=false`）。正确值是 **56**。
3. **一次 publish tick**：见 §7，窗口期内取不到。

prewarm 的 stderr 有一行 `prewarm: NHMS_DISPLAY_CACHE_WARM_TOKEN unset; discovery may see up to 45 s stale catalog` —— 见 §8 发现 1。

## 6. 容量、缓存有界、keep 水位

```
/                 98G  76G  18G  81%          （窗口前后同值）
/home            1.7T 477G 1.1T  31%          ← NHMS_MVT_FILE_CACHE_DIR=/home/nwm/.cache/nhms/mvt 所在卷
/data/GHDC        15T 2.0T  12T  15%
```

- `precip/**` PNG 文件数 **0 → 12**（`du` 616K）；瓦片 `.pbf` 11423 → 11423（不增）；缓存总量 1.9 G。
- 上界 `2 × 57 × K`：本次只触达 K=1 个周期/源（gfs `2026091400`、IFS `2026091412`），上界 114，实测 12 ≤ 114。PASS。
- 缓存目录与镜像一一对照：`precip/gfs`、`precip/IFS` 各 1 个周期目录，**无**「有缓存无镜像」的孤儿目录（`window/inventory-after.txt` 无 `ORPHAN_PNG_DIR` 行）。
- keep 水位不等式 `oldest_listed_cycle − 24h ≥ display_watermark − retention_days`：`display_watermark = 2026-09-14T12:00:00Z`、`retention_days = 14` ⇒ 右式 `2026-08-31T12:00:00Z`。

| 源 | `oldest_listed_cycle`（API） | 左式 | 右式 | 成立 | 最旧镜像周期 |
|---|---|---|---|---|---|
| gfs | 2026-09-04T12:00:00Z | 2026-09-03T12:00:00Z | 2026-08-31T12:00:00Z | ✅ | 2026083112 |
| ifs | 2026-09-04T12:00:00Z | 2026-09-03T12:00:00Z | 2026-08-31T12:00:00Z | ✅ | 2026083112 |

镜像周期数：gfs 28、IFS 29，各周期 `prcp_rate_or_amount/` 分别 56 / 53 个 `.nc`（IFS 144 h 后 6 h 节奏，非缺片，口径同 #2010 receipt §1）。

## 7. 浏览器证据（Playwright，真实站点，零 mock）

`scripts/node27_display_v2_browser_evidence.mjs`（本 PR 入库）对本机与公网各跑一遍，两侧 `pass: true`、`failures: []`。报告 `window/browser-local/browser-evidence.json`、`window/browser/browser-evidence.json`。

| 断言 | 实测 |
|---|---|
| header 高度 | **84 px**（两侧四个场景全部） |
| 底部控制条高度 | **64 px** |
| 横向滚动 | 无（`scrollWidth 1920 == clientWidth 1920`） |
| 标题 | 含「全国水文模拟系统（V2.0）」 |
| 默认首屏 | 降水图例在位、降水 PNG 有请求、控制条默认 `+0h`（截图左下时间轴锚在 `+0h`，时刻 `2026-09-14T00:00:00.000Z`） |
| 切 IFS | 流量瓦片 URL 与 gfs 场景**零交集**；降水 PNG URL 亦不同 |
| 切时次（+24 h） | 流量瓦片 URL 与默认场景**零交集** |
| `precip=0` | 降水 PNG 请求 **0 条**、降水图例不渲染；河网与流量层不受影响 |

判据说明：落定信号取 `[data-testid="m11-overview-loading"]` 消失且 `m11-fullscreen-map` 在位。该提示的渲染条件是 `mapBootstrapLoading || (!overview.bootstrap && !bootstrapError)`（`apps/frontend/src/pages/OverviewPage.tsx:504,566`），**严于**单看 `mapBootstrapLoading` 落 false。
`precip=0` 场景仍会发一条 `/api/v1/precip/.../index` 目录探测，那是目录请求不是图层绘制，脚本因此只以 PNG 请求为「图层是否在画」的判据。

截图 4 张 × 2 侧留在 node-27 `/home/nwm/tmp/2017/window-20260916T081507Z/{browser,browser-local}/`，按 `apps/frontend/e2e/m11-visual-evidence.md` 的既有约定**不入库**；sha256：

```
eb9f6c66fba86cd1c824c77a4b6a2e577872af95a744083f898426d3628547c4  browser/overview-default.png
5205db6457eb916a827191eb3b1d59df4d3cd75183e30a76eef63e0e5eee052f  browser/overview-ifs.png
e10623e084bbc87715185874f08654b0133ebc4b624c7d7b4e0c056a1d4cac22  browser/overview-precip-off.png
f0a06858341b4e20ef3a32e46881796512dd1ada34dc7a897c5d17b2314ced76  browser/overview-valid-time-plus-24h.png
131334d0ab25ab457aa5695f955840ad5ab218236dc3db34243ddc5df06c9396  browser-local/overview-default.png
c1fed933699df11aaf40caf03a0cc526071fcc6cae6a5aa3f727851cd9fd9e01  browser-local/overview-ifs.png
ebf0b1de38a200b042473ecbd3666c21703b0b6095cfa76c09f9c52a7b8d2965  browser-local/overview-precip-off.png
15f49eab86eeb5e6f8a11d29e8ee62c07d6d4a08aad096d7c14779dc1d2187ec  browser-local/overview-valid-time-plus-24h.png
```

## 8. 本窗口取不到的一项，与四条范围外发现

**取不到（按实况记录，不伪造）**：Evidence Floor 要的「一次 **publish** tick 的 `autopipe: done rc=… elapsed_sec=…`」。窗口期内不可能制造：镜像与 DB 的最新周期都停在 gfs `2026-09-14T00Z` / IFS `2026-09-14T12Z`，无新周期可发布。日志里最后一次**真做了事**的 tick 是 `2026-09-15T05:51:40Z` 起（`processed=38, published=38`），其 `autopipe: done rc=0 elapsed_sec=1222` 落在 `2026-09-15T06:12:02Z`；
当时活动树还在 `415cbd1e`（master 恢复发生在 2026-09-16T07:13Z，见 #2162 target 2 receipt）。**master 代码上尚无 publish tick**，本窗口内的 4 次 tick 全是 no-op（`published: 0`，`elapsed_sec=11`，ingest 段 9 s）。

**范围外发现（报告不修）**：

1. **prewarm 拿不到 warm token**：`scripts/node27_autopipe_cron.sh` 不 source `infra/env/display.env`，于是 cron 起的 prewarm 每次都报 `NHMS_DISPLAY_CACHE_WARM_TOKEN unset`，目录发现可能读到最多 45 s 陈旧的 catalog。#2079 配的 token 只有 display 进程自己看得见。本次手工调用同样未设 token，故 §5 的读数与生产 tick 同条件。
2. **PNG 缓存剪枝 lane 休眠**：`infra/env/node27-raw-retention.env` 缺 `NHMS_MVT_FILE_CACHE_DIR`（模板 `node27-raw-retention.example:63` 有该行），最近一次汇总 `raw-retention-20260916T033532Z.json` 里 `precip_cache_root: null` → `precip_cache_root_unconfigured` 跳过。
   即 §6 的「有界」目前**只由镜像周期数与 `2×57×K` 算术保证，没有剪枝在兜底**；镜像侧的 canonical 剪枝配置正常（root/sources/days 齐全）。属 #2011 面。
3. **上游周期停滞 ~2 天**：node-27 侧观测，canonical 镜像与 DB 的最新周期同为 gfs `2026-09-14T00Z` / IFS `2026-09-14T12Z`，两侧一致说明不是 ingest 落后，而是上游（node-22）自 09-14 起没有新周期产出。本 receipt 的所有「最新周期」读数都建立在这个事实上。
4. **`2026-09-17T03:35Z` 的 raw-retention tick 预期标红**：master 的 `node27_raw_retention.py` 带 #2252 的 copyback 互斥，unit 以 uid 1005 运行而锁属 uid 1103，陈旧 canonical 周期上按设计 rc=1 `lock_unsafe`。与本次部署无关，先记在此免得被读成回归。

## 9. 回滚

窗口已过门禁、部署成立，未触发回滚。若需回退降水服务（不需要回退代码）：

```bash
BK=/home/nwm/nhms-display-v2-backup-2017-20260916T081507Z
cp "$BK/home/nwm/NWM/infra/env/display.env" /home/nwm/NWM/infra/env/display.env
grep -c '^NHMS_PRECIP_MIRROR_ROOT=' /home/nwm/NWM/infra/env/display.env   # 期望 0
systemctl --user restart nhms-display-api.service && curl -s http://127.0.0.1:8080/health
```

回退后降水两条路由回到 404 `mirror_root_unconfigured`，前端按 #2015 的既有分支隐藏降水层并给原因；河网、流量、目录三条读面不受影响。

## 10. 验收对照

| 验收项 | 证据 |
|---|---|
| receipt 含 7.2 括号内每一项实测值 | §3 §4 §5 §6 §7 |
| `GET /api/v1/layers` 冷 p95 ≤ 200 ms | §3，0.1119 s |
| `mapBootstrapLoading` 落 false < 1 s（降水开启） | §3 §7，本机中位 549 ms / 公网 554 ms |
| 河网瓦片 `feature_coordinate_count < 50000` 且 overflow 0 | §4，max 19528 / overflow 0（516 张） |
| `precip/**` PNG 数 ≤ `2 × 57 × K`，无镜像缺失的缓存目录 | §6，12 ≤ 114，孤儿目录 0 |
| 浏览器截图：标题 / 河网 / 降水 / 默认 lead 0；切源与时次 URL 变化；`precip=0` 隐藏；84 px / 64 px / 无横向滚动 | §7 |
| 逐源 keep 水位不等式 | §6，两源均成立 |
| 本地 `ruff` + `pytest` + `openspec validate` | §11 |
| runbook 新增降水镜像/回填步骤；`project-profile.md` 更新 | `docs/runbooks/canonical-precip-mirror-backfill.md`；`openspec/project-profile.md` |

runner md5：`stage.sh` = `8a80c2dfedb4f94f846168ef1ea5d624`。

## 11. 本地闸门与偏离

按本 change 第 7 组的规定，测试集合**不手挑**：把本 PR 相对 `origin/master` 的全部改动路径喂给
`scripts/select_ci_tests.py --changed-file`（CI 用的同一入口），打印它返回的集合，再 `uv run pytest -q`
恰好跑这个集合。

```
39 changed paths -> tests/test_node27_river_tile_coordinate_evidence.py
                    tests/test_river_segment_write_surface_scan.py
                    tests/test_select_ci_tests.py
                    tests/test_timescale_write_guard_wire_site_invariant.py
uv run ruff check .                     -> All checks passed!
uv run pytest -q <上面四个套件>          -> 747 passed in 490.69s
openspec validate display-v2-national-timeline-precip-overlay --strict --no-interactive -> valid
markdownlint-cli2 (docs/** 两份新文件)   -> 0 errors
```

偏离记录：

1. **动了 `scripts/select_ci_tests.py` 与 `tests/test_select_ci_tests.py`**，超出「本单不改功能代码」的字面范围。
   原因：新增的 `tests/test_node27_river_tile_coordinate_evidence.py` 在模块层 import
   `apps.api.routes.hydro_display` 与 `services.tiles.mvt`，选择器的两条元守卫
   （`test_guarded_module_rules_cover_their_non_gated_importer_closure` /
   `test_hydro_display_rule_covers_its_derived_importer_closure`）因此判定规则表漏了这个套件而变红。
   改动是把该套件登记进 `hydro_display` 规则与 guarded-module closure，并按
   `test_select_tests_maps_mvt_tiles_without_core_smoke_fallback` 自述的流程用选择器自身输出同步互补 pin。
   零选择行为回归：`tests/test_select_ci_tests.py` 715 项全绿。
2. **不重建前端**：本次 `git pull --ff-only` 是纯文档提交，且 #2162 target 2 已实测在架 `dist` 与 master 构建逐字节相同。
3. **浏览器截图不入库**，按 `apps/frontend/e2e/m11-visual-evidence.md` 的既有约定，只记 sha256 与节点路径（§7）。
4. **z≥9 强制截断抽查无有效样本**，按 2.5 的口径记「无有效样本」而不是记 PASS（§4）。
