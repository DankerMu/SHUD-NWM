# Legacy Slurm Templates (Deprecated)

These templates are **not used** by M3+ array orchestration.
The canonical real Slurm template set is `infra/sbatch/`.

See `services/slurm_gateway/config.py` for the authoritative
`SlurmGatewaySettings.template_dir` default and job-type mapping.

These files are retained for reference only and may be removed
in a future milestone.
