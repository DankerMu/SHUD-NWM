"""Grid registry worker package (Epic #897 canonical-source-grid-registry).

Task 3.1a defines the input-record contract only. Task 3.1b will add the
registration entry point ``python -m workers.grid_registry``.
"""
